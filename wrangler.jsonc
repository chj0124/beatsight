/* Cloudflare Workers 静态资源（Static Assets）唯一一份入库配置。
   Workers 的 assets 必须由 Wrangler 配置文件声明资源目录，否则 Cloudflare 构建里的
   `npx wrangler versions upload` 会在"解析配置"这一步直接失败（线上一直是旧版就是这么来的）。

   build.command（v2.0.4 审计 B4）：把"构建什么、怎么构建"从 Cloudflare Dashboard 收回仓库，
   使本地 `npx wrangler deploy` 能完整复现线上构建——先跑全量自验（不通过即中止，`&&` 短路），
   再把 9 个产物装配进 dist/。这样"部署配置"也进了版本库：可 review、可 diff、可回滚。

   --strict-env（v2.0.5，审计 P0-5）：自验链里 ESLint / tsc 是**可选加强项**，缺 node_modules 时
   它们会标 ⊘ 跳过而不是失败——本地这样设计是对的（零依赖产物不该被开发依赖卡住），
   但在**构建环境**里就成了漏洞：忘跑 `npm ci` 时两步永远是 ⊘，汇总照样写"全部通过 · 实跑 8/10"，
   而"实跑 8/10"那个分母没人会去核对。加了它之后，构建环境里缺依赖会直接报错中止。

   ⚠ 注意（Cloudflare 官方既有行为，非本项目可改）：**Workers Builds（Git 集成构建）不读
   wrangler 配置里的 Custom Builds**——所以线上那次构建仍以 Dashboard 里配的构建/部署命令为准；
   本字段只保证"本地 `wrangler deploy` / `wrangler versions upload` 会执行它"，即本地可复现。
   由此推出一条**必须说清的结论**（v2.0.5 审计 A1/E1 修正了此前文档的说法）：能称作"硬闸门"的
   只有本地/命令行那条路；线上那次构建到底跑没跑自验，取决于 Dashboard 里那串命令，
   而它不在版本库里（改不动、review 不了、diff 不出来）。别把"Cloudflare 会拦"当成可以
   跳过本地全量自验的理由——它拦不拦取决于一个仓库外的东西。
   （官方原文用的是 "Currently, Workers Builds does not honor the configurations set in
   Custom Builds within your Wrangler configuration file." ——即"目前不支持"，非永久性设计，
   值得后续回看：<https://developers.cloudflare.com/workers/ci-cd/builds/configuration/>）

   ★ Dashboard 的 Build command 该填什么（v2.8.3）：**只填一行 `npm run ci`**。
   不要再把命令内容抄进 Dashboard —— 那样等于把三个文件路径钉在仓库外，改脚本名或挪路径会让
   线上构建**静默断掉**。`npm run ci` 的定义在 package.json（版本库内），
   内容 = `npm ci && node tools/check-all.js --strict-env && npm run build`，
   与下面 build.command 的语义完全一致。仓库外只剩这 11 个字符，几乎永不改动。

   ⚠ 该脚本里 `npm ci` 与 `--strict-env` **缺一不可**（v2.8.3）：Cloudflare Workers Builds
   默认注入 `CI=true`，但 `check-all.js` 只认 `--strict-env` 这个 **CLI flag**、**不读 `CI`
   环境变量**（L63 是 `process.argv.includes(...)`）。所以若在 Dashboard 里写成不带 flag 的
   `npm run verify`，构建镜像缺 node_modules 时 ESLint / tsc 会静默标 ⊘、汇总照样打印
   「全部通过 · 实跑 10/12 项」——**恰好复现 --strict-env 当初要防的那个假绿**。
   （这也是对 v2.0.5 条目里「`CI=true` 且缺依赖时报错」那句描述的更正：实现落地为显式 flag。）

   ★ Wrangler 版本已钉（v2.8.5）：`package.json` 的 devDependencies 现在显式声明 `wrangler`。
   官方文档在 deploy command 与 preview deploy command 两处都写着
   「Workers Builds will use the Wrangler version set in your `package.json`」——
   而在此之前本仓库**根本没有声明过它**，于是线上那次 deploy 用哪个 Wrangler 版本由 npx 解析决定、
   不由仓库决定；上面那句「本地 `npx wrangler deploy` 能完整复现线上构建」在 deploy 这一步
   其实不成立（两侧版本都没被钉住，只是碰巧一致）。现在两侧都读同一份 `package-lock.json`，
   deploy 用的 Wrangler 版本才算真的可复现。同一张清单上的**最后一处已闭合**：Node 版本。
   v2.8.8 起 `.nvmrc`（内容 `22`）已入库，`ci.yml` 显式消费（`node-version-file`），
   Workers Builds 构建镜像亦读 `.nvmrc`（**需实测**确认 Cloudflare 构建镜像确实兑现）。

   ⚠ 已修（v2.0.6，审计 P1-8）：上面这条命令原先用 `rm -rf` / `mkdir -p` / `cp`，需要 POSIX shell，
   **Windows 的 cmd 下跑不了**，与"本地可完整复现"的目标有落差。装配那半段现已收进
   `tools/build-dist.js`（fs.rmSync / mkdirSync / copyFileSync，全平台可用），
   命令里只剩两个 node 调用，两处平台（本地 Windows / Cloudflare 的 Linux 构建镜像）行为一致。

   dist/ 由 build.command 现场生成：8 个上站产物（index.html / sw.js / manifest.webmanifest /
   icon.svg + 4 个 PNG 图标回退）
   加 `_headers`，共 9 个条目；本地不产出、已 gitignore。 `_headers` 由 Workers 解析为响应头，
   本身不作为静态资源对外提供。 */
{
  "name": "beatsight",
  "compatibility_date": "2026-09-15",
  "build": {
    "command": "node tools/check-all.js --strict-env && node tools/build-dist.js"
  },
  "assets": {
    "directory": "./dist"
  }
}
