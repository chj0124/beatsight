# Cloudflare 静态资源自定义响应头（v2.0.3 审计 C3；v2.4.4 补齐 CSP 家族）。
# 构建时被拷进 dist/（见 wrangler.jsonc 的 build.command），由 Workers 解析并作用于
# 静态资源响应；本文件自身不会作为资源对外提供。所有头都对功能无影响，属"默认关上更安全"：
#   X-Content-Type-Options: nosniff  禁止浏览器按嗅探出的类型解释响应（防 MIME 混淆）
#   Referrer-Policy: no-referrer     跳转不回传 Referer（本应用不读 Referer，也无外链统计）
#   Content-Security-Policy          本应用零外链、无 eval、无动态脚本，故可收紧到 self；
#                                    脚本与样式全部内联在 index.html，必须保留 'unsafe-inline'
#                                    （单文件架构的既定取舍）；img-src 放 data: 供未来内联位图；
#                                    base-uri/form-action 锁死，页面不含表单也无 <base>。
#   frame-ancestors 'none'           等效 X-Frame-Options: DENY，禁止被任何站点 iframe
#                                    嵌套（点击劫持面）；现代浏览器以 CSP 此项为准。
#   Permissions-Policy               本应用不用麦克风/摄像头/地理定位，全部显式关闭。
#   Strict-Transport-Security        Cloudflare 边缘默认已强制 HTTPS，此处显式声明兜底。
/*
  X-Content-Type-Options: nosniff
  Referrer-Policy: no-referrer
  Content-Security-Policy: default-src 'self'; script-src 'self' 'unsafe-inline'; style-src 'self' 'unsafe-inline'; img-src 'self' data:; font-src 'self'; connect-src 'self'; worker-src 'self'; manifest-src 'self'; base-uri 'none'; form-action 'none'; frame-ancestors 'none'
  X-Frame-Options: DENY
  Permissions-Policy: microphone=(), camera=(), geolocation=(), payment=(), usb=()
  Strict-Transport-Security: max-age=31536000; includeSubDomains
