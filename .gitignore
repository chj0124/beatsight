Thumbs.db
.DS_Store
_check.js

# 真浏览器验收工具（v2.4.2）。零依赖 CDP 自验脚本 + 它产出的报告与截图：
# 一次性验收用，跑完就该删；留在这里只是方便复跑，**不属于上站产物**，
# 也不该进版本库（截图是二进制、报告是本机绝对路径）。
# 注意 `_headers` 与它们只差一个下划线前缀，但**必须入库**（Cloudflare Pages 配置）——
# 所以这里逐个列举，不用 `_*` 这类宽通配（宽通配会连 `_headers` 一起忽略掉）。
_cdp-verify.js
_cdp-report.json
_cdp-shot.png
_cdp-shot-notab.png
_cdp-seed.html
# v2.4.3：定位「箭头被裁」时按单个几何问题拆出的几个探针（各自只答一个问题，
# 比反复读一张截图快且准）。同样是**一次性验收工具**，定位完即弃。
_cdp-clip.js
_cdp-span.js
_cdp-dup.js
_cdp-dir.js
_cdp-dir.png
_cdp-tab.js
_cdp-tab.png
_cdpdbg-*.json
# 本机预览服务（v2.4.1 起用来核对渲染结果）。它按需重建，不入库。
_serve.js
NVIDIA Corporation/
dist/

# 开发期自验工具链的依赖树（只跑本地，不进上站产物）
# 产物始终只有 index.html / sw.js / manifest.webmanifest / icon.svg
# package.json 与 package-lock.json 要提交（前者声明 eslint devDependency，后者锁版本保证规则集可复现）；
# 只有装出来的 node_modules 不入库。
node_modules/

# IDE 的「分享 HTML」功能会把产物打成 zip 丢在这里（v1.8.2 清理）。
# 它们不是项目产物，上站也不需要（Cloudflare 只拷 4 个上站产物 + `_headers` 到 dist/），
# 且 .tmp-* 明显是临时残留 —— 一律不入库，防止下次再被塞回来。
.trae-html-share-packages/
*.html.zip

# 会话/IDE 的临时上传落盘目录：里面是 UUID 命名的图片残留，不被任何文件引用，
# 也不是产物（v2.0.2 审计 D7 清理）。
.uploads/
