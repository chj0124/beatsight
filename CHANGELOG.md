# 变更记录

## v1.9.0 · 功能：扫弦方向标注 ↑↓ + 练习量控制（2026-09-15）

两个新功能，都在**不碰音频时间轴**的前提下完成：一个补记谱层，一个管练习回合。

### 一、扫弦方向标注 ↑↓

内置预设「民谣扫弦 · 下-下上-上下上」的名字里一直写着 ↓↓↑↑↓↑，但界面**完全表达不出来**——
方向只存在于标题字符串里。本版补上。

- **数据模型加可选字段 `dir`**（`'D'` 下扫 / `'U'` 上扫），省略 = 不标注。
  内置民谣扫弦补上 `DDUUDU`，与它自己的名字逐字对应（顺手修掉一处名不符实）
- **`dir` 是纯记谱层字段**：`scheduler` 一行都不读它。这是本项风险可控的根本原因——
  对比 Swing 的既有先例（"演奏参数不入时值"）。T47e 用「同一段节奏加/不加 dir，
  逐个音的时刻/音高/类型必须逐位相同」把这条钉死
- **渲染**：格子内的 `.cell-strum` 徽标（↓ / ↑）。刻意**不跟随主题色**——绿/蓝在
  `.cell.played` 的纯白填充上对比不足，改用「深底 + 亮描边 + 近白字形」，两主题通吃，
  不必往观测台覆盖块再加一份。徽标画在**格子内部**而不是时值标签行：标签只给 t≥24 发声，
  画在标签行会丢掉切分位上那颗下扫，而它恰恰是民谣扫弦里最需要提示的一颗
- **`fitCellLabels` → `fitCellAnnotations`**：纳入徽标的窄格隐藏（阈值取常量 20px 而非
  scrollWidth，因为徽标是定宽元素）。调用点不变，仍在 `buildViz` 末尾与 resize 后——
  `paintFrame` 里依然一次 `offset*` 都不读（v1.3.0 P1-4 的性能承诺不受影响）
- **编辑器**新增「扫弦标注」三档（↓ 下扫 / ↑ 上扫 / 不标注），作用于选中的音符：
  未选中、或选中休止符时三档禁用并给出原因（休止不承载扫弦动作）。重复点当前档位不推撤销栈
- **导入校验口径**：`dir` 脏值**静默降级为"不标注"**，不让整条预设失败。与 v1.2.4
  「淘汰项不静默丢弃」不冲突——那条针对的是会让节奏型**不可用**的结构性损坏；
  箭头丢了节奏型依然完整可弹，为它弹窗报错反而更糟。此判据已写进代码注释与 DEVELOPMENT §3.1
- 旧数据零迁移：无 `dir` 的预设导入后就是"无箭头"，`rep4` / 编辑器 `{...s}` /
  `JSON.stringify` 撤销栈 / 导出全部**自动**带上它，无需为它加代码

### 二、练习量控制（练 N 小节 / N 分钟，到点自动停）

此前只能手动按停，全应用只有变速训练器会自动停止。「练 20 遍」「练 5 分钟」是吉他练习的
常规形态，缺它就没法把节拍器当练习回合用。

- **新档位**：不限 / 8 小节 / 16 小节 / 32 小节 / 5 分钟 / 10 分钟。档位表 `LIMIT_PRESETS`
  同时充当**加载校验白名单**——`S.limit` 只接受表内存在的 `(mode, n)` 组合，脏值一律回退
  「不限」；比"mode 白名单 + n 任意正数"更严，也免了另写一套域校验
- **两种模式的计数源**：`bars` 数小节（调度器在小节边界累加——只有它看得见小节边界，
  这是唯一与发声同源的视角）；`min` 数音频时钟差（与 `beatsight.log` 的会话时长同口径）。
  判定挂在 `scheduler` 每个 25ms 周期的最前面：`bars` 天然按小节对齐，`min` 则与 BPM/拍号
  完全无关（30BPM 的 7/4 一小节 16 秒，若按小节边界判会晚停十几秒）
- **到点处置走钩子**（`onLimitPulse`）：Audio 声明在 Controls 之前，直接调 `Controls.stop()`
  属反向引用，会被 `tools/check-module-order.js` 拦下。与 `onFrameError` /
  `Store.setPersistFailHandler` 同一套「模块只暴露回调、跨模块装配交给装配层」模式
- **预备拍不吃额度**：`limitT0` 在预备拍数完那一刻才取。反向验证证实——去掉这次赋值，
  min 模式的停止时刻其实不变（`start()` 算 `loopStart` 时已把 N 拍顺延进去，值等价），
  但进度区会在预备拍期间就显示「已练 0:00 / 5:00」。所以它不是冗余代码，注释已写明
- **每轮开始归零**：否则第二轮一开就"秒到点"（T48g 钉住）
- **播放中改档位立即生效**：已超额时下一个调度周期即停，不必停下重开（T48h 钉住）
- **与变速训练器共存**：两者职责分离（训练器管"练多快"、练习量管"练多少"），
  同时开时**谁先到谁停**，停止文案区分两种原因（T48e 双向钉住）
- **进度显示**：`已练 12 / 32 小节` 或 `已练 2:05 / 5:00`。刷新由 `onLimitPulse` 驱动并做
  字符串去重——25ms 一次无条件写 `textContent` 会持续触发样式重算，不值得
- **持久化**：进热键 `beatsight.state`（仍 328 字节，远低于 1 KB），只写 `{mode, n}` 两个字段

### 已知行为（与既有变速训练器同源，非本版引入）

- `bars` 模式的**墙钟**停止时刻会早于小节边界最约 0.85s（前瞻窗口 150ms + 一个音符时长）。
  这是设计使然：小节计数在**排程**时累加，而 `stop()` 只停时钟与画面、**不取消已排入音频
  时钟的振荡器**——所以那 N 小节的音一个不少地响完。T48b 用「发声个数恰好 = N 小节 × 每小节
  音数」把音频层面的"不多不少"钉死（比任何时刻断言都硬），另用区间断言钉住墙钟边界。
  既有训练器自动停走的是同一条路径，行为一致

### 自验

- `node tools/check-all.js` 全绿 **8/8**；行覆盖率 **99.9%**（仍只剩 Audio 的 3 行防御分支）
- 新增 **T47（扫弦方向，5 个场景组）+ T48（练习量，8 个场景组）**，断言总数 731 → 849
- **反向验证 13 个变异全部命中**：扫弦 6 个（徽标渲染 / 校验接受 dir / 窄格隐藏 /
  三档可用性与高亮 / 休止符拒绝 / 编辑器内联显示）+ 练习量 7 个（每周期判定 / 小节累加 /
  预备拍基准 / 加载白名单 / 进度刷新 / 每轮归零 / 到点文案）。每个变异都产生**具名失败**，
  不再让测试以崩溃形式告负——反向验证过程中发现两处断言会炸掉整套（惰性创建的 DOM 桩被
  直接解引用），已按 v1.3.1 的教训改为安全读取

## v1.8.2 · 工程：仓库卫生收口 + 文档对齐（2026-09-15）

无新功能、无行为变化——`index.html` 只动了版本号一行（`const VERSION` 1.8.1 → 1.8.2，遵守
v1.6.4 起的发版规则）。本版清理的是**仓库自身的卫生问题**，不是页面。

### 修复

- **删除 `.trae-html-share-packages/`（224 KB 入库垃圾）**：里面是 IDE「分享 HTML」功能产出的
  4 个 zip（`index.html.zip`、`docs/prd.html.zip`，以及两个明显的临时残留
  `.tmp-m-classic.html.zip` / `.tmp-m-obs.html.zip`）。它们不是项目产物、上站也不需要
  （Cloudflare 只往 `dist/` 拷 4 个文件），白白让仓库多了 224 KB，还让 clone 的人以为
  `.tmp-*` 是项目的一部分
- **`.gitignore` 补两条**（`.trae-html-share-packages/` 与 `*.html.zip`）：删掉现存的不够，
  不挡规则下次还会被塞回来。注释里写明"这不是项目产物"的判据，避免后人看到规则不明所以
- **README「功能现状」表补齐 v1.7 / v1.8**：该表停在 v1.6，而 `const VERSION` 已经到 1.8.1——
  v1.7（双主题）与 v1.8（待命球起跑预备）两个**用户看得见的功能**在门面文档里完全缺席，
  顺手也把 v1.6.4–v1.6.6 三连工程版补了一行
- **README 文档表加入现行方案 `docs/PLAN-v1.9.md`**（v1.5 那份已归档，之前表里只有归档版）

### 工程

- **装了 pre-commit 钩子**（`sh tools/install-hooks.sh`，设 `core.hooksPath=hooks`）。
  脚本与 `hooks/pre-commit` 早就在仓库里，但从未跑过安装——于是"提交自动跑快速自验"
  一直是句空话。注意 `core.hooksPath` 是**本机配置、不随仓库走**，clone 后仍需各自跑一次
- **新增 `docs/PLAN-v1.9.md`**：v1.8.2→v1.10 的后续开发方案（工程债收口 / 扫弦方向标注 /
  练习量控制 / 听辨训练 / 曲式编排立项评估），含盘点结论、优先级、技术改动表、分阶段步骤、
  10 条风险与缓解措施

### 盘点结论（本次实测，一并记下）

- `node tools/check-all.js` → **8/8 全绿，4.6s**；行覆盖率 **99.9%**（2351/2354）
- **默认预设渲染经探针核实无 bug**：4 小节的格子数与宽度百分比、标签逐格一致。
  截图上「第 1 小节与 2–4 小节看起来不同」属低清读图误判——再次印证 DEVELOPMENT.md §5
  「量布局不要靠眼睛」这条规矩的价值
- **`*.workers.dev` 在沙箱内无法访问**：DNS 解析到无关 IP（本地解析器给 Dropbox 网段、
  8.8.8.8 给 Facebook 网段），典型污染特征。故本版**未能**验证 Cloudflare 线上是否已到 1.8.1，
  需人工在浏览器确认

## v1.8.1 · 修正：起跑预备改为整条抛物线复制（含水平分量）（2026-09-15）

用户实拍反馈：v1.8.0 的待命球预备动画是**直上直下**的——只复制了终端弧下落段的垂直分量，
水平方向钉死在落点正上方，读起来不像"跳进来"。本版把终端弧**整条抛物线**（起跳+下落，
含水平分量）完整平行复制到下一行：待命球从新行首 onset **左侧起跳**，与主球同相位、同高度、
同形变（直接复用主球本帧的挤压/拉伸值），小节边界同时触地。水平跨距 = 终端弧跨距、
封顶行宽 20%（长弧/窄屏不飞出视口）。交接时刻仍分毫不动；T41 改为逐帧对照复制抛物线
（容差 3px）+ 水平单调性 + 起跳/触地点断言，反向验证已跑。

## v1.8.0 · 体验：每小节开头，待命球复制上一小节末尾的下落弧（2026-09-15）

小功能，零机制改动——音频、onset 表、落点时刻、物理参数一律未动。跨小节接力时，上一小节的
球在行尾跳最后一弧（终端弧）；从该弧过顶点（apex）起，下一行起跑线上的待命球把这段**下落
半弧平行复制**到自己正上方：同一高度、同一条抛物线的下落段、同步下落，两颗球在小节边界
**同时触地**——随后正式球在同一点落地挤压接管，「下落 → 挤压 → 回弹 → 第一跳」速度连续，
接力变成双重奏。小节开头不再需要眼角余光去找行高亮：球是"落"进来的。静音拍训练（第 4 小节
纯靠视觉默数）是最大受益场景。

### 设计纪律

- **交接时刻分毫不动**：动画只作用在待命球的位移与形变上，终端弧终点（= 小节右缘音频
  时刻）不参与任何计算变更；tests T41 断言过界第一帧正式球已在新行首 onset（反向验证已跑：
  关掉下落后"起点同高/空中拉伸"断言变红，交接断言仍绿）
- **不提前抢戏**：终端弧前半（apex 之前）待命球保持原贴地停泊位——半透明"下一个是你"
  的提示语义不变，下落只在后半段发生
- **动效降级**：`prefers-reduced-motion` 下整段不下落，保留贴地停泊位（纯装饰第一个被降级）
- **不设新开关**：归并到现有「弹跳球」开关，不为纯装饰新增持久化字段

## v1.7.0 · 主题：新增「数据观测台」第二主题，经典主题逐像素保留（2026-09-15）

新功能，无行为变化——节拍内核、调度、物理、存储结构一律未动。顶栏新增「主题」切换钮，
在 **经典**（原有 Spotify 风荧光绿）与 **观测台**（工程师深色仪表盘：近黑深蓝底
`#0A0C12` / 面板 `#11141C` / 1px 半透明白描边 / 电光蓝 `#3B82F6` 主强调 / 青绿
`#2DD4A8` 进行态点缀 / 分区编号 01–06 + 大写英文标签 + 全等宽数字）之间切换。

### 实现方式（为什么敢说不碰原版）

- **经典主题样式一行未改**：观测台的全部差异收在 `body[data-theme="obs"]` 作用域的增量
  覆盖块里（`<style>` 末尾，块头附简短设计规范注释）；为它新增的 DOM（6 处分区编号角标
  `.sec-tag`、顶栏 `themeToggle` 按钮）在经典主题下 `display:none` 或不显式出现差异
- **持久化走独立冷键** `beatsight.theme`：不进 `beatsight.state` 热键，现有 5 个持久键的
  键名与数据结构零改动；写失败静默降级（视觉偏好不值得触发保存失败上报）
- **JS 仅两处触碰既有代码**：换拍脉冲与十六分闪烁的 WAAPI 硬编码绿色改为按主题查常量表
  `FLASH_THEME`（读 `body[data-theme]` 属性而非 getComputedStyle——热路径禁布局读取）；
  其余新增逻辑全部在装配区末尾（读键 → 应用 → 绑点击）
- **回归**：tests T38 断言默认经典、切换落 attribute、冷键读写、重载恢复、闪烁配色跟随，
  以及「切换主题只新增独立冷键、现有持久键一个不碰」

## v1.6.6 · 工程：补最小 wrangler.jsonc，修复 Cloudflare 构建一直失败（2026-09-15）

无新功能、无行为变化——`index.html` 只动了版本号一行（`const VERSION` 1.6.5 → 1.6.6，遵守
v1.6.4 起的发版规则）。这次修的是**部署链路**，不是页面本身。

### 修复

- **现象**：Cloudflare 侧 `Workers Builds: beatsight` 每次构建都是红色失败（起止时间同一秒的
  瞬时失败），线上长期停在 v1.6.0。回查 GitHub check-runs 发现**仓库里 4 个提交全部失败**，
  连 v1.6.5 之前的 `1cb60fc` 也不例外——所以与 ESLint / v1.6.5 那批改动无关，是纯配置问题
- **真因**：本项目在 Cloudflare 上是 Workers **静态资源**（Static Assets）托管，而 Workers 的
  assets **必须**由 Wrangler 配置文件声明资源目录（`assets.directory`）。此前仓库刻意不放任何
  Cloudflare 配置文件（`docs/DEVELOPMENT.md` §2 原文"零构建文件"），构建里的部署命令解析不到
  要发布的文件，当场失败 → 每次构建都红 → 生产环境一直停留在最后一次手工部署的版本
- **修复**：新增最小 `wrangler.jsonc`，只声明 `name` / `compatibility_date` /
  `assets.directory = ./dist`。**构建命令 / 部署命令 / 根目录仍全部在 Dashboard 里配**，仓库里
  依旧没有 `_headers` / `_redirects` / `functions/`，也没有 Worker 脚本（纯静态托管、Worker
  不参与请求）
- **同步修正文档**：`docs/DEVELOPMENT.md` §2 仓库树补一行 `wrangler.jsonc`，并把"仓库里也不放
  任何 Cloudflare 配置文件……保持'零构建文件'"改写成上面这条实际约束
- **产物仍不入库**：`dist/` 由 Cloudflare 构建命令现场生成（只拷 4 个产物），`.gitignore` 里
  早已忽略，`file://` 直开与"上站仍只有 4 个文件"的性质不变

### 注意（Dashboard 侧，仓库改不了）

- 「部署命令」若是 `npx wrangler versions upload`，它只会**上传一个版本、并不提升到生产**（那是
  非生产分支的默认值）；生产分支应为 `npx wrangler deploy`。此项只能在 Cloudflare Dashboard 改

## v1.6.5 · 工程：接入 ESLint 本地自验（可选加强项）（2026-09-15）

无新功能、无行为变化——`index.html` 只动了版本号一行（`const VERSION` 1.6.4 → 1.6.5，遵守
v1.6.4 起的发版规则）。落实 `docs/DEVELOPMENT.md` §6「已埋的技术债」里那条：*静态检查仍是自写
的窄规则集……要更全套就加 `package.json` + ESLint devDependency——只用于本地自验，不进产物*。

### 工程

- **新增开发期自验工具链**（`package.json` + `package-lock.json` + `eslint.config.js` +
  `tools/check-eslint.js`），唯一 devDependency 是 `eslint`。**运行时零依赖不变**：`index.html`
  仍可 `file://` 直开，上站产物仍只有 `index.html` / `sw.js` / `manifest.webmanifest` / `icon.svg`
  这 4 个文件
- **`tools/check-eslint.js` 是三件事的包装**：① 用现成的 `scan-util.extractScript()` 抠出内联脚本
  （ESLint 读不了 `.html`）；② 以合成路径交给 ESLint 10 的 flat config 跑；③ 把 ESLint 行号加回
  `index.html` 的行偏移（`htmlLine = eslintLine + 545`，即 `<script>` 所在行之前的新行数），
  打印 `index.html:<行>:<列>` + 出错行的 caret 框
- **规则口径「只漏报、不误报」**：只启用**当前 0 命中**的规则，让这道闸门开箱即绿、不留噪音。
  覆盖死代码 / 重复键 / switch 穿透 / 恒真条件 / 条件里赋值 / 变量遮蔽 / NaN·typeof 误用等的
  AST 与数据流检查；`no-empty` 开 `allowEmptyCatch` 放行 16 处刻意的静默降级 `catch(e){}`，
  `no-shadow` 白名单放行顶级 tick 助手 `D`（`Audio` 里两处有意重名的鼓音色变量）。
  **`no-undef` 故意关闭**（未装 `globals` 包）：宿主全局审计仍归 `check-lint.js` 手维护的
  `GLOBALS` 白名单，两者**刻意不重叠**，分工理由写在 `eslint.config.js` 文件头
- **接入 `tools/check-all.js` 第 4 步（现共 8 步）**：插在「零依赖 lint」与「DOM 引用」之间。
  它是**可选加强项**——发布链路（Cloudflare 自动跑 `check-all.js`）不保证先 `npm install`，
  所以缺 `node_modules` 时它主动打印「跳过」并 `exit 0`，**绝不会因为"没装开发依赖"堵住上线**
- **`.gitignore` 只忽略 `node_modules/`**：`package.json` / `package-lock.json` 要入库（前者声明
  devDependency，后者锁版本保证规则集可复现），并由注释放明这条边界
- **改正两处过时的文档断言**：`tools/check-lint.js` 与 `tools/scan-util.js` 的文件头原写「实测
  `npx --yes eslint@9` 直接被 SIGTERM（离线拉不到包）」——本次实测 registry 可达、`npm install`
  正常，该断言已证伪，两处改写为「零依赖硬闸门 vs ESLint 可选加强项」的真实分工

### 自验

- `node tools/check-all.js` 全绿 **8/8**（语法 / 架构约束 / 零依赖 lint / **ESLint（加强）** /
  DOM 引用 / 测试 / 看门狗 / 覆盖率），行覆盖率 **99.9%**（2305/2308，仍只剩 Audio 的 3 行防御分支）；
  ESLint 那步 0.70s
- **反向验证（证明这道新闸门真能拦住东西）**：往内联脚本注入 `if (window.__eslintProbe = 1) …`
  （条件里赋值 + 恒真条件）→ `tools/check-eslint.js` 立刻变红并报 `no-cond-assign` /
  `no-constant-condition`，**行号列号精确落在 `index.html:547:20`**（caret 对准 `window` 的 `w`）；
  同一份改动 `tools/check-lint.js` 却**全绿**——这正是它两条正则的盲区（`eqeqeq` 只认 `==`/`!=`，
  `no-undef` 的赋值正则排除前导 `.`），说明新增能力与既有能力确实互补。改动已按 sha256 原样还原
- **优雅降级验证**：临时移走 `node_modules` → `tools/check-eslint.js` 打印「⊘ 未安装 eslint，跳过」
  且 `exit 0`；`node tools/check-all.js --quick` 仍 8/8 全绿，证明"没装开发依赖"不影响任何产物

## v1.6.4 · 阶段三（工程深化）：快捷档抽取通用 preset row 组件 / 版本徽章对齐（2026-09-15）

无新功能，纯内部重构，外加一处早该做的版本号对齐（`index.html` 有改动，行为零变化）。落实
`docs/DEVELOPMENT.md` §6「已埋的技术债」里那条：*快捷档值 `CONFIG.speedPresets` 目前只服务 BPM；
若日后音量、拍号也要常用值，考虑抽成通用 preset row 组件，别复制三份*。

### 重构

- **共享区新增 `buildPillRow(host, items, opt)`**（与 `setPressed` 相邻，位于所有模块之前）：
  把「一行 pill 由值表生成」收成一处——清空容器 → 逐值建 button → 写 `aria-pressed` /
  `aria-label`（可选 `data-*`）→ 绑点击。`opt.text/label` 出文案与无障碍名，`opt.key`
  声明 `data-*` 键（不需要就略去），`opt.isOn` 定初始选中态，`opt.onPick` 是点击回调（blur 已代劳）。
  仍用 `createElement` + `appendChild` 而非 `innerHTML` 拼串：测试沙箱靠 `appendChild`
  记录子元素，才能断言生成数量、文案与点击行为。放共享区而非某模块内：将来 Presets / Editor
  也要用时不必再登记 R3 反向引用白名单
- **两个消费方改为复用**（原先同形态各写一遍，正是「别复制三份」的隐患）：
  - BPM 快捷档（`Controls` 的 `presetBtns`）：`CONFIG.speedPresets` 仍是唯一数据源，
    5 个按钮的 `dataset.bpm` / 文案 / `aria-label` 由组件统一产出
  - 奇数拍重拍分组（`refreshAccRow`）：改为 `buildPillRow($("accRow"), ACC_LABELS[S.sig], …)`，
    选中态由 `isOn` 表达，点击仍走「落值 → 重建行 → 持久化」
- **顺带补齐无障碍同源**：`syncBpmUI` 原先只改 `classList` 的 `.active`，现同时写 `aria-pressed`，
  与 `#sigRow` / `#swingRow` / `#timbreRow` 三组的既有做法对齐（T28 一直守着「视觉高亮与
  aria-pressed 一致」，BPM 行此前是漏网的第四组）
- 滑杆手绘刻度（`<i>` + 百分比 `left`）**未动**：它是刻度层不是 pill 行，仍直接读 `CONFIG.speedPresets`

### 测试

- **断言一条未改、总数不变：659 PASS / 0 FAIL**。T19（档位/文案/`aria-label`/点击直达/非档位
  不高亮/训练置灰）与 T31（`accRow` 生成 2 个分组档 + 点击落值）**原样通过**——组件化没有偷换契约
- **反向验证**：把 `buildPillRow` 里的 `host.appendChild(b)` 改成永不成立的条件后，T19 立刻变红
  （「生成 5 个快捷档按钮」期望 5 实际 0 等 3 条断言失败，随后取 `pills[0]` 抛错），改动已还原
- `docs/DEVELOPMENT.md` §6 该条从「待办」改为**已完成（v1.6.4）**；顺手改正同节一处过时表述
  （原文称静态检查「无 `no-undef`」、只有「四项 lint」，实际该规则自 v1.6.2 起已就位、现为五项）

### 版本号

- **徽章 v1.6.0 → v1.6.4**：`const VERSION` 停在 `1.6.0` 之后就再没动过——v1.6.1~v1.6.4 连发四版
  全都没 bump，于是**线上页面的版本徽章长期显示「v1.6.0」**。改动其实都上线了（v1.6.1 的 SW 缓存
  加固、本次的 `buildPillRow` 都在跑），但看号的人只会得出「部署没生效」的结论
- **确立发版规则（自 v1.6.4 起）**：每次发版都 bump `VERSION`，功能版与工程版一视同仁。版本号是
  用户唯一能看到的「这批代码是哪一版」的凭据，工程版跳过 bump 就等于让这个凭据说谎（对照：
  CHANGELOG 记到 v1.6.4、徽章写 v1.6.0，两者不自洽）。规则已写进 `README.md` 发布前清单、
  `docs/DEVELOPMENT.md` §5，以及 `index.html` 唯一真相源处那段注释
- 三处显示仍全部派生自这一行（`<title>` / 品牌区 / chip），T25 断言的是「形如 `x.y.z` 且三处一致」
  而非某个具体号，故 bump 前后均绿

### 文档

- **发布渠道描述改为「两条并存」**：`README.md` / `docs/DEVELOPMENT.md` 原文把发布写成「统一走
  WorkBuddy」，与事实不符——实际是 **① Cloudflare**（仓库接 Git，推 `main` 即自动构建部署，
  构建命令里串了全量检查，不通过即不部署）与 **② WorkBuddy**（只在使用者用 WorkBuddy 打开项目
  并发布时才更新）并存，后者**滞后是常态、不是故障**。两处已改为分渠道如实说明，并写明「判断线上
  是不是最新以 Cloudflare 为准」。这是纯文档改动，不占版本号（上站产物仍是那 4 个文件）
- **顺带划清两处易混边界**：「不用 GitHub Actions」≠ 没有机器门禁（门禁是 `tools/check-all.js`，
  它在 Cloudflare 那条部署路径上是强制的，WorkBuddy 与提交时不是）；`README.md` 里程碑表里
  v1.3.2「发布统一走 WorkBuddy」保留为历史记录，仅加注当前已扩为双渠道

### 自验

- `node tools/check-all.js` 全绿 **7/7**（语法 / 架构约束 / lint 五规则 / DOM 引用 / 测试 / 看门狗 / 覆盖率），
  行覆盖率 **99.9%**（2305/2308，仍只剩 Audio 的 3 行防御分支）
- `FULL_SCAN=1 node tests/run.js` → **659 PASS / 0 FAIL**

## v1.6.3 · 阶段三（工程深化）：弹跳球触顶钳制补测（2026-09-15）

无新功能，`index.html` 一字未改——只补一条此前仅"顺带覆盖"的断言。

### 测试

- **弹跳球触顶钳制补测**（`tests/cases/t30-wiring-and-lifetime.js` · T30 ⑧）：
  `Viz.paintBall` 的 `H = min(H, A.y + 6)`（「顶点不出容器空域」）此前只在 T30 ② 里跟着
  抛物线公式**顺带**复算，而默认 96 BPM 下未钳制跳高 46.9px 仅比上界 44px 高 2.9px，
  余量太薄——默认速度一改就会悄悄失效。新增场景把 BPM 降到 60（每弧 1.0s），
  未钳制跳高 48px 明显高于上界 44px，钳制成为唯一约束，断言：
  - 构造前提成立（上界 44px < `CONFIG.bounce.max` 48px，钳制确有生效空间）
  - 弧足够长（未钳制 48px > 上界 44px，T=1.00s）
  - **实测跳高 = 钳制值 44px**（而非未钳制的 48px），且明显低于未钳制值
  - 顶点始终未越过空域上界（最高点 y=−6 ≥ −6px）
  - **反向验证**：注释掉 `index.html` 里那行钳制后，该组断言如期变红
    （实测 48px ≠ 44px、最高点 y=−10px），改动已还原
- 断言总数 **653 → 659**，其余用例一处未动
- `docs/DEVELOPMENT.md` §6「已埋的技术债」同步改写：该条从"没单独构造触顶场景"改为"已补专门场景"

### 自验

- `node tools/check-all.js` 全绿 **7/7**（语法 / 架构约束 / lint 五规则 / DOM 引用 / 测试 / 看门狗 / 覆盖率），
  行覆盖率 **99.9%**（2288/2291，仅剩 Audio 的 3 行防御分支）
- `FULL_SCAN=1 node tests/run.js` → **659 PASS / 0 FAIL**

## v1.6.2 · 工程加固：测试套件拆分 / lint 第五规则 no-undef / 文档归档（2026-09-15）

无新功能，`index.html` 一字未改——本轮只动 `tests/`、`tools/`、`docs/`（工程与交接质量）。

### 工程

- **测试套件拆分**（`tests/`）：单个 `run.js`（含沙箱、桩与 43 个场景组）拆成
  `tests/lib/harness.js`（共享 vm 沙箱 + DOM/音频桩 + `drive`/`driveFrames` + 断言工具）
  与 `tests/cases/*.js`（8 个用例文件，每个文件一个场景组），`run.js` 瘦身为约 20 行的
  **装配器**（`CASE_FILES` 数组按 `require` 顺序决定执行顺序）。新增用例只需往数组末尾追加，
  既有用例的报错定位保持稳定
- **断言总数一个不少**：拆前拆后跑 `FULL_SCAN=1 node tests/run.js` 均为 **653 PASS / 0 FAIL**，
  断言全量 diff 为空；`hang-guard`（进程隔离看门狗）与行覆盖率工具**无需任何改动**即可继续工作

### 代码卫生

- **lint 增第五规则 `no-undef`**（`tools/check-lint.js` + `tools/scan-util.js`）：
  - `scan-util` 新增 `maskStrings()`（长度保持的字符串/模板内容掩码，避免把字符串里的标识符
    误认成代码）+ `collectDeclarations()`（全文件**扁平**声明表，不做作用域分层）
  - 规则本身取**最窄口径**：赋值目标 `name =`（排除 `==` / `=>`）指向未声明标识符 → `error`
    （隐式全局）；裸调用 `name(` 指向未声明标识符 → `warn`（大概率是宿主全局）。配 `GLOBALS`
    宿主白名单（语言内建 + 定时器/帧调度 + 浏览器宿主）
  - **设计取向是「故意欠报」**：假阴性（漏报）可以接受，假阳性（误报）不可接受——一个会误报的
    检查器最终只会被关掉。因此不做作用域分层（同名遮蔽等会被漏掉），只看「全文件压根没声明过」
  - **零假阳性**：当前代码 495 个声明 / 625 个作用域块 / 2557 行下全部通过；赋值目标命中 0 处，
    裸调用命中 14 处且**全部**是真实宿主全局（`String`/`Date`/`setTimeout`/`matchMedia`/`btoa` …）
  - **反向验证**：对 `index.html` 注入拼错变量名——`lastRulerElZ =` 如期报 `error` 并退出 1；
    `requestAnimatonFrame(` 如期报 `warning`。两类命中都抓得到
- `tools/check-all.js` 与 `docs/DEVELOPMENT.md` 的 lint 描述同步为「五条规则」，
  DEVELOPMENT 补一条反向验证命令

### 文档

- **`docs/PLAN-v1.5.md` 归档**：顶部加归档横幅——P0–P3 已全部落地（v1.4.1/v1.5/v1.6 均已发布），
  保留仅为追溯当时排期与取舍；现行约定以 CHANGELOG 与 DEVELOPMENT 为准
- **`docs/prd.html` 横幅纠偏**：原横幅写「已演进至 v1.0.0+」，与事实不符（早已 v1.6.x）。
  改为「v1.0 原始需求文档（历史存档），功能现状一律以 CHANGELOG.md 与 docs/DEVELOPMENT.md 为准」
- **`README.md` 文档表**：补 `docs/PLAN-v1.5.md` 一行，并给 PRD 与 PLAN 两处标注「已归档」
- **Editor `innerHTML` 残留审视**：核查确认 v1.6.1 已收口——全文件 `innerHTML` 仅剩「清空」语义
  用法（`= ""`）与注释，无模板拼接残留，本轮无需再改

### 自验

- `node tools/check-all.js` 全绿 **7/7**（语法 / 架构约束 / lint 五规则 / DOM 引用 / 测试 / 看门狗 / 覆盖率），
  行覆盖率 **99.9%**（2288/2291，仅剩 Audio 的 3 行防御分支）
- `FULL_SCAN=1 node tests/run.js` → **653 PASS / 0 FAIL**
- lint 反向验证：注入拼错变量名可被 `error`/`warning` 捕获（见上）

## v1.6.1 · 健壮性收口：SW 缓存加固 / 上下文重建清缓冲 / 导入护栏 / 提交门禁（2026-09-15）

无新功能，一轮防御性修复与流程加固。

### 修复

- **Service Worker 缓存加固**（sw.js）：导航与静态请求都只缓存 `r.ok` 的同源 http(s) GET 成功响应——原先 500/404 错误页会入缓存污染离线回退，跨源与非 http(s) 请求（如 chrome-extension://）进 `cache.put` 产生 unhandled rejection 噪音
- **AudioContext closed 重建路径补清 `noiseBuf`**：重建时 ctx/masterGain/时钟/onsetBuf 都重置了，唯独噪声 buffer 留着旧上下文的——木鱼/鼓组音色在重建后跨上下文复用 buffer 是否可用依赖浏览器实现（老 Safari 抛错），不再留这个侥幸
- **Editor 两处 `innerHTML` 模板拼接改 `createElement`/`textContent`**（小节标签、音符块库项）：数据本是常量无注入风险，但文件头明令「禁止 innerHTML 拼接」，未登记的例外会被后来者照抄到用户字符串上。此后全文件 innerHTML 仅剩「清空」语义用法
- **导入预设体量护栏**：文件 >2MB（change 事件按 `f.size` 快速失败，不进 readAsText）、文本 >2MB（进 JSON.parse 前拒绝）、条目 >500（进逐条校验前拒绝），均有可读错误文案；阈值入 CONFIG（`importMaxBytes`/`importMaxCount`）

### 流程

- **提交门禁**：新增 `tools/install-hooks.sh`——装一次即把 pre-commit 钩子指向仓库内 `hooks/`（core.hooksPath），每次提交自动跑 `check-all.js --quick`；本项目没有 CI，这一步把「全靠自觉」变成「默认拦截」。README 补发布前清单

### 测试

- 新增 T29c（4 断言）：wood 音色播放中 closed 重建后，送进 BufferSource 的噪声 buffer 必须出生自新上下文；反向验证：修复前该断言如期变红
- 新增 T5b（3 断言）+ T5 补 3 断言：体量护栏的文本/条目/文件三条路径，拒绝后 customs 不受影响
- 测试桩：`createBuffer` 产物标记出生上下文（`_ctx`），噪声 hit 记录 `bufCtx`

### 自验

`node tools/check-all.js` 全绿（653 断言）。

## v1.6.0 · 统计增强：导出 / 30 天视图 / 各节奏型速度纪录（2026-09-15）

- **导出练习记录**：统计 overlay 新增「导出记录」——JSON 下载（`beatsight-log-YYYYMMDD.json`），与预设导出同一通道；空记录时给提示而不是下载空文件
- **近 7 / 30 天视图切换**：`Stats.summarize(list, now, rangeDays)` 参数化（缺省 7，向后兼容）；30 天视图下横轴改标日期刻度（每 5 天一格 + 今天），避免 30 个星期几挤成糊；视图选择是会话内状态，重开 overlay 复位 7 天
- **各节奏型速度纪录**：按节奏型分组取最高 BPM，降序最多 3 条（「民谣扫弦 · 120 BPM」）——比全局纪录更有训练参考价值

### 测试

- 新增 T46（17 断言）：30 天口径含/不含边界、byPattern 同名取最高降序、rangeDays 缺省兼容、UI 切换与高亮同源、重开复位、导出序列化结构与空记录提示
- 测试桩复刻 `statsRangeRow` 静态 pill 组（与 sigRow/swingRow/timbreRow 同一模式）
- 反向验证：禁用 30 天分支 → 4 条断言如期变红
- T38 文案断言跟随更新（说明文案改为「柱高 = 当天练习分钟数」）

### 自验

`node tools/check-all.js` 全绿（638 断言）。

## v1.5.0 · 7 天爬升训练计划（2026-09-15）

路线图 M8 收尾项。把「继续上次」升级成「有计划地练」。

### 功能

- **生成**：训练面板新增「生成 7 天计划」——把当前起始/目标 BPM 切成 7 段（跨度 ÷7 取整），Day i 练 `dayStart(i)→dayTarget(i)`，Day 7 顶到总目标
- **今日卡**：计划进行中面板常显（即使训练开关关着——计划卡要在），显示「7 天计划 · Day 3/7 · 今日 84→88 BPM」+「开始今日训练」（一键写入当天参数并起播）+「退出计划」（确认弹窗后清除进度）
- **推进规则**：只有「练到当天目标自动停止」才算完成一天；中途手动停 → 当天不算、明天还练这段（缺练顺延，不搞补卡）；完成时状态栏与读屏播报「第 N 天完成 · 明天 X→Y BPM」
- **收官**：Day 7 完成 → 计划清空 + 「7 天计划完成 · 已达成 X BPM」
- **与「继续上次」互斥**：计划进行中 trResumeBtn 隐藏——计划本身就是接续机制，两个入口并存会抢语义
- 手动改参数不触碰计划（以手动为准；计划只负责给「开始今日训练」铺参数）

### 数据与测试

- 计划存热键 `beatsight.state.plan`（{baseStart, baseTarget, day}），加载走白名单抽取 + 域校验（起点 30–236、目标 ≤240 且 > 起点、day 1–7），脏计划整条丢弃
- 新增 T45（24 断言）：生成切分、今日参数写入与输入框同步、手动停不推进、完成推进、持久化、Day 7 收官清空与文案、退出流程、无计划时点退出不弹窗、与 trResumeBtn 互斥、脏计划回退
- 反向验证：把完成推进（`S.plan.day++`）变异为自赋值 → 3 条断言如期变红

### 自验

`node tools/check-all.js` 全绿（626 断言）+ 无头截图控制台 0 消息。

## v1.4.1 · 修：木鱼/鼓组响度不足——滤波噪声路径补 makeup gain（2026-09-15，用户实拍反馈）

用户报：「木鱼调到最大音量依然听不清；鼓组略小；电子正常。」

### 根因（定量）

三条噪声路径（木鱼三档 / 军鼓 / 踩镲）与振荡器路径共用同一个包络峰值，但**白噪声经窄带滤波后能量大幅衰减**，滤波路径没有做任何补偿（缺 makeup gain）。以 click 正拍为参照推算：木鱼带通 Q=8 ≈ **−23dB**（幅度 1/14，叠加 decay 30ms 比 click 的 90ms 更短促），军鼓 ≈ −14dB，踩镲 ≈ −7dB；click 与底鼓走振荡器无滤波损失。与听感逐项吻合。

### 修法

- `CONFIG.timbres.wood` 加 `makeup:14`（decay 顺带 30→60ms）；新增 `CONFIG.drumMakeup = { snare:5, hat:2.5 }`
- `noiseHit()` 送增益改为 `min(makeup, peak × makeup)`；上限取 makeup 本身——它等于「满音量重拍」在该路径应达到的增益，天然是天花板（peak 在 playClick 已钳 ≤1，只有脏值穿透才需要它）
- **增益 >1 的安全性**：削波看信号幅度不看增益值；滤波后噪声 RMS 仅为峰值的 3%–14%，放大 14 倍后输出幅度仍远低于 0 dBFS。振荡器路径（click/底鼓）维持 ≤1 钳制不变（T18 不变量原样通过）
- 层级保持：同音色各档 makeup 一致，重拍 > 正拍 > 细分的相对关系不变

### 测试

- 新增 T44（10 断言）：三路径补偿值精确断言、层级保持、底鼓/click 不受影响、满音量顶到天花板不越界、脏 makeup 回退不补偿
- 反向验证**揪出一个真问题**：首版 T44 从 CONFIG 读 makeup 算期望值——自指断言，把 makeup 改成 1 后两边一起变、测试照样全绿（假绿）。改为硬编码规格值后重做反向验证：变异体 6 条如期变红
- 测试桩增强：噪声链路 src→filter→gain 的增益峰值现在能被记录（原先只记到 filter 为止）

### 自验

`node tools/check-all.js` 全绿（600 断言）。**真人试听校准待做**：makeup 是理想滤波器模型的推算值，最终听感（尤其手机小喇叭对 1500–2000Hz 的表现）需人耳确认后微调系数。

## v1.4.0 · 练习闭环 + PWA 离线 + 后台保活 + 训练接续（2026-09-15）

路线图 M8 四件事一次做完：练习统计、PWA 离线安装、后台持续发声收尾（保活侧）、训练计划第一刀（上次训练一键继续）。

### 练习闭环（原路线图第 1 优先级）

- **自动记录**：停止播放时，有效练习（**≥30 秒**且非编辑器试听）自动入账到新的冷键 `beatsight.log`（`{v:1, sessions:[{t, sec, bpm, name}]}`）。短于 30 秒视为误触/试音，不进统计。试听（S.preview）明确排除——编辑节奏型不算练习
- **时长取音频时钟差**（`ctx.currentTime`），不用 `Date.now`：时钟挂起（来电/锁屏/iOS 后台）期间练习本就等于暂停，不应计入
- **冷键语义**：只在停止时立即落盘，不进 250ms 防抖热键；环形截断最近 400 场（`CONFIG.logMax`）；加载即校验，脏条目丢弃
- **统计 overlay**（顶栏「练习统计」入口）：本周时长（周一起）/ 连续天数 / 速度纪录 / 累计场次四卡 + 近 7 天条图（纯 div，不引图表库）+ 清除记录（确认弹窗）
- 连续天数口径：**今天没练不归零**，从昨天往回数；汇总 `Stats.summarize(list, now)` 是纯函数，`now` 可注入（测试用固定时钟断言）
- 无障碍与焦点陷阱复用既有机制：overlay 打开时背景 inert、Esc 关闭、空格不误触播放

### PWA 离线安装

新增三个独立文件：`manifest.webmanifest` / `icon.svg` / `sw.js`。**这是对「单文件」原则的首次、也是唯一一次妥协**，由两个客观限制决定：

- Service Worker 只能以同源 URL 注册，**Blob URL 注册被浏览器禁止**（原 DEVELOPMENT.md 里「manifest/sw 用 Blob 内联注册」的设想在 SW 上不成立）
- manifest 内图标按 manifest 自身 URL 解析相对路径，Blob 形态下解析失败

`index.html` 里的注册逻辑按协议收口：**仅 `http(s)` 才注入 manifest link 并注册 `sw.js`；`file://` 下整段跳过**，单文件双击直开零副作用。SW 策略：导航请求 network-first（线上更新刷新即生效，不必手工 bump 缓存版本）、断网回退缓存的 index.html；静态资源 cache-first。

### 后台持续发声收尾（保活侧）

- 调度侧阻塞点 v1.3 已打通（自适应前瞻窗口 + 回前台补排 + 饥饿兜底）；本版补**系统侧保活**：新开关「后台保活」（默认关，持久化到热键）
- 两级策略：优先 `navigator.wakeLock.request("screen")`（页面隐藏时系统自动释放，回前台经 visibilitychange 重新申请）；不支持或被拒时降级为**程序生成的 0.5s 静音循环 WAV**（`document.createElement("audio")`，零采样文件原则不变）
- `KeepAlive.sync()` 挂在 start/stop 上（白名单 R3：Controls→KeepAlive），状态迁移即同步
- **仍需真人验收**（无法自动化，同 v1.3 的约定）：真实浏览器播放 → 切后台 30 秒 → 回来确认不断音~~；iOS 上开关打开后锁屏/切后台的保活效果~~（2026-09-15：桌面 Chrome 验收通过；iOS 已明确不作为目标平台，不再跟进）

### 验收与平台决策（2026-09-15 追加）

- 桌面 Chrome「切后台 30 秒无断音」真人验收**通过**——v1.3 起的「后台持续发声」阻塞项至此完全关闭
- **iOS 明确不作为目标平台**（用户无 iOS 设备、以后也不考虑）：iOS 专属验收项不再跟进；代码里的静音 WAV 兜底与 ctx 状态监听保留（对无 wakeLock 的桌面/Android 浏览器同样有效）

### 训练计划第一刀：上次训练一键继续

- 训练开着时结束播放 → `Trainer.consumeSession()` 收成 `{reached, done}` 存入 `S.trainer.last`（随热键持久化，加载走白名单校验、reached 钳 30–240）
- `done` 由 `trDone` 显式标记，**不靠 reached≥target 推断**——手动停在目标级但没练满不算完成
- 秒停不记录（一个小节边界都没过视为误触），防污染接续点
- 「继续上次」按钮放开关行而非 tr-panel 里——面板在训练关闭时是隐藏的，最需要它的恰恰是还没开训练的那一刻。未完成 → 起始推进到上次练到的级别并起播（爬坡语义：起点跟进度走）；已完成 → 原配置再来一轮

### 架构与测试

- 模块扩到 10 个：`Stats`、`KeepAlive` 排在 Editor 之后，`tools/check-module-order.js` 的 EXPECTED_ORDER 与白名单同步登记（Controls→Stats ×2、Controls→KeepAlive ×2）
- 测试桩补三样：`document.head`（manifest link 落点）、`createElement("audio")` 最小 play/pause（静音兜底路径）、可注入的 `location`/`navigator`/`btoa`（PWA 与保活分支）；T28 的开关计数断言 4→5
- 新增 T37–T43（7 场景 60+ 断言）：汇总口径固定时钟断言 / ≥30s 入账 / 秒停与试听排除 / 400 截断与脏条目 / 训练完成与中途停收成 / 接续行为与脏 last 回退 / wakeLock 申请释放与静音兜底 / PWA 协议收口
- 反向验证三刀全部如期变红：① logMinSec 改 99999 → T38 入账断言红；② 禁用 start 推进 → T41 三条红；③ 禁用 sync 与 https 判断 → T42/T43 八条红
- 结果：590 断言全绿；行覆盖率 99.4%（KeepAlive 分区曾因 vm 沙箱缺 btoa 掉到 84.9%，桩补上后回到阈值内）
- 期间顺手修了 T38 自身的健壮性：入账断言失败后读 `rec.sec` 会把整套测试炸掉（遮蔽后续所有场景），改为失败也继续报数

### 自验

`node tools/check-all.js` 全绿（语法 / 架构约束 / lint / DOM 引用 / 全量测试 / 看门狗 / 覆盖率）；无头 Chrome 桌面截图主界面与统计 overlay 各一张，控制台 0 条消息。



用户报："播放过程中调整 BPM，球的动画就跟不上播放头的动画了，只有暂停再重新开启才一致。"

## v1.3.4 · 修：播放中改 BPM 后播放头与弹跳球永久脱轨（2026-09-14，用户实拍反馈）

用户报："播放过程中调整 BPM，球的动画就跟不上播放头的动画了，只有暂停再重新开启才一致。"

### 先复现并量化（三行数字说清）

拿测试骨架切了个一次性诊断脚本，逐帧采样「播放头位置」与「球的 x」换算成同一单位（小节内 tick）：

```
改速前                    播放头=86.02t  球=86.02t  差=0.00t   ← 完全同步
→ 播放中改 96 → 200 BPM
  改后 0.1s              播放头=102.02t  球=93.70t   差=-8.32t
  改后 0.3s              播放头=134.02t  球=109.06t  差=-24.96t
  改后 1.5s              播放头=134.02t  球=84.19t   差=-49.82t  ← 不再收敛
  改后 6.0s              播放头=86.02t   球=36.19t   差=-49.82t  ← 永久锁死
```

**永久偏 −49.82 tick ≈ 1.04 拍**；连拖 20 次滑杆后偏 −37.3 tick。与用户描述完全吻合。

### 根因

`setBpm` 在播放中重映射 `loopStart`，让"**时钟推出来的位置**"在那一瞬连续。但**已经排进音频时钟的 onset 绝对时刻无法回改**——它们是按旧 spb 算出来的。

于是「前瞻窗口（150ms）+ 最多一颗音符时长（96BPM 四分 = 625ms）」这段里，两边以不同速率前进，累积出错位；此后已排程的 onset 换成新 spb，两边**同速**前进 → 错位**永久保留**。只有重开播放（清 onset 表 + 重建时间轴）才恢复——正是用户观察到的现象。

训练器「自动爬坡」走的是同一条 `setBpm` 路径，所以同样受影响。

### 修法：让播放头与球共用同一份 onset 表

1. **新增 `Viz.audioPosAt(now)`**：从 onset 表插值出当前的「小节 + 小节内 tick」，**与弹跳球同源**（同 prev/nxt 端点、同终端弧算式），于是两者从结构上不可能分叉——和「全量/增量共用 `setCell`」是同一个思路。onset 表覆盖不到时（预备拍、刚启动）返回 null，由原来的时钟公式兜底。
2. **`setBpm` 改锚在「排程游标」`nextNoteTime`**：游标处的 tick 在旧时间基下是已知的，且等于那颗音符真实的 `cumT`——也就是不变量 `nextNoteTime == loopStart + tick×spb/TPB` 成立的地方。锚在这里，这条不变量在新 spb 下继续成立，`resyncToNow`（靠它算「就地接续」点）才不会算错。锚在 `currentTime` 会破坏它。

**中途踩到的一个坑**：`audioPosAt` 第一版漏了「缓冲区里没有未来端点时回退到 `onsetNext`」——而 `paintBall` 有这个回退。于是两边一个走「正常弧」一个走「终端弧」，产生 14 tick 的瞬态（0.6s 后收敛，但肉眼可见）。补齐后逐帧差 = 0.00。

### 回归测试 T36（6 条 + 3 条不变量）

每帧采样两个位置的最大偏差，容差 1.5 tick（终端弧上球停在行右缘 −10px、播放头在 100%，2px ≈ 0.64 tick 是既有设计差异）：

| 场景 | 修复前 | 修复后 |
|---|---|---|
| 静止播放（基线） | 0.00 | 0.00 |
| 96→200 BPM | 永久 49.82 | **0.65** |
| 连续 20 次改速 | 37.26 | **0.64** |
| 200→30 BPM | — | **0.14** |
| 30→240 BPM | — | **0.62** |

另加 3 条不变量断言（改速后 `onsetNext` 仍在游标处、时间基推出的游标 tick == 真实 tick）。

**反向验证**（硬规矩）：把三处修复分别退回，确认断言真的会失败——

- 退回播放头公式 → T36 失败（48.98 tick）✓
- 只退回 `onsetNext` 回退 → T36 失败（151.58 tick）✓
- 退回 `setBpm` 锚点 → 播放头断言**全绿**（因为它已不依赖 `loopStart`）→ 这才发现锚点修改原本毫无覆盖，于是补了那 3 条不变量断言；补完后退回即失败（推出 662.527 vs 真实 624.000）✓

### 自验

`node tools/check-all.js` 七项全过，测试 **515/515**，覆盖率 99.8%（Viz 段仍 100%）。
真实浏览器核验：无头环境音频时钟被冻结（`currentTime` 恒 0、调度器一个音符都排不出），
因此**主路径只能由确定性测试覆盖**（那条路径跑的是真实 `scheduler` + 真实 `paintFrame`，只把时钟换成假的）；
冻结状态下截图确认**兜底路径行为与改动前一致**（播放头 0%、球停在第一格），无视觉回归。

## v1.3.3 · 预备拍拍数输入挪到它所属的开关旁（2026-09-14，用户实拍反馈）

用户截图提问「这有什么问题」。**逐像素核对了半天，技术上其实一切正常**——先把这半边结论记下来，因为它们排除了更可怕的猜测：

| 核对项 | 实测 | 应当 | 结论 |
|---|---|---|---|
| 预备拍 文字亮度 | 峰值 254 | `--t1 #FFFFFF`(255) 开启色 | ✓ |
| 弹跳球 文字亮度 | 峰值 179 | `--t2 #B3B3B3`(179) 关闭色 | ✓ |
| 弹跳球 旋钮位置 | x 288–303（轨道 286–322） | 关闭位 `left:2px` | ✓ |
| 四个开关的 class / aria / 轨道色 / 旋钮 left | on→绿+18px，off→`#4D4D4D`+2px | 一致 | ✓ |
| 桌面宽度下的排列顺序 | 静音拍→变速训练→预备拍→弹跳球 | 与标记一致 | ✓ |

（中途一度以为"静音拍/变速训练的旋钮也在右边"，是小图看错了；用 `getComputedStyle(sw,'::after').left` 一问浏览器就清楚了——**别用眼睛读小截图，去问计算样式**。）

**真正的问题是信息架构**：那个「4」是**预备拍的拍数**，但它排在 `.config` 行的**最末**（弹跳球之后），而且**没有任何可见标签**（只有给读屏用的 `aria-label`）。于是打开预备拍时，行尾会凭空冒出一个孤零零的「4」，视觉上完全像是「弹跳球 · 预判提示」的参数。

- 把拍数输入移到「预备拍」开关**紧后面**，并补一个可见单位「拍」→ 现在读作 `预备拍 [开关] [4] 拍`，是一个完整的语义单元
- 隐藏逻辑随之从输入框移到外层容器（`.inp-with-unit`）。**坑**：`[hidden]` 的 `display:none` 会被类选择器的 `display:flex` 覆盖，必须显式写一条 `.inp-with-unit[hidden]{display:none}`——和既有 `.tr-panel[hidden]` 是同一个坑
- 补两层断言锁住位置：标记层（拍数容器夹在 `countInToggle` 与 `bounceToggle` 之间）+ 运行时（开关切换时容器显隐），免得以后又被挪回行末

顺带记录一个**未改**的观察：「弹跳球 · 预判提示」是纯显示设置，却被放在「训练模式」这一组里（与静音拍/变速训练/预备拍同组）。分组是否要调整属于设计决策，没有擅自改。

### 自验

`node tools/check-all.js` 全部通过：语法 ✅ · 架构约束 ✅ · lint ✅ · DOM 引用 ✅ · 测试 **506/506** ✅ · 看门狗 ✅ · 覆盖率 99.8% ✅
视觉确认：重渲染后该行为 `静音拍 · 每 4 小节 [关]  变速训练 · 自动爬坡 [关]  预备拍 [开] [4] 拍  弹跳球 · 预判提示 [关]`

## v1.3.2 · 发布渠道收口：移除 GitHub Actions，改由 WorkBuddy 发布（2026-09-14）

用户决定：**以后发布应用只用 WorkBuddy**，不再走 GitHub Pages / GitHub Actions。

- **删除 `.github/workflows/test.yml` 与 `.github/workflows/pages.yml`**，连同整个 `.github/` 目录。在线版改由 WorkBuddy 发布：<https://beatsight-68235.app.workbuddy.host/>
- **新增 `tools/check-all.js`**：CI 没了，"每次改动都跑一遍机器检查"这件事必须有本地入口兜底，否则那些检查只会在我记得的时候才跑，等于没写。一条命令按**先便宜后贵**的顺序跑完 7 项（语法 → 架构约束 → lint → DOM 引用 → 全量测试 → 死循环看门狗 → 行覆盖率），约 6 秒；前面失败就停，因为后面的检查建立在前面是对的之上。`--quick` 跳过 T21 的 243 组全量扫描（约 2 秒）
- README 的「快速开始」改为：在线版链接 + 本地双击 + 开发者一条命令；不再提 GitHub Pages 的 Settings 前置条件
- DEVELOPMENT.md：文件结构去掉 `.github/`、加上 `check-all.js`；§5 自验流程改为以 `check-all.js` 为入口；技术债里新增一条**"检查全靠自觉"**——移除 CI 后没有任何机制强制跑它，上线前那一步要真的跑
- tests/README.md 里的 CI 表述同步改写

**取舍说明**：CI 的价值是"没人能跳过"。移除它换来的是一套更简单的发布路径（不用维护 workflow、不用管 Pages 设置、不用 GitHub 账号权限），代价是门禁靠自觉。补偿办法就是 `check-all.js` 把七项检查压成一条命令——**跑它的成本低到没有理由跳过**。

**留档**：CHANGELOG 里更早的版本条目（v1.0 起的 CI 上线、v1.3 的 Pages 部署等）保持原样不动——那是历史记录，改掉就等于伪造当时发生过什么。

### 自验

`node tools/check-all.js` 全部通过：语法 ✅ · 架构约束 ✅ · lint ✅ · DOM 引用 ✅ · 测试 **502/502** ✅ · 看门狗 **41 PASS / 0 死循环** ✅ · 覆盖率 **99.8%** ✅

## v1.3.1 · 遗留收口：覆盖率、球体物理断言、接线层补测、旧键清理（2026-09-14）

v1.3 收尾时列的 4 项遗留，处理掉 3 项，第 4 项（后台持续发声的真人验收）确认**无法自动化**并写进了自验清单。

### 覆盖率：用 Node 内置的 V8 覆盖率，零依赖拿到 99.8%

审计 P1-7 的最后一条子项是「无覆盖率统计——**无法回答"渲染层到底覆盖了多少"这个本次审计的核心问题**」。P0-1（渲染层异常导致 rAF 循环死亡）能从 CI 漏出去，就是因为没人能量化"Viz 有没有被执行过"。

- 没用 c8 / nyc / istanbul，**改用 `NODE_V8_COVERAGE`**——Node 自带，连 CI 工具都保持零依赖（本沙箱 `npx` 拉包本来就会被 SIGTERM，装了也验证不了）。实测 `vm.Script` 编译的沙箱脚本同样会被采集（`index.inline.js`，236 个函数）
- 新增 `tools/check-coverage.js`：按行判定（取"包含该行首字符、start 最大"的嵌套区间），按模块分区统计，未覆盖行给 **index.html 绝对行号**（V8 的偏移是相对内联脚本的，直接报脚本内行号会把人引到错的地方——实测误导过一次）
- **双阈值**：总 97% + 分区 90%。只看总数会掩盖"某个模块烂掉、另一个模块补偿"
- 当前 **99.8%**（12 个分区里 11 个 100%），唯一未覆盖的 3 行是 `scheduler` 的 `MAX_SCHED_STEPS` 硬上限分支——单轮调度要处理超过 512 个音符才会触发，属刻意保留的防御性代码，不为了数字去造人工状态点亮它

### 覆盖率工具指出的最大空白：整个「接线层」

第一次跑出来只有 89.7%，最低的 Presets 才 66%。看未覆盖行才发现问题不是"逻辑没测"，而是**事件处理器体整层没被跑过**——既有测试都直接调模块函数（`setBpm`、`setSig`），而 `$("sigRow").addEventListener("click", …)` 里读 `btn.dataset` 的那段接线从没执行过。

补完这一层后到 99.8%，并且真的抓到缺陷。新增场景：

- **T30 弹跳球物理逐帧数值断言**：这是 Viz 里最容易悄悄改坏的一块（有真实的手感迭代历史，改一个系数人眼很难发现）。断言落点贴音符块左缘（±2px）、弧内每帧 y 符合 `y=yBase−H·4p(1−p)`、实测跳高 = 公式值、触地挤压/空中拉伸、影子随高度变小变淡、接力待命球只在终端弧出现、reduced-motion 去形变但保位置
- **T31 交互接线**：三组 pill 真实点击（含点到空白处提前返回）、动态重拍分组档、音量滑杆、快捷速度档、播放键与空格键、±5 步进、BPM 弹窗、TAP 测速、预设列表、导入导出、删除预设、编辑器调色板/撤销/复制/清空、Esc/Ctrl+Z、试听开合、弹窗两种取消路径
- **T33/T34/T35 补完剩余接线**：一键切回、导入失败原因、窗口 resize（播放中只重采几何缓存不打断动画）、弹窗键盘两条确认路径、编辑器选中→替换语义→删除、v0.4 老数据 `sel` 下标→id 迁移（含越界回退）、组标签高亮、`predictNext` 全休止回退、`resyncToNow` 顺延下一小节

### 测试桩又修了 5 处（每处都揭出假绿或假红）

| 桩缺陷 | 后果 |
|---|---|
| `fireWin`/`fireDoc` **丢掉事件载荷** | 键盘/文件类接线（空格键、Esc、导入文件）`e.code`/`e.target.files` 永远是 undefined → **永远不匹配而静默通过** |
| `setTimeout` 不记录回调 | 藏在防抖里的真实路径（resize 重建、TAP 文案复位、导出后 revokeObjectURL）永远跑不到 |
| `FileReader` 是空构造函数 | `importFile` 处理器一调 `readAsText` 就 TypeError，导入接线没法测 |
| 无 `matchMedia` | `REDUCE_MOTION` 加载期求值，动效降级分支测不了 |
| `offset*` 恒为 0 | 弹跳球"顶点不出容器空域"的钳制算出**负跳高**（球向下飞），抛物线断言全是假的 |

补了 `pill()`（模拟点击带 `data-*` 的按钮）、`runTimers()`（手动冲刷定时器）、`click()`、`setNow()`、以及**行几何模型**（行按创建序分层，格子 left/width 由 style 百分比反解）。

### 旧键清理：不再让老用户永远背着一个 715 KB 的废弃键

`beatsight.m2` 在 v1.3.0 起已降级为只读的迁移来源，但迁移完不删——500 个预设的整包最大 715 KB 会一直留着。现在拆分成功后删除它，靠两道保证：

1. **写后校验**：写完立刻读回来确认能解析出预期结构，读不回就不删
2. **必有备份**：删除前确保 `beatsight.m2.bak` 存在（已存在则不覆盖——保留最早那份最原始的数据，可能是 v1 浮点格式）

触发条件只认"热键缺席"（真正从 v1.3 之前升级上来）。**"热键在、冷键缺"的半迁移状态不动旧键**——那种情况下旧键内容可能陈旧，按它重建会把用户已删掉的预设复活。写后校验未过（如配额超限）时也保留旧键，下次启动重试。

### 未处理（已确认无法自动化）

**后台 30 秒不断音的真人验收**：`setInterval` 的后台节流是**浏览器层面的行为**，无头环境里页面始终被视为可见、不会真的节流；脚本改写 `document.hidden` 只能验证窗口切换逻辑（那部分已由 T26 覆盖），验证不了"浏览器真按 1s 节流时还够不够"。已把这条（连同"后台切回后点下即生效最多延迟 1.2s"）写进 DEVELOPMENT.md §5 的必须人工验收清单。

### 自验

- 语法校验通过；**tests 502/502 PASS**（35 个场景组，抽样与全量两种模式）
- 看门狗 41 PASS / 0 FAIL / 0 死循环超时
- `tools/` 四项检查全通过；**行覆盖率 99.8%**（总阈值 97% / 分区 90%）
- 覆盖率工具自身也做了反向验证：把分区范围写成互相重叠时立刻暴露（"各段总数各不相同"），修正后才得到合理的分区表
- 无头 Chrome 截图 + 控制台零报错

## v1.3 · 审计第二/三梯队：把「承诺」变成「可执行检查」（2026-09-14）

接 v1.2.4 的第一梯队，本次处理审计报告的第二梯队（4 项）与第三梯队（7 项）。主题不是加功能，而是**把项目里那些"靠注释和自觉维持"的东西变成机器检查**——审计的结论原话是：现在缺的不是设计，而是「一条把架构规则和输入校验变成代码的流水线」。

### 第二梯队

**P1-5 持久化冷热分离（消除主要的主线程阻塞源）**

原实现把整个预设库塞进**一个** key，而 `persist()` 挂在几乎每个交互上（调速、每个开关、TAP、拍号、音色、音量）。实测载荷 10/100/500 个预设 = 14 KB / 143 KB / **715 KB**——每次点击都要全量 `JSON.stringify` + 同步写盘，主线程阻塞 5–20ms，**而这正好会触发音频掉音**。

- 拆成两个 key：`beatsight.state`（热，< 1 KB，每次交互写，250ms 尾部防抖）+ `beatsight.customs`（冷，只在预设增删改时**立即**写——丢掉一个手写节奏型代价太大）
- 旧键 `beatsight.m2` 降级为**只读的迁移来源**：旧键存在而新键缺失时一次性拆分写入 + 备份 `.bak`
- 防抖窗口内不丢：`visibilitychange`（转隐藏）与 `pagehide` 都强制 `flush()`
- **写失败不再静默**：`catch(e){}` 改为上报 → 顶栏状态点变红 + chip 文案改「保存失败」+ 一次性弹窗。原先用户攒了几十个预设却存不进去时**毫无察觉**

**P1-3 后台播放：自适应前瞻窗口（打通 M8 的技术阻塞点）**

调度时钟跑在主线程 `setInterval(25ms)` 上，而浏览器对后台标签页的定时器节流下限是 **1000ms**（Chrome 隐藏 5 分钟后还有 intensive throttling，约 1 次/分钟）。固定 150ms 窗口在后台等于「每次唤醒排 150ms 的音，然后静音 850ms」→ 后台播放必然断续。

- 窗口按可见性自适应：前台 150ms（低延迟）/ 后台 1.2s（> 节流下限，留 20% 余量）
- 回前台（`visibilitychange`）**立即补排一次**，不等下一个 25ms 周期；顺序上「先补排、再收窄窗口」
- **调度饥饿兜底**：游标落后实时时钟超过一个窗口时，**以当前时刻重新锚定**整条时间轴，而不是把过去几十秒的音符一次性排到"现在"（那听感是一坨同时爆响）。用「重新锚定」而非「按整循环前移」：前移粒度至少一小节（慢速 7/4 可达 16s），很容易落到窗口之外反而多出一整格静音
- **行为变化**：后台下已排入的音符无法撤销，「点下即生效」在后台最多延迟一个窗口（1.2s）；前台不受影响

**P1-4 渲染性能：几何缓存 + 增量重绘**

- **帧内零 `offset*` 读取**：几何一律读缓存 `rowGeo`，只在 `buildViz` 末尾与 `resize` 后采集。原先每帧先写 7 处 style、紧接着 `paintBall` 里去读 `offsetLeft/offsetWidth`——浏览器被迫在**帧中途做一次同步重排**（layout thrashing，rAF 里最典型的性能反模式）
- **增量重绘**：换音符时只改变化的那几格（约 6 次 className 写入），只有换小节才全量重扫。原先每次换音符都双层循环重写 4 小节全部格子（Funk 十六分 = 128 次写入/换音符，240BPM 下约 2000 次/秒）
- 全量与增量**共用 `setCell()` / `setGroup()`**，所以「增量结果 == 全量结果」是结构性保证而非巧合

**P1-7 CI 强化：把架构规则和输入校验变成流水线**

- **新增 `tools/check-module-order.js`**：「任何模块不得反向引用后方模块」此前**只存在于注释里**，没有任何机器检查。本次先把它精确化为三条可执行不变量——R1（IIFE 顶层执行期，零例外）/ R2（每帧渲染热路径，零例外）/ R3（运行时回调，白名单逐条登记且条目失效也报错）。检查器首次运行就发现审计报告自身的一处不一致：报告把 `scheduler` 划进 R2，却又把其中的 `Audio→Trainer` 归为「合法的运行时调用」；同时报告还**漏数了 2 处**（`Audio→Presets`、`Controls→Editor`）
- **新增 `tools/check-lint.js`**：零依赖实现 no-var / eqeqeq / no-redeclare / no-unused-vars。不用 ESLint 的原因是**本沙箱 `npx eslint@9` 直接被 SIGTERM**（离线拉不到包），而把一个无法验证的 lint 步骤塞进 CI 比没有 lint 更糟——它要么常年红，要么被人关掉。作用域按真实花括号块划分（按缩进会把两个兄弟函数体里的同名变量误判成重复声明）
- **新增 `tools/check-dom-ids.js`**：`$("x")` 悬空引用检查（审计时是人工核对的，现在机器化）。顺带确认了审计发现的那 2 个纯样式钩子（`editorCardTitle` / `editorDot`）
- actions 全部**固定到 commit SHA**（并注明对应版本）、显式 `permissions: contents: read`、失败时上传测试输出为 artifact
- **为了消除热路径反向引用，改用了"装配层注入钩子"**：`onFrameError`（Viz 异常恢复不再直接调 `Controls.stop()`）。与 `Store.setPersistFailHandler` 同一套模式：模块只暴露回调，跨模块装配交给 init 段

### 第三梯队

- **P2-9 版本号单一真相源**：`const VERSION` 一处定义，`<title>` / 品牌区 / chip 三处由它派生；标记段（去注释）零硬编码版本号已成为断言
- **P2-10 抽取重复逻辑**：`selectedPreset()`（原在 Presets 写了两遍）、`defaultAccents(meter)`（原在 basicPattern 与 Editor 各写一条阶梯）、tick 具名常量 `T16`/`T8`（`12`/`24` 在同一文件里既当 tick 又当 CSS 像素值出现几百次）
- **P2-11 无障碍分级改造**：`#statusText` 每换一个十六分音就更新，**绝不能挂 `aria-live`**（读屏刷屏）——另设 `#srAnnounce` 只在「开始/停止/训练完成」写入；4 个开关补 `role="switch"` + `aria-checked`，并把「改 className」与「改语义」收成同一个 `setToggle()`（原先 4 个开关各写一遍 className，加语义后必然漂移）；三组 pill 补 `aria-pressed`（`setPressed()` 让视觉与语义同源）；播放键标签随状态变化；`prefers-reduced-motion` 下关装饰性动画、并把弹跳球的挤压/拉伸置为无形变（**只降形变、保留位置**——球何时落地是核心功能提示）；弹窗/编辑器打开时背景置 `inert`，用**重算**而非置位（嵌套弹窗不会提前摘掉）
- **P2-12 PWA 元信息 + Pages 部署**：补 `theme-color` / `description` / SVG data-URI icon / apple-touch-icon 相关 meta；新增 `.github/workflows/pages.yml`；**并修正 README 里"照做会 404"的 Pages 指引**（改为写明前置条件：需在 Settings → Pages 把 Source 设为 "GitHub Actions"）
- **P2-13 音频生命周期补全**：补 `ctx.onstatechange`——认 iOS 的 `interrupted`（通话/闹钟抢占音频会话，Safari 私有状态，原实现不认 → **通话结束后可能永久无声**）与 `closed`（重建上下文 + 因旧时钟失效而重新锚定游标）；`ensureCtx()` 的 resume 判定同时认这两个状态；补 `pagehide` 停播（移动端否则「切走了还在响」）
- **P2-14 跨 origin 迁移提示**：README 同时推荐「双击 index.html」与在线版，但两者是不同 origin、`localStorage` 不共享——用户先攒了预设再打开在线版会看到空的且**收不到任何提示**。预设库为空时提示一次，确认后持久化不再打扰
- **P2-17 测试自身提速**：内联脚本只编译一次（原先每个用例都 `new vm.Script` 重编译 1900+ 行）；T21 的 243 组全组合扫描加 `FULL_SCAN` 开关（默认抽样 16 组，CI 跑全量）。全量耗时从"数十秒"降到 **0.6s**

### 测试

从 23 个场景组 / 264 断言 → **29 个场景组 / 350 断言**，新增 T24–T29 覆盖上述每一项。

本次还**补了测试桩的忠实度**（都是踩到才发现，且每一处都揭出了假绿）：

| 桩缺陷 | 掩盖了什么 |
|---|---|
| `classList` 与 `className` 是两份独立存储 | 「视觉高亮与 `aria-pressed` 是否一致」这类跨视图断言写不出来；语义属性没同步发现不了 |
| `innerHTML = ""` 不清空 children | 按「行/格」检查渲染结果会读到上一次 `buildViz` 的残留 |
| `hidden` 不解析标记的初始值 | **错误弹窗其实没打开**却被判为通过（T23f 一直是假绿） |
| `document` / `window` 监听器不可触发 | 回前台补排、pagehide 停播等生命周期行为完全无法断言 |
| 静态 pill 组不存在 | `setQuerySelectorAll("#sigRow .pill")` 返回空集合，`setPressed()` 空转 |

最后一条尤其值回票价：修桩后 T23f 立刻暴露**本次重构引入的真实回归**——v1.2.4 原来把「停播」与「弹窗告知」放在两个独立 `try` 里（注释明确写了原因：异常源头可能就在这些 DOM 写入里，不能让"告诉用户"这一步被一起带走），抽成 `onFrameError` 钩子时丢掉了这个性质。已修回并加注释锁定。

**反向验证**（v1.3 起的硬规矩）：把 8 项修复逐条临时退回，确认目标断言**真的会失败**（7/8 如期失败，1 项由相邻断言捕获，回归同样被发现）；另用 v1.2.4 跑新测试，首条 v1.3.0 断言即失败。没做过反向验证的测试等于没有测试。

### 自验

- 语法校验通过；**tests 350/350 PASS**（29 场景组，抽样与全量两种模式都跑）
- 看门狗 39 PASS / 0 FAIL / 0 死循环超时
- `tools/` 三项检查全通过（架构约束 R1/R2 零例外、lint 四条规则、DOM 零悬空引用）
- 反向验证：8 项修复逐条退回均可被断言捕获
- 无头 Chrome 截图 + 控制台零报错

### 未处理

- **覆盖率采集**（审计 P1-7 第 4 条）仍未做；若需要，正路是加 `package.json` + ESLint/c8 devDependency（只进 CI 不进产物——"零依赖"约束针对的是 `file://` 直开的运行时产物）
- **后台持续发声的真人验收**：自适应窗口只能用假时钟断言，真实浏览器下仍需「切后台 30s、回前台核对无空隙」的耳朵验收
- `Viz.paintBall` 物理动画仍无逐帧数值断言

## v1.2.4 · 持久值健壮性收口：渲染层不再能被脏数据/空小节杀死（2026-09-14）

起因是一次代码审计：审计用自建 vm 沙箱灌入「JSON 合法但值非法」的持久数据（v1.2.3 之前的测试只覆盖了「JSON 本身坏掉」这条路径），**实测出 2 个必现崩溃 + 1 个主线程死循环**。三者的共同根因是同一个：**「统一入口收口」只落实到了 BPM，没落到拍号、音量、预设数组上**——`Controls.setBpm()` 初始化时会被调用并钳制，而 `setSig()` 是裸赋值。

### 修复的缺陷

| 输入 | v1.2.3 实测表现 | 后果 |
|---|---|---|
| `sig: -3`（或 0、`"abc"`） | `scheduler()` 空小节分支**永不推进** → 主线程死循环 | **标签页卡死**，只能强杀 |
| `sig` 脏值 + 空小节 customs | `paintFrame` 读 `cellEls[bar][0].offsetHeight` → `TypeError` | **画面永久冻结，但音频定时器还在响**——最差状态 |
| `vol: 1e6` | 增益送到 **+120 dBFS** | 削波爆音，戴耳机直接伤耳 |
| `bpm` 脏值 | 位置换算得 `NaN` → `rowEls[NaN]` | 渲染帧 `TypeError` |
| **试听中点「清空当前小节」** | 草稿出现空小节 → `curPattern()` 返回草稿 → 下一帧 `TypeError` | **不需要任何损坏数据，正常操作即可触发** |

### 改动

- **Store 加载路径收口（P0-1 / P0-2）**：`VALID_T` / `VALID_METER` 上移到 `S` 创建之前，加载路径复用与编辑器同一份校验
  - `bpm` / `vol` / `accentVol` / `sig` 全部经 `numOr` / `clamp01` / 白名单取值；`setSig()` 不再是裸赋值
  - `customs` 逐项过 `validatePreset()`；被淘汰的**不静默丢弃**，写入 `beatsight.m2.quarantine` 并在控制台留一条 `warn`，用户可人工找回
  - trainer / accents 改为**显式白名单抽取**，不再 `Object.assign` 整包——顺手消除了原型污染依赖「`Object.assign` 只拷自有属性」的侥幸安全
- **渲染帧外壳 + 边界守卫（P0-1）**：`paintFrame` 拆成外壳 + 主体两层
  - 主体**不碰 `requestAnimationFrame`**，续期只在外壳一处——「任何分支 return 都不丢下一帧」从此是一眼可验证的性质（原实现主体内 3 处各自续期，一旦中途抛异常循环永久死亡）
  - 外壳 `try/catch` 兜底：异常时**停播 + 弹可见提示 + `console.error`**，用户至少知道出了什么事；恢复动作各自包 `try`，避免异常源头就在 DOM 写入里时把「告诉用户」也一起带走
  - 主体新增守卫：`barTicks` / `posT` 非有限即跳过本帧、行未渲染出格子即返回、空小节即返回（与 `scheduler` 同名守卫对齐）
- **调度器防死循环（新发现）**：空小节跳过分支强制正向推进 `nextNoteTime`，并加 `MAX_SCHED_STEPS = 512` 硬上限——任何情况下单轮调度都不会无限循环
- **发声末级钳制**：`playClick` 送出增益前过一道 `Math.min(1, Math.max(0, …))`。对合法输入是**无操作**（合法上界本就是 1），只在持久值被改坏时兜住
- **顺带一处性能修正**：`paintFrame` 全量重绘原先**每格调一次** `activePattern()`，自定义预设下等于每次重绘 64 次线性查找；改为渲染前取一次快照

### 测试

- 新增 **T23 系列（T23–T23g，66 断言）**，并把「rAF 置空、渲染层从不执行」的测试桩缺口补上：新增 `driveFrames()` 同时推进音频时钟与渲染帧，渲染层首次进入自动化覆盖
  - T23 脏拍号 6 例（含 `-3` 死循环用例）· T23b 空小节 customs 淘汰 + 隔离备份 · T23c 音量钳制（断言**送达增益峰值 ≤ 1.0**，不只是状态值）· T23d 脏 BPM · T23e 试听中清空小节（真实用户路径）· T23f 帧内异常被外壳兜住且用户可见 · T23g 正常路径 + 6/8、5/4、7/4 防误伤
- 自验：tests **264/264 PASS**（22 → 23 场景组），全套 0 FAIL
- 独立子进程对照（每用例 8s 超时，超时即死循环铁证）：**原版 20 PASS / 16 FAIL / 1 死循环 → 修复版 39 PASS / 0 FAIL / 0 死循环**
- **新增 `tests/hang-guard.js` 死循环看门狗**并接入 CI：`run.js` 是单进程跑的，若有用例把主线程卡进死循环，进程会挂住——**CI 上的表现是跑满 6 小时超时而不是干脆失败**（单线程被占死时，进程内的 `setTimeout` 看门狗根本轮不到执行）。故死循环类风险改为**每用例独立子进程 + 超时强杀**，「被强杀」即判定依据。已用 v1.2.3 原版反向验证：`✗ sig_neg 超时强杀 ← 主线程死循环`，退出码 1

### 工程卫生

- 新增 `LICENSE`（MIT）——README 早已声明 MIT，但仓库此前没有许可证文件，法律上等于「保留全部权利」，**「README 里写了 MIT」不构成授权**
- 版本号三处同步 v1.2.3 → v1.2.4（`<title>` / 品牌 `<span class="ver">` / chip）

### 未改动

- 音频调度前瞻窗口（25ms / 150ms）、`activePattern()` 快照、`vizSig` 解耦、Swing 共享纯函数等既有设计决策一律未动
- 版本号仍是 5 处硬编码靠人工同步（P2-9）、CI 无 lint / 覆盖率（P1-7）等审计中的**第二、三梯队项**未在本次处理

## v1.2.3 · 弹跳球改「接力制」：每小节一颗球，自始至终跳完本行（2026-09-14）

用户设计决策：**第一小节一颗球从头到尾，第二小节换一颗新球**——替代 v1.2.1/v1.2.2 的跨行长跳方案（smoothstep 缓动、55% 原地留守等全部移除）。跨行飞行的语义问题从根上消失：球永远不会中途离开本行。

- **终端弧**：每小节最后一弧终点 = 本行右缘，时刻 = 小节边界（与下一小节第 1 拍发声同时），抛物线完整落在本行内
- **接力待命球**：终端弧飞行期间，下一小节的球以半透形态停在新行首 onset 处等候；小节边界处正式球就位、待命球隐去，同像素无缝交接
- **小节前导休止**：本行尚无已落地 onset 时，球停在首个 onset 处待命（不再从上一小节跨行飞来）
- **onset 缓冲修剪窗口修正**：保留最近 1s 且至少 8 条——接力制下「上一颗本行 onset」可能是一整小节之前（30BPM 的 7/4 小节长 16s），原 1s/2 条窗口会让球在慢速长小节中途错误待命
- 胶片带复验：最后一拍球从 beat-4 块正常飞向右缘（x 706→924 单向向前），待命球全程停在第二行起点；交接帧正式球在第二行起点就位
- 自验：tests 198/198 PASS（数据层仅修剪窗口调整，T22 断言同步更新）

## v1.2.2 · 弹跳球跨小节「原地跳完整拍再俯冲换行」（2026-09-14）

用户复验 v1.2.1：**问题仍存在**——最后一拍发声时球已在飞往下一小节的路上。

- **胶片带取证**（Playwright 逐帧截图 + transform 数值，不再凭推断修动效）：第 4 拍发声瞬间球已离块 5% 行宽，且 v1.2.1 的 smoothstep 全程都在左移，读作「这一拍没人值守」。另发现跨行长跳 77px 顶点飞出 .viz 容器顶（元素截图里球消失）
- **语义修正**：跨行弧前 55% 水平位移锁零——球在最后一拍音符上方**原地完成完整的垂直弹跳**（这一拍属于这个音），最后 45% 才一次快速俯冲转移到下一行，落地仍精确卡下一小节第 1 拍发声时刻
- **跳高钳制**：H 不超过起跳基线（顶点不出容器空域，第 1 行起跳最多 ~44px）
- 自验：胶片带复验（第 4 拍全程球静止于块上方 x≈712，末 280ms 俯冲跨行）；tests 198/198 PASS（纯渲染层改动）

## v1.2.1 · 弹跳球跨小节长跳改为「先留后跃」（2026-09-14）

用户反馈：**球在跳到一小节的最后一拍时，没有跳完这一拍全程，就直接往下一小节的第一拍移动了。**

- **排查结论：时序无 bug**。跨小节弧从最后一拍发声瞬间起跳、下一小节第 1 拍发声瞬间落地，飞行恰好占满整拍——预判式设计本应如此。真正的问题是弧的**形态**：行尾→行首约 -800px 匀速左移 + 基线下沉一行 + 跳高只有 47px，读作一坨「贴地斜滑」，观感就是提前逃跑
- **修复**：跨行弧（`a.bar !== b.bar`）水平位移改用 smoothstep 缓动 `p²(3−2p)`——前 ~1/3 拍球仍守在最后一拍音符上方（垂直弹跳照常），中段一次明确的跨行长跃，落地减速；跳高按水平距离加成（`min(|dx|×0.15, max×1.6)`），让长跳有长跳的样子。同小节弧不变
- 时序不变量不动：落地时刻仍精确等于下一小节第 1 拍发声时刻（由 onset 表保证，与本次形态调整正交）
- 自验：tests 198/198 PASS（纯渲染层改动，数据层无变化）；无头 Chrome 截图控制台零报错

## v1.2 · 弹跳球：预判式节拍可视化（2026-09-14）

借鉴 Bounce Metronome 的「重力弹跳」哲学：普通节拍器是反应式反馈（拍点发生了你才看到/听到，演奏者平均偏离 30–50ms），而连续运动轨迹持续回答「离下一拍还有多远」，演奏者看着球下落就能提前出手——与乐团看指挥棒同理。时值块回答「多长」，弹跳球回答「何时」，且**跳距与时值成正比**，与原设计语言一致。

- **双层结构**：时值网格上方新增球域（`.viz` 顶 padding 58px，窄屏 50px），球在相邻发声点之间跳抛物线，落点水平对齐对应音符块左缘；跨小节/循环回卷时贴地滑行换行，不瞬移
- **落点 = 真实发声时刻（最关键决策）**：球的端点来自 `onsetBuf`（scheduler 排程时顺手写入，含 Swing 偏移、静音小节照记、休止跳过），不是网格理论时刻——Swing 67% 下落点真切后移、跳弧一短一长，与听觉完全一致
- **预测终点**：调度器只前瞻 150ms，而 96BPM 四分一跳 625ms——落地时下一跳落点还没进缓冲，球会僵住（demo 实测）。新增 `predictNext()` 用不变量 `nextNoteTime == 小节 tick 0 时刻 + 累计 tick` 反推小节起点，纯函数算出下一发声点 `onsetNext`；与排程同源（同快照、同 `swingShift()`），预测值与实际入缓冲值严格相等，无缝替换
- **真实球体物理**（demo 三轮手感迭代收敛）：
  - 跳高 H = clamp(120·T², 10, 48)——重力一致，跳得越久飞得越高（96BPM 四分≈47px，十六分贴地快跳）
  - 触地 70ms 挤压回弹（0.68→1 easeOut）+ 空中随垂直速度拉伸（体积近似守恒）+ 落地前 70ms 预压
  - 地面投影随高度缩小变淡；**不做滚动旋转**——纯色球旋转不可见，按弧起算的角度会在接缝回卷（demo 实测「左跳」伪影）
- **Swing 重构**：偏移公式抽成共享纯函数 `swingShift()`，排程与落点预测同源，行为零变化（T10 回归全绿）
- **开关**：训练模式行新增「弹跳球 · 预判提示」（默认开，`S.bounce` 持久化）；只控显隐，onset 表照常维护，重开无需暖机
- 测试：新增 **T22（14 断言）**——落点=真实发声时刻（与 hits 逐一对齐）、严格递增、预测在未来且总在缓冲之后、Swing 长短交替、静音照记/休止跳过、resync 后预测立即按新节奏型
- 自验：tests **198/198 PASS**（21 → 22 场景）；无头 Chrome 桌面截图控制台零报错；动态手感经独立 demo（beatsight-bounce-demo.html）三轮真人验收后移植

## v1.1.1 · 播放中切节奏型改为「点下即生效」（2026-09-11）

用户反馈：**播放三连音基础时点「四分基础」，第 1 小节仍走三连音，要等小节边界才换。**

- **根因**：`Presets.refreshAfterPatternChange()` 在播放中一律 `pendingPattern = true` 挂起，交给 `scheduler()` 在小节边界消费。这是 v0.4.0 起为「避免跳针」定的策略，但它把「换节奏型」的反馈延迟放大了整整一个小节——96 BPM 4/4 下最坏等 2.5 秒，且点击越靠近小节开头等得越久
- **修复**：播放中先试 `Audio.resyncToNow(newPat)` **就地接续**，成功即立刻生效，不再挂起
  - 做法：**不动时间轴**（`loopStart` 保持不变），只用「已真实经过的 tick 数」在新节奏型的循环网格里重新求出 `(小节, 小节内 tick)`，再取新节奏型中第一个「起始 tick ≥ 该位置」的音符接续
  - 同拍号：换算结果就是原位置——`cum(schedStep)` 恒等于已走 tick，故本函数对「同节奏型」**可证明幂等**，重复点同一个节奏型相位零跳动（T20 用「跨点击整段间隔恒为四分」守住这条）
  - 拍号变化：位置按新拍号重新解释（小节线本来就会挪，播放头一次像素位移不可避免），但仍不排到过去、不重现已走过的部分。相比旧行为「等整个 4 小节循环」（96 BPM 下最长 10 秒），体验差距极大
  - **只向前推进**：接续点取「第一个 ≥ 当前位置的音符起点」，故新 `nextNoteTime` 恒不早于原值，绝不会把音符排到过去
  - 罕见兜底：新节奏型在当前小节已无音符起点可接时（小节末尾的稀疏型）返回 false，仍走原有挂起路径
- **顺带修掉一个半切换残留**：点击时 `buildPresetList()` 会立刻把档位高亮切到新节奏型，但标题与可视化要等边界——用户截图里就是「档位高亮=四分基础、标题与网格=三连音基础、状态=已停止」。原因是挂起期间按停止，`pendingPattern` 永不被消费。新增 `Presets.flushPending()`，由 `Controls.stop()` 调用，停止时把挂起就地落定
- 测试：新增 **T20（13 断言）** + **T21（2 断言 / 243 组扫描）**
  - T20：点下立刻换型、发声快照同步、不留挂起、不排到过去、切换后密度即为四分 0.625s、播放不中断、跨重复点击间隔恒为四分（不跳针）、停止后标题与选中一致
  - T21：9 个代表性节奏型两两对切 × 3 个点击相位共 243 组，守四条硬不变量（立即切换 / 不排到过去 / 时刻严格递增 / 播放不中断）——相位换算最易在「稀疏↔密集、奇数拍↔4/4、小节末」出破例，单点用例覆盖不到
  - **T15 契约更新**：原断言「挂起窗口内按旧拍号渲染」已随本次改动失效（常规路径不再有挂起窗口）。改写为守真正的不变量：切拍后立即按新拍号发声与渲染，状态栏小节/拍位读数始终落在新拍号合法范围内（不回卷、不越界、不脱轨）
  - 测试桩：`window.__beat` 增加 `activePattern` 导出（断言「正在发声的快照」需要）
- 自验：tests **184/184 PASS**（19 → 21 场景，0.44s）；`node --check` 通过；无头 Chrome 截图控制台零报错
- 未改动：`±1` 步进、`consumePending` 挂起机制（兜底路径仍在用）、`S.preview` 试听链路

## v1.1 · BPM 常用速度档 + ±5 粗调 + 滑杆刻度（2026-09-11）

练琴时最常落在几个习惯速度上（60 慢练 / 72、84、96 中速 / 120 提速），而 30→240 的滑杆拖到目标值要么靠运气要么靠长按 +1。本次把「跳到某速度」变成一次点击。

- **常用速度快捷档（A 方案：新增一行）**：BPM 组下方新增 `60 / 72 / 84 / 96 / 120` 五枚 pill，点击即跳到该速度。放在指标区下方而非塞进箭头行，是为了不挤压 46px 大数字与 TAP 按钮的既有呼吸感（实测该行高 174px，运输卡 344px，1440 / 500 两档视口均无横向溢出）
  - 当前 BPM 恰等于某档时该档高亮；由滑杆或 ±1 调出的非档位值（如 97）全部不高亮——避免"看似选中其实不是"的误导
- **滑杆刻度（C 方案）**：五个档位在滑杆上各画一条刻度，与档位按钮**同源**生成（`CONFIG.speedPresets` 单一数据源，杜绝两处各写一份数值）
  - 踩坑记录：最初用 `<input list><datalist>` 实现，**Chrome 对 range 的 datalist 刻度根本不渲染**，即使不加 `appearance:none` 也不画（已用最小页面单独实测确认，非本项目样式冲突）。遂改为绝对定位的刻度层 `<div class="bpm-ticks"><i>`，按 `(v-min)/(max-min)` 落点
  - 刻度层左右内缩 `--thumb-r`（= 圆钮半径 8px，窄屏 22px 圆钮时为 11px），使刻度对齐的是**圆钮圆心行程**而非轨道全宽——否则每个刻度都会偏移，越靠边偏得越多
  - 取值域从滑杆的 `min/max` 属性读取，不硬编码 30/240
- **±5 粗调**：箭头行由 `−1 / +1` 扩为 `−5 / −1 / +1 / +5`。60→120 从 60 次点击降到 12 次；单击一步、长按连发沿用既有 `bindStep`，越界仍走 `setBpm` 的 30–240 钳制
- **训练模式冲突处理**：训练开启时 BPM 归训练器阶梯管（每练满 `everyN` 小节会 `setBpm(bpmFor(trStepIdx))`），手点档位 / ±5 会被立刻覆盖。故训练中**置灰快捷档与 ±5**（±1 维持原有行为不动），关闭后自动恢复
  - 置灰逻辑收口在 `Controls.syncBpmUI()`，由 `setBpm` 与训练开关共同调用，高亮与可用性永远跟随真实 BPM
- 测试：新增 **T19（25 断言）**——档位值与常量一致、按钮/刻度同源生成、刻度元素类型与落点递增、点击直达并同步数字/滑杆/高亮、非档位值不高亮、±5 单步与连击累计、两端钳制、播放中点击不打断、训练置灰与恢复；测试桩补 `createElement` 的 `tagName` 与元素 `min/max/value` 复刻（stub 不解析 HTML，缺了就全变 NaN%）
- 自验：tests **164/164 PASS**（18 → 19 场景）；无头 Chrome 实测刻度 5 条已渲染（2×12px，落点严格递增），无横向溢出

## v1.0.1 · 修复重拍增强量倒挂 + 音量行错位（2026-09-11）

用户反馈线上音量面板截图排查出的两项缺陷：

- **重拍增强量倒挂（音频，严重）**：`playClick` 此前把 `S.accentVol` 直接当增益倍率，而正拍固定 ×0.8、细分 ×0.5。于是「重拍」滑块只要低于 80%，重拍就比正拍还轻——总音量 80% + 重拍 56% 时，重拍 0.448 / 正拍 0.640 / 细分 0.400，强调层级完全倒挂。默认值 100%（=1.0）恰好安全，所以只有用户下调滑块才暴露
  - 修复：滑块语义改为**重拍增强量**，`accentGain = accentMin + (accentMax-accentMin) × accentVol`（0% = 与正拍齐平，100% = 最大强调）。单调递增、全量程恒有 `重拍 ≥ 正拍 > 细分`，且 100% 时增益恰为 1.0，**默认听感与 v1.0.0 完全一致**
  - 常量入 `CONFIG`：`levelBeat / levelSub / accentMin / accentMax`（依 DEVELOPMENT §6「勿散落硬编码」）；原 0.8 / 0.5 一并收编
  - 标签随之更名：`重拍` → `重拍增强`，组标题 → `音量 · 总音量 / 重拍增强量`（旧标签下的百分比会被误读成"音量"）
- **音量两行滑块错位 12px（布局）**：`.vol-row` 是各自独立的 flex 行，标签「总音量」3 字（≈36px）与「重拍增强」4 字（≈48px）宽度不同，导致两行整行错位——轨道起点、终点、右侧百分比全部错开 12px（无头渲染实测 647 vs 635）
  - 修复：`.vol-row>span:first-child{min-width:52px}` 标签定宽，两行轨道起点齐平（实测均为 x=663）
- 测试：新增 **T18（7 断言）**——按发声频率区分层级读包络峰值，断言增益公式、56% 不再倒挂、0–100% 全量程不变量、以及 0%/100% 两端语义；测试桩 `FakeParam` 补 `_peak`（末次归零 ramp 会覆盖 `_rampTo`）
- 自验：tests **135/135 PASS**（17 → 18 场景）；无头 Chrome 实跑截图确认两行对齐、控制台零报错

## v1.0.0 · 依赖方向净化 + Editor 测试 + CI（2026-09-11）

复盘报告（beatsight-review-v0.9.0）结构项与基建项，标记架构进入稳定期：

### 结构

- **依赖方向净化（H2）**：`activePattern`/`draft`/`appliedPat` 所有权上移至共享状态区（该区整体移到 Store 之后）。消除两处反向依赖：Viz.paintFrame → Presets.activePattern（每帧热路径）、共享 curPattern → Editor.draft。现在模块严格单向：数据 → Store → 共享状态 → Modal → Viz → Audio → Trainer → Controls → Presets → Editor → init
- **共享状态前向引用消除（M5）**：spb/curPattern 等不再引用未声明的 const（此前靠箭头函数惰性求值侥幸安全，新人极易踩 TDZ）
- 模块边界规则更新为："不得反向引用后方模块；运行期热路径只读共享状态区与前方模块"（写入 index.html 头注释与 DEVELOPMENT §3.0）

### 测试

- **M1：Editor 全流程覆盖（T17，12 断言）**：打开生成副本草稿 → palette 追加致超限 → 保存禁用+校验提示 → 撤销恢复 → 保存 → customs+1/S.sel 指向/编辑器关闭/持久化/当前节奏型切换
- 总计 17 场景 128 断言
- **H3：CI 上线**：`.github/workflows/test.yml`——push/PR 自动跑语法校验 + tests/run.js（ubuntu + Node 22，零依赖零安装）

### 文档与基建

- **M2**：docs/prd.html 加置顶存档横幅（v1.0 原始 PRD 仅存档，现行功能以 CHANGELOG + DEVELOPMENT.md 为准）
- **M3**：DEVELOPMENT §5 自验流程 macOS 化；新增 `tests/screenshot.sh` 把无头截图的沙箱坑（--no-sandbox、后台+pkill、user-data-dir 换新）固化成一条命令

### 自验

- tests 128/128 PASS；语法校验通过；`tests/screenshot.sh` 实跑截图确认渲染正常、控制台零报错
- 重构不改变任何行为（128 断言含 v0.5.0 以来的全部回归场景护航）

## v0.9.1 · 防御性补丁（2026-09-11）

复盘报告（beatsight-review-v0.9.0）高/中优先级项的第一批：

- **persist() 写路径容错**：此前 loadSaved 有 try/catch、迁移备份有 try/catch，唯独 persist 裸奔——隐私模式/配额超限时 setItem 抛 QuotaExceededError，会把调用它的每个交互（播放、调速、开关训练）整个炸断。修复：try/catch 静默降级（状态在内存生效，只是不落盘）。tests T16 用「setItem 必抛错」的 stub 锁定：播放/调速/切拍号全程不抛
- **iOS 后台返回音频自愈**：新增 `visibilitychange` 监听——回前台且播放中且 ctx 被挂起时自动 resume（首次 pointerdown 的 once 解锁覆盖不到这个场景）。真机验证项
- **跑道脉冲动画去重**：换拍脉冲与预备拍计数脉冲的 WAAPI 关键帧完全重复，提取 `pulseZone()`（Viz 内部）
- **导入 accents 归一**：validatePreset 对 accents 去重 + 升序（`[3,0,3]` 这类脏数据此前能过校验，重拍落点会错乱）
- 测试：新增 T16（5 断言），总计 16 场景 116 断言
- 自验：tests 116/116 PASS；语法校验通过

## v0.9.0 · 预备拍 + 播放中切拍号可视化脱轨修复（2026-09-08）

### 新功能：预备拍

- 训练模式区新增「预备拍」开关 + 拍数输入（1–8 拍，默认 4，脏输入回退原值）：点播放后先数 N 拍（首声重拍音、其余正拍音，当前速度），数完自动进入节奏型——DAW/实体节拍器标配的 count-in
- 可视化：预备拍期间播放头压在最左不动，状态栏显示「预备拍 · n / N」，跑道拍点随计数脉冲（拍数超出一小节时按拍号折行）
- 联动约定：与变速训练同开时按起始 BPM 数拍、爬坡计数从正式第 1 小节起算（预备拍不占训练小节）；编辑器试听同样生效；静音拍不影响预备拍（它的职责就是让你听见）；播放中改设置下次播放生效
- 持久化：`S.countIn = {on, beats}` 并入 beatsight.m2（v:3 结构内新增字段，旧数据缺省回退 {off, 4}）

### 修复：播放中切拍号时可视化脱轨（用户实拍"不好描述"的 bug）

- **根因**：播放中切到不同拍号（如 4/4→3/4）时，S.sig 立即变化，但新节奏型为不跳针要等循环起点才应用（最长 3 小节的挂起窗口）。窗口内声音走 v0.7.0 修的 activePattern 快照（正确），但 paintFrame 用**新拍号**算播放位置——播放头按 3/4 小节长提前回卷（声音还在第 4 小节、播放头跳回第 1 小节）、格子高亮错行、刻度闪烁漂移
- **修复**：Viz 新增 `vizSig`——buildViz 时锁定的"当前渲染拍号"，paintFrame 全程只认 vizSig（位置取模、小节长、跨小节闪烁、座次尺、状态栏），与新拍号在循环起点同步切换；scheduler 的空小节跳过与 swing 判定同步改用快照拍号 `pat.meter`
- 顺带删除已无引用的 `loopTicks()` 共享函数
- 回归断言：T15 复现窗口内场景（4/4 播放 1 秒后切 3/4，旧循环第 4 小节中段手动驱动 paintFrame，状态栏必须是「第 4 小节」而非回卷「第 1 小节」）
- **BPM 部分走查结论**：单独调整 BPM 的时钟重映射是连续的，未发现独立 viz 缺陷；若训练开启时手动调速被下一小节边界覆盖属既定行为（v0.5.0 起文档化）。如用户仍复现，再挂探针

### 测试

- 新增 T14（12 断言：预备拍时刻/音高层次/状态栏计数/重新播放重数/与训练器组合/关闭零变化回归）+ T15（3 断言），总计 15 场景 112 断言
- 测试基建：元素 stub 的 `closest()` 返回带 classList 的代理（paintFrame 首次纳入驱动范围后暴露的 stub 缺陷）

### 自验

- `node tests/run.js` 112/112 PASS；JS 语法校验通过；无头 Chrome 桌面截图确认训练模式区三开关布局正常（预备拍拍数输入关闭态隐藏）；控制台零报错
- 真人验证项：预备拍计数听感（尤其配合变速训练起始速度）、播放中切拍号后画面是否还脱轨

## v0.8.0 · 三套程序合成音色（2026-09-07）

### 新功能：音色扩展（路线图 v0.9 提前，练习统计顺延）

- 走带区新增「音色」三枚胶囊：**电子**（现状振荡器 Click）/ **木鱼** / **鼓组**，整体切换，播放中变更即时生效（下一发音符即新音色，不打断播放）
- **零采样文件硬约束保持**：噪声 buffer 程序生成（0.5s 白噪声，`AudioBuffer` 逐样本填充），三套音色全部实时合成
- **木鱼**：带通滤波噪声（Q=8，30ms 短包络），层级差体现在中心频率——重拍 2000 / 正拍 1500 / 细分 1100Hz，最接近实体节拍器，长练不疲劳
- **鼓组**：重拍=底鼓（正弦扫频 150→50Hz，120ms 衰减）、正拍=军鼓感（带通噪声 1800Hz，80ms）、细分=踩镲感（高通噪声 8000Hz，20ms，音量 ×0.7 防高频刺耳）；适合扫弦节奏型跟弹
- `S.timbre` 持久化（非法值回退 click）；全部音色参数集中在 `CONFIG.timbres`，延续「音色约束集中管理」约定

### 测试

- 新增 T13（11 断言）：默认/脏值/持久化、wood 全层级带通噪声与重拍中心频率、drum 三层级发声路径（底鼓扫频终点/军鼓带通/踩镲高通）、播放中切换不打断
- 测试基建升级：伪造 AudioContext 增加 createBuffer/createBufferSource/createBiquadFilter，osc 扫频目标与噪声滤波参数均可断言

### 自验

- `node tests/run.js` 97/97 PASS；JS 语法校验通过；无头 Chrome 桌面截图确认音色组渲染与布局无挤压；控制台零报错
- 听感验证需真人：鼓组细分的踩镲音量系数（0.7）与木鱼 Q 值（8）是真机调优候选

## v0.7.0 · tick 制节奏模型 + 三连音/Swing/奇数拍（2026-09-07）

### 数据层：浮点拍数 → tick 制（PPQN=48）

- 时值单位从浮点拍数 `d` 改为整数 tick `t`（TPB=48，48=2⁴×3，整除 2/3/4/6/8/12/16/24，覆盖到三十二分三连音）；四分=48t、八分=24t、八分三连音=16t、十六分=12t、十六三连=8t、三十二分=6t
- 小节校验从 `1e-9` 浮点容差改为**整数严格相等**（`barSum === meter×48`）
- **迁移**：加载时检测 `v!==3` → 整包备份到 `beatsight.m2.bak`，customs 内所有 `d ×48 → t`；`persist()` 写入 `v:3`。四个同步点（buildViz/scheduler/paintFrame/editor draft）全部改 tick
- 导入导出格式升级 v:2（tick），**向后兼容 v:1**（浮点 d 自动换算）；校验集合改合法 tick 表，拍号开放到 {2,3,4,5,6,7}

### 新功能

- **三连音**：编辑器音符块库新增「八分三连音组」「十六三连音组」——按组插入 3 枚（单个三连音符在记谱上无意义）；可视化按 tick 比例自然变窄；三连音块不画十六分刻度线（网格对三连音无意义，画了是误导）；组标签沿用合并标注（「八三连 ×3」）
- **Swing**：走带区三档胶囊 直(50)/Swing(67)/重 Swing(75)——演奏参数非时值：拍内后半的八分（位置 %48===24 且时值恰为 24t）发声延后 (swing-50)/50×24t，**时间轴不动**（下一颗按记谱时间进入）、**块宽不变**（时值=长度是核心认知翻译）；6/8 自带摇摆属性不套用；播放中切换即时生效
- **奇数拍 5/4、7/4**：拍号 pill 增至 6 枚；pattern 新增 `accents` 重拍位置数组（支持跨拍如 1.5），重拍音（1568Hz triangle）在 accents 落点触发；选中 5/7 时拍号行下方出现分组胶囊（5/4：2+3 | 3+2，默认 2+3；7/4：3+2+2 | 2+2+3，默认 3+2+2），作用于基本回退节奏与新建自定义
- **新内置预设 4 个**（内置库 8→12）：三连音基础（4/4 八分三连音 ×12）、Take Five 律动 · 5/4（3+2）、Money 型 · 7/4（3+2+2）、特雷斯略 3+3+2（4/4 跨拍重音 [0,1.5,3]）
- 音符块库补齐**附点四分**（72t，摇曳 6/8 的构成单元此前无法手动添加）与**三十二分**（6t）

### 附带修复（均为 tests 固化后捕获）

- **播放中切换节奏型可越界崩溃（v0.4.0 起潜伏）**：S.sel/S.sig 在切换瞬间即变，scheduler 直读 curPattern() 会在小节中途拿到新节奏型，schedStep 越界（如 4/4 六音符切 3/4 三音符）。修复：新增 `Presets.activePattern()` 快照——播放/试听期间读 appliedPat，仅在小节边界 applyPatternChange 时换快照（T12 锁定）
- **同毫秒连续导入 id 冲突**：`c<Date.now()>-i<n>` 在快速连续导入时与上一批撞 id，改会话内单调递增序号（T5 锁定）

### 测试

- tests/ 扩至 12 场景 86 断言：新增 T8 旧浮点数据迁移+备份 / T9 三连音校验（含残缺组拒绝）/ T10 swing 偏移量精确断言（osc.start 时刻记录）/ T11 奇数拍重拍分组发音频率序列 / T12 播放中切换 tick 制回归；T5 重构为 tick 制 + v1 兼容 + 5/7 拍开放
- 方法论沿用：修 bug 先加复现断言（本轮两个存量 bug 均按此流程修复）

### 自验

- `node tests/run.js` 86/86 PASS；JS 语法校验通过；无头 Chrome 桌面 1440 / 窄屏 800 截图：6 枚拍号 pill、Swing 组、12 个内置预设渲染正常，控制台零报错
- 播放发声验证需真人点击（浏览器音频手势策略）：重点试听 Take Five 的 3+2 重拍与 Swing 两档差异

## v0.6.0 · 单文件模块化重构 + 预设导入导出 + 持久化测试（2026-09-07）

### 架构：IIFE 模块化（行为零变化重构，v0.7 tick 制的前置）

- `<script>` 重组为 8 个 IIFE 逻辑模块，按依赖方向排序、禁止反向引用：**Store**（持久化/状态创建/迁移/导入导出）→ **Modal**（应用内弹窗）→ **Viz**（时值可视化）→ **Audio**（Web Audio 前瞻调度）→ **Trainer**（变速训练器）→ **Controls**（播放控制/BPM/拍号/音量/静音拍）→ **Presets**（预设库/回退提示/播放中切换挂起）→ **Editor**（自定义编辑器）
- 边界规则：模块间只通过暴露接口通信（如 `Trainer.updateProg()`、`Presets.consumePending()`），禁止直读内部变量；跨模块共享的可变状态（S、customs、音频时钟变量）集中在「共享状态」区声明——v0.6 收敛的是函数归属与调用接口，状态容器化留待后续
- 关键接口变化：scheduler 内的 pendingPattern 判定收敛为 `Presets.consumePending(schedBar)`；stop() 的视觉复位收敛为 `Viz.resetForStop()`；追踪变量复位收敛为 `Viz.resetTracking()`；编辑器草稿经 `Editor.draft()` 访问器供 curPattern 使用
- 新增 `window.__beat` 调试/测试句柄，暴露 8 个模块接口（tests/run.js 的断言入口）
- HTML/CSS 不动布局，仅新增预设导入导出按钮与 `.preset-io` 样式

### 功能：预设导入导出

- 预设库新增「导出预设 / 导入预设」：导出为 JSON 文件（`{app:"beatsight", kind:"presets", v:1, presets:[…]}`，文件名带日期）；导入经隐藏 file input + FileReader
- 导入校验：拍号 ∈ {2,3,4,6}（5/7 属 v0.7）、恰好 4 小节、时值 ∈ {4,2,1.5,1,0.75,0.5,0.25}、每小节时值和严格等于拍数；任一预设不合格整包拒绝，错误文案经 `Modal.uiAlert`（新增纯通知弹窗，hideCancel）展示
- 导入 id 一律重新生成（`c<时间戳>-i<n>`），杜绝跨设备 id 冲突；无自定义预设时导出按钮禁用
- 侧栏说明文案补充备份引导

### 附带修复（tests 固化后捕获的存量 bug）

- **trainer 脏值清洗绕过**：`isFinite(+null)`、`isFinite(+true)` 均为 true——`null`/布尔脏值穿过清洗覆盖默认值（如 `everyN:null`）。修复：清洗条件排除 null 与布尔（tests T2 断言锁定）

### 测试：tests/ 持久化

- 新增 `tests/run.js`（零依赖，Node ≥18，`node tests/run.js`）：vm 沙箱提取内联脚本运行，Map 版 localStorage 按场景预置、伪造 AudioContext 手动推进时钟逐 tick 驱动 scheduler、rAF 置空只测引擎与状态层
- 7 场景 56 断言：坏 JSON 容错 / trainer 脏项回退 / 爬坡序列 70→80→90→95+自动停止+文案（v0.5.0 核心回归）/ 参数钳制 / 导入导出全分支+往返 / curPattern 回退 / 模块装配完整性
- v0.5.0 的"临时副本伪造 AudioContext"测试方法自此固化，不再依赖 CHANGELOG 文字传承

### 自验

- JS 语法校验通过；`node tests/run.js` 56/56 PASS；无头 Chrome 桌面 1440 / 窄屏 800 截图确认布局与新增按钮渲染正常、导出按钮禁用态正确；控制台零报错
- 环境备忘：本机（macOS）无头 Chrome 在 Bash 沙箱内因 `sandbox initialization failed` 起不来，需 `--no-sandbox`；`--virtual-time-budget`/`--timeout` 组合会挂起不退，需后台跑 + 到时 pkill

## v0.5.1 · 标题显示版本号（2026-09-07）

- 顶栏品牌标题与 `<title>` 增加 v0.5.1 版本号；顶栏过时的「M2 · 预设 + 自定义编辑器」里程碑标签同步更新为「M3 · 变速训练器」（v0.4.3 清理标题里程碑字样先例的延续，避免与版本号并排矛盾）
- 版本号以弱化样式呈现（t3 灰 + mono 小徽章），不与品牌主标题争抢视觉
- 自验：JS 语法校验通过；无头 Chrome 桌面截图确认顶栏版本徽章渲染正常；控制台零报错

## v0.5.0 · 变速训练器（M3-2）（2026-09-07）

- 新功能：「训练模式」区新增**变速训练 · 自动爬坡**——参数 {起始 / 目标 BPM、步长 ±、每级小节数}；从起始速度起步，每练满 N 小节自动升一级，到目标速度并练满一级后自动停止，状态栏提示「训练完成 · 达到 X BPM」；面板实时显示「当前 85 BPM · 第 6/13 步」
- 实现要点：复用 `scheduler()` 的小节边界（schedStep 环绕点）计数，爬坡经 `setBpm()` 统一入口（音频时钟重映射，不打断播放）；完成自动停止时调度器立即退出本次循环；参数即时钳制（起始 30–236、目标 31–240 且必须高于起始、步长 1–20、每级 1–8 小节，脏输入回退原值）
- 交互约定：开启训练即把 BPM 定位到起始值；每次点播放都从起始速度重新爬坡（会话语义——完成/停止后训练开关保持开启，便于重复练习同一组参数）；训练播放中手动调速会在下一级边界被训练进度覆盖
- 持久化：`trainer` 配置并入 beatsight.m2（含开关状态），缺项/脏项回退默认值 {70, 120, 4, 4}
- 附带修复：清除预览工具再次注入的 `data-page-node-id` 残留 114 处（v0.4.0 清理项的回归）；.gitignore 屏蔽显卡驱动自动生成的 NVIDIA Corporation 日志目录
- 自验：JS 语法校验通过；无头 Chrome 伪造 AudioContext + 手动驱动 scheduler 的自动化测试 17 项断言全 PASS——参数钳制（999→236、目标≤起始抬回 +1）、爬坡序列 70→80→90→95（每级 2 小节）、完成自动停止与文案、重启会话从起始重爬、面板开合与进度清空；桌面 1440 / 窄屏 500 截图（面板展开、flex 换行正常、BPM 定位 70）；控制台零报错

## v0.4.7 · 手机端卡片头不再被挤换行（2026-09-07）

- 问题（用户实拍）：手机端播放时标题「时值可视化 · 4/4 · 同屏 4 小节」与状态灯互挤、各自断成两行。根因：状态文字播放时由「已停止」（3 字）变为「播放中 · 第 1 小节 · 3a」，静音拍时更长（「静音拍 · 第 4 小节 · 心中默数」），宽度暴涨 2–3 倍挤走标题空间——**静止态完全看不出问题，必须在播放/静音态验证**
- 修复：≤560px 窄屏下 `.card-head` 改纵向堆叠（标题独占一行、状态灯另起一行），`.status` 加 `white-space:nowrap`
- 自验（定量探针，非肉眼）：390px 视口下测量元素高度——修改前 PLAY/MUTE 态 title 38px（两行）、status 32px（两行）；修改后一律 19px / 16px（单行）；静止态前后均单行
- 测量方法备忘（本次踩坑）：① headless=new 有 ~500px 最小窗口宽度，且 `--force-device-scale-factor` 只缩放渲染、**不折算 CSS 视口**（--window-size=780 实测 innerWidth=764），要测真实手机宽度必须用 390px 宽的 iframe 承载页面（配 `--allow-file-access-from-files`）；② 判断是否换行看元素 **height**（单行 19px / 双行 38px），`getClientRects().length` 对块级元素恒为 1，不能当行数用

## v0.4.6 · 原生弹窗全部换成应用内弹窗（2026-09-07）

- 问题（用户实拍反馈）：在沙盒 iframe（预览面板）中，编辑器「返回练习」点击无反应——浏览器静默拦截原生 confirm 并直接返回"取消"，退出流程中止；ESC 同理失效。代码路径本身健康（自动化测试证实：confirm 被吞→仍开着，放行→正常退出，无修改→直接退出）
- 修复：新增应用内弹窗组件（uiConfirm/uiPrompt，深色卡片 + 遮罩，z-index 200 在编辑器之上），替换全部 5 处原生弹窗：返回丢弃确认、删除预设（红色危险钮）、复制到全部、清空小节（红色危险钮）、BPM 输入（顺带解决原生 prompt 与深色主题割裂的老问题）
- 键盘细节：弹窗打开时捕获阶段接管 Enter/Esc（stopImmediatePropagation），主键盘逻辑加 modalOpen 守卫——弹窗上按 Esc 只关弹窗、不会连带触发编辑器退出确认；BPM 弹窗输入框内 Enter 直接确认
- 自验：JS 语法校验通过；无 confirm(/prompt( 残留；五项功能测试全过（弹窗弹出/取消保持/确认退出/BPM 写入/Esc 分层关闭）；桌面截图确认视觉融入深色系统

## v0.4.5 · 跑道底部与格子齐平（2026-09-07）

- 问题（用户实拍）：暗格底部读作"没填满"——跑道（beat-zone）高 47px、格子 44px，底部外扩 3px。该外扩源自 v0.3.1「上下外扩留呼吸感」，v0.4.2 修缝隙探出时已削掉顶部，底部保留后形成不对称，单独看像格子没坐满
- 修复：跑道高度与格子齐平（桌面 47→44px，移动端 37→34px）。容器感改由拍与拍之间的缝隙承担；换拍绿色脉冲、拍分隔线均不受影响
- 自验：4x 截图 + 底部 12x 放大确认唇边消失、底边干净；整屏回归正常

## v0.4.4 · 白格边缘「台阶」修复（2026-09-07）

- 问题（用户实拍连续四图定位）：已弹白格左边缘顶部凸出 1px×12px 的小台阶——网格线（拍分隔竖线）画在拍边界正中央，但格子右缩 4px 留缝使下一颗格子的左边缘正好压在拍边界上，网格线因此压住白格左边第 1 个像素；且网格线从 viz 顶部 12px 才开始（top:12px），顶部无覆盖段就凸出一小块。深格不可见，白格现形
- 定位方法：4x 缩放截图 + canvas 逐行像素扫描白格左边缘坐标（修复前 y808-824 处 x=946、y828 起 x=950，1px 跳变；修复后 y806-882 全程 x=946 笔直）
- 修复：网格线左移 2px 落在 4px 缝隙正中央（`calc(X% - 2px)`），不再接触任何格子
- 自验：JS 语法校验通过；4/4 像素扫描确认边缘笔直；6/8 回退场景（用户原始截图场景）4x 截图确认凸起消失

## v0.4.3 · 设计审计修复批次（2026-09-07）

### 实证缺陷修复（P0，均有代码级证据）
- **拍号回退提示条补上样式**：`.fallback-note` 此前无任何 CSS 规则（`--amber` 声明后零引用），触发时是裸文本——补琥珀底+边框样式，并处理 `[hidden]` 与 `display:flex` 的冲突（需 `.fallback-note[hidden]{display:none}`）
- **当前小节高亮从死代码变为可见**：`bar-row.current` 类 JS 每帧切换但无样式，补 7% 绿调行底（与播放头同色，语义连贯；3.5% 白实测不可见）
- **编辑器点空白轨道切换目标小节后立即重绘**：此前缺 `renderEditor()` 调用，「· 编辑中」与库标题停留在旧小节，会把音符追加到错误位置
- **状态灯语义修正**：「已停止」不再绿色（绿=进行中），文字与圆点默认灰、仅 `.dot.live`（播放中）绿；编辑器 `.status.ok/.bad` 不受影响

### 体验与健壮性
- 静音拍视觉预告：开启后第 4 小节音符块降透明 + 行上沿「静音小节」标记，提前预知接下来没声
- BPM/音量滑块拖动过程不再每帧写 localStorage（input 只更新 UI，change 统一落盘）
- localStorage 解析加 try/catch（loadSaved），数据损坏回退默认不再白屏；初始 BPM 走 `setBpm()` 统一钳制 30–240
- playClick 音色频率/包络改用 CONFIG 常量（消除与 DEVELOPMENT.md「勿散落硬编码」的偏差）
- 触控目标：BPM 步进钮 40→44px；移动端滑块 thumb 16→22px
- 编辑器小节标签 64→78px + 不换行，修复「· 编辑中」折成三行
- 页面标题去掉「· M2」里程碑字样

### 自验
- JS 语法校验通过；无头 Chrome 桌面 1440 / 移动 500 / 回退提示+静音+播放态合成 / 编辑器四视角截图验证；控制台零报错

## v0.4.2 · 正在弹=白填充+绿描边（2026-09-05）

- 问题（用户实拍确认）：正在弹的块「绿填充+绿底衬+绿光晕」三重绿色叠加——四分音符块宽、停留 1 拍，整屏看着一直是绿的；十六分块小、翻篇快，弹完即白。两种时值观感分裂
- 修复：正在弹的块改为**白色进度填充 + 绿色描边/光晕**（回归设计规范「绿只做功能强调，最高强调用近白」）；弹完定格纯白，颜色连续不跳变
- 休止符正在弹的填充从浅绿改为浅白（rgba(255,255,255,.35)），与主规则一致
- 经过闪烁配色简化：正在弹的块也是白底，闪烁统一为绿光（删除"绿块上闪白光"分支）
- 同时撤销 v0.4.1 的「整块立即变绿」方向（会让绿色更多），恢复进度条式填充
- 刻度线修正（用户实拍反馈）：① 满格高，不再上下悬空留 6px；② 末格不画右刻度线——格边缘即边界，右缘再画会垂出多余竖线
- 窄格子（不足半拍，如十六分）不做过程填充：0.25 拍一闪而过，进度填充只会呈现为一根闪动的竖条；轮到即整块变白。半拍及以上宽格子保留进度填充
- 接缝凸起修正（用户实拍反馈）：跑道（beat-zone）顶部不再外扩 3px——外扩边会从格子缝隙探出，在已弹白块旁读作"凸出一小块"；顶边与格子顶对齐，底部外扩保留
- 自验：模拟同一播放瞬间（第 1 小节 60%）截取四分基础/Funk 十六分对比图，两预设观感一致；刻度线满格高且无多余竖线；窄格无竖条噪声

## v0.4.1 · 当前音符整块变绿（2026-09-05，已废弃）

- 该方向（正在弹整块立即变绿）经用户实拍验证会让绿色占比更大，与"白色为主"的期望相反，已在 v0.4.2 撤销填充逻辑改动；仅保留记录

## v0.4.0 · 交互健壮性与质感统一（2026-09-05）

### 配色与质感统一
- **修复四分音符与十六分音符表面质感不一致**：十六分刻度线改为「每个小格统一带右刻度线、无例外」——每个十六分网格位置都有刻度，四分/十六分呈现同一把"尺子"（注意：带「末格除外」例外的方案会让单格十六分音符一条线都没有，已踩过）
- 休止符虚线描边提亮（--border #4D4D4D → 新增 --rest-line #5A5A5A），余光场景可辨
- 三级文字 --t3 #737373 → #8C8C8C（小字号灰字对比度提到 WCAG AA 安全线上方）；座次尺桌面 9→10px、移动端 8→9px
- 新增 --amber #E3B341 用于提示条（非错误、非功能的第三类语义色）

### 教学信息补全
- **短音符合并标注**：连续 ≥2 个无独立标签的短音符（十六分等非休止）共享组标签「十六 ×n」，随播放绿色高亮；过窄自动隐藏。最难辨认的音符不再没有名字

### 编辑器健壮性
- 新增撤销栈（最多 50 步）：任何草稿变更前快照，Ctrl+Z / 「撤销」按钮回退
- 「复制小节到全部」「清空当前小节」加二次确认；返回/ESC 时若草稿未保存提示丢弃确认
- **替换/追加行为可预期**：当前编辑目标小节标签显示「· 编辑中」；音符块库标题实时说明行为（「点击替换选中的「八分」」/「点击追加到「小节 2」」）
- 编辑器标题改为「基于「xx」创建」，明确复制再改模型，消除"改坏内置"的心理负担
- 状态灯收归 CSS 类（.status.ok/.bad），移除内联样式与 JS 打架
- ESC 关闭编辑器；overlay 补 role="dialog" aria-modal

### 交互反馈
- **拍号不匹配不再静默回退**：选中节奏型与当前拍号不符时，标题下方显示琥珀色提示条「已回退为基础节奏」+ 一键切回原拍号
- **播放中切换预设不再跳针**：挂起到小节边界无缝应用（拍号变化则等到循环起点）
- TAP 测速按钮实时读数（再点一次 → TAP · 96 BPM，2.6s 后还原）
- BPM ±1 支持长按加速（400ms 后每 90ms 一步）；BPM 数字可点击直接输入
- 删除自定义预设加确认
- 试听结束/保存后还原主界面拍号（修复试听 6/8 草稿后主界面拍号被悄悄改掉的遗留 bug）
- 滑块补 aria-label；「配置」区升级为「训练模式」分区（M3 变速训练器落位点）

### 数据与代码
- **修复内置数据 bug**：Funk 十六分每小节只有 15 格（3.75 拍），不满足自己的校验规则，补齐为 16 格
- 预设名/描述等用户可控字符串一律 textContent 赋值，堵掉 innerHTML 自注入路径
- 自定义预设 id 化（sel 存 id 不存下标，删除不再下标漂移；旧数据自动迁移）
- 拍号切换收敛为 setSig() 单一入口（原 4 处复制粘贴）
- 新增 CONFIG 常量区（音色频率/包络/调度窗口），与 DEVELOPMENT.md 硬约束一一对应
- 清除设计工具出码残留的 data-page-node-id 属性（约 200 个）
- 修复 patternMeta 的 BPM 不随调速更新的显示滞后

### 自验
- JS 语法校验通过；无头 Chrome 桌面（1440）/ 移动（390）/ Funk 十六分局部（2x）/ 编辑器四视角截图验证；控制台零报错

## v0.3.2 · 音符配色与质感统一（2026-09-04）

- 问题：同为「已弹」的四分音符，在已过小节是纯白、在当前小节被整条尾迹光晕染成浅绿——同色不同状态；且小格盒式描边让长音符呈斑马条，与短音符质感不统一
- 尾迹光晕改为短尾迹：只跟在播放头身后（最多 10% 小节宽），不再覆盖全部已弹区域；已弹 = 恒定纯白
- 十六分小格从「内嵌盒」统一改为「细分刻度线」（1px 左分隔线，首格无）：任何时值音符表面质感一致，长音符不再斑马化；移除 v0.3.1 的 .subs.solo 补丁（刻度线方案下不再需要）
- 跑完小格闪烁的结束态改为无盒阴影（与刻度线一致），闪烁动画本身不变
- 自验：民谣扫弦（混合时值）+ Funk 十六分（全十六分）播放态截图，四种时值配色与质感一致

## v0.3.1 · 跑道视觉层级修正（2026-09-04）

- 问题：v0.3.0 跑道太淡、块内小格描边太重，6/8 等场景呈「盒中盒」，与 demo 的容器感不符
- 跑道容器感加强：背景 .04→.06、描边 .05→.10、上下外扩 3px 留呼吸感、跑道间距 3→6px
- 块内小格描边 .16→.10 减弱，层级让位给跑道
- 单格音符（十六分等）小格铺满整块（.subs.solo inset:0），消除盒中盒
- 自验：Funk 十六分（全十六分，对齐 demo 灯珠感）+ 摇曳 6/8 播放态截图通过

## v0.3.0 · 「跑道灯」拍点提示（2026-09-04）

- 拍点容器（跑道）：每拍一个半透明定位层垫在音符块下，换拍瞬间绿边脉冲（320ms WAAPI）；纯定位叠加层，附点/切分/6/8 跨拍音符天然兼容，不改数据结构
- 拍内座次尺：每拍下方标注 1 e & a（6/8 为 n +），当前十六分位置绿色高亮；标签独立一行在音符块外，不被裁切
- 下一颗音符预亮：active+1 的音符块绿框呼吸（跨小节指向下一行第一颗）
- 状态栏读数升级：`播放中 · 第 X 小节 · 拍号+座次`（如「第 3 小节 · 2e」）
- 小节行加高以容纳座次尺：桌面 68→86px，移动端 52→66px
- 自验：JS 语法校验通过；无头 Chrome + 假音频时钟驱动播放态截图验证 4/4 与 6/8 两种拍号

## v0.2.1 · 可视化交互迭代（2026-09-04）

- 音符块加入进度条式填充：当前音符随播放从左到右填满（绿），已演奏填满定格（白）
- 十六分小格全高分格：每个音符块内按十六分等分；**跑完**一个小格时该格四边亮一下（闪烁时机为离开时刻，跨小节闪上行最后一格）
- 闪烁配色按底色反转：绿块上闪白光，白/暗块上闪绿光（WAAPI 动态取色，260ms）
- 播放头尾迹光晕：当前小节内播放头左侧经过区域渐强绿光带，换小节自动换行
- 时值标签移到格子下方（格子回归纯时值块），当前音符标签绿色加粗；标签比格子宽时自动隐藏
- 格子扁平化：格高 64→44px（移动端 34px），小格呈横条形
- 移动端适配修复：顶栏芯片窄屏隐藏、节奏型标题行可换行

## v0.2.0 · M2（2026-09-03）

- 预设库扩至 8 个内置：民谣扫弦 / 四分基础 / 八分摇滚 / 附点布鲁斯 / 切分节奏型 / Funk 十六分（含休止符）/ 华尔兹分解（3/4）/ 摇曳（6/8）
- 数据模型重构：`pattern = {name, meter, bars[4][{d, rest}]}`，支持逐小节不同
- 自定义节奏型编辑器：逐格编辑（选中替换 / 追加 / 删除 / 复制到全部 / 清空）、逐小节时值校验（不完整标红 + 禁保存）、试听、命名保存到「我的预设」（localStorage）
- 时值可视化升级：同屏 4 小节；音符块内十六分小格；跑完小格四边闪烁（配色按底色反转：绿块白光、白/暗块绿光）；当前音符进度条式填充
- 休止符渲染为虚线框，占时不发声

## v0.1.0 · M1（2026-09-03）

- Web Audio 前瞻调度内核（25ms 轮询 + 150ms 窗口），重拍/正拍/细分三档音色
- 时值可视化：音符块宽度与时值成正比
- 走带控制：播放/停止、BPM 30–240（±1 / 滑块 / TAP 测速）、拍号 2/4·3/4·4/4·6/8、总音量/重拍分层
- 播放中途调速不中断（音频时钟重映射）
- 静音拍训练（每 4 小节静音第 4 小节）
- localStorage 持久化、空格键控制、移动端自适应
