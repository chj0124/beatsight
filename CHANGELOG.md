# 变更记录

> ★ **分卷口径（v2.17.0 起）**：本文件**只保留当前大版本线（v2.x）的记录**。
> 旧大版本各归各档：[`docs/CHANGELOG-v1.md`](docs/CHANGELOG-v1.md) · [`docs/CHANGELOG-v0.md`](docs/CHANGELOG-v0.md)。
> **为什么分**：主文件一度长到近 5000 行、139 个版本条目，「查一个历史版本要在几千行里翻」——
> 而 v0/v1 两条老线的条目早就不会再改，留着只是噪音。
> **什么时候再分**：下一次 bump 到新的大版本号时，把刚过去的那条线整体挪进 `docs/CHANGELOG-v<旧大号>.md`（`tools/check-docs.js` 的第 9 项会把这事变红提醒你，不用记）。
> **条目格式不变**：每条仍是「## vX.Y.Z · 标题（日期）」+ 根因 / 取舍 / 自验数字。

---

## v2.34.0 · 音量 % 列与节奏型名恢复「同一右缘对齐」（2026-09-26）

**根因**（用户截图指出）：时值可视化卡左列（320px 定宽）里，音量行的百分号右缘只到 278px，
而上方节奏型名（`#patternName`）右缘在 368px——v2.10.17 立的「节奏型名与音量行百分号列
**同一右缘对齐**」契约只有名的一侧成立。差距 90px 全是行尾死空间：音量滑杆是 `120px` 定宽
（`input[type=range].vol{width:120px}`），行内（标签 52 + 滑杆 120 + % 38）没有弹性分配，
剩余宽度堆在 % 右侧。

**修法**：一条作用域限定规则 `.card-head-left .vol-row input[type=range].vol{flex:1;width:auto}`
——滑杆吃满行内剩余宽度，% 列自然贴到列右缘（368）。★ **不波及 BPM 的结构性保证**：BPM 组是
卡片头行 flex 的独立子项，其 x 坐标由「左列 320px 定宽（v2.10.18 钉死）+ 排列方式」决定；
本改法只重排左列**行内**三个子元素的宽度分配，列宽一像素不变——改动前后实测 BPM 组 left
均为 416，逐位相同。设置弹窗里的同款 `.vol` 滑杆（壁纸遮罩 / 延迟补偿）不在 `.card-head-left`
上下文，保持 120px 定宽不受影响。

**取舍**：滑杆变长是这 90px 的去向——拖动精度（120px 内 100 档 → 220px 内 100 档）顺带变好；
% 列 `min-width:38px + 右对齐` 既有规则不变，三条 % 天然成一条右对齐列。

**自验**：无头 Chrome 实测 1440px 下 `pct.right = pat-now.right = 368`、BPM 组 left 前后均 416；
窄屏 1024（BPM 折行到下方）同规则成立；t90d 补 CSS 契约断言（源码文本级，同 t101/t107d 口径）；
`check-all` 全量 14/14；`tests/run.js` 全绿。

---

## v2.33.0 · 设计稿遗漏补齐：＋ 搬进曲式库行、菜单点窗外即关、段卡片范围联动（2026-09-26）

**根因**（用户实测指出 + 对照设计原稿逐项盘点）：「＋ 新建」从顶栏搬进曲式库行尾、模板菜单
"点窗外即关 + 展开高亮"，是设计原稿的既定方案，但在 v2.30.0 把设计稿翻译成函数级清单时被
**静默漏掉**（D4 只拍了 chips 化与复制收敛，入口搬家没立条）；菜单交互沿用了 v2.27.0 的
"再点一次按钮 = 收起"toggle——按钮展开后外观毫无变化、菜单项又是同款胶囊挤在同一区域，
用户"点了像没点"，再点一下反而收起（实测困惑）。同批补齐另两项同类遗漏：段卡片与播放范围
无联动反馈（方案 L3 层：覆盖段绿左条 / 未覆盖段 dim 0.55）、换型候选标题缺上下文（P2-10 只修了一半）。

**修法**：

| 块 | 做法 |
|---|---|
| 入口搬家 | 「＋ 新建」从顶栏搬进曲式库行尾（`argLibRow` 包裹层，id=argNew 不变、样式改虚线胶囊）；顶栏只剩曲式级 ⋯ |
| 菜单交互 | 模板菜单宿主换成 argLibRow 就地展开；**点窗外即关**（overlay 根 pointerdown，沿 parentNode 上溯、命中触发钮/菜单本体不关）；展开时触发钮切 `.open` 绿描边 + aria-expanded；"再点一次收起"降级为防御路径 |
| 范围联动 | 段卡片 ↔ arrangeSel：覆盖段 `play-in-range`（绿左条 3px）、未覆盖段 `play-dim`（透明 0.55）；判据单一事实源在 `syncPanelRead`（拖滑块实时跟随，初挂同源补齐——首版挂在 buildSecRow 被反向验证判为冗余后收敛到一处） |
| 候选标题 | 「选」→ **「换型：第 N 段 · 块 M」**（写明上下文） |

**取舍**：互斥纪律保持（＋ 菜单与 ⋯ 菜单至多一张，两容器都扫）；点触发钮的 toggle 收起保留为
防御路径（主收口是点窗外）；曲式级重命名仍未做（D4 遗留项，需行内编辑原语，单独立项）。

**自验**：3500 PASS / 0 FAIL（新增 t109 四场景：入口位置源码断言 / 展开高亮 + 点窗外即关 /
段卡片联动含拖动中实时跟随 / 候选标题；t54/t105/t106 的菜单宿主定位 argActions→argLibRow 全量迁移，
t106d 残影防御扩到两个容器）；反向验证 5 组变异全红（含一次自我纠错：buildSecRow 初挂块被
M2 判为死代码 → 收敛到 syncPanelRead 单一事实源后 M3' 单点变异 3 断言齐红）。

---

## v2.32.0 · 编排重设计 S3：歌词默认折叠，锚点提示音全局化（2026-09-26）

**根因**（PLAN-v4 S3，拍板 D2）：歌词编辑（贴词 / 拖字块）是"贴词后的微调"进阶操作，却让每段
常驻一整条编辑器（开关 + 按钮 + 粘贴框 + 字块轨 + 提示 ≈ 120px/段），10 段示例曲的编排页被撑到
3–4 屏，段高过大又放大了 S2 修掉的"开练动线断裂"；「锚点提示音」是全局开关（`S.lyricCue`）却在
每段歌词行重复渲染（10 段 = 10 个同款开关），语义误导 + 视觉噪音。

**修法**：

| 块 | 做法 |
|---|---|
| 歌词折叠 | 每段歌词区默认折叠为**摘要行**：状态签（歌词 ✓ N 字 / 未贴歌词）+ 前几字预览 + ▸ 展开编辑；粘贴框 / 清除 / 字块轨 / 循环本行只在展开态渲染；段高从 ~130px 压到一行 |
| 展开态 | 内存 Set（键 = arrangeId\|secUid）跨渲染存活（落库重渲染不弹回），**不持久化**（D2：折叠是纯浏览态，"上次展开" ≠ "这次想展开"），open()/close() 清空 |
| 锚点提示音 | 收进开练面板（`argLyricCue`，全书唯一一份），段行内不再重复渲染；开关不再触发整页重渲染（旧实现每次点击刷 10 个副本） |

**取舍**：贴词多一步展开点击——换来编排页一屏能看到全部段结构；展开态不持久化是刻意的
（持久化反而违背"默认折叠降噪"的初衷）；「循环本行」挪进展开区（它是歌词行循环语义，
与折叠摘要不冲突）。

**自验**：3471 PASS / 0 FAIL（新增 t108 三场景：摘要内容与控件不渲染 / 展开收起 + 重渲染保持 +
关浮层重置 / 锚点开关唯一性与读写；t60f 迁移——先展开再测编辑轨内部，锚点断言改用面板按钮）；
反向验证 3 组变异全红（折叠门被拆 / 面板开关不写 S / 展开态下关浮层不清 Set——★ 第二组曾因
"先收起再关浮层"的测试流程打不红，已把流程改成展开态直接关，变异验证才真闭环）；
帮助页歌词词条补折叠与锚点位置说明（无版本号字面量）。

---

## v2.31.0 · 编排重设计 S2：开练面板——歌曲地图 + 双滑块，「起 / 终」退役（2026-09-26）

**根因**（PLAN-v4 S2，拍板 D1/D5）："设范围 → 开练"动线断裂——播放范围区沉在整页底部，
10 段示例曲要滚 3–4 屏；范围入口还分散三处（段行「起/终」、底部只读行、侧栏滑块），
两种心智模型（"点哪段设哪段" vs "拖小节区间"）并存不打招呼。

**修法**：

| 块 | 做法 |
|---|---|
| 开练面板 | 编排卡片改**双栏**：右栏「开练」面板（`#argPanel`，sticky 吸视口）= 歌曲地图 + 范围双滑块 + 范围循环 + 全部 + 从头播/播选中范围 + 问题提示；≤960px 面板提前到段落结构上方，≤640px 变**底部固定条**（safe-area 适配） |
| 歌曲地图 | 每段一粒胶囊、宽度按段小节数等比（`flexGrow`），范围覆盖段绿框高亮；**点某段 = 只循环练这一段**（setRange 同一出口） |
| 双滑块 | 侧栏 `.demo-range` 那套原生叠层的编排侧翻版：小节刻度 1..TB、input 只写 S+视觉 / change 提交才跑 setRange 重活、起点越过终点 → 对方被顶走（双向归一） |
| 练这段 | ⋯ 菜单新增**第一位**菜单项：一键范围=本段起止 + 循环恒开（接棒段行「起/终」，覆盖"只练副歌"） |
| 退役 | 段行「起 / 终」两钮删除（ops 收成 [▶, ⋯]）；旧「播放范围」只读行删除；「全部」转正为面板静态按钮；`setRange` 增第三参 `{loop}`（缺省 true，既有调用方零改动） |

**取舍**：范围入口三处收敛为两处（地图/滑块 + 练这段），精确到小节的"段中间开始"场景
由双滑块承担；「练这段」恒开循环——"只放一遍不循环"的语义已有「▶ 试听」（额度到点停），
不再给它第二个入口。

**自验**：3447 PASS / 0 FAIL（新增 t107 四场景：地图比例与高亮 / 双滑块两级处理与双向归一 /
起终退役 / 窄屏 CSS 契约；t54e/f 的起终定位迁移到「练这段」+ `argAllRange`，t106/t104 的 ops
口径同步 [▶, ⋯]）；反向验证 4 组变异（地图等分 / input 阶段跑 setRange / 删双向归一 / 点段
不设循环）全部打红；帮助页「播放范围」词条改写（无版本号字面量）。

---

## v2.30.0 · 编排曲式重设计 S1：段卡片化，操作收进 ⋯ 菜单（2026-09-26）

**根因**（重设计方案 PLAN-v4，用户已拍板 D1–D6）：段行是"8 颗小钮平铺一行"的横向单行 flex——
范围 / 试听 / 重排删除三种语义混排、文字与符号混杂，移动端必然折行；块列被段名与操作列挤到
只剩约 1/3 宽，型名竖排成 3–4 行不可读；「复制」与新建菜单里的「复制当前曲式」是同一动作的
两个入口（help 页"两个编辑入口"的混淆在曲式层重演）；390px 下 `.editor-topbar` 没有任何
窄屏规则，「返回练 习 / 曲式编 排 / 新建曲 式」整行断字。

**修法**：

| 块 | 做法 |
|---|---|
| 段操作 | ops 收成 **[起, 终, ▶, ⋯] 四颗**：重排（上移/下移/移到最前/移到最后）与删除收进段行 ⋯ 菜单（`mkSecMenu`，全文字标签 + aria 定位，动作后自收）；▶ 试听保持常显；`secMenuOpen` 单例、与换型候选 picking 互斥 |
| 段卡片 | `.arg-sec` 从"单行 flex + 底分割线"改**卡片**（flex-wrap + CSS order 重排：第一行 = 段号徽章 + 段名 + 操作，块行 / 歌词行各占整行）；**DOM 顺序一个不动**——t54/t60 的裸下标与歌词轨 children[4] 定位零迁移 |
| 块胶囊 | 横排不折行（`nowrap + overflow-x:auto`），胶囊化（圆角 999 + 描边），触控目标加大（`.arg-mini` 5px 10px） |
| 曲式库 | 竖列表改**横排 chips**（`.arg-item` 结构与 `.sel` 选中态不变，纯 CSS）；「新建曲式」＋ 仍在顶栏 |
| 曲式级 ⋯ | 「复制 / 删除」收进顶栏 `argLibMore` 菜单（`toggleLibMenu`），与新建模板菜单互斥；arrangeRender 重建时摘掉两张菜单（unbind 后的残影 = 死 UI） |
| 窄屏 | `.editor-topbar` 首次有了 ≤640px 规则（缩字距 / 缩按钮 / 禁收缩），断字修复 |

**取舍**：不做自由拖拽排序与段多选（测试桩驱动不了自绘拖拽，菜单五项覆盖既有场景）；
「复制」从一键变两步（开 ⋯ → 点复制）——换"同一动作只有一个入口"；起/终钮**本阶段保留**
（S2 才随开练面板退役，阶段间单一职责）。

**自验**：`tests/run.js` 3423 PASS / 0 FAIL（新增 t106 四场景 21 断言；t54/t103/t104/t105
的裸下标定位迁移到「开菜单 + aria 定位」，断言口径变更理由已写进各用例注释）；
反向验证 4 组变异（more 不收候选 / pick 不收菜单 / 重渲染不摘残影 / mkEnd 不收菜单）
全部打红且命中具名断言；DOM 静态标记净 0（删 argCopy −1、增 argLibMore +1）。

---

## v2.29.0 · 弦区三档化：中弦区退役，全扫归位（2026-09-26）

**根因**（用户多轮问答校准）：弦区的真实语义是「拨片扫过哪几根弦」——低=④⑤⑥、高=①②③、
**全扫=6 根**；而「中弦区」从来没有明确的弦数对应，是模型里一个无法回答"扫哪几根"的悬空档。
更早的病灶在示例曲录入：参考谱的「全」（F）当年被映射成 `zone=1`，而工具模型把 1 当中弦区——
《在他乡》里所有该全扫的重拍听感发闷。视觉层（v2.4.2 起）一直把 1 画成贯穿六线的 `.kF` 全跨度，
于是「看着是全扫、听着只有中频段」的视听分裂长期存在。

**修法**：

| 块 | 做法 |
|---|---|
| 模型 | 弦区档位 = **全扫（缺省不写 zone）/ 低弦区(0) / 高弦区(2)**；老数据 `zone=1` 一律**读作全扫**（当年谱面映射 1 就是"全"），零迁移。`VALID_ZONE` 仍收 1（合法别名），脏值照旧静默降级 |
| 发声 | `strumZoneHit` 两频段化：低(700Hz)/高(2800Hz) 单频段照旧；**全扫 = 低+高同刻各一声**（六根弦全响理应更满，两颗各按全额 peak；`CONFIG.strumZones.MID` 退役）。dir-only 的格由调度入口补成全扫（沿用旧值 1，时刻一字不动） |
| 示例曲 | P1/P4/P5 三处实扫 `zone=1` 删字段（=全扫）；谱面注释勘误「1=全」的映射说明 |
| 编辑器 | 弦区行四档→三档「全扫（默认）/ 低弦区 ④⑤⑥ / 高弦区 ①②③」（弦数上按钮，自解释）；新音符缺省即全扫；老值 1 高亮「全扫」档（不孤悬）；`.ed-zone` 徽标 1→「全」 |
| 使用方法 | 弦区释义重写：三档 + 弦数对应 + **方向与弦区互不约束** + 缺省即全扫 + 全扫=两档一起响 |

**取舍**：不新增「中弦区」迁移——1 读作全扫与视觉层既有行为一致（本就画 `.kF`），
老数据零迁移、视听终于统一；「全扫两颗各按全额 peak」而非折半——物理上六根弦本就比三根弦响，
且 700/2800Hz 分居两端不互相掩蔽。

**自验**：`tests/run.js` **3386 PASS / 0 FAIL**（t62 契约重写：全扫双频段同刻、空扫静默、
旧值 1 读全扫的发声/徽标/高亮三口径；t20 同刻不变量两声→三声；t68c/t76 dir-only 计数翻倍）；
**变异反向验证 4/4 变红**（全扫丢高频段 / 示例曲 1 回潜 / 高亮映射回退 / 徽标回「中」各被对应断言抓住，
还原后 `grep MUTATION` 归零）；`tools/check-all.js` 14/14（见下）。

---

## v2.28.0 · G1 入口收敛：删「整首连播」按钮，读数行接棒状态指示（2026-09-26）

**根因**（PLAN-v3 阶段三，优先级最低但改动面最小）：曲式条目点击**已经等于**播放整首
（v2.5.0 起 `playArrange` 是唯一出口，按钮只是它的第二入口）——同一动作摆两个入口，
用户要多处理解一次"这两个按钮有什么区别"，而答案是没有区别。

**修法**：

| 块 | 做法 |
|---|---|
| 删按钮 | `buildDemoSongRow` 行内只剩状态读数一个子节点；`demoPlayAllBtn` 引用与 whole 高亮段整条删除。**行类名 `demo-play-row` 不变**（t68/t63 按它定位） |
| 读数接棒 | `whole`（曲式模式 + 范围恰是整首）的唯一呈现面改为读数行：整首播放中 → **「整首连播 · 播放中 · 第 N/总 小节」**；停机 → 「整首连播 · 已就绪 · 共 N 小节」；局部范围维持原两种说法。位置读数（T68j）一位未动 |
| 帮助页 | 词条改写：入口 = **点曲式条目**；状态看读数行（两态都说清）；旧「分组顶部那个按钮」提法删除 |
| README | 「曲式编排」条目同步入口收敛说明；文档表标阶段三已交付 |

**零交互损失论证**：条目点击（播整首）+ 读数行（状态）= 原按钮的全部用户价值；
少一个与条目等价的按钮，少一处"两个入口同一动作"的认知负担。`playArrange` 一行未动。

**自验**：`tests/run.js` **3379 PASS / 0 FAIL**（t55 补 3 条内容契约：入口 = 点条目 / 读数两态 / 旧按钮提法绝迹；
t68g/t73/t74/t75/t88 的 `playAllOf` 助手从"点行内按钮"改写为"点示例曲条目"——曲式条目自 v2.25.0 起
挂在曲式分组容器内，不是 presetList 的直接子节点；t88j 从"按钮高亮"改写为"读数三分支"）。
**反向验证（3 个定向变异）**：① 读数播放中分支删 whole 判定 → **2 条红**（t68g/t88j 前缀断言）；
② help 恢复旧「按钮」提法 → **1 条红**（t55 绝迹断言）；③ 连读数一起删 → **装配期 ReferenceError**
（loadApp 直接失败，应用崩溃级——smoke 的"启动成功"断言必红；非具名断言，如实记录）。
还原后全绿。`tools/check-all.js` 14/14。

**PLAN-v3 全部三个阶段至此闭环**：G3 段稳定 uid（v2.26.0）→ G2/G4 编辑效率（v2.27.0）→
G1 入口收敛（v2.28.0）。当初拍板排除的项（变拍号 / 段落类型标签 / 多范围保存 / 自由拖拽 /
换型搜索框）维持不做。

---

## v2.27.0 · G2/G4 编辑效率：模板新建 · 复制曲式 · 移到首尾 · 候选三区 · 逐段试听（2026-09-26）

**需求**（PLAN-v3 阶段二，优先级 G2 > G1；G4 成本极低作附属项一并落地）：编排面板的四个高频痛点——
建曲式要从一段白手起家、没有"复制一条改一改"的路径、把第 3 段挪到第 1 位要连点两次「↑」、
候选是 24 个型平铺一长串找不到；外加"想听某一段编得对不对，只能整首从头放"。

**修法**（全部**纯数据预填 / 复用既有机制**，零新抽象）：

| 块 | 做法 |
|---|---|
| 模板新建 | 「新建曲式」改成四选一菜单：**空白**（= 旧一键直建的同款产物，逐位不变）/ **主副歌骨架**（2 段）/ **完整流行曲式**（前奏-主歌-副歌-间奏-主歌二-副歌二 6 段）/ **复制当前曲式**。骨架与完整流行的遍数 = ceil(目标小节 ÷ patBars(当前选中型))，引用 = 当前选中型（custom 也行）；产物一律过 `Store.upsertArrange`（校验 / uid 发放 / 持久化同一条边界）。菜单是**运行时生成**的一行 pill（静态标记零增） |
| 复制曲式 | 顶栏动作区加「复制」pill（静态 +1 节点）：深拷贝 + **摘掉 id**（带着原 id 去 upsertArrange 会命中"同 id 替换"，表现为"点复制没反应"）+ 副本命名（重名自动"副本 / 副本2 …"）。**歌词行不跟着复制**（词挂原件；与导出整包的合并语义一致）。段的 uid 随段带过来——跨曲式重复是允许的（唯一性判据是同曲式内） |
| 移到首尾 | 段操作钮扩为 [起/终/↑/↓/✕/**⤒/⤓/▶**]：splice + unshift/push 一步到首/尾，首段的 ⤒ 与末段的 ⤓ 置灰。★ 新钮**追加在 ✕ 之后**——t54 按裸下标定位既有三钮，插前面会把定位全部挪位 |
| 候选三区 | 候选从平铺改**节拍 / 扫弦 / 自定义**三区（判据与侧栏同一份 `hasStr`，两处看到的分类必然一致）+ **当前型置顶**（带 ✓；原分区里不再重复）。空区不摆空标题。点当前型 = 空操作 |
| 逐段试听 | 段行加「▶」：范围 = 该段起止小节 + `loop:false` + **playQuota = 段小节数**（复用听辨训练那套「到点停」——v2.10.12 后它是该机制唯一消费者，额度必须在 `Controls.start()` **之前**设，Ear 同款时序）。不动 `loopLyricSection`（那是"循环+爬坡"的另一语义） |

**DOM 预算**：静态净增 1（`#argCopy`）——v2.26.1 瘦身腾出的 89 个余量在这里花掉 1 个，
播放态实测 **1212 / 预算 1300**。帮助页「曲式编排」章节补 2 条词条（三区换型 + 模板/重排/试听）。

**自验**：`tests/run.js` **3376 PASS / 0 FAIL**（新增 `t104-arrange-editor-v3` 3 场景、
`t105-arrange-templates` 3 场景；t54 的模板菜单流同步改写）。
**反向验证（4 个定向变异）**：① 模板脏写绕过 upsertArrange → **7 条红**（冷键没写 / uid 没发 / 结构没校验）；
② 候选删「当前型置顶」→ **3 条红**（t104b + t54d 下标位移连锁）；③ stop 不清会话额度 → **1 条红**
（T104c「不污染下一次播放」）；④ 复制浅拷贝 → 无观察面（如实记录：落库时 `normArrange` 会从
传入对象**重建**段对象，浅引用在落库路径上被遮蔽——深拷贝断言保留，作为未来直改库路径的护栏）。还原后全绿。
`tools/check-all.js` 14/14（含 ESLint / tsc 各 0 错误）。

**顺带**：桩补标准方法 `removeChild`（模板菜单开合用它移除运行时节点；桩原先只有
appendChild / insertBefore，「移除」表达不出来）。编排面板动作区容器加 `id="argActions"`
（不加节点）——测试桩从标记**惰性**建元素、不建静态父子关系，任何"靠 parentNode /
兄弟位置"的定位在桩里都不可靠，按 id 取容器才是稳的。

---

## v2.26.1 · DOM 瘦身：填充层与六线底纹改由 CSS 画（1299 → 1211）（2026-09-26）

**动因**（阶段二开工前的硬前提）：v2.25.3 结尾时 DOM 节点数是 **1298 / 预算 1300**，
`tools/smoke.js` 的注释明写「再贴近应先瘦身而非放宽预算」。阶段二要加 UI（模板菜单、块胶囊、
移到首尾钮、复制 pill），**不能靠放宽预算腾地方**，故先瘦一轮。

**两处改动，都在"纯装饰、从不参与帧内更新、也没有测试按它定位"的节点上**：

| 块 | 原来 | 现在 | 净减 |
|---|---|---|---|
| 格子填充层 | 每格一个 `.fill` 子节点，`style.transform = scaleX(x)` | `.cell::before` 伪元素，推进量写格子的 **CSS 变量 `--f`** | **−64**（16 格 × 4 行） |
| 六线底纹 | 每层 `.tab` 里 6 个 `<i>`，逐条写 `top: calc(--gtop + li × --gt)` | `.tab` 一层**背景渐变**（步长 `--gt`，限高到第 6 条），零子节点 | **−24**（6 × 4 行） |

实测 DOM 节点 **1299 → 1211（−88）**，预算余量从 1 个回到 89 个。

**为什么填充层能这么改**：它既不承载状态（状态在 `.cell` 的 `played`/`active` 上），
也不被任何测试按类名定位；`--f` 与 `scaleX(x)` 逐位等价，`left/top/bottom/width/transform-origin/z-index` 全部照搬。
★ 顺带修掉一处"两处几何"：六线的间距此前 JS 里写一遍（`li × --gt`）、CSS 里再写一遍，现在只剩 CSS 变量一份。
★ 渐变**必须限高**（`background-size` 到第 6 条线 + 1px）：`.tab` 铺满整行而不是 44px 格子，
不限高就会一路重复画出第 7、8 条弦线。

**护栏**（改装饰层最怕"悄悄不画了"，故两边各钉一条）：
- **桩**（`T79h`）：值**被写进 `--f`** —— 见过 0<scaleX<1 的中间态 + 停机后全部复位为 0；
- **真机**（`smoke.js` 新增 1 条）：播放态读 `.cell::before` 的**计算 transform**，scaleX 必须 > 0.05
  （伪元素与样式引擎桩都没有，只有真机量得到"真的填白了"）。
- 反向验证：① 把六条 `<i>` 加回来 → 2 条红（T47 / T86「零子节点」）；
  ② 删掉逐帧推进那一行 → 1 条红（T79h「填了一半的中间态」）。还原后全绿。

**自验**：`tests/run.js` **3302 PASS / 0 FAIL**；`tools/check-all.js` **14/14**
（含 ESLint / tsc 各 0 错误）；`smoke.js` 65 项断言（DOM 1211 / 帧率 61fps / 控制台零报错）。

**顺带**：`tests/lib/harness.js` 的元素桩补上 `setProperty / getPropertyValue / removeProperty`
——CSS 自定义属性**没有** `style.foo` 这种写法（`style["--f"]=1` 在真实 DOM 里静默无效），
桩原先只模拟属性读写，产品一用 `setProperty` 就抛"不是函数"。

---

## v2.26.0 · G3 段稳定 uid —— 歌词跟着段走，不再跟着位置走（2026-09-26）

**根因**（真实缺陷）：歌词行此前按 `(曲式id, 段下标)` 寻址。下标是**位置**不是**身份**——
把第 1 段下移一位、或删掉中间某一段之后，挂在这个下标上的那行词就**跟着位置走**，
落到另一个段上。而用户的心智是「这行词属于我写的那一段」，段挪到哪儿词就该跟到哪儿
（PLAN-v3 阶段一，优先级列在编辑效率与入口收敛之前）。

**修法**：

| 块 | 做法 |
|---|---|
| 段身份 | `normArrange` 给每个段发一个 `uid`（`"s" + Date.now() + "-" + 序号`，同 `arrSeq`）。三档判据：自带有效且未重复 → 用它；否则**按位置继承**库里同 id 旧版本的 uid；都没有 → 补发 |
| 歌词寻址键 | 行形状 `{arrangeId, sec, chars}` → `{arrangeId, secUid, chars}`；`findLyric / upsertLyric / deleteLyric` 与 `lyricSpanTicks / lyricCharsAt` 全部改收 `secUid`；调用方（`mkLyricRow` / `distribute` / 歌词轨 / `emitLyricCues` / `ensureDemo`）按 `sections[at.sec].uid` 换算一次 |
| 冷键迁移 | `beatsight.lyrics` **v:1 → v:2**：加载期与导入期都过一遍 `lyricMigrate` —— ① 已带 secUid 直接用（幂等）；② 旧 `sec` 下标 → 该段 uid；③ 下标越界 → 按 `secName` 再匹配一次；④ 都失败 → 计入坏行丢弃（口径同结构校验失败） |
| 整存整取 | `upsertArrange` 把库里的旧版本递给 `normArrange`：**只在调用方没带 uid 时**按位置继承。示例曲重建（spec 恒不带 uid）因此段身份不变，挂着的词不会失配 |
| 删曲式 | `deleteArrange` 连带清掉它的歌词行——曲式删了再重建是**全新的段、新 uid**，旧行留着只会白占 256 行配额且永远不可见（同 v2.25.0「组里不留幽灵成员」那条纪律） |

**★ 语义边界（重要，别被当成 bug）**：迁移把**当前**的下标对应关系冻结为 uid 绑定，
修复的是「**未来不再错位**」；历史上已经错位的歌词**不追溯修复**——机器无从判断本意，
那需要用户人工核对。段序大改之后建议顺手核对一次歌词。

**自验**：`tests/run.js` **3287 PASS / 0 FAIL**（新增 `t103-lyric-sec-uid` 7 场景 48 条；
t60 / t61 / t63 / t76 / t79 / t83 / t96 的寻址键断言同步改写为段 uid）。
**反向验证（5 个定向变异，逐个隔离 + 还原）**：
① 段移动后 uid 按位置重发 → **4 条红**（T103a 两处「下标 N 现在是某段」+ uid 跟随 + T103f 范围）；
② 删掉「按段名匹配」兜底 → **4 条红**（T103e）；
③ `loopLyricSection` 改回按下标 → **3 条红**（T61a / T63d / T103f）；
④ 落盘仍写 v:1 → **2 条红**（T60a / T103c）；
⑤ 加载期不做迁移 → **10 条红**（T103c / T103d / T103e）。
还原后全绿。`tools/check-all.js` 14/14。

**不动的**：`arrNextBar` 节目单步进、scheduler、时间轴不变量、窗口/预告行逻辑——
本轮全部落在数据层与歌词轨的寻址层。UI 结构零改动（DOM 节点数不变）。

---

## v2.25.4 · 「新曲式」裸文本 chip 化 + 弦区释义补"哪几根弦"（2026-09-26）

**需求**（用户实拍 + 追问）：① 预设卡「编辑节奏型 / 编排曲式」按钮后面跟着一个裸的「新曲式」文案，看着像 bug——排查后**不是孤儿文案**：它是 `#argNowName`（「整首连播 / 范围滑块」当前服务的曲式名，四个状态写入点共用），只是裸文本无样式、无上下文，读起来像残留；② 弦区释义太粗糙——追问"低弦区代表的是哪几根弦"。

**修法**：
| 块 | 做法 |
| --- | --- |
| 当前曲式 chip（①） | 纯 CSS：`.arg-now-name` 描边胶囊 chip + `::before` 前缀「当前曲式 · 」（四个 textContent 写入点一行不改）；`:empty` 时整枚藏起（无曲式不留空壳）。裸文本的"bug 感"来自缺上下文与缺样式，前缀 + chip 让它读作有意为之的状态显示 |
| 弦区释义（②） | 补"哪几根弦"：以标准调弦（⑥E ⑤A ④D ③G ②B ①E，⑥ 最粗）说明——低弦区偏向 ④⑤⑥ 粗弦音闷、高弦区偏向 ①②③ 细弦音亮、中弦区介于两者；并点明本质是**音色档位**而非逐弦开关（应用以三档滤波频率 ≈1:2:4 模拟拨弦位置偏低的明暗），不改节奏与发声时刻 |

**自验**：`tests/run.js` **3237 PASS / 0 FAIL**（T90z 新增 chip CSS 契约 3 条；t55 弦区断言补弦数与"音色档位"）。变异反向验证：移除 ::before 前缀规则 → T90z 红；还原全绿。`tools/check-all.js` 14/14。

---

## v2.25.3 · 使用方法补两块重点讲解：两个编辑入口的分工 + 弦区四态释义（2026-09-26）

**需求**（用户提出）：① 重点讲清「编排曲式」与「编辑节奏型」的区别——这两个按钮并排在预设卡片上，是最容易混用的地方；② 补上编辑器「弦区」行（默认（中）/ 低弦区 / 中弦区 / 高弦区）的解释。

**修法**（纯说明文字，零逻辑改动）：
| 块 | 做法 |
| --- | --- |
| 两个编辑入口 | 展开版**第一条**新增「先分清两个编辑入口」dl：编辑节奏型 = 定义零件（一小节/几小节的音型，可单独循环，改了所有引用同步变）；编排曲式 = 装配单（段/块/遍、顺序、范围，段落里放的是引用不是拷贝）；一句话记 =「节奏型是零件，曲式是装配单」；何时用哪个。简明版（不展开也能看到）加一条 help-tip 路标 |
| 弦区释义 | 「自己画节奏型」章节的空扫条目之后新增一条：默认（中）/ 低弦区 / 中弦区 / 高弦区四态逐一解释（低=闷、高=亮、中=居中、默认=不标注时的中弦区，老数据零迁移），并说清边界——**只改音色明暗（滤波频率），不改节奏与发声时刻** |

**自验**：`tests/run.js` **3231 PASS / 0 FAIL**（t55 新增 4 条内容契约：入口分工 / 零件-装配单 / 弦区四态 / 只改音色边界）。变异反向验证：删除弦区释义条目 → t55 两条红；还原全绿。`tools/check-all.js` 14/14。
**顺手（DOM 瘦身纪律）**：新增说明把 DOM 节点顶到 1313（smoke 预算 ≤1300，其注释明言"再贴近应先瘦身而非放宽"）——四个 help 词条列表全部由 dl（每词条 dt+dd 两节点）改 ul（单节点，粗体引导词读感等价）、四个 tip 合并进相邻节点，净减 15 节点至 **1298**，预算未放宽。`.help-dl` 样式随之清空删除。

---

## v2.25.2 · 使用方法去重与排版改版：「主界面控制项」收窄 + 词条列表单列堆叠（2026-09-26）

**需求**（用户实拍）：「使用方法页的排版格式混乱，内容也有重复」——v2.25.1 新增「设置里的每一项」时没同步清掉旧「全部控制项」里与它重复的五个词条（Swing / 音色 / 延迟补偿 / 弹跳球 / 后台保活），两份说明背靠背；且 `.help-dl` 是双列 grid（max-content + dt 右对齐），词条名一长（"音频延迟补偿（蓝牙耳机 / 无线音箱）"）列宽就被撑爆、右对齐让视线来回跳。

**修法**：① 「全部控制项」收窄为**「主界面控制项」**——只留卡片上的三条（速度 BPM / 拍号 / 音量三滑杆），其余词条只住在「设置里的每一项」，文末加指路提示；设置小窗里「使用方法」按钮下的章节索引文案同步更名。② `.help-dl` 改**单列堆叠**：词条一行加粗、说明缩进在下，对任意长词条稳定，窄屏天然可用——原 640px 媒体查询（双列转单列的补丁）随之删除。

**自验**：`tests/run.js` **3227 PASS / 0 FAIL**（T102b 新增去重契约：旧标题更名 + 四个重复词条 dt 清零 + 「主界面控制项」在前；T55 展开版章节名单同步更新）。变异反向验证：把重复词条 `<dt>Swing</dt>` 塞回主界面控制项 → 去重断言变红；还原全绿。`tools/check-all.js` 14/14。

---

## v2.25.1 · 设置→使用方法层叠修复 + 「设置里的每一项」详解章节（2026-09-26）

**需求**（用户实报三连）：① 点 设置→使用方法，说明打开后设置小窗**没有自动消失**——Help 是全屏 overlay、设置是浮层小窗，两者各自 `.open` 叠着，读完说明「返回练习」眼前还糊着一层设置；② 使用说明缺「后台保活」的像样说明（原文一行"耗电换连续，按需开"，说不清它到底做什么、代价是什么）；③ 缺设置里各选项的逐项详解——Swing 与延迟补偿最容易被问「这是啥」。

**修法**：
| 块 | 做法 |
| --- | --- |
| 层叠修复（①） | `Help.open()` 先 `Settings.close()` 再开自己（Settings 声明在前，前向引用合法）。不做成"读完结回设置"——说明是低频一读，返回练习落回主界面比落回一个小窗更符合预期 |
| 内容（②③） | helpMore 里新增「设置里的每一项」dl 章节（训练模式之后、曲式编排之前）：主题 / 壁纸 / 弹跳球与六线 / 音色 / **Swing**（前长后短的直觉 + 直/67%/75% 三档含义 + "只改耳朵不改画面"的设计边界）/ **延迟补偿**（蓝牙 100–300ms 成因 + 校准向导敲八下 + 按设备存配置 + 有线保持 0）/ **后台保活**（症状=系统暂停网页声音、两级手段=屏幕不熄灭与 iOS 静音音频、代价=耗电、默认关）/ 导入导出 / 诊断。章节插在「训练模式」之后——读者已知主界面，自然轮到"藏在设置里的东西" |

**自验**：`tests/run.js` **3220 PASS / 0 FAIL**（新增 T102 两场景 11 条：层叠修复真 DOM 事件驱动（开设置→开说明→设置已关→关说明→可重开）+ 内容契约（章节位置与三类关键词全覆盖））。变异反向验证：层叠修复禁用 → T102a 两条红（设置未关 + 关说明后幽灵回归）；还原全绿。`tools/check-all.js` 14/14。

---

## v2.25.0 · 预设库分组重构：区标题 ＋ 新建 + 拖拽归组，三区全开放（2026-09-25）

**需求**（用户提出，四项设计 AskUserQuestion 全按推荐拍板）：① ✎ 改名图标常显 → 悬停显示；② 去掉条目上的 📁「移入分组」，改在**区标题**（节拍/扫弦/自定义）旁加「新建分组」＋，组建于标题下一级，条目**拖拽**入组。②与 v2.22.0 的「自定义区不参与」边界冲突，用户拍板**三区全开放**（组员 = 曲式 id，即"练习歌单"用法）；触屏保留 📁（原生拖拽在触屏不可用，按钮是唯一归组入口）；移出 = 拖到区标题/散员上松手；不做组内排序（成员保持原库序，数据模型零改动）。

**修法**：
| 块 | 做法 |
| --- | --- |
| ✎/📁 设备分流 | 纯 CSS：`@media (hover: hover) and (pointer: fine)` 内 ✎ opacity 0→1（悬停/`:focus-within`，键盘可达性不丢）、📁 display:none（`:focus-within` 回来）；触屏（不进该块）两者常显。DOM/JS 路径单一，分流只发生在 CSS 层 |
| 区标题 ＋ | `pushSection` 加 `.sec-add` 按钮——**铁律不破**：t63/t84b 逐字比对标题 textContent，按钮不带任何文字节点（字形走 CSS `::before`），click stopPropagation 防误折叠；后端 `Store.groupCreate`（空组架子，同名幂等） |
| 拖拽归组 | 原生 HTML5 DnD，零依赖。拖拽身份走**模块级 `dragItem`**（{ref, zone}）而非 dataTransfer 序列化——测试桩不必实现 DataTransfer，drop 处理器天然拿到带类型 ref；`dragover` preventDefault 放行，`dragenter/leave` 用 WeakMap 每元素计数器（子元素反复触发 enter/leave 对，布尔位会闪）。落点四类：组头/组成员 = 移入该组（折叠组也能接，移入自动展开）、区标题/散员 = 移出。跨区拖放按 zone 校验整体忽略 |
| 自定义区 | `buildArrangeGroup` 重构：控件行（整首连播/滑块）保留在 wrapper 首位，其后组头+成员（带 data-grp）+散员；`applyFold` 对 wrapper 内部套同一份两级真值表（wrapper 自身按区折叠）。**刻意不扁平化**：t63/t68/t73/t74/t75/t88/t89/t93 九个测试以 `.preset-arrange-group` 为曲式区容器契约，提升为直接子节点会全线失配——与节拍/扫弦区的"直接子节点"铁律分容器适用 |
| Store | `GrpRef` 加 `{type:"arrange", id}`、`Grp.zone` 加 "custom"（加载白名单/groupMove/groupOf 同步三向化）；新增 `groupCreate` / `groupPruneArrange`（`deleteArrange` 挂清理口，对称 `groupPruneCustom`）。冷键结构不变，**老数据零迁移** |

**自验**：`tests/run.js` **3208 PASS / 0 FAIL**（基线 3178 = +30：T101 五场景——区标题新建端到端/拖拽归组/移出与跨区忽略/自定义区与删除清理/✎📁 CSS 分流；T98h 脏区样例改 "foo"（custom 已合法）、T98i 改判为"曲式条目有 📁"）。变异反向验证 2/2 被网住：①＋ 点击空操作 → T101a 四条红；②跨区守卫移除 → T101c 跨区断言红（扫弦组误收 beat 条目）。T27 检查器随 v2.24.1 的预告行感知升级后对本版零误报。`tools/check-all.js` 14/14。

---

## v2.24.1 · 预告行免已弹刷白推广到预设：格子恒按未弹画，底纹与箭头透出来（2026-09-25）

**根因**（用户实拍对比，v2.24.0 上线后）：「希望预告行像曲式那样（半透明、内容完整），而不是像预设那样遮挡住部分内容。」病灶在 `setCell` 的已弹刷白判据——「行号 < 当前行 → 已弹 → 白填充 scaleX(1)」：这条对曲式长窗口成立（上面的行真的是刚弹过的小节），但对预设绕行语义是错的——**非在播行是"绕回的未来"，不是"过去"**（v2.23.0 淡显的注释自己写着这句话，刷白判据却没跟着改）。白填充叠上预告淡显 = 一排灰色实心块，把六线底纹和箭头全盖住。曲式之所以没这个问题，是因为 v2.7.0 起有 `isPreview` 豁免（"预告行免已弹刷白"），但豁免条件写死了 `playMode === "arrange"`，预设享受不到。此问题 v2.23.0 淡显阶段就已存在，v2.24.0 给预告行挂上胶囊、成为明确的"预告行"后，与曲式一对比才显眼。

**修法**：豁免判定收口为 `isPreviewRow(b, bar)`，预告行恒按未弹画——四类：① 曲式带预告的第 1 行（原有）；② **预设短型（P<W）的全部非在播行**（绕行的行都是未来）；③ 预设轻量预告（P≤W 标记亮起）的第 1 行；④ 预设重建预告（winPrevSeg ≥ 0）的第 1 行。**长型/等宽预设的非预告行不在豁免内**——它们是本页真实弹过的小节，已弹刷白照旧（与曲式非预告行同行为）。豁免对 `.next` 预告格透明（该亮的一格照亮），fill 恒 scaleX(0)。配套把 T27 的「增量==全量」检查器期望值升级为预告行感知（行上有 `.preview-row` ⇒ 整行 upcoming）——旧口径「行号<active→played」会把新契约误判成破例。

**自验**：`tests/run.js` **3178 PASS / 0 FAIL**（T100b/T100c 新增"预告行格子恒干净"断言：`.preview-row` 行内不得有 `played` 格；T27 检查器升级后原场景全绿）。变异反向验证：把豁免退回"曲式限定"→ T100b（违例 50）/T100c（违例 196）/T27 三处变红，遮挡当场复现；还原后全绿。`tools/check-all.js` 14/14。

---

## v2.24.0 · 预设待命球绕行落行 + 预告行扩展到全部型：短型与 P===W 轻量标记（2026-09-25）

**根因**（用户实拍两连，扫弦预设库）：①「扫弦区除了民谣扫弦，其他型的弹跳球·预判提示都有问题：同屏行数大于 1，播放到第二/三/四小节前，预判动画都显示在第一小节上」；②「扫弦区所有项目，播放到当前页最后一行时都不显示预判行」。两个症状一个共同背景——v2.23.0 的「预设窗口手感对齐曲式」只修了一半：① 的病灶在 `paintBall` 的**待命球**分支，落行用旧公式 `segNext % vizBars`（那行注释自己写着"型长>行数时它才是对的答案"），主球的行号已改圈号解析（`rowOfOnset`），待命球这处漏网——短型（P<W）下 `segNext` 恒 <W，待命球恒钉第 1 行；民谣扫弦是 `rep4` 4 小节型（P===W），旧公式恰好逐位正确，所以"只有它是好的"。② 的病灶在 `presetPreviewSeg` 的旧守卫 `P <= W 一律不预告`：扫弦 6 项在 4 行档下**全部** P≤W（示例 5 型 = 1 小节、民谣扫弦 = 4 小节 = 行数），预告行对它们永不出现——节拍区 12 型同为 rep4，4 行档下同病；淡显（短型的 v2.23.0 替代品）替代不了预告行的心理预期。开工前用 AskUserQuestion 请用户拍板两点（带示意图）：短型加预告行 ✓；P===W 也加（**方案 B：轻量挂法**，避开页末整树重建）✓。

**修法**：两条链，全部收在既有收口上——

| 链路 | 做法 |
| --- | --- |
| 待命球落行（修①） | `paintBall` 预设分支落行改为**优先问 `rowOfOnset(nxt)`**——短型绕行下 `nxt` 就是下一圈的首个端点，行号由圈号解析给出（与主球同一真相源，不新增第二套机制）；`rowOfOnset` 给不出（-1：长型页末 `nxt` 已翻到下一页）时回落旧公式，长型"页末让给预告行 → 第 1 行"的既有行为逐位不变 |
| 预告扩展（修②） | `presetPreviewSeg` 改收**可听端点**（页末判据 = `rowOfOnset(hit) === W−1`，与主球同源——短型/等宽的型内片段号永远到不了 W−1，旧判据对这两类恒假），并按「预告内容是否 = 第 1 行现内容」分两档：相同（P===W，或 P<W 且 W 是 P 的整数倍）→ **轻量标记**：返回哨兵 `seg:-2`，不换格子、不整树重建，只挂 `.preview-row` + 「下一小节 · 型名」胶囊（`applyPresetPvMark`，状态翻转时才写 DOM）；不同（P>W 真翻页；P<W 非整数倍如 3 小节型+4 行档）→ 走既有 `winPrev*` 重建链，与曲式同款 |

轻量挂法是 P===W 预告能落地的前提：v2.23.0 排除它的两大顾虑里，"页末整树重建打翻取一次 `internals()` 再驱动的整类用例"（T36 实测球读数冻结偏差 186 tick）被轻量标记**从结构上根治**（本版变异③实测：去掉轻量捷径、让 P≤W 走重建链，T36 的 186 tick 冻结当场复现）；"预告零信息量"经用户拍板接受——标记的价值正是"要绕回了"这个视觉信号本身。摘显用 `hidden` 而不是 `remove()`（元素留作下一圈复用，也避开测试桩 `remove()` 为空操作的坑）。清理链补齐三处：`buildViz`（行元素全新，标记状态作废）、`resetForStop`（停播后"下一小节"失去语义）、`resetWindow`（重锚后球回行 0，陈旧信号由下一帧标记块摘除）。

**自验**：`tests/run.js` **3178 PASS / 0 FAIL**（基线 3158 = +20：T100 五场景——T100a 待命球逐行接力（第 k 圈终端弧备在行 (k+1)%4）/ T100b P===W 轻量预告端到端 + 停机清理 / T100c 短型胶囊与绕行淡显共存 / T100d 长型 `winPrev*` 重建链回归 / T100e 长型页末待命球回落第 1 行；T99e 按新契约改写——轻量标记只在页末行出现 + 零重建哨兵保留）。变异反向验证 3/3 被网住：①待命球退回旧公式 → T100a 红（"恒在第 1 行"逐字复现用户症状）；②恢复 P≤W 排除 → T99e/T100b/T100c 三处红（标记 0 帧）；③去掉轻量捷径 → T36 冻结复现（186 tick）+ 三处零重建断言全红。`tools/check-all.js` 14/14 全绿。

---

## v2.23.0 · 预设模式窗口手感对齐曲式：短型球逐行绕行 + 非在播行淡显 + 长型页末预告（2026-09-25）

**根因**（用户实拍）：「播放十六分满扫（《在他乡》前奏），同屏行数为 2，但可视化区域始终在第一行播放。我理想中：播放到第 2 行时，第 1 行为预告行。」三点结构性缺口：① 预设短型（型片段数 P < 窗口行数 W）自 v2.10.2 起「绕回铺满 + 页锚恒 0」，球行号 = 型内片段 % W——P=1 时恒 0，球永远钉在第一行，行 2 只是无球摆设；② 预告行是曲式独有（v2.7.0），预设窗口没有「下一片段」的预告；③ 真实浏览器探针（CDP 实测）证实曲式模式早已有理想行为——球逐行推进、页末行期间第 1 行挂预告，两模式手感割裂。开工前用 AskUserQuestion 请用户拍板两点：短型 → **球逐行绕行**；长型页末预告 → **一并补齐**（均选推荐项）。

**修法**：三链一体，全部收在 Viz 侧既有收口上（`rowOfOnset` / `winPrev*` / `.preview-row`），不新增第二套机制——

| 链路 | 做法 |
| --- | --- |
| 短型绕行 | `rowOfOnset` 预设分支行号 = (圈号 × 每圈片段数 + 圈内片段) % W。**圈号是解析量**：端点发声时刻 `e.t` 对 `(loopStart, 圈长)` 取商，每圈片段数 = 循环区间的片段数（无循环 = 整型）——自然回卷时 t 跨圈边界圈号自动 +1，变速重锚保证位置连续故圈号不抖。 |
| 非在播行淡显 | 短型播放中，球所在行之外的全部行挂 `.preview-row`（复用曲式 opacity .55），只在球换行时写 class（每圈至多一次）；带 `playMode` 守卫——`vizSegs()` 在曲式 = 当前型片段数，「1 小节型 + 4 行档」是常态，不守卫会泄漏进曲式。 |
| 长型页末预告 | `presetPreviewSeg` 与曲式 `previewSegFor` 同构：球走到页内最后一行期间，第 1 行换成页外下一片段。复用 `winPrev*` 全链（胶囊「和弦名（下一小节 · 型名）」、预告行免已弹刷白），零新增样式。 |

两条边界刻意收窄：**P === W 不预告**——窗口恒第 0 页无翻页，预告内容 = 第 1 行现内容（零信息量），且页末整树重建会换掉全部 DOM 引用，打翻「取一次 internals() 再驱动」的整类用例（T36 实测偏差涨到 186 tick ≈ 一小节）；**预告挂 `onsetHitAt` 守卫**——onset 表未落地时 `audiblePatSeg` 走调度游标兜底口径，比可听域早一个前瞻窗口，起播头几帧会把「页末」误判成立、预告凭空亮一帧又熄灭（探针实拍 `[1,2]→[4,2]→[1,2]`）。预告是可听域语义，兜底口径不参与。初版圈计数曾是帧间差分状态机（`phase < lastPhase − 0.25` 记一圈），被 setBpm 重锚的相位缩放误触发——球跳一整行，正是这次改解析计算的原因。

**自验**：`tests/run.js` **3158 PASS / 0 FAIL**（基线 3141 = +14 条 t99 五场景 + +3 条 t87c/d 预告语义断言；t87c/d 的翻页断言改**预告感知口径**——预告亮起/熄灭也被 driveSigs 记成 flip，「row=0」只对真翻页断言，预告亮起钉在页末行 row=1）；变异验证 **4/4 被网住**（圈号恒 0 / 淡显失去模式守卫 / P===W 误报回归 / 可听域守卫移除，各自让对应断言必红）；真实 Chromium 冒烟两场景全过（S1 球行 `0→1→0→1` + 80/80 帧淡显正确；S2 第 1 小节 22/22 无预告、页末 18/18 预告正确）；tsc 0 错误、ESLint 0 错误、check-all 14/14。

---

## v2.22.1 · 治「沙箱假绿」：CI 抓到 no-shadow 一处、tsc 首跑现形 12 条类型账——本地门禁自此与 CI 同口径（2026-09-25）

**根因**：一桩事故，两个病灶，同一个根——第 8/9 项门禁（ESLint / tsc 加强项）是**可选**的，缺 node_modules 时标 ⊘ 跳过，而开发沙箱从没装过依赖，这两项自引入可选跳过以来在沙箱里**一次都没真正跑过**。于是：① v2.22.0 的 `groupOf` 写成 `const g = Store.groups.find(g => …)`——同一行外层承接变量与内层箭头参数同名，`no-shadow` 规则拒绝；本地 14 项门禁「12/14 绿」看着没事，推上去 CI（有 node_modules）第一项加强就把它打红。② tsc 那边更欠：v2.20.0 的示例迁移与 v2.22.0 的组代码共 **12 条类型标注账**（隐式 any / 判别收窄）——tsconfig 注释里记着 2026-09-17 曾把全文件补到 0 报错，这 12 条全是那之后的新代码欠的。CI 只报 ESLint 不是 tsc 干净：check-all 遇错中止后续步骤，第 9 项根本没轮到跑——若只修 ① 就推，CI 会换个报错接着红。

**修法**：① `groupOf` 内层参数 `g` → `grp`、承接变量 → `const hit`，纯改名零行为变化。② 12 条类型账分三类清偿——**上真类型**：组数据补 `GrpRef` / `Grp` 两个 typedef（成员引用与曲式块/S.sel 同协议），校验产物 `out` / `members` 从 any[] 收紧，Store.groups 全链获得类型；**显式 any**：宽容解析冷键脏数据的回调（组校验的 g/r、示例迁移的 s/b）与 `grpId`（string|null）——「刻意宽容」从隐式变显式；**判别收窄改写**：`m.type === ref.type && (ref 是 builtin ? m.idx : m.id)` 里 TS 不认跨变量比较的收窄（TS2339），改为按 ref 分支各查各的 `m.type`——逐分支语义等价；顺手把迁移里的 `has(r.id)` 换成 `get() !== undefined`（同一信息一次取回，TS2532 消）。全部改动只动类型标注与等价控制流，测试数字一字未变即为证。

**顺手修（工具链，本版真正的收益）**：沙箱按 lock 精确版本装上开发依赖（eslint 10.10.0 / typescript 7.0.2），第 8/9 项从 ⊘ 变真跑——**本地门禁自 v1.10.0 引入可选跳过以来第一次与 CI 同口径**，那两条「加强」不再只是 CI 的牙齿。教训入档：汇总里的 ⊘ 不是绿，是「没跑」；`--strict-env` 在 CI 逼装依赖，开发沙箱从此自觉装。

**自验**：`tests/run.js` **3141 PASS / 0 FAIL**（与 v2.22.0 持平——纯类型账，不加不改测试）；ESLint 0 错误、tsc 0 错误（两闸门首次在沙箱实跑，tsc 从 12 → 4 → 0 两轮收敛）；`gen-index` 重写 16 条行号（改动平移最大 18 行）后逐条一致；`tools/check-all.js` **14/14 全绿**——历史首次无 ⊘，真实 Chromium 冒烟同跑。

---

## v2.22.0 · 区内子分组：节拍/扫弦两区可建命名组——📁 移入、可折叠、删组散员（2026-09-25）

**根因**：侧栏三区堆叠后，节拍区 11 型 + 扫弦区 6 型全部平铺——条目一多，「我只练这几个」的组合（如热身的两三个型、某个曲子用到的扫弦组）只能靠肉眼在几十行里扫。曲式（自定义区）早有整首组织能力，节奏型却没有任何用户侧的组织手段。这是**视图组织缺失**，不是数据模型问题——组不该成为条目的新家，条目本体必须照旧住在 BUILTINS / customs 里。

**修法**：节拍/扫弦两区内可建**命名的子分组**。三条交互 + 两条铁律：

| 交互 | 语义 |
|---|---|
| 📁 移入 | 条目上的移入按钮 → 输入组名（**同名组直接移入**，无选择列表层级）；预填当前组名；**清空并确定 = 移出分组**（回到散员流） |
| 组头 | 「组名 · N 个」，点击折叠（如分区）、✎ 改名、× 删组（确认框明说「条目会移回原区，条目本身不受影响」） |
| 排序 | 各组在前、散员在后（散员保持 BUILTINS/customs 原序）；**无组时渲染与 v2.21.0 逐字节一致** |

铁律一：**组是视图组织不是数据容器**——成员是引用（builtin 下标 / custom id，与曲式块/S.sel 同协议），删组 = 组对象消失 = 成员自然散回，无需"搬回"逻辑；删自定义型时 `groupPruneCustom` 同步摘引用，冷键不留幽灵成员。铁律二：**组头与条目同为 `#presetList` 直接子节点**（组头 `.preset-group`，绝不套 wrapper）——测试逐层遍历 children，多一层全线失配；组归属判定靠**显式标记**（组头 `data-gid`、成员 `data-grp`）而不是"紧跟组头"的位置——组之后渲染的散员与组成员同为 `.preset-item`，位置猜测必然把散员一起藏掉（第一版实现就栽在这，本版用测试钉死）。数据落冷键 `beatsight.groups`，逐组白名单校验（缺名/坏 zone/重 id 整组丢弃、坏 ref 逐条丢弃、重复 ref 单归属取先者）。`applyFold` 重写为两级真值表：条目 hidden = 区开 &&（是组成员 ? 组开 : true）——成员按标记匹配所属组，对不上一律按散员显示（fail-safe 方向是多显示）。

**取舍**：① 移入用**输入组名**而不是选择列表——Modal 现有三件套（alert/confirm/prompt）没有列表选择器，为 v1 引入一个下拉组件不值；同名并入让"建组"与"移入"合并成一个动作，配合预填当前组名，常用路径零输入。② **空组保留**：成员全走了架子还在（还能再移入），自动清空组反而制造"我建的组不见了"的惊吓。③ 内容漂移（自定义型被编辑改谱后节拍↔扫弦）时成员**退回散员流**——组是"区内子分组"，条目漂走了就回区里排队，比强行显示在错的区诚实；组头计数只数可见成员，不撒谎。④ v1 边界：自定义区（曲式）不参与（曲式有自己的节目单组织）、无拖拽（v1 移入即归位）、组不随预设备份导出（组是本机的视图组织，接收方自己组织）。

**顺手修**：`Modal.uiModal` 的输入框显隐判据 `!opts.input` 把**空串预填**当成"不要输入框"——输入框整个隐藏、确认时 `onOk` 收到 `undefined` 而不是 `""`。改名对话框的 initial 永远是当前名（非空）从未踩到，移入分组是第一个"预填可为空"的调用方。判据收窄为「未传 input」（undefined/null 隐藏，空串照常显示），对既有调用零影响。

**自验**：`tests/run.js` **3141 PASS / 0 FAIL**（v2.21.0 为 3079；净增 62 = 新增 t98 九场景：T98a 无组回归 / T98b 建组移入端到端 / T98c 同名并入+跨类型同组+清空移出 / T98d 组折叠与"区收着盖组、区展开组仍收"的两级真值表 / T98e 组改名 / T98f 删组散员 / T98g 删自定义型联动清理 / T98h 脏冷键白名单 / T98i v1 边界与导出不含组）。反向验证做了两次变异，各自证明一条链：① `groupMove` 的单归属摘除注释掉（移入不从原组摘、移出不生效）→ T98c 变红 3 条（成员数 / 散员回归 / 冷键同步）；② `applyFold` 的标记匹配改成恒真（复刻位置猜测 bug）→ T98d 的「散员仍显示」变红 1 条。恢复后全绿。`tools/check-all.js` 全链 12/14 绿（2 ⊘ 缺 node_modules，同前两版）；`tools/smoke.js` 真实 Chromium 冒烟见提交自验。

---

## v2.21.0 · 节奏型条目改名：✎ 一步到位——内置 = 覆盖可还原，自定义 = 直改无副作用（2026-09-25）

**根因**：预设库的节奏型条目**没有任何改名入口**，名字是死的——自定义型手滑起错名，唯一出路是删了重建，而新条目是**新 id**，曲式里所有指向旧条目的引用当场断链（曲式块按 custom id 引用）；内置型想叫自己的名字（如把「十六分满扫（《在他乡》前奏）」叫成「我的前奏型」）更是无从谈起。同库里**曲式条目早有 overlay 内改名**（点开即改），节奏型却同库不同权。这是入口缺失，不是数据模型缺陷——两条后端（覆盖 / 直改）都不动既有引用协议。

**修法**：每个节奏型条目名旁挂 **✎ 图标**，点开走既有 `Modal.uiPrompt` 通用输入框（预填当前名），两条后端按条目性质分治：

| 条目 | 后端 | 持久化 | 恢复原名 |
|---|---|---|---|
| 内置型 | **覆盖**：运行期改写 `BUILTINS[i].name`——显示层十几处读取点（侧栏、主界面标题、编辑器、导出……）全链路生效 | 冷键 `beatsight.builtinNames` = `{v:1, names:{"12":"覆盖名"}}`，加载时逐条白名单校验后套上 | **清空并确定**：删覆盖 + 还原源名 |
| 自定义型 | **直改** `c.name`（用户数据本就归用户） | 照常落 `beatsight.customs` | 无需恢复——它没有"源名" |

支撑件三件：`BUILTIN_SRC_NAMES` 快照（5 个示例型挂载进 `BUILTINS` **之后**、Store 之前拍下，是还原基准，永不被覆盖污染）；`renameBuiltin(idx, name)`（非法下标 → false，空名 → 恢复源名，trim + 40 字上限）；`renameCustom(id, name)`（空名 → false = 取消，同口径截断）。UI 侧一个 `pushRenameBtn` helper 两条渲染路径（`pushBuiltinItem` / `buildPresetItem`）同构挂载，零新增模态代码。内置型的对话框提示**带源名**（「重命名内置型「十六分满扫（…）」（清空并确定 = 恢复原名）」）——覆盖态下用户也看得到原名是什么。

**取舍**：① 覆盖按**下标**对号入座：`BUILTINS` 序列由出厂代码决定且是追加式（v2.20.0 也是尾挂），升级不重排；万一脏冷键里出现越界/非数字/空白条目，白名单逐条丢弃、不污染内存——最坏情况是覆盖没套上，不会崩。② **不查重名**：识别靠内容/下标/id，名字不是任何一方的命名空间，重名只影响人的扫读、不影响任何行为——与曲式名同口径（曲式也不查重）。③ 曲式条目不在本批：它已有改名入口，不重复造。④ 自定义型改名对曲式零影响是**结构保证**（块按 id 引用），不需要迁移或修复逻辑。

**自验**：`tests/run.js` **3079 PASS / 0 FAIL**（v2.20.0 为 3038；净增 41 = 新增 t97 五场景）。测试面：T97a 内置改名端到端（✎ → uiPrompt → BUILTINS 覆盖 → 侧栏/主界面标题跟新 → 冷键落盘 → 重启仍在）；T97b 清空恢复源名（快照还原 + 冷键不留幽灵条目 + 重启稳定）；T97c 自定义直改（c.name 落盘、曲式块 id 引用分毫未动、曲式长度不变、重启保持）；T97d 校验边界（空白名拒绝 / 40 字截断 / 非法下标 false / 脏冷键白名单——合法条目照常应用、脏条目丢弃）；T97e 与 v2.20.0 迁移共存（覆盖名不碰内容识别、老 custom 照常收走、引用照常重映射、覆盖名两套冷键互不干扰）。反向验证做了两次变异，各自证明一条链路：① `renameBuiltin` 空名恢复分支短路成"假成功"→ T97b 变红 3 条（恢复源名 / 冷键清除 / 重启稳定）；② 加载时"覆盖套上 `BUILTINS[i].name`"一行注释掉 → T97a/d/e 变红 5 条（重启即套 / 白名单应用 / 迁移共存下的覆盖保留）。恢复后全绿。

---

## v2.20.0 · 示例曲 5 型内置化：删不掉、内容恒对、坏曲式升级即治愈（2026-09-25）

**根因**：《在他乡》的 5 个示例扫弦型此前落库为**自定义预设**（customs），于是自带删除按钮——用户随手一删，示例曲式的引用就断了（曲式块按 custom id 引用，id 没了即坏引用，表现为「整首不能播」）。这是把**内置素材**误建模成**用户数据**的错：它们和「民谣扫弦」一样是出厂内容，不该以可删条目的形态住在用户的库里。

**修法**：5 个示例型整体并入 `BUILTINS`（下标 `DEMO_BUILTIN_BASE=12` … +4，挂载在 `demoSpec()` 之后、任何运行期消费之前）。三条链路因此零改动或收口：

| 链路 | 改动 |
|---|---|
| 引用协议 | 曲式块从 `{type:"custom", id}` 改写为 `{type:"builtin", idx: 12+refIdx}`——`resolveRef` / `normArrange` / `patLenOf` 对 builtin 引用的支持是**既有的**（「新曲式」默认块就引用 builtin），解析/校验/播放零改动 |
| 数据迁移 | 新增 `migrateDemoToBuiltins(customs, arranges)`（取代 v2.9.0 的改名迁移 `migrateDemoPatternNames`）：用 `demoCustomAt` 的四级兜底（内容 → v2.3 旧前缀名 → 当前名 → v2.9.0 旧短名）认出所有名字世代的示例 custom → 曲式引用重映射 → 从 customs 收走。**旧名不必先改名**，一步到位 |
| 落后检测 | `demoStale` 改口径：「型内容落后」的检查删除（内置型内容恒对）；保留曲式结构检查，**新增坏引用检查**（块引用解析不到即算落后）——v2.19.x 时代"删示例型"留下的坏曲式，升级后由启动层自动 `ensureDemo` 重建，**坏块一并治愈** |

**UI 行为**：示例 5 型改经 `pushBuiltinItem` 渲染——**没有删除按钮**（删除路径在 UI 上不存在）；选中它们 → 顶栏「编辑节奏型」→ 基于 `curPattern()` 建「-副本」草稿 → 保存进自定义库，即"另存为自定义"的合法改编路径（编辑器既有语义天然覆盖内置源，零新增代码）。`buildDemoNoteIfNeeded` 的"型还在 → 不提示"豁免随内置化删除：用户主动删曲式后侧栏亮出「恢复示例曲」（不自动复活，但给显式入口）。迁移发生时顺手 `S.migHint = true`——老用户的自定义库转空后，不再被 v1.3.0 的「库空提示跨地址不共享」突然打扰（新用户首开仍按原口径提示一次，t24 契约不变）。

**取舍**：① 内容对不上但恰好叫示例当前名的用户型会被迁移收走——与 v2.19.1「收敛回参考谱」是同一权衡（名字高度特异、变体请另存名字），编辑器产物带「-副本」后缀不会命中任何兜底；② 导出的预设备份不再含示例 5 型（它们是出厂内容，接收方也有），用户另存的副本照常导出；③ `ensureDemo` 返回值去掉 `created/updated`（它们描述的"落库预设增减"已不存在），保留 `{ok, id}`。

**顺手修**：`tools/gen-index.js` 的 `rewrite` 有个与 v2.8.8 负号死循环同族的既有 bug——gap 正则 `^L\d+` 锚定行首，而索引条目行以缩进开头，正则恒失败、恒走默认 2 空格替换；唯一 5 位数行号的「初始化」条目（短名列对齐补偿后 L 后只有 1 空格）从此**静默回写失败**（`--write` 报"已回写"、诊断原样再打印）。去掉 `^` 锚修掉。

**自验**：`tests/run.js` **3038 PASS / 0 FAIL**（v2.19.1 为 3021；净增 17 = t74 重写为迁移/改编契约后的断言差）。测试面：t63（带出语义改内置）、t74（T74c 迁移端到端+幂等 / T74d S.sel 重映射+旧名认回+坏引用治愈 / T74e 删曲式不复活+恢复入口 / T74f 无删除按钮+另存路径）、t47/t69/t70/t84/t68 随内置化更新分界线断言。反向验证做了两次变异，各自证明一条链路：① 重映射偏移 `+1` → 测试**依旧全绿**——错位引用越界（idx=17）被 `resolveRef` 判空、`demoStale` 坏引用检查抓住、启动层 `ensureDemo` 当场治愈回正确下标——治愈链路被证明真实工作；② 短路"迁移不收走"（removed 恒 0）→ T74c/d 具名断言变红 8 条（收走 / 重映射 / refIdx 对齐 / 幂等 / S.sel / 旧名认回）。恢复后全绿。`tools/check-all.js` 全链 12/14 绿（2 ⊘ 可选加强项缺 node_modules）；`tools/smoke.js` 真实 Chromium 冒烟见提交自验。

---

## v2.19.1 · 扫弦示例 5 型改名：可弹特征 +《在他乡》出处；两批旧名一次性迁移（2026-09-25）

**根因**：扫弦区的 5 个示例型用《在他乡》曲内的**场景词**命名（副歌扫弦 / 主歌扫弦 / 雨夜扫弦 / 桥段扫弦），没扫过这首歌的用户无从知道它是什么扫弦；P1 的短名「十六分满扫」也缺出处，与其他歌曲的扫弦型难以区分。

**修法**：名字 = **可弹特征 + 出处标注**。特征部分描述每小节的可弹形态（形态 + 密度），出处部分用音乐规范的段落术语标注它来自示例曲哪里：

| 旧名 | 新名 | 谱面特征（每小节 16 个十六分格，偶格下、奇格上的交替骨架） |
|---|---|---|
| 十六分满扫 | **十六分满扫（《在他乡》前奏）** | 16 格全发声，无切音无留白 |
| 副歌扫弦 | **下上扫 · 密（《在他乡》副歌）** | 10 发声 + 4 切音，首拍重扫后稳定"下…上下"律动 |
| 主歌扫弦 | **下上扫 · 疏（《在他乡》主歌一）** | 与"密"仅差第 3 拍少一记上扫（9 发声），如实表达为密度差 |
| 雨夜扫弦 | **前密后疏扫（《在他乡》主歌二）** | 前两拍密 → 第 3 拍整拍休止 → 末拍"下上"收（7 发声） |
| 桥段扫弦 | **切音满扫（《在他乡》桥段）** | 全小节连续下上、切音最多（11 发声 + 5 切音） |

「人静的雨夜」段有完整歌词（28 字）、不是间奏，音乐上属主歌二（Verse 2），故与 P3 的「主歌一」以序号区分；出处只标主要使用段落（P1 也出现在收束/收尾小节、P2 也出现在收束段），不逐段罗列。

**旧数据迁移**：v2.9.0–v2.19.0 期间落盘的两批旧名（P2–P5 场景名 + P1 短名）纳入 `migrateDemoPatternNames` 一次性收敛——`DEMO_LEGACY_V2_NAMES`（现含 P1 短名，5 条）按名字认回下标、改成当前名；`demoCustomAt` 由三级兜底扩成**四级**（① 内容 → ② 在他乡旧前缀 → ③ 当前名 → ④ v2.9.0 旧名），内容识别依旧优先，仅当内容对不上时才按旧名认回。幂等：迁移后的名字不再命中旧名表，二次启动零写盘。认型规则三处共用（ensureDemo / demoStale / migrate）的契约未变，只是第四级与前三级看同一份映射。

**取舍**：① 名字变长（约 14–16 字）：预设条目名无 nowrap/ellipsis 设置、自然折行，扫读性换信息量；状态灯行的当前型名保留 v2.10.18 的省略号收缩（列宽固定是那条的既定决策，全名在预设库可见）。② 「密/疏」是相对词：P2 与 P3 的谱面差异只有一格（第 3 拍的一记上扫），名字层面如实表达为密度差，完整动作骨架由可视化网格承担。③ 曲内引用这些型的位置词（段名「副歌 · 上」等）不动——改的只是**型名**，曲式块按下标引用，改名对数据零影响。

**自验**：`tests/run.js` **3021 PASS / 0 FAIL**（v2.19.0 为 3013；净增 8 = 新增场景 T74f「改名迁移端到端」的 8 条断言）。反向验证：摘掉映射表中的「雨夜扫弦」条目 → T74f 具名变红 5 条（收敛失败 / 旧名残留 / id 断链），恢复后全绿。`tools/smoke.js` 真实 Chromium 冒烟 **63 项断言全过**（版本一致、控制台零报错、SW 注册、播放态 61fps、DOM 1210/1300）。

---

## v2.19.0 · 诊断代码从装配层抽成第 14 个模块 `Diagnostics`（P1-1）；P2-1 复核**推翻我自己上一版的结论**（2026-09-24）

**来源**：同 v2.17.0 / v2.18.0——代码库分析报告列的 P1/P2 项，用户要求一次处理完。上一版把
**P1-1（装配区免疫 R1–R4）**与 **P2-1（DOM 预算余量）**留作"待拍板"，本版按用户指示收口这两条：
P1-1 落到代码（抽模块），P2-1 复核后**推翻我自己的结论**（无可安全瘦身项，**不动代码**）。

### ① P1-1 · 诊断代码抽出装配层 → 第 14 个模块 `Diagnostics`

- **先重申这条不是"漏洞"**（v2.18.0 已论证）：R1–R4 保护的是**模块↔模块**的反向引用，而装配区在
  **所有模块之后、没有任何下游**，它**本来就该**调用全部模块（组合根 / composition root）
  ⇒ "免疫闸门"是结构使然，不是检查器失明。**真正可动的是它 1159 行的体量**，本版就动这一半。
- **抽哪块**：装配区里那一整段连续诊断代码——计数器 `diag`、面板 `diagPaint/diagBuild`、
  错误原文环形缓冲 `diagLast/diagNote`、产物戳记自检 `stampCheck`、`?debug=1` 开关 `diagOn`，
  **共 180 行**（原 `L9778–L9957`）。高内聚、只读既有注入钩子、不引入新的依赖边。
- **顺带解决的四件事**（这才是抽它而不只是"减重"的理由）：
  ① 进 **R1–R4 视野**——它只引用 `$` / `VERSION` / `Modal` / DOM API，全在它之前声明 ⇒ R1 天然为零；
  ② 进**模块索引**（行号由 `gen-index --write` 自动跟随，不再靠人记）；
  ③ **单独进覆盖率分区表**——新分区 `Diagnostics · 诊断与产物戳记` 实测 **97.1%（165/170）**，
     高于 90% 分区阈值。此前它被平均在装配区里，**烂掉也看不见**；
  ④ 装配区从约 1159 行降到约 **980 行**，拆成"接线"与"诊断"两件事，各自可读。
- **分工没变**（刻意的）：模块只提供计数与自检；`onFrameError` / `onSchedError` / `onReanchor` /
  `onKeepAliveFail` 与 `Store` 的两个持久化钩子**仍由装配层接线**，全局错误监听与 `stampCheck()`
  的调用时机也留在装配层——这正是"跨模块装配交给装配层"那条纪律。
- **连带改动清单**（就是 v2.18.0 里我列的那几个"牵动面"，逐项落地）：
  `check-module-order.js` 的 `EXPECTED_ORDER` 13 → 14；`NON_MODULE_IIFE` 由 `["diagOn"]`
  **清空**（`diagOn` 已进模块，不再是"非模块 IIFE"——留着会被闸门自己报"登记已失效"）；
  模块索引 16 → 17 条（`gen-index --write` 回写 17 条行号）；**R3 白名单与 R4 扇出均不受影响**；
  `AGENTS.md` / `README.md` / `docs/DEVELOPMENT.md §3.0` 里写死的模块数与顺序一并改成 14。
- **行为不变的确证**：`tests/run.js` **3013 PASS / 0 FAIL**，且**与 v2.18.0 逐条同数**——纯搬移的判据。
- **反向验证（4 组定向变异，每个都必须让闸门报出具名失败）**：
  · 摘掉 `EXPECTED_ORDER` 里的 `Diagnostics` → `✗ 新模块逃逸：Diagnostics`（exit 1）；
  · 把 `diagOn` 登记回 `NON_MODULE_IIFE` → `✗ NON_MODULE_IIFE 登记已失效：diagOn`；
  · 删掉索引里的 `Diagnostics` 条目 → `✗ 条目数不符：索引 16 条，实际模块 banner 17 个`；
  · 把 banner 短名改成 `Diag` → `✗ 第 16 条对不上：索引写「Diagnostics」，banner 是「Diag」`。
  本版**没有新增行为断言**——它是纯结构调整，"行为不变"的证据就是那 3013 条**一条未动**。

### ② P2-1 · DOM 预算余量 —— **推翻我自己在上一条里的结论：没有安全瘦身项**

- v2.18.0 我写的是：`.sub`（十六分小格线，约 64 个）是**纯装饰**，可用 CSS
  `repeating-linear-gradient` 替代、**观感不变**、省约 5% 预算。
- ★ **核实后这句是错的。** `.sub` 不只是刻度线，它同时是**拍点闪动动效的宿主元素**——
  `paintBeatFlash()` 里 `sub.animate([{boxShadow: inset …, backgroundColor: …}, …], {duration:260})`
  由它承载，配色随主题（`flashTheme()`）并受 `REDUCE_MOTION` 守卫。
  ⇒ 换成背景渐变，换掉的不只是"64 个节点"，而是**把闪动效果一起改掉**；改挂到宿主 cell 上则
  闪动范围从"那一格十六分"变成"整格"，**观感会变**。它**不是**"观感不变的纯装饰"。
- **结论**：P2-1 **没有可安全瘦身的项**，本版**不改 DOM、不动预算**（仍 1300）。当前约 1210/1300
  尚有余量，而仓库自己的口径是"贴近上限时先瘦身、不要继续放宽"——还没到需要付观感代价的时候。
- ★ 教训（与本版 ① 那条同源，都是"**先验证再下结论**"）：**"看起来纯装饰"的东西，先查它是不是
  某个动效／状态的宿主**（`grep 该 class + animate`），再谈删。判定成本一次 grep，
  误判的代价是一次观感回归。结论已写进 `tools/smoke.js` 的 `PERF_BUDGET` 注释（"别再重新发现一遍"）。

### ③ 顺带订正两处"语义已过期、但读起来仍然正确"的文档/注释

`check-docs.js` 管得到"手写数字"，管不了这一类——只能靠人读。本版读到两处，一并改正：

- `index.html` 诊断块里原写「★ 为什么放在装配层而不是新开模块」。抽出来之后这句就成了假话，
  已改写为 v2.19.0 的订正说明（并写清"当时的理由为什么不成立"）。
- `docs/DEVELOPMENT.md §3.11` 与 §3.0 的 R4 条目里写「`Controls` 当前已顶到上限（指向它全部的
  7 个下游）」「`Controls` 现在就顶在上限上」——这是 **v2.18.0 把扇出降到 2 之后没跟着改**的残留。
  已改写为 `KEY_LAYERS` 注册表的现状，并把"顶格时先看是不是同一件事抄了多遍、而不是抬高常量"
  这条教训正式写进 §3.11（此前只写在检查器的注释里）。
- ★ 还逮到一处**纯粹漏抄**：§3.0 的 `<script>` 顺序里**早就漏了 `Arrange` 与 `Help` 两个模块**
  （v2.0.0 / v2.0.1 加入后再没人补过）。本次一并对齐成 14 个模块的完整顺序。
  `check-docs.js` 只管"手写数字"，这类"漏了一整项"它看不见——与上面两条同源。

### 自验数字

- `tests/run.js` **3013 PASS / 0 FAIL**（与 v2.18.0 逐条同数 ⇒ 纯搬移）；
- 行覆盖率 **98.9%**（总阈值 97%）；新分区 `Diagnostics` **97.1%**（分区阈值 90%）；
- 架构闸门：**`Diagnostics` 已进 `EXPECTED_ORDER`（14 个模块）**，R1/R2 零例外、R3 5 条白名单、
  R4 扇出 `AudioEngine×2 · Controls×2 · Trainer×1`（`Diagnostics` 扇出 **0**——它在依赖链末端）；
  模块索引 17 条逐条对齐；DOM 引用零悬空；装配完整性 12 个钩子 + `patLenOf` 全接上；
- 反向验证：本轮 **4 组变异**（全部落在**闸门**上，见 ①）——本版是纯结构调整，
  行为不变的证据是 3013 条既有断言一条未动。

## v2.18.0 · 架构收口：叠加层键盘路由改注册表（Controls 扇出 7→2）+ 装配区覆盖复核（2026-09-24）

**来源**：同上一版——代码库分析报告列的 P1/P2/P3 项，用户要求一次处理完。本版处理
**架构与覆盖率**这两条；并把另外两条（装配区免疫 R1–R4、DOM 预算余量）**核实到底**，
结论是**诊断需要修正 / 执行需要你拍板**，故只给结论与方案、不改代码（详见本文末）。

### ① `Controls` 扇出顶格 7/7（P1-2）→ 收敛成注册表，**降到 2**

- **问题不只是"顶格"**：原来那五段 `if (某个叠加层开着) { Escape → 关掉; return; }` 里，
  **四段（设置 / 听辨 / 曲式 / 使用方法）逐字同构**，第五段（编辑器）多一条 Ctrl+Z。
  代价是 `Controls` 的直接下游引用被顶到 `MAX_FANOUT=7` 的上限——每新增一个叠加层都要
  "在 Controls 里再抄一遍 + 抬 `MAX_FANOUT` + 往 R3 白名单加一条"。
- **修法**：在**共享状态区**加一个 `KEY_LAYERS` 注册表 + `registerKeyLayer()` / `overlayKeyLayer()` 工厂；
  **各叠加层模块在自己体内登记**（四行 `registerKeyLayer(overlayKeyLayer({ isOpen, close }))`，
  编辑器因多一条 Ctrl+Z 手写 `handles`）；`Controls` 只剩
  `for (const layer of KEY_LAYERS){ if (layer.handles(e)) return; }`。
- ★ **方向合法性**：注册表在所有模块之前，各模块在**自己体内**调用它 = 引用前方 ✓；
  Controls 读注册表 = 读共享状态，不再引用后方模块 ✓。
- ★ **顺序与语义逐位不变**：登记顺序 = 模块声明顺序（设置 → 听辨 → 曲式 → 使用方法 → 编辑器），
  与改造前那段 if 链相同；"叠加层开着吞掉所有按键、只有 Escape 关"这条语义照旧。
- ★ **闸门自己给出了结论**：改造后 R4 打印 **`Controls×2`**（只剩 Presets + KeepAlive），
  并准确报出 5 条 R3 白名单"已失效，应删除"——已删；`MAX_FANOUT` **留在 7**（它是"中枢又胖一圈"
  的可见化闸门，不是"当前用量的记录"），注释改为记下这次复核的教训：
  **顶格时第一反应应当是"这几条引用是不是同一件事被抄了多遍"，而不是抬高常量。**
- **反向验证**：3008 条既有断言（含 T57/T82 等 overlay 键位归属用例）**全绿、一条未动**
  ⇒ 键位行为确实逐位不变。

### ② 装配区覆盖率洼地（P3-4）→ 逐行核实：**不是缺陷**，只补了两处真缺口

- **实测**：装配区 953/991 = 96.2%，是 17 个分区里唯一明显低于邻居（其余 16 条 98–100%）的一档。
- ★ **但闸门口径是「分区 ≥ 90% / 总体 ≥ 97%」**——它**一直合规**。原先"唯一低于 98%"那句是
  **跟邻居横向比**得出的观察，不是违规则。
- **逐行核过那 38 行**，全部落在仓库自己声明的两类里（**刻意保留的防御性代码 / 不可达或环境受限分支**）：
  戳记版本不符告警、`unhandledrejection` 兜底、遮罩落盘失败与延迟配置条数超上限的弹窗、
  以及**壁纸降采样那 21 行**（测试桩拿不到 2d 上下文，代码里本就写着"没有 canvas 就直接用原图"）。
- **只有两处属"该测而没测"**，本版补上：
  · **T92m** `#wallPickBtn` 的点击必须转发给隐藏的 `<input type=file>`（用探针替换 `click` 后断言被调到）；
  · **T95g** 「节拍音量为 0 时不许开校准」守卫（断言**可观测结果**——向导不进入 run、面板不展开，
    再把音量拉回来验证边界是 `vol > 0`）。
  **两条都做过定向变异反向验证**：拿掉守卫 → T95g 那 2 条转红；拿掉转发 → T92m 那 1 条转红。
- **结果**：装配区 **96.2% → 96.9%**（未覆盖 38 → **31 行**），总覆盖率 98.8% → **98.9%**。
- **并已把核实结论写进 `docs/DEVELOPMENT.md §6` 的技术债清单**（"别再重新发现一遍"）：
  余下那些**不要为了数字去点亮**，也**不要把"比邻居低"当成缺陷**。

### ③ 未执行、待你拍板的两条（只给结论与方案）

**P1-1 装配区免疫 R1–R4 —— 诊断需要修正，不是漏洞。**
`check-module-order.js` 的 R1–R4 保护的是**模块↔模块**的反向引用；而装配区在**所有模块之后**、
**没有任何下游**可以反向引用它，它**本来就该**调用全部模块（它是组合根／composition root）。
⇒ "它免疫闸门"是**结构使然**，不是"检查器失明"。真正可处理的是它**1159 行的体量**。
可抽的候选是那块**连续约 280 行的诊断代码**（`diag*` / `stampCheck` / `diagOn`，L9778–10060 一带）。
**但不建议顺手做**：抽成模块意味着**模块数 13 → 14**，要连带改 `EXPECTED_ORDER`、
`NON_MODULE_IIFE`、模块索引（`gen-index --write`）、以及 AGENTS.md / README / 本文档里
**多处写死的"13 个 IIFE 模块"**——而收益只是"减重"，不是修漏洞。**要做请说一声**，
我会先出"抽哪块、牵动哪些文件、测试怎么补"的影响面清单。

**P2-1 DOM 预算余量 93%（1210 / 1300）—— 已逐块审计，没有"白捡"的大头。**
真浏览器实测 1207 节点，按"最近的有标记祖先"分组：
- **172 个（14%）在 `[hidden]` 子树里**，大头是**「使用方法」页的静态标记**（`.help-li` 77 + `.help-dl` 28）——
  那是**刻意的**设计（"没有 JS 也能看到全文，也不会被 JS 故障连带弄丢"，见 Help 模块注释），
  **不该为了省节点把它改成懒渲染**；
- 其余是网格主体（`.cell` 64）、进度填充（`.fill` 64）、座次尺（48）、歌词字块（72）等**必要内容**；
- **唯一安全可瘦的**是 `.sub`（十六分小格线，64 个）——它是**纯装饰**，可用 CSS
  `repeating-linear-gradient` 替代，**观感不变、省约 64 个节点（≈5% 预算）**。
⇒ 本条**没有动预算**（仓库自己的口径就是"再贴近时先瘦身、不要继续放宽"，而 1210 < 1300 尚未越线），
也**没有动渲染层**（省 5% 要改 `buildRowCells` + 相关断言，收益与风险不成比例）。
**要做那 64 个的瘦身也请说一声。**

### 自验数字

- `tests/run.js` **3013 PASS / 0 FAIL**（v2.17.0 是 3008 ⇒ +5 条：T92m ×1 + T95g ×4）；
- 全量 `node tools/check-all.js --strict-env` **14/14**；行覆盖率 **98.9%**（阈值 97%）；
- 架构闸门：**`Controls×2`**（原 7），R3 白名单 10 条 → **5 条**；
- 反向验证：本轮共 4 组变异（注册表 0 组——它靠 3008 条既有断言证明"行为不变"；
  覆盖率 2 组；另见上文各条）。

## v2.17.0 · 文档卫生收口：把「可现算的数字」与「无界增长的文档」一起收进闸门（2026-09-24）

**来源**：v2.15.1 时期的代码库分析报告（`BeatSight-代码库分析报告-v2.15.1.md`）列出的 P3 级工程化问题，
用户要求一次性处理。本版处理其中**三条纯文档/工具链**的；架构与性能那几条另版（见下）。

### ① `AGENTS.md` 的体积声明漂移 +42%（P3-1）→ 修 + **补闸门**

- **实测**：L7 写「`index.html` ~672 KB」，而当时已是 957,603 B（935 KiB）。git 追溯确认这行
  **曾经是准的**（`fed5e2d`/v2.10.12 改成 672 KB 时实际就是 673,446 B），此后文件长了两百多 KB
  再没人动过 ⇒ **不是笔误，是制度性盲区**：`check-docs.js` 原有七条规则全是管
  README / docs / tests 的，**`AGENTS.md` 根本不在任何闸门的管辖里**（`grep -l AGENTS tools/*.js
  tests/*.js` 零命中）。而它偏偏是"给协作者与 AI 看的活规则"，写错会直接误导判断
  （本次排查预告行问题时就被它带偏过一轮）。
- **修法**：那一行改成**不写数字**、改指 `wc -c index.html` 现查，并把漂移史写在原地；
  新增 `check-docs.js` **第 8 项**（`SIZE_CLAIM_FILES = ["AGENTS.md"]`）把守。
  ★ 口径刻意很窄：只认「~N KB/MB」这一种形状、且只管这一份文件 ——
  `index.html` / `tests/` 里「约 5 MB」（localStorage 配额）、「~180 KB」（壁纸档位讨论）
  是**概念说明与历史叙述**，一并拦下就是假红（全仓库共 6 处这种合法写法）。
  ★ 本项的**第一条反向验证来自它自己**：闸门加上去、AGENTS.md 还没改时当场转红（`AGENTS.md:7「~672 KB」`）；
  改完后转绿。另外踩了个小坑并已按规则 7 的既有口径处置：**修复说明里若把旧句子整句引进来，
  会被自己拦下**——故那一段刻意不复现「~N KB」这个形状，具体数字挪到本条 CHANGELOG（不受该规则管辖）。

### ② `docs/DEVELOPMENT.md §6` 路线图"看起来还在生效"（P3-2）→ 补性质说明

- **实测**：§6 只有一副"截至 v2.0.2 队列已清空"的静态快照，此后已走出十几个 minor 版本，
  读者（尤其接手的人）**无从判断这表是现行排期还是历史遗留**。
- **修法**：在该句下方补一段「本节的性质」，把三件事分开指路——
  "后来又做了什么"→ `CHANGELOG.md`；"现在是什么状态"→ `index.html` 的 `const VERSION` + 本文档 §3（活文档）；
  "接下来该做什么"→ 本节下面的技术债清单。并写明这是 `check-docs.js` **管不到**的一类漂移：
  它管"手写数字"，管不了"**语义已过期但看起来还在生效**"的章节。

### ③ `CHANGELOG.md` 体量失控、无切分（P3-3）→ **按大版本线分卷** + 补闸门

- **实测**：4,947 行 / 139 个版本条目，**没有任何切分**，且**会无界增长**（每次发版加一段、永不减少）。
  构成：v0.x 21 条 / v1.x 32 条 / v2.x 86 条 —— 两条老线的条目早已不会再改。
- **修法**：**主文件只保留当前大版本线**，旧线各归各档：
  `docs/CHANGELOG-v0.md`（299 行）· `docs/CHANGELOG-v1.md`（1134 行）；
  `CHANGELOG.md` **4947 → 3536 行（−29%）**，首条仍是当前版本（`check-version.js` 不受影响）。
- **新增 `check-docs.js` 第 9 项**（三条机器可判的断言）：① 主文件里每条 `## vX.Y.Z` 的大号都等于
  VERSION 的大号；② 归档文件名里的大号必须小于当前大号；③ 归档正文里不得混入 ≥ 当前大号的条目。
  ★ **这条口径自带"下次翻页时会提醒你"**：bump 到 v3.0.0 的当天主文件里就出现大号 ≠ 当前的条目
  ⇒ 第 9 项变红 ⇒ 按提示把 v2 整线挪进归档。**不需要谁去记"该归档了"**。

### 自验数字

- `node tools/check-docs.js` **九条规则全绿**（新增第 8/9 项）；`node tools/check-version.js` 通过
  （CHANGELOG 首条 = VERSION ✓，分卷未影响它）。
- 版本次条另附：**架构类**（装配区免疫 R1–R4、`Controls` 扇出顶格、装配区覆盖率洼地）
  与**性能类**（DOM 预算余量 93%）另版处理。

## v2.16.0 · 页锚点改为**相对播放范围**对齐（修预告行残留错位）+ 另两处同源缺陷（2026-09-24）

**来源**：v2.15.2 修好了"范围短于同屏行数"时预告行不出现的主症状，但用户继续试出**跨页**那一档仍错：

| 同屏 | 范围 | 修前 | 修后 |
|---|---|---|---|
| 4 行 | 2–5 | 第 3 小节**误报**一次、第 4 小节（范围末）**漏报** | 只有第 4 小节预告 ✓ |
| 3 行 | 2–4 | 第 2 小节误报、第 3 小节漏报 | 只有第 3 小节预告 ✓ |
| 2 行 | 2–3 | 第 1 小节误报、第 2 小节漏报 | 只有第 2 小节预告 ✓ |
| 4 行 | 单小节（范围中 / 歌尾） | **误报** | 不预告 ✓ |

### 根因：v2.15.2 只补了判据，「页」本身没对齐

`winAnchorSeg(seg) = floor(seg/W)*W` 拿的是**歌曲片段的绝对编号**，与 `S.arrangeSel.from` 无关。
于是范围一横跨页边界就两头出错：

- 范围末小节恰好落在某页的**第 1 行** → 那正是在播行，**没有更陈旧的行能让出去**
  （`seg <= winStart` 守卫只能拦下）⇒ **漏报**；
- 旧的"页末"判据又会在**范围中间**的某小节触发一次 ⇒ **误报**。

### 修法：`winAnchorSeg(seg, arr)` 在曲式模式下相对播放范围起点对齐

```js
return from + Math.floor((seg - from) / W) * W;      // from = S.arrangeSel.from 折成的片段号
```

★ 三个"不改"的保证（这也是改动能收敛在"锚点取哪一页"上的原因）：
1. **`from = 0`（整首连播 / 未选范围）与预设模式下逐位等于旧口径** ⇒ 零影响；
2. **`seg < from`（跳段、循环回卷的过渡帧）回落旧口径** —— 否则窗口会不含当前片段，
   而 `rowOfSongPos` / `rowOfPatSeg` / `mutedRow` / `chordAtRow` 等一大批 `winStart + bar`
   推导**全部**依赖"窗口包含当前片段"这条前提；
3. 锚点上限钳到最后一小节，避免歌尾越界。

★ 顺带把**帧内唯一的曲式快照**沿参数传下去（`winAnchorSeg` / `previewSegFor` 的第二参数），
与 `audibleSongBar` / `rowOfSongPos` 同一套写法 —— 若让 `winAnchorSeg` 自己现查，
会踩 T24 的"稳态帧中位查找 1 次"性能断言（实测：现查 = 3 次/帧，传参 = 1 次/帧）。

### 可见行为变更（用户已确认接受）

**窗口起点跟着播放范围走**：4 行档 + 范围 2–3 时，窗口从「歌曲第 1–4 小节」变成「第 2–5 小节」——
**第 1 行恒等于"这轮要练的第一个小节"**，读谱不必再自己数。代价是范围之后的小节会多露一点。
`from = 0` 时不变。

### 一并修掉的两处同源缺陷

1. **`Presets.onRangeInput` 的 `input` 分支只写 `from/to`、不写 `loop`**：
   `loop = true` 只在 `change`（松手提交）那步由 `Arrange.setRange` 补上，而 `S.arrangeSel`
   的初值是 `loop:false` ⇒ "拖到一半关掉页面 / change 没走到"会把**「有范围、没循环」落盘**。
   现与 `setRange` 同口径：拖动中就把 `loop` 置 true。**刻意不动"进入曲式模式只在提交发生"那条规矩**。
2. **侧栏/编排面板那颗 `#argLoopBtn` 只在 `arrangeRender()` 里同步**，而浮层不常开、
   改 `loop` 的路径（`setRange` / 跳段 / 退出曲式回单型）大多只走 `refreshBar`
   ⇒ 出现「字段已是 false、钮还亮着」的外观漂移（排查本轮问题时被这条误导过一轮）。
   现 `refreshBar()` 里一并同步，两颗钮恒一致（跳段行那颗要绕开 `setToggle`，理由见其原注释）。

### 自验数字

- `tests/run.js` **3008 PASS / 0 FAIL**（v2.15.2 是 2983 ⇒ +25 条：T75g 新增 4 个场景、
  T88d 追加 1 条、T88l 新增 1 个场景、T89c/T89f 按新布局重推）；
- 全量 `node tools/check-all.js --strict-env` **14/14**；
- **反向验证（三处修复各自变异，互不掩盖）**：
  · 退回绝对分页 → **7 条**新/改断言转红（正是钉这条的那些）；
  · 去掉 `loop = true` → **1 条**转红（T88d）；
  · 去掉 `argLoopBtn` 同步 → **1 条**转红（T88l）。

### 影响面复核

`winAnchorSeg` 的 5 个调用点全部有曲式快照在作用域内（`arr` / `a` / `arrCur`），故传参未新增任何查询；
预设模式那处**显式传 `null`**，明示它走绝对分页。

## v2.15.2 · 修「范围比同屏行数短时，循环回卷前没有预告行」（2026-09-24）

**来源（用户实拍两张截图 + 两轮配置确认）**：同屏 **4 行**、侧栏播放范围只设 **第 1–2 小节**、
范围循环**开着**，播到第 2 小节（= 范围末小节）时，网格第 1 行**没有**变成预告行
（仍是普通历史行：不透明、格子被「已弹」刷白、和弦胶囊只写「C」）；
把同屏行数改成 **2**、同一个范围就正常。用户给的理想态是 4 小节范围 + 4 行档那一张
（第 1 行降透明 + 胶囊「C（下一小节 · 十六分满扫）」）。

**根因**：`Viz.previewSegFor()` 的触发判据只有一条——

```js
seg === winAnchorSeg(seg) + arrWinBars() - 1        // = "播到的片段 == 页的最后一行"
```

而 `winAnchorSeg(seg) = floor(seg / W) * W` 是拿**歌曲片段的绝对编号**分页的，
**与 `S.arrangeSel.from`（播放范围起点）毫无关系**。于是只要**范围长度 < 同屏行数**
（本例 2 < 4），范围里就没有任何一片段能落在"页的最后一行"上——
**循环回卷前永远没有提前量**，而回卷恰恰是最需要它的时刻（回到范围起点时型可能整个换掉，
右手没有准备时间；本例回卷目标是「十六分满扫」，与在播的「副歌扫弦」完全不同型）。
2 行档之所以"看起来正常"，是因为 `范围 1–2 + W=2` 恰好让范围末片段与页末**重合**——
不是它没这个缺陷，是它踩中了特例。

**修法（一行判据扩为两条，取或）**：把触发条件从"页的最后一行"扩为
**"页的最后一行 **或** 播放范围循环的最后一片段"**：

- 页末那条**原样保留**（长范围靠它做翻页提前量）；
- 新增回卷那条：`seg === sel.to * K + K − 1`（K = 每小节片数，整小节档下 K=1 ⇒ 即范围末小节）。
  预告内容仍走既有的 `arrNextBar`（范围循环时它本来就回**范围起点**，不必另写一套）；
- ★ 两条都必须让开**正在播的那一行**：预告行恒挂第 1 行，而在播片段的行号 = `seg − winStart`。
  为 0 时（`W=1` 的降级态，或**单小节范围正好落在页首**）预告行会把在播行整个让出去，
  而 `setCell` 的 `isPreview` 豁免令该行永远拿不到 `.played` / `.active` → 白填充画不出来
  （与 v2.10.3 消掉的那个 `N=1` 症状同源）。故加 `seg <= winStart` 直接不预告；
  页末那条天然满足 `seg = winStart + W − 1 > winStart`（W ≥ 2），**不受影响、零回归**。

**影响面（已逐个核过，只有第 1 项真的改了）**：`winPrevSeg` 的全部消费点
（`buildBarRow` 的行内容/座次尺/和弦胶囊、`winPat.bars[0]` 内容替换、`.preview-row` 降透明、
`buildLyricLane` 的歌词轨预告行、`setCell` 的 `isPreview` 豁免）用的都是
"**第 1 行 = 预告行**"这条通用语义，不需要改；
弹跳球的"下一行"走 `rowOfSongPos(nrow.lin, 0, a)` **实算**该小节落在哪一行
（v2.10.8 起就如此），本来就不假设"预告行在页末"，故**未动**。

**自验数字**：`tests/run.js` **2983 PASS / 0 FAIL**（比 v2.15.1 的 2968 多 15 条 = 本组 T75f）；
新增 4 个场景（4 行档+范围 1–2 主场景 / 2 行档对照 / 4 行档+范围 1–4 对照 / 循环关闭反例）。
**反向验证**：把判据退回"只看页末"后，T75f 主场景的 3 条断言当场转红
（`preview-row` 标识、胶囊文字、无已弹填充），其余 2980 条不动——证明新断言真的钉住了这个缺陷。

### 同类但**未**修的两条（本轮只动用户报的那一处，避免混批）

1. **`Presets.onRangeInput` 的 `input` 分支只写 `from/to`、不写 `loop`**：
   `loop = true` 只在 `change`（松手提交）那一步由 `Arrange.setRange` 补上，
   而 `S.arrangeSel` 的初值是 `loop:false` ⇒ "范围看着变了、循环却没开"。
2. **侧栏那颗 `#argLoopBtn` 的选中态只在 `arrangeRender()`（编排浮层重绘，L9125）里同步**，
   不在 `refreshBar()` 里；而 `onPlayModeChange` 只调 `refreshBar`
   ⇒ 改了 `loop` 却不重绘浮层的路径会让它**保持旧外观**。
   （跳段行那颗 `#argJumpLoopBtn` 由 `refreshBar` 同步，主界面看到的那颗是可信的。）

## v2.15.1 · 修构建红灯：ESLint no-shadow + tsc 可空收窄（2026-09-24）

**来源**：推 v2.15.0 到 main 后，Cloudflare 构建与仓库内 CI 的 `verify` job 双双变红
（「真实浏览器冒烟」job 是绿的——那条路不受影响）。

### 根因（两处，且**本地都没跑到**）

1. **ESLint `no-shadow`**：`previewSegFor` 里 `let … schedBar = -1` 遮蔽了模块级的
   `schedBar`（曲式会话游标）。CI 报 `index.html:4404:35`。
2. **tsc TS18047**：`paintBall` 待命球分支里 `if (pCur && …) … atCur.schedBar`——
   `pCur` 由 `atCur ? resolveRef(atCur.ref) : null` 的**另一支**得来，TS 不会跨
   "pCur 非空 ⇒ atCur 非空"这条推导收窄 `atCur`。

★★ **为什么本地没拦住（本轮最重要的教训）**：本机没装 `node_modules`，ESLint / tsc 作为
"可选加强项"被标 **⊘ 跳过**——**⊘ 是"没查"，不是"查了通过"**；而构建环境 `npm ci` 装齐依赖后
它们真跑，第一处当场红。而且 CI 在第一步（ESLint）停下后，**tsc 那颗根本没机会执行**——
它是补装依赖、手工补跑 tsc 之后本地才抓到的第二颗雷。

### 修法

1. `schedBar` → `barIdx`（局部改名，语义零变化）。
2. `if (pCur && …)` → `if (atCur && pCur && …)`（判空写进**同一个条件**，TS 才收窄得动），
   并留注释写明这条推导的坑。
3. 本地补装依赖：`npm install --ignore-scripts`（`--ignore-scripts` 绕过 wrangler 安装脚本
   在本机沙箱的 `spawnSync EBUSY`），把两条加强闸门**真跑**一遍：
   `node tools/check-eslint.js` 0 错误；`tsc -p`（手工通道，shell 直拉）exit 0。

### 自验

- `node tools/check-all.js`（**带 node_modules**）→ ✓12 · ⚠2（tsc / 看门狗＝本机沙箱
  spawn 限制，两条均手工补跑：tsc exit 0、看门狗逐用例 20/20）· 实跑 12/14；
  **ESLint 这一步终于真跑**（1.25s）；行覆盖率 98.8%；冒烟真浏览器 ✓；
  自动化测试 2968 PASS / 0 FAIL；`npm run build` 九条目产物齐、戳记正常。
- ★ 纪律补记（写进 DEVELOPMENT §5）：**推 main 前若动过 JS，先本地把两条加强闸门真跑**
  （`npm install --ignore-scripts` + `node tools/check-all.js`，tsc 用 shell 直拉补跑）——
  别把 ⊘ 当通过；CI 会在第一步就停，后面的闸门不会替你跑。

## v2.15.0 · 窄屏自适应分片：一行不再硬塞一小节（2026-09-24）

**来源**：用户反馈——「手机上竖屏时，一小节都挤在一起，难以阅读。能否根据当前宽度
自适应调整，特别窄时就只显示半小节、或者四分之一节？」

### 变更

1. **行的粒度可以在小节内再切**：档位收敛成一个数——**每行几拍**（`vizRowBeats`）。
   候选 = 拍数的约数（降序）：4/4 → 4/2/1；3/4 → 3/1；5/4 → 5/1；6/8 → 6/3/2/1。
   **切点必落在整拍上**（不出现半拍的切点）；整屏统一一档（行与行的时值比例可比）。
2. **判据 = 最短那格的宽度**（内容密度相关，`chooseRowBeats`）：宽到够读（≥ `SLICE_MIN_CELL`
   = 24px）就保持整小节——所以同样宽度下四分型不拆、十六分型才拆；极窄时兜底「每行 1 拍」。
3. **映射层从「小节」升级为「片段」**（一行 = 一小节的一片）：窗口 / 翻页锚点、端点 → 行
   （`onsetPos` 一次算清片内 tick）、预告行（下一片段 = 同小节下一片，或节目单下一小节首片）、
   弹跳球与待命球、座次尺（只报本行覆盖的拍、拍号标签折回小节全局）、拍网格线、状态栏读数
   （仍报「第几小节 · 第几拍」）、静音行标识、行首和弦名（只挂小节第一片那几行）、
   歌词轨（按片切、跨片延音在本行右缘截断）。
4. **跨行长音画成「续接片段」**：两侧各一段、断开的那侧改虚线描边 + 圆角收平（`.cutl/.cutr`），
   **截断片段不打时值标签**（截断后的 t 不是真实时值）——两处重写 className 的路径
   （`setCell` 逐帧重绘 / `resetForStop` 停机复位）都带上该标记（实测踩过：只写在建树里，
   播放第一帧的全量重绘就把虚线边洗掉了）。

### 取舍

- **判据用「最短格宽度」而不是「屏宽」**：同样宽度下不同型的最优档不同——四分型手机上本来就
  不挤（不拆），十六分型才需要拆。单一屏宽阈值无法两全。
- **切点只落整拍**：半拍的切点把"拍"劈开，读谱无从解释；奇数拍因此没有"半小节"档，
  直接退到「每行 1 拍」（5/4 → 5 行/小节）。
- **档位不持久化**：它是布局的派生值（行宽 × 内容密度），每次 buildViz 现算；旋屏当帧不重建
  （resize 播放中"只重采几何"是既有承诺），最迟下次翻页 / 换型生效。
- **分片档下不做静态「静音行」标识**（一页只覆盖乐句一段、落点随翻页轮转）——与既有
  「档位 ≠ 4 时不标」同源，到点那一刻由状态栏说明。
- **★ K=1 是安全边界**：每行 = 整小节时全链逐位等于分片之前（切片函数原样返回原型数组、
  对象身份都保持）——既有 t79 / t87 / t89 等契约**原样全部通过**，就是这条边界的证明。

### 测试

- 新增 `tests/cases/t96-adaptive-slice.js`（5 场景）：判据边界矩阵（含奇数拍与脏输入）/
  同一十六分型 600px 与 300px 的对照（格数、座次尺、跑道、网格线）/ 跨行长音的续接片段与
  标签压制（含宽屏对照）/ 帧路径翻行与状态栏小节口径 / 曲式模式 + 歌词轨的片口径。
- `t47b` 按新行为重排：切片让「窄行宽 ≠ 窄格子」（自适应会先把一小节拆成几行）——
  要把 6t 格压到 14px 以下得用 96px 行宽。被测规则一字未变，只换构造方式。
- **反向验证（三轮定向变异，逐项复原）**：让选择器永不切分 → **35 条转红**；
  关掉截断片段的标签压制 → **2 条转红**；退回 `audioPosAt` 的片内折算 → **1 条转红**
  （★ 第三轮直接复现了实施期真实漏改的那一处——T96d 当初就是这么抓住它的）。

### 自验

- `node tools/check-all.js` 全绿（⊘2 = ESLint/tsc 缺 node_modules；⚠1 = 看门狗为本机沙箱
  spawn 限制，按仓库惯例逐用例直跑补验）。
- 自动化测试 **2968 PASS / 0 FAIL**（v2.14.0 的 2914 + 净增 54）。
- 版本 bump 2.14.0 → 2.15.0（VERSION / package.json / package-lock.json ×2 / README 对齐；
  CHANGELOG 本条即首条）；`node tools/gen-index.js --write` 重算模块索引行号。
- ★ **需真机验收**：手机上竖屏的观感（拆到半小节 / 每拍一行时的可读性、24px 阈值是否合适）
  ——冒烟脚本量得出几何、量不出"读起来舒不舒服"，阈值本身就是首版待复核项。

## v2.14.0 · 音频延迟补偿：蓝牙耳机下让「听到的」对齐「看到的」（2026-09-24）

**来源**：用户反馈——蓝牙耳机连电脑时「有明显的延迟」。落地为三件套：设置里的
「音频延迟补偿」+ 跟拍校准向导 + 按设备保存的多套配置。

### 根因与语义

蓝牙链路把**每一颗音**都推迟 100–300ms 送达耳朵（画面是即时的）——"看着弹"与"听着弹"
对不上。补偿 = 把发声时刻**统一提前**相应毫秒，耳机里听到的那一刻回到时间轴上。
★ 只动发声：时间轴（`loopStart` / `nextNoteTime`）、onset 表、播放头与弹跳球**一行未动**——
它们标的是"节拍器时间轴"，补偿的是"耳朵收到的那一刻"。

### 变更

1. **发声侧收口**（AudioEngine）：`playClick` / `lyricCueHit` 两个出口统一提前 `latencyMs`
   ——节拍声 / 扫弦声 / 预备拍 / 歌词锚点音 / 试听全部生效，无第三条路径可漏。
2. **排程前瞻量随补偿加长**（`look = schedWindow + 补偿`）：否则靠近窗口边缘的音会被排到
   "过去的时刻"而被 Web Audio 立即播放（补偿打折扣；offset 大于窗口时甚至越补越晚）。
   游标推进与重锚 / 饥饿判定口径不变。
3. **数据 = 独立键 `beatsight.latency`**：`{v:1, profiles:[{id,name,ms}], currentId}`。
   不进热键（低频设备配置，同主题 / 壁纸判据）、不进「导出全部数据」（换设备本来就该
   重新校准）。键不存在 = 从没做过选择 → 内存态一条「默认」（ms 0），不落盘。
4. **设置新分组**：按设备选择配置（下拉）+ 新建 / 改名 / 删除 + 补偿滑杆（0–500ms、5ms 步长、
   `input` 生效 / `change` 落盘）+ 校准向导。
5. **跟拍校准向导**：排 8 声固定间隔点击（**不吃补偿**——测的就是"没补偿时多晚到"）→
   用户跟着敲 → 每次敲击与最近一声求差 → 中位数 = 建议值 → 「采用」写入当前配置。
   ★ 诚实口径：建议值含**人的反应时间**（约几十毫秒），刻意不扣除（宁可稍早、不可再晚），
   向导文案明说"先用它、再用滑杆微调到声画重合"；样本 < 4 下判为测不准；抢拍 / 杂敲
   （配不上任何一声）直接忽略，不污染样本。
6. **顺带修正帮助页 4 处过期文案**（都是已删功能 / 已改行为的残影）：练习量 ×2、练习统计、
   变速训练「起始」；听辨训练的候选描述改准（是「给记谱、不给名字」，不是「不给谱」）。
   并按新功能补了「延迟补偿」条目。

### 取舍

- **不做"自动测出延迟"**：浏览器拿不到设备延迟、也认不出"现在用的是哪副耳机"——"自动"的
  边界是"记住 + 下次自动套用 + 换设备手动切一下"；测量必须用户参与一次（跟拍向导）。
- **向导值不扣反应时间**：扣一个拍脑袋的常数只是把一种系统性偏差换成另一种；宁可整体
  略偏大（声音稍早）也不偏小（再晚一次就白做了）。
- **首声无法提前**（起播那一刻的那颗音）：已经发生的时刻排不到"过去"，第二声起完全生效
  ——物理限制，写进文档、不写进"待办"。
- **校准期间先停播**（混着节拍器声量出的差值不干净）；`S.vol = 0` 时直接提示先拉音量。

### 测试

- 新增 `tests/cases/t95-latency-compensation.js`（7 场景）：读档校验矩阵 / 发声统一提前
  （onset 与时间轴不动的对照断言）/ 前瞻量扩容 / 建议值计算（中位数·剔非数·钳制·步长）/
  校准发声不吃补偿 / 向导端到端（跟拍 → 建议 → 采用，含抢拍忽略与样本不足）/
  配置 CRUD 与两级落盘、重载恢复。
- **反向验证（三轮定向变异，逐项复原）**：抽掉 playClick 补偿 → **4 条转红**；
  退回前瞻量 → **1 条转红**；放宽读档钳制 → **5 条转红**。
- ★ T95b 当场抓出一处理解偏差（第一版写"补偿不改变发声个数"）：补偿让**前瞻量**加大，
  同一墙钟窗口会多排到后面的音——断言改为"前 N 声逐位前移 + 多排的是时间轴上的下一拍"。

### 自验

- `node tools/check-all.js` 全绿（⊘2 = ESLint/tsc 缺 node_modules；⚠1 = 看门狗为本机沙箱
  spawn 限制，按仓库惯例逐用例直跑补验）。
- 自动化测试 **2914 PASS / 0 FAIL**（v2.13.1 的 2818 + 净增 96）。
- 版本 bump 2.13.1 → 2.14.0（VERSION / package.json / package-lock.json ×2 / README 对齐；
  CHANGELOG 本条即首条）；`node tools/gen-index.js --write` 重算模块索引行号。
- ★ 冒烟的 **DOM 节点预算按既定定档逻辑复核：1200 → 1300**（新增设置组 22 个节点、实测
  1209）——仍低于 Lighthouse 错误线（~1400），并在 `smoke.js` 注释里写明"下次贴近 1300
  应先瘦身、而不是继续放宽"。
- ★ **需真机验收**：跟拍向导的手感（敲击节奏、滑杆微调幅度）与补偿后的视听觉对齐，
  冒烟脚本看不出来。

## v2.13.1 · 空格键全局化：主界面任何位置都能开始/停止（2026-09-24）

**来源**：用户实报——「在点击过页面其他位置或功能后，空格键就不能起到暂停或播放的作用了。
我希望按空格键是能始终暂停或播放的。」

### 根因

空格能不能用取决于**焦点停在哪个元素上**。旧规则（v2.0.2）按焦点元素排除
（`INPUT` / `BUTTON` / `SELECT` / `TEXTAREA` 与 `role=button`），初衷是"不抢控件自己的
键盘契约"——但它挡不住**留焦点的控件**：滑杆（BPM / 音量 / 播放范围）与侧栏预设项点完
焦点留在原地（按钮们各自 `blur()` 自救，它们没有），空格于是要么被排除、要么被预设项的
`itemKeys` 抢去当"重新选中"。用户实报的两类路径都能复现。

### 变更

1. **全局空格规则改为「只排除正在输入文字」**（`Controls` 的 window keydown）：
   文本类 input / textarea / contenteditable（`isTextEntry` 反向白名单）之外，
   一律 `preventDefault` 后切播放。连按钮的原生「空格激活」也让位——按钮用 **Enter** 激活。
2. **主界面 role=button 项改 Enter-only**（`itemKeys(fn, {enterOnly:true})`，
   4 个调用点：曲式条目 / 内置项 / 自定义项 / 分区标题）；空格冒泡到全局分支切播放。
   面板（overlay）内维持 Enter/Space 双键——面板打开时全局分支本就早退，互不冲突。
3. **顺带两处收口**：`e.repeat`（按住空格不再来回切）与修饰键
   （Ctrl/Cmd/Alt+Space 有系统语义，不抢）。
4. 帮助页「键盘」条目改写为新口径；两处相关注释（`bindStep` 的 click 兜底、
   `bpmNum` 的 blur 说明）同步更新。

### 取舍

- **键盘用户失去「空格激活聚焦控件」**（预设项/分区标题）：Enter 仍是激活键。
  规则唯一（空格只做播放/暂停）比双轨好记，且这是用户明确要的「始终」。
- **面板内不变**：设置/编排/听辨/使用方法打开时空格仍不误触播放——面板里有输入框与
  危险操作，那一层归面板管。
- **桩的教训（写进实现注释）**：`isTextEntry` 对缺失的 `type` 按「非文本输入」处理——
  真实浏览器里 `input.type` 永不缺失（缺省即 text）；桩里想测「文本输入不让位」必须把
  type 显式摆出来，与 t86 的教训（桩初值与标记不同形会架空断言）同源。

### 测试

- 新增 `tests/cases/t94-space-global.js`（7 场景）：滑杆 / 按钮 / 预设项三条修复路径 +
  文本输入四种形状的例外 + range 反例对照 + 修饰键与 repeat + 面板回归钉子。
- 契约更新：`t30`（预设项 Space 不再激活）、`t85`（分区标题 Space 不再开合）、
  `t58`（断言文案去掉「Space」）。
- **反向验证（两轮定向变异，逐项复原）**：退回全局规则 → **10 条具名断言转红**；
  退回 `itemKeys` → **3 条转红**（t30/t85/t94d）。★ 过程中发现 t94d 第一版断言过弱
  （对同一项重复选中，值不变、测不出变异），当场加严为「向另一项发空格」后变异如期转红。

### 自验

- `node tools/check-all.js` 全绿（⊘2 = ESLint/tsc 缺 node_modules 可选加强项；
  ⚠1 = 看门狗为本机沙箱 spawn 限制，按仓库惯例逐用例直跑补验 20/20）。
- 自动化测试 **2818 PASS / 0 FAIL**（v2.13.0 的 2786 + 净增 32）。
- 版本 bump 2.13.0 → 2.13.1（VERSION / package.json / package-lock.json ×2 / README 对齐；
  CHANGELOG 本条即首条）；`node tools/gen-index.js --write` 重算模块索引行号。

## v2.13.0 · 首次打开的默认状态（2026-09-24）

**来源**：用户提的四件事——「第一次打开这款节拍器时」应该是什么样。
四项都落在**出厂状态**上，且都遵守同一条纪律：**只改"从没做过选择的人"看到什么，
绝不越过存档去改老用户已经做过的选择。**

### 变更

1. **预设库三分区出厂展开态改版**：节拍**展开** / 扫弦收起 / 自定义**展开**（此前是"全部收起"）。
   首屏因此能直接点到一条节拍型或一首曲式，而不是只见三个标题行。
   - ★★ 难点不在默认值，而在**「没存过」与「存过 false」必须分开判**：折叠态每次交互都写进热键，
     所以**老用户的存档里有显式的 `false`**；若照旧用 `=== true` 一个判据，改默认值就只对新用户生效、
     老用户打开还是全收起——等于没改。故加载判据从"严格 true"改为**先判 own property 存在性**：
     有该键就尊重用户（哪怕与出厂值相反），没该键才用出厂值。脏值（存在但非布尔）不算"没存过"，
     一律不展开。
   - 扫弦区仍收起：它数量少、属"专门去看"的一类，展开会把节拍区挤下去。
2. **首次打开默认选中示例曲《在他乡》**（闩 `beatsight.demoSeeded` 首次落位时顺手选中）：
   `playMode = arrange`、范围 = 整首、`loop = false`；侧栏那一行首屏就是「整首连播 · 已就绪 · 共 N 小节」。
   - ★★ **绝不自动播放**：刻意不走 `playArrange()`（它写完状态就 `Controls.start()`）。
     浏览器 autoplay 策略下首屏那一下必然静音失败，用户看到的是"在播放但没声音"这个最坏的第一印象；
     选中只是把状态摆好，等他按播放键（那时 AudioContext 已在手势里解锁）。测试用三条独立证据钉死：
     `S.playing === false`、调度器 interval 数为 0、调度游标停在 0 小节——并配一条**正控**
     （同一个桩里真的播一次，三条立刻反过来），以免"整个音频栈没起来"造成假绿。
   - 与"示例曲只带出一次"共用同一个闩：老用户（闩已落）完全不受影响，他自己的 `arrangeSel` 一位不动。
   - 落盘：显式触发一次 `persist()`。**准确地说不是为了"防丢"**——Store 已在 visibilitychange /
     pagehide 上挂 flush，正常关标签页必然写下（变异测试实测：删掉这行 T93b 仍绿）；
     它的价值是"不必等到用户切走"，首屏画完不到 0.3 秒就落盘，强杀进程也不会退回未选中。
3. **同屏行数 4 / 拍号 4/4**：代码本来就是这两个默认，本次只是**把它们钉进测试**
   （状态与控件高亮两边都判——状态对了但按钮高亮在别处，用户不知道该信谁），顺带复钉脏值回落口径。
4. **出厂默认背景壁纸**（Pexels 可商用素材，摄影师 waltonlee / 编号 35831987）：
   - 内联为 `data:image/jpeg;base64,…` 常量（**长边 1280 / JPEG 70 = 139 KB，base64 186 KB**）。**为什么内联**：
     单文件是这个应用的架构底线（双击 .html 就能用、离线、零依赖），外链 `images/wall.jpg`
     会让 file:// 直开时背景消失，还要在发布目录多维护一个资源；而壁纸渲染管线本来就吃 data URL。
     **为什么比用户上传的 1600 长边小一档**：它是压着 55% 暗遮罩的照片背景，不会被当图片细看，
     却要长期躺在仓库里、每次冷启动被解析进内存。
   - ★★ **尺寸/质量是量出来的，不是估的**（换档对比，2026-09-24）：判据用「**等效高斯模糊 σ**」——
     把原图铺满同一视口后虚化到与候选一样的高频强度需要多少像素的模糊半径（σ≤0.4 基本看不出、
     0.4–0.8 轻微、>1.2 一眼就软，经验阈值）。结果：
     | 候选 | base64 | σ@1440 | σ@1920 | σ@2560 |
     |---|---|---|---|---|
     | 1024 / q60 | 113 KB | 0.40 | 0.56 | 0.72 |
     | 1024 / q72 | 138 KB | 0.39 | 0.55 | 0.71 |
     | 1024 / q88 | 218 KB | 0.38 | 0.54 | 0.69 |
     | 1280 / q70 ← **最终采用** | 186 KB | **0.27** | **0.45** | **0.56** |
     | 1280 / q78（初版） | 221 KB | 0.26 | 0.43 | 0.55 |
     | 1600 / q82 | 343 KB | 0.16 | 0.33 | 0.40 |
     **关键发现：低分辨率档下「质量档」几乎买不到清晰度**——1024 长边从 q60 提到 q88，
     体积 +93%（113→218 KB），σ 只从 0.40 降到 0.38。那笔预算该买**分辨率**而不是质量：
     同样 ~180 KB，1280/q70（σ0.27）明显优于 1024/q88（σ0.38）。同理初版 1280/q78 偏保守，
     降到 q70 只多 0.01px 的发虚却省 35 KB。**最终定档 1280/q70，文件再瘦 187 KB**（1.10 MB → 0.89 MB）。
   - ★★ **三态存档口径**（默认图进来后，"没有壁纸"不再是唯一的一种"空"）：
     | 存档 | 含义 |
     |---|---|
     | 键**不存在** | 出厂默认图（用户从没做过选择 → 也**不落盘**，不该让"什么都没做"变成一个存档） |
     | `{img:"default", dim}` | 默认图 + 用户自己调的遮罩（落的是**记号**，不是那 186 KB 的副本） |
     | `{img:null, dim}` | 用户明确点过「移除」→ **默认图也不复活**（同示例曲"删不掉的东西"口径） |
     | `{img:"data:…", dim}` | 用户自己的图 |
     脏值 → 回落**出厂默认图**（v2.12.0 时回落"没有壁纸"；"读不懂的偏好"应当落回出厂状态，
     而不是落回那个只有用户点过移除才该有的态）。
   - **新增「恢复默认」按钮**，只在**当前不是默认图**时出现（已移除 / 用了自定义图）——
     否则用户移除默认图之后就只剩"挑一张自己的图"这一条路，"我想看回出厂那张"没有入口。
     它做的就是**删键**（回到"键不存在"那一态 = 显示默认图），不把常量抄进存储。
   - **「移除」从"删键"改为写 `{img:null}` 哨兵**：删键等于回到"从没选过"→ 刷新后默认图又冒出来，
     用户会认为"移除坏了"。
   - **顺带修掉一个真 bug（由本次新断言当场抓出）**：遮罩读取原来用 `isFinite(Number(x))` 一把收，
     而 `Number(null) === 0`、`Number("") === 0` —— 一份脏存档会把壁纸读成"完全不压暗"，
     用户看到的是"我的背景突然亮得刺眼"（默认 55 变 0）。现在只认**数值类型**且有限，其余一律回默认。
     我们从不写出这些形态（写盘只写 Number），所以收紧不会误伤任何真实存档。
5. **新增测试组 T93（首开默认 · 验收面）**、并改写受影响的 T85（折叠判据）、T92（壁纸三态）、
   T91e、T68g、T88d、T88j 的前提。
   **反向验证（变异测试）8 个有效变异全部产生具名断言失败**：折叠判据退回旧写法 → 19 条；
   首开顺带起播 → 10 条；首开不选中 → 12 条；选中但不切模式 → 6 条；无键当没壁纸 → 19 条；
   遮罩改回 Number 一把收 → 5 条；移除改回删键 → 4 条；默认图照抄进存储 → 1 条。
   ★ 过程中修掉两处"测试会崩而不是变红"的陷阱（`JSON.parse(String(undefined))`、
   `null.dim`）：崩溃不是证据，把测试改成先判存在性再取值，才让变异体现为具名失败。
6. **真机验收（真实 Chrome + 空 profile）28 项全绿**，并因此抓到一个**桩永远测不出的真 bug**：
   把完整 data URL 填进一个**已经带了 `data:image/jpeg;base64,` 前缀**的占位符里 →
   前缀写重、base64 多出 15 个字节，**浏览器根本解不开这张图**（背景一片空白），
   而所有桩测试当时全绿（它们只看"style 里有没有 data: 前缀"）。
   由此新增三条回归钉子：data URL 必须是**单份前缀 + 纯 base64**（正则带尾锚）、
   用 Node 的 Buffer **独立解一遍**验 JPEG 魔数 `ff d8 ff` 与结尾 `ff d9`、体积落在（0, 上限）。
   教训：**"这个文件里有一张图"和"浏览器能画出这张图"是两件事**——桩拿不到真实解码器，
   这类断言只能靠真机；能落回桩的部分（魔数、EOI、前缀唯一性）要在桩里补上。

### 兼容性

- 老用户的折叠状态、壁纸、播放选择**一位不变**（判据全部是"没存过才用出厂值"）。
- 从没设过壁纸的老用户升级后会**首次看到**这张默认背景——这是本次的预期效果；
  不喜欢可点「移除」，之后不会再回来。
- 文件体积：`index.html` 由 710 KB 增至 **0.89 MB**（+186 KB 内联默认图；定档前试过 1280/q78 = 381 KB，
  按上面的 σ 实测降到 q70 后观感无损）。仍是零依赖单文件。

## v2.12.0 · 自定义背景壁纸（2026-09-24）

**来源**：功能规划 F-11 / 想法 7。用户拍板两条：**① 壁纸不进「导出全部数据」；② 给「遮罩深浅」滑杆。**

### 变更

1. **新增设置 → 外观与辅助 →「背景壁纸」**：选择图片（JPG / PNG / WebP）、遮罩深浅滑杆、移除。
   三个控件**默认隐藏**，装上壁纸才露面（没有壁纸时给"遮罩深浅""移除"是空的入口）。
2. **落盘形态**：独立冷键 `beatsight.wallpaper` = `{v:1, img:"data:image/jpeg;base64,…", dim:55}`。
   - ★ **不进热键** `beatsight.state`：热键是 328 字节、每次交互 250ms 防抖整份 stringify；
     塞进几百 KB 就把「点一下就 stringify 一大坨」变成常态——正是 §3.6 冷热分离要防的事
     （预设库曾撑到 715 KB、每次点击阻塞 5–20ms 触发掉音）。
   - ★ **不进「导出全部数据」**（用户拍板）：它是这台机器的视觉偏好，不是数据资产。
   - **遮罩与图片同键同写**（`{img,dim}`）：分两个键就会出现"换了图、遮罩还是上一个值"的错位。
3. **为什么是 `data:` URL 而不是 blob URL**（两条都是硬理由）：
   - blob URL 刷新即失效——它不是可持久的字符串；
   - `_headers` 的 CSP 是 `img-src 'self' data:`：**`data:` 已在白名单里**（那行注释本就写着
     "供未来内联位图"），**一个字都不用改**；`blob:` 不在白名单，线上会被拦。
   ★ 已在真实 Chrome + 线上同款 CSP 下实测：`data:` 背景正常绘制、刷新后仍在、超额抛
     `QuotaExceededError`（可 catch）。
4. **体积与格式护栏**（三道，缺一不可）：
   - 原文件 > 24 MB **在解码前**就挡掉（解码一张 50 MB 的图会把主线程卡住）；
   - 降采样到**长边 1600 像素** + JPEG 0.82（压完仍超限就降到 0.7 再压一次）；
   - 落盘上限 1200 KB（base64 字符数），超了给出带数字的提示。
   - **格式按魔数（文件头）判，不按扩展名判**：扩展名可以随便改，文件头改不了。
     **SVG 一律不收**——它是 XML，能内嵌脚本与外部引用；开了这条口子，后面很容易被改成
     `innerHTML` / `<object>` 去用，而且能靠实体膨胀把体积做到任意大。
5. **一个容易被忽略的注入面**：`img` 要被拼进 CSS 的 `url("…")`，所以校验用的是
   **正则白名单**（`^data:image/(jpeg|png|webp);base64,[A-Za-z0-9+/=]+$`）而不是"看着像图片"
   ——只要允许出现一个双引号，localStorage 里的一个字符串就能逃逸出 `url()` 去改别的样式。
6. **遮罩深浅滑杆**：0–80、默认 55、步进 5，**上限只给到 80% 而不是 100%**（压到全黑等于没壁纸）。
   与音量滑杆同一套两级口径：`input` 只改观感、`change` 才落盘（每帧写几百 KB 会卡）。
   遮罩颜色**随主题走**（经典压 `#121212`、观测台压 `#0A0C12`），换主题会重画壁纸层。
   ★ 没有壁纸时动滑杆**不写盘**（不产孤儿存档）。
7. **壁纸层是独立一个 `<div id="wallLayer">`（`z-index:-1`、`aria-hidden`、`pointer-events:none`）**
   而不是直接改 `body` 的背景：`body` 的背景被主题占着（两个 `--bg` 各写一份，观测台还叠了
   radial-gradient），会互相覆盖；且"只压暗图片、不动内容"要靠同一层里 background-image 的两段
   叠加（**先遮罩渐变、后壁纸**）才能做到。卡片都是不透明的，所以壁纸只在留白里露出来——
   文字可读性天然不受影响。

### 验证
- 新增 **T92 系列 8 组**（2649→2651 断言，0 失败）：格式魔数（含"MIME 撒谎但内容是文本"）、
  CSS 注入面（含引号 / 换行 / `javascript:` / `data:text/html`）、体积上限、
  脏档丢弃（且**只有遮罩脏时图片仍保留**）、不进热键与导出包、遮罩两级口径、
  选图的四种失败都有话说、换主题换遮罩色。
- **反向验证**：① 删掉魔数校验 → 「MIME 写 png、内容是文本」一条立刻变红；
  ② 把正则放宽成不含 `$` → 「含双引号」一条立刻变红（两条都实测过）。
- 桩里拿不到 canvas 的 2d 上下文，故整条链设计成"拿不到就用原图"——
  校验与落盘在桩里完整可测，真正的绘制交给浏览器冒烟。

## v2.11.3 · 补网：三处「练习记录」残留、lint 加两条硬规则、诊断能留下错误原文并进设置（2026-09-24）

**来源**：全仓库审计后确认的一批"没人拦得住"的缺口。都不是新功能，是**把网补上**。

### 变更

1. **修「练习记录」的三处残留**（该功能 v2.10.12 已随练习统计删除）：
   - **导入全部数据的成功弹窗会显示「练习记录 undefined 场」**——`res.added` 里早就
     没有 `log` 字段，弹窗却还在读它。改为读 `ear`（战绩合并项数），且只在真合并时
     才出现这一截（避免每次都挂一句"0 项"）。
   - 导出全部数据的**读屏播报**仍念「… / 练习记录 / 听辨战绩」，四类改三类。
   - README 的备份段、DEVELOPMENT §3.6 持久化表里的 `beatsight.log` 一行，一并更正。
   - ★ 顺带修掉本轮 v2.11.2 自己引入的漂移：`beatsight.state` 字段表里的 `plan`、
     模块地图里的 `sessStartT`、Trainer 描述里的「上次训练接续」、ear 行里那句
     "同时显示在统计面板"（面板已删）。
   - ★ 历史文档（CHANGELOG / PLAN / AUDIT / spec.md）里对练习记录的记述**刻意保留**——
     那是史实，不是现状说明。
2. **零依赖 lint 加两条硬规则（五条 → 七条）**：
   - `no-eval`：禁 `eval(` / `new Function(`。★ 为什么必须硬拦：线上 CSP 是
     `script-src 'self' 'unsafe-inline'`，代码里出现 eval 在 `file://` 下能跑、
     一上 Cloudflare 就被**静默掐死**——本地测得好好的、线上悄悄没反应，且无任何报错。
   - `no-innerhtml`：`.innerHTML =` 只允许赋 `""`（清空）。拼串既是注入面（预设名 /
     歌词 / 导入 JSON 都是用户可控串），也会让**测试桩与真实 DOM 的结论分叉**。
     ★ 边界：不管 `outerHTML`——它只有两处、替换的是代码内常量 SVG 模板。
   - 两条均做了**反向验证**：注入 `innerHTML = "<b>" + 1` 与 `eval("1")` 各被精确抓到；
     `no-eval` 在开发本条时还**真的拦下了自己写的 `msg == null`**。
3. **诊断：从"只报计数"到"留下原文"，且不再只有 `?debug=1` 够得着**：
   - 新增 `diagLast`（**环形缓冲 20 条、每条截断 200 字符、只留内存不落盘**）：
     「脚本错 3」能说明出了 3 次，却说不出出了什么——报障时最有用的那句恰恰在计数里没有。
     ★ 不落盘是刻意的：错误原文可能含用户数据（预设名 / 歌词 / 导入的文件名）。
   - `window error` 与 `unhandledrejection` 两个监听现在除计数外**各留一条原文**，
     并对"没有 message 的资源加载失败""`reason` 缺失"等形态做了容错——
     **诊断代码自身绝不再抛异常**（那会掩盖真正的故障）。
   - 设置弹窗新增**组④「诊断与自验」→「复制诊断信息」**按钮：报障时用户想不起加
     `?debug=1`。它只写剪贴板、**不上传不联网**；剪贴板不可用则弹窗显示。
     再次打开设置时按钮文案复位（否则第二次看到的是上一句的"已复制"，像卡住了）。
4. `.trae/rules` 里那句「推送前须 10/10 全绿」**改为不写死步数**——自检链自 v2.0.6
   起已扩到 14 步（当前实跑 12、⊘2）。数字写死在给 Agent 看的规则里会腐坏，
   且腐坏后 Agent 会按错误口径判断。同时写明 `⊘` = 未执行不算失败。

### 验证
- `node tools/check-all.js`：**14/14 全绿**（本机首次装上 devDependencies，
  ESLint 与 tsc 两项 ⊘ 也跑起来了）；自动化测试 2572 PASS / 0 FAIL；行覆盖率 99.2%。
- 新增/改写断言：T56g（环形缓冲：上限、截断、不落盘、进报告）、
  T56h（设置里的复制入口与文案复位）、T58a（trainer 默认值改为断言四个留存字段）。
- **T91 老数据搬家回归矩阵**（6 组，与上面三件事配套）：本轮改的都是**数据形状**，
  而迁移代码只在"新老数据相遇"时才跑 —— 天然处在"覆盖率照绿、功能照坏"的盲区。
  矩阵按「数据来源（旧热键 / 新热键 / 全量数据包）× 数据年代（v1 / v2 / v3）」铺开，
  造包一律用**老形状**（歌词段叫 `lines`、块引用 `{type:"builtin",idx:0}`、带已删的 `log` 一类）
  —— 拿当前形状造包根本测不出迁移。反向验证：把 `start/last/plan` 整包捡回来
  （用 `Object.assign` 而非白名单抽取），10 条具名断言同时变红。

## v2.11.2 · 变速训练瘦身：删「起始」参数与 7 天计划 / 接续按钮，参数移到开关右侧（2026-09-24）

**来源**：用户实报——打开变速训练后，相关设置项在**可视化区域的下面**，离开关太远，
调一次参数要往下跳一大截；顺带拍板删掉两个用不上的东西。

### 变更

1. **删「起始 BPM」参数**——起点改为**开启训练那一刻的当前 BPM**（会话值 `trBaseBpm`，
   不持久化）。理由：起始值绝大多数情况就是"我现在弹的速度"，单独留一个输入框多余又要跑到
   别处改。连带效果：**开开关不再把 BPM 跳到某个存档值**（起点就是当前速度）。
   - 边界处置（用户选 **A · 不给开并提示**）：当前速度已 ≥ 目标时**不给开启**，弹提示
     「先把速度调低一点，或把目标调高一点」——不偷偷改用户的速度。
     拦的理由：不拦的话 `total()` 算出 1 级、`bpmFor(0)` 立刻 ≥ 目标，表现是"刚打开就报完成"。
   - 老存档里的 `start` 键由 `TR_KEYS` 白名单自动忽略，**零迁移代码**。
2. **删「7 天计划」（v1.5）**——它只是"把起始/目标切成 7 段"的二次封装，起始没了它也没有
   立足点。整条下线：`S.plan` 字段、`planDayStart/planDayTarget/refreshPlanUI`、三个按钮、
   以及完成时的"推进到明天"分支。
3. **删「继续上次训练 / 再来一轮」按钮（v1.4）**——连带删掉它唯一消费的接续记录
   `S.trainer.last` 与产出它的 `consumeSession()`。口径同 v2.10.12 删练习记录：
   功能删了，它唯一消费的数据也删，不留没人读的存档。
   ★ 这两个按钮其实是**同一个** `#trResumeBtn`，文案在运行时改写（练完→"再来一轮"、
     中途停→"继续上次"），只看标记里的静态文案是找不到的。
4. **剩下的三个参数（目标 / 步长 / 每级）与进度文案搬到 `.viz-toggles` 里开关的右侧**
   ——不再隔着整个可视化网格。**所有 id 一个都没换**（`trainerPanel` / `trTarget` /
   `trStepInp` / `trEvery` / `trainerProg`），接线与既有断言照旧有效。
   - 参数行只在开关打开时露面；`.tr-panel` 自带的 `margin-top:12px` 在 `.viz-toggles`
     作用域内归零（否则与 gap 叠成 24px，看着又离开关远了）。
   - 窄屏由 `.viz-toggles` 既有的 `flex-wrap:wrap` 自动折行。

### 测试

- `t37`：`trResumeBtn` 相关 6 组场景与 7 天计划相关 6 组场景**整组删除**；
  新增「当前速度 ≥ 目标 → 不给开 + 提示」与「起点 = 开启时的当前 BPM」两组。
- `t01`：`trStart` 的钳制场景删除（输入框已不存在），改为断言老存档里的 `start` 被忽略。

### 自验

自动化测试与 `check-all` 见本节末。

## v2.11.1 · 文案收口：图例与快捷键提示移入「使用方法」+ 删侧栏长说明（2026-09-24）

**来源**：用户三条——①「时值 = 长度」图例移到页底（v2.11.1 开发中先做了，随后用户改主意）；
②删侧栏长说明；③页底文案区有 bug（实截图）——最终拍板：**干脆移进「设置 → 使用方法」，
并确认无重复内容**。

### 变更

1. **「时值 = 长度」图例直接删除**——使用方法「画面怎么看」已逐条全覆盖
   （时值=长度 / 休止符等长空白占位 / 底部 1 e & a 拍内座次 / 跑道亮边=换拍 / 弹跳球预告落拍），
   移过去就是纯重复。
2. **快捷键提示拆点并入**：「空格开始 / 停止」键盘节已有；两个未覆盖的点补进帮助——
   「首次点击页面解锁音频」进「三步上手」第 3 步、「画面动画由音频时钟驱动（声画偏差 < 16ms）」
   进「画面怎么看」。
3. **页底文案区（.page-foot）整块删除**（含全部对齐修正与 CSS）——开发中曾按
   「内边距 48/32 分档对齐 + 限宽 + align-items:start」修过一轮，随元素删除一并退役；
   `.main{align-items:start}`（侧栏按内容收高）保留。
4. **删侧栏「自定义节奏型保存在本机浏览器中…」长段说明**——备份入口就在同卡按钮上，
   且「变速训练器在『训练模式』区」的提法已过期。

### 测试

- smoke：caption/hint 探针与断言随元素删除退役；t90 jumpRow 切片止标改 `</section>`。
- 使用方法内容变更由 t55（分节标题清单）覆盖，未破坏。

### 自验

自动化测试 **2573 PASS / 0 FAIL**；`check-all` 其余 13 项通过
（帧率断言因无头 Chrome 环境态 30fps 未过——空白页对照实验证明与代码无关，备查）。

## v2.11.0 · 界面重塑：设置浮层 + 布局重排 + 间距体系 + 跳段行图标化（2026-09-23/24）

**来源**：连续十轮界面反馈（开发期版本 v2.10.13–v2.10.22，未单独发布），合并为一个大版本。
每一轮均与用户确认后落地；本轮起版本号从 v2.10.12 直接跳到 **v2.11.0**。

### 一、设置浮层（Dialog）
- 设置从全屏页面式改为**浮层小窗**：不全屏、backdrop 压暗 + **毛玻璃模糊**（blur 8px）、
  **点窗外即关**（backdrop 即根元素，target === currentTarget 判据）；
  「返回练习」工具栏 → 右上角 ✕（#settingsClose id 不变）；Esc / 焦点陷阱 / inert 同协议。
- 内容 10 项分三组：外观与辅助（主题/弹跳球/六线底纹）、**发声（音色 + Swing 三档）**、
  数据与说明（四个导入导出 + 使用方法）。

### 二、布局重排
- **走带卡（原 02 Transport）整卡删除**：BPM 组进时值可视化卡卡片头（右列、紧凑 48px 列距）、
  音量三条进左列、播放键进跳段行中间、拍号在「同屏行数」右侧、Swing 进设置。
  角标重排为 **01–07**（01 Rhythm Map / 02 Library / 03 Pattern Editor / 04 Settings /
  05 Ear / 06 Arrangement / 07 How to use）。
- **卡片头**：标题 #vizTitle 删除（与状态灯重复）；左列三行 = 角标 → 状态灯 + 节奏型名
  （`#statusText{min-width:16em}` 防抖——播放文案每个十六分音都在变长变短）→ 音量组
  （`flex:1` 撑满列高、滑杆行距均摊）；**左列固定 320px**（节奏型名变长变短不再推移 BPM 组）。
- **当前节奏型行（.pattern-head）整块删除**：#patternName 进状态灯行、#patternMeta 删除
  （updateMeta 全链拆除）。
- **跳段行**：三列 Grid——三键组（⏮ ▶ ⏭，实心单三角图案）**精确居中**，范围循环双态图标钮
  （关 = 顺序循环 / 开 = 单曲循环 + 1，`aria-checked` 驱动切换、开启绿色点亮）紧随其后；
  上/下段键与循环钮**常显**（不可用置灰，置灰判据含模式检查）；#argNowMeta 删除。
- **训练模式标题行删除**（开关在预备拍行、面板按需出现）；训练组壳子 `display:contents`
  （消除隐藏时占用的间隔槽）。
- **快捷键提示移到页面最底部**（`.page-foot`，两栏栅格之外通栏）。

### 三、间距与字号体系
- 块间纵向间隔**统一 16px**；行数/拍号面板对齐（gap 10px，修 3px 错位）；pill 高度统一 36px
  （删除行数缩小规则）。
- **BPM 组降一档**：大数字 40px/80（窄屏 34/70、观测台 48、观测台窄屏 36/76）、步进键 40px、
  快捷档 32px——收紧「状态灯↔音量」空隙的直接杠杆。
- 跳段键 52px 圆钮、单三角 ◀/▶ 图案（六案对比用户选定）；播放键 72px 仍为页面最大圆。

### 四、bug 修复
- **六线底纹窄屏铺不满**：v2.8.0「铺满格子」契约只改了桌面档（--gtop:0/--gt:8.8px），
  ≤960px 档还留着 v2.4.3 的上下留白老几何——补齐窄屏变量（--gt:6.8px、--gtop:0，线落 0..34）。
- **playArrange 顺序缺陷**：先 setMode（钩子同步刷新跳段键）后写 arrangeSel，键被错误置灰——
  调换为先写数据再切模式。
- **找回被误删的 T90g**：v2.10.17 批量替换测试文件时区间跨掉了该场景（套件照绿未察觉）——
  重建并确立「替换前列场景清单、替换后 grep 场景号核对」的纪律。
- **范围循环钮双图标同显**：`applyToggle` 会**整写 className**（"toggle-pill on/off"），
  把循环钮身上的 `jump-btn loop-btn` 类抹掉 → 双态 SVG 切换 CSS 全部失效、两枚图标同时显示
  （用户实报"还是两个单独的按钮"）。修：该钮不再走 `setToggle`，只写 `aria-checked`；
  t77 的开/关观察面从 className 改为 aria-checked。
- **循环钮零背景图层**：不要灰圆底也不要开启态的绿色染底——`background:transparent`
  （含 hover 与观测台主题边框覆盖一并去除），图标直接浮在页面背景上；开启态只靠图标变绿点亮。
- **BPM 组两行按钮两端对齐**：步进行与快捷档行拉伸到组宽后 `space-between`——
  -5↔60 左缘对齐、+5↔120 右缘对齐（两行同宽由列布局 stretch 保证）。
- **节奏型名与百分号列对齐**：状态行撑满左列宽 + `#statusText{margin-right:auto}`——
  名字被推到行尾，与音量行的百分号列同一右缘。

### 测试

- 每一轮均做**反向验证**（对上一版跑全套，失败全部落在本轮新增断言，账目自洽）。
- t90 系列（a–h）按最终形态重写；t30 的重建观察面改 `PROBE.layoutReads`；
  t53/t68/t70/t73/t75/t76/t79/t88 的标题/meta 断言随元素删除退役（覆盖转由网格内容与侧栏滑块承担）。
- **tools/smoke.js**：左边缘基准改左列「音量」组标签（原 #vizTitle 删除；.sec-tag 经典主题
  display:none 不能当基准）；#bpmNum 规格断言 80×48/40px。

### 自验

`node tools/check-all.js --strict-env` → **全部通过 · 实跑 14/14 项**；自动化测试
**2570 PASS / 0 FAIL**；真实浏览器冒烟（桌面 + 390px 双通道 + 布局探针）；行覆盖率 99.2%。

## v2.10.12 · 顶栏收口为三个入口 + 设置弹窗；删除练习量与练习统计（2026-09-23）

**来源**：用户提出五项界面调整，逐条确认后落地——
① 新增「设置」按钮与设置弹窗，把**主题切换 / 使用方法 / 弹跳球 · 预判提示 / 六线 · 扫弦谱底纹 / 音色**五项收进去；
② **删除**「练习量 · 到点自动停」与「练习统计」两个功能及其界面元素；
③ 「听辨训练」移到顶栏原「练习统计」那一格；
④ 追加：「编辑节奏型」移到「编排曲式」左边；
⑤ 追加：「导出预设 / 导入预设 / 导出全部数据 / 导入全部数据」四个按钮也移进设置弹窗。
确认时拍板的四件事：**练习记录一起删（甲）**、正确率小字取消、设置弹窗**分三组**、**音量留在主界面**。

### 顶栏改后

`[设置] [听辨训练] [后台保活]` —— 主题切换 / 使用方法 / 弹跳球 / 六线底纹 / 音色 / 四个导入导出
全部进了设置弹窗（共 **9 项，分三组**：外观与辅助 / 发声 / 数据与说明）；
练习统计与练习量整块删除。判据写在设置 overlay 标记处：「**你在练习过程中会不会去点它？**」

### 变更

1. **新增 `Settings` 模块**（顶栏「设置」按钮打开 `#settingsOverlay`，`open/close/isOpen`）。
   ★ 模块数**仍是 13**：`Stats` 撤掉的架构槽位正好由 `Settings` 接上（`EXPECTED_ORDER` 换名不改数）。
   ★ 为什么是模块而不是装配层的一段：诊断面板那条先例成立的前提是"只读注入钩子、不引用模块"；
     设置要被 Controls 的键盘路由查询 `isOpen()` —— 必须占架构槽位。
   ★ **被搬进来的控件，handler 一行都没动**：它们全是按 id 绑定的（主题在装配段、弹跳球/六线在
     Controls 的 `bindToggle`、音色的档位点击在 Presets、四个导入导出在 Presets、使用方法在 `Help`）——
     **搬的是元素，不是接线**，因此不会产生任何新的反向引用（教训记在 T90 的注释里）。
2. **删除「练习量」**：`LIMIT_PRESETS` / `LIMIT_OFF` / `S.limit` 字段（含加载校验、persist、
   `__beat` 出口）/ `limitHit()` / `syncLimitUI()` / `syncLimitProg()` / `limitT0` / `.limit-panel` UI。
   ★★ **内核必须保留**：`limitBars` 计数与 `onLimitPulse` 钩子是听辨训练「每次只放 2 小节」
   额度的**基础设施**（`playQuota` 与它比较、`Controls.stop` 里那段分支依赖它）——
   删它俩会让听辨训练的自动停播**静默失效**（无任何报错）。
3. **删除「练习统计」（用户选"甲"：练习记录一起删）**：`Stats` 模块整块、`#statsBtn`、
   `#statsOverlay`（含「导出记录 / 清除记录」两钮）、练习记录 `beatsight.log` 全链
   （`KEY_LOG` / `logSessions` / `appendSession` / `clearLog` / `serializeLog` / `CONFIG.logMinSec+logMax` /
   `Controls.stop` 的自动入账 / `sessStartT` 计时起点 / `__beat` 的 `logSessions` 出口）、
   以及「导出全部数据」里的 `log` 段——**旧数据包里的 `log` 字段现在被静默忽略**（不报错）。
   ★ 听辨战绩 `Store.earStats` **保留**（它有自己的消费者：听辨训练 overlay 里的正确率）。
4. **「编辑节奏型」跨列搬家**：从主列当前节奏型行 → **侧栏**「编排曲式」左边（`.arg-now` 行首）。
   `.preset-io` 整行消失（四个按钮搬进设置弹窗、两个隐藏 file input 随行）；
   侧栏那段说明文字同步改写（原先指向被搬走的按钮）。
5. **`.ph-controls` 包裹层撤掉**（音色走了、编辑节奏型走了，当前节奏型行右侧只剩**音量**）。
6. **角标重新连续**：设置弹窗用 **06**（原「练习统计」腾出来的号），06 → 09 不再有空号。

### 测试

- **删除 `t48-practice-limit.js`**（8 个场景全是练习量）、**`t50-practice-coverage.js`**（5 个场景全是练习分布）。
- **`t37-stats-and-training.js` → 改名 `t37-training-and-keepalive.js`**，删掉 T37 / T38 / T39 / T46（统计与练习记录）四组场景，保留 T40 / T41 / T45（训练计划）、T42 / T42b（保活）、T43（PWA）、T44（音色响度）。
- **`t46b` 改写**：原第二条出口是「导出记录」（已随统计删除），改用「导出全部数据」——
  两条出口仍共用同一份 `downloadJSON`（去重下载的契约不变）。
- **`t49g` 改写**：入口已迁顶栏 + `S.limit` 不留空壳；**练习量的"额度分支优先于练习量"口径随之消失**。
- **`t56e` 改写**：驱动 `limitPulse` 的现在是**听辨训练的会话额度**（进入听辨 → `playQuota = 2 小节` → 放满自动停 → 计一次），计数语义不变。
- **`t53e` 精简**：原练习量 × 曲式的叠加场景删除，保留「曲式播放不改动 S.sel」。
- **`t01` / `t30` / `t57` / `t85` / `t90b` 同步**：模块清单 `Stats` → `Settings`、`OVERLAY_IDS` 与
  overlay 协议表加 `settingsOverlay`、侧栏"导出/导入区常显"两条改写、`t90b` 重写（音色进设置、编辑节奏型去侧栏、音量留主行）。
- **新增 `tests/cases/t90-control-layout.js` 已在 v2.10.11 登记**，本版追加设置弹窗的结构断言（三组标题 + 9 项 + 两个 file input）。

### 自验

`node tools/check-all.js --strict-env` → **全部通过 · 实跑 14/14 项**（含 ESLint / tsc / 真实浏览器冒烟）。自动化测试 **2523 PASS / 0 FAIL**（v2.10.11 的 2704 − 被删功能的既有断言 181 条 + 新增/改写后净变化）；真实浏览器冒烟 **69 项断言**（含**桌面 + 390px 两遍**的左边缘对齐断言）；看门狗 **58 PASS / 0 FAIL**；行覆盖率 **99.2%**（6861/6918）。

**反向验证（对 v2.10.7 的原始快照）**：**2450 PASS / 65 FAIL** —— 65 条**全部**来自四版（2.10.8–2.10.12）新增或改写的断言（Settings 模块 / 设置弹窗 9 项与三组标题 / chip 文案 / 状态点进徽章 / 听辨入口迁顶栏 / 拍号与音色音量的落位 / 范围短于窗口的小球落点），**没有一条范围外的失败**。
★ 其中 6 条是 `t57` 的 overlay 协议断言——为此给 `t57` 的遍历加了**元素缺失守卫**（具名失败而不是崩溃：崩溃会中断整套件、让后面的用例都没机会跑，崩溃不算证据；且只查**入口按钮**——弹层容器是惰性创建的，开合之前 `els[ov.id]` 必然 undefined）。
★ **架构闸门单独验证**：`check-module-order.js` 在旧版与新版都通过——新版把 `Stats` 换成 `Settings` 后声明顺序、双向 diff、R1–R4 全绿。

★ **视觉仍需人眼验收**：本轮动了**两列的整体结构**（顶栏收口、训练模式区瘦身、侧栏加入编辑入口），冒烟量的是"元素在不在该在的位置"，量不出视觉平衡——尤其 390px 窄屏下顶栏三控件与侧栏加入「编辑节奏型」后的观感，请实机过一眼。

## v2.10.11 · 控件搬家（拍号→行数右侧、音色/音量→编辑节奏型左侧）+ 左边缘对齐（2026-09-23）

**来源**：用户提出三条布局需求，并在逐条确认后拍板。确认阶段的两项工作是这次的关键——
**先量再改**：用 CDP 在 390 / 1440 两个视口量各控件的 `getBoundingClientRect`，并**独立地**
把用户截图按像素解出来量了一遍文字的最左列（两处结论一致），才敢动手。

### 需求与变更

1. **「拍号」整块移到「同屏行数」右侧**：`#sigRow` 所在的 `.group`（含 `#accGroup` / `#accRow`
   重拍分组）从**走带卡**整块搬进**时值可视化卡**，与 `.viz-rows-panel` 同处新的 `.viz-rows-row`
   （`flex` + `gap:28px` + `flex-wrap:wrap`）。走带卡因此只剩 BPM 与 Swing，多余的 `.vdivider`
   一并删掉（原 5 个 group 之间有 4 条，现只剩 1 条——多留一条会连成双竖线）。
2. **「音色」「音量」移到「编辑节奏型」左侧**：两块从走带卡搬进**当前节奏型行**（`.pattern-head`），
   排在 `#editBtn` **之前**；三件套包一层 `.ph-controls` 让窄屏**整组一起折行**。
   两块 `.group-label` 按用户要求缩短为「音色」「音量」（完整口径仍在帮助页「音色 / 音量」条里）。
3. **左边缘对齐**（用户选"丙"）：`.viz-toggles .toggle-pill{padding-left:0}` —— 作用域**限定在这一行**，
   不动全局 `.toggle-pill`（那会是全站开关的观感变更）。
4. **`.pattern-head` 补齐内边距**（用户选 (ii)）：它是**没有卡片外壳的裸行**，补上 `padding:0 24px`
   （≤960px 为 `0 16px`，与 `.card` 同步）——否则它的内容比卡片内容偏 24px / 16px，
   音色音量搬进来后这偏差会当场变成"看得见的左边缘不齐"。

### 实测数据（改动前后，同一支 CDP 探针）

| 量什么 | 390px 改前 → 改后 | 1440px 改前 → 改后 |
|---|---|---|
| 卡片标题文字左 | 32 → 32 | 48 → 48 |
| **开关行「预备拍」文字左** | **48 → 32** | **72 → 48** |
| 「同屏行数 · 扫弦与歌词共用」文字左 | 32 → 32 | 48 → 48 |
| 行数 pill 的**盒子**左 | 32 → 32 | 48 → 48 |
| **当前节奏型行名称文字左** | **16 → 32** | **24 → 48** |

**根因（为什么要动这两处而不是别处）**：四行的**容器**左边缘本来就都在卡片内容边缘上，
差的是**按钮自身的内边距**——`.toggle-pill` 左 16px、`.viz-rows .pill` 左 14px。开关那行尤其显眼，
因为 `.toggle-pill` 的底色 `var(--card)` 与卡片底色**完全相同**（看不见"底"），那 16px 就被读成了缩进；
而 pill 有可见底色，它的**盒子**本来就在内容边缘上（用户报的"文案不齐"里，pill 那行是盒子对齐、
只有文字被自身 padding 内缩，属正常，故本次不动）。

### 用户拍板的四件事（备查，避免以后再纠结）

① 窄屏折行可以接受（实测 390px 下卡片内容可用宽仅 326px，而拍号 6 颗 pill 单行需 **384px**，
**原本就在折行**）②③(ii) 对齐 `.pattern-head` 到卡片文字列 ④ label 缩短成「音色」「音量」。

### 未做（留给后续）

- **`.col` 里另两块裸块仍与卡片内容差 16/24px**：训练模式 `.group`（03）与底部 `.hint`。
  实测两套左边缘一直存在（裸块在 16/24、卡片内容在 32/48），本次按批准范围**只对齐了
  `.pattern-head`**；要不要把这两块也补齐，等用户决定（补了页面才只有一条左边缘）。
- ④ 那排 pill 的**文字**没有去贴内容边缘：贴了就得把它的盒子拉到卡片内边距里（只剩 2px 余量），
  比"文字内缩 14px"难看得多——这是用户选"丙"时一并确认的取舍。

### 测试

- **新增 `tests/cases/t90-control-layout.js`**（5 场景 / 56 条断言），已登记进 `tests/run.js`：
  T90a 拍号的落位与内容完整性（6 档 + `#accGroup` 跟随）/ T90b 音色音量在 `#editBtn` **之前**与顺序 /
  T90c 走带卡收口（只剩 1 条 `.vdivider`、2 个 group）/ T90d 两条 CSS 契约 / T90e **搬家不得改变行为契约**
  （id、初始态、`hidden` 初值、以及"点档位仍生效"的接线——最后这条是搬家最容易踩坏又最难发现的）。
  ★ 用**标记字符串**断言：桩不解析 HTML，静态元素是孤立桩（`parentNode` 恒 null），走路树断言不出来。
- **`tools/smoke.js` 新增"真几何"断言**（新增独立探针 `layoutProbe()`，**两种视口各跑一遍**）：
  开关行文字 / 行数标签文字 / pill 盒子 / 说明行 / 当前节奏型名称，**五者与卡片标题文字同一条左边缘**
  （容差 0.5px）；桌面下拍号在行数右侧、顺序为 音色 → 音量 → 编辑节奏型；窄屏下拍号折行。
  ★ 这是桩**根本测不到**的那一类（"文案是否真在同一条线上"）；这是继 v2.10.10 的状态点之后
  第二条真布局断言。窄屏那一遍是本次特意加的——用户报的那次就发生在窄屏。
- **既有断言一行未改**：控件 id 与文本契约全部保留（搬块不换 id），t24 / t30 / t50 / t79 / t87
  对这些控件的引用照旧通过。

### 自验

`node tools/check-all.js --strict-env` → **全部通过 · 实跑 14/14 项**（含 ESLint / tsc / 真实浏览器冒烟）。自动化测试 **2704 PASS / 0 FAIL**（v2.10.10 的 2648 + 本次净增 56 条）；真实浏览器冒烟 **39 → 69 项断言**（新增的 15 条布局断言 × `file://` 与 `http://` 双通道）；看门狗 **58 PASS / 0 FAIL**；行覆盖率 **99.2%**。

**反向验证（实测，对着紧邻的 v2.10.10）**：把 v2.10.10 重建出来跑同一份套件 → **2677 PASS / 27 FAIL**（2677 + 27 = 2704，账目自洽）。27 条失败**全部属于 T90a–T90d**（结构落位与 CSS 契约）——即"本次搬了家"这件事确实被断言抓住了；其中几条的失败信息本身就把旧状态说得很清楚：`拍号 6 档无一丢失（期望 6，实际 0）`（当时 `.viz-rows-row` 还不存在）、`只剩 1 条 .vdivider（期望 1，实际 4）`、`本卡恰好 2 个 group（期望 2，实际 5）`。
★ **T90e（行为护栏）在搬家前后都全过**——这是"只动了位置、没动行为"的直接证据：id、初始态、`hidden` 初值、以及"点档位仍生效"三组接线在 v2.10.10 与当前版本上都是绿的。

★ **视觉仍需人眼验收**：冒烟能量出"五处左边缘与标题同一条线"（0.5px 容差）、"拍号在行数右侧"、"音色 → 音量 → 编辑节奏型"，但**量不出"好不好看"**——尤其：390px 下当前节奏型行折成 4–5 行是否显得太高、拍号折到第二行后与「同屏行数」的疏密是否合适、`gap:28px` 是否偏大。请实机过一眼。

## v2.10.10 · 顶栏只留一颗状态点，并把它并进左边的版本徽章（2026-09-23）

**来源**：用户在 v2.10.9 之后追加要求 —— 「能否只保留绿点表示稳定版？可以的话，我还想将这个绿点移到左边的徽章里」。即两件事：① 去掉 chip 里那行文字（`稳定版` / `保存失败`），**只留颜色**；② 把这颗点从顶栏最右**移进左边那个版本徽章**。

**它不是 v2.10.9 的重复**：v2.10.9 只拿掉了 chip 里的**版本号**（解决"重复显示"），chip 连同它的文字仍占着顶栏最右一格；本次把那行文字也去掉、点搬进徽章——**chip 整块消失**。

### 变更

1. **标记（`index.html`）**：`<span class="ver" id="brandVer">` → `<span class="ver"><span class="dot" id="persistDot" aria-hidden="true" title="本地保存正常"></span><span id="brandVer"></span></span>`，读作「● v2.10.10」；顶栏最右那个 `.chip` 整块删除。
   ★ **`#brandVer` 不能当容器**：装配层用 `textContent` 写版本号，点若是它的子节点会被连同**一起清掉**——故点与版本号是**兄弟**，`.ver` 退成容器（`inline-flex` + `gap:5px`），类名从 `#brandVer` 移到外层。取 `#brandVer` 文本的既有断言（t24 / 冒烟）因此逐位不变。
2. **无障碍——本次真正的设计约束**：原先"颜色 + 文字"两个通道，现在只剩颜色 = 对读屏用户**零信息**、对分不出红绿的用户也近乎为零。故三件事一起做：
   - 点加 `aria-hidden="true"`（纯装饰，不冒充信息）；
   - 状态文案**保留但退成 `sr-only`**（`#brandChip`：视觉不可见、读屏照读，位置紧随徽章）——**DOM 与 JS 三个赋值点一字未改**；
   - 点加 **`title`**，并在两个持久化钩子里跟着状态改（`本地保存正常` / `本地保存失败`）——给鼠标用户一条文字线索，也补上"颜色不是唯一线索"。
3. **注释同步 6 处**：`VERSION` 的显示处说明、徽章/chip 标记处、保存失败与恢复两个钩子、`Store` 里两处提"顶栏 chip"的地方。另在顶栏原 chip 位置留一条"**别再往顶栏加回第二个状态/版本类元素**"——那正是 v2.10.9 修掉的重复。

### 保留 / 未做

- **`.chip` 这个 CSS 类保留**：预设计数 `<div class="chip" id="presetCount">` 还在用它，删样式会连带打坏那一处。
- **「使用方法」页未改**：核对过，它本来就没描述那颗点（原先靠 chip 的文字自解释）。
- **不引入第二个可见元素**：用户明确要"只保留绿点"，所以没有把状态做成"点 + 小字"或色盲友好的形状差异；补的是一个 `title`（悬停可见、平时不占位）。

### 测试

- **`t25` 新增 5 条"标记结构"断言**：点已进徽章且排在版本号**之前** / 点 `aria-hidden` / 点带 `title` / 顶栏不再有 `.chip` / 状态文案退成 `sr-only`。
  ★ 走**标记字符串**而不是 DOM 父子：**桩不解析 HTML**，静态元素都是孤立桩（`parentNode` 恒 `null`），走路树断言不出来（已实测确认）。
- **`tools/smoke.js` 新增 2 条"真布局"断言**：点的矩形落在徽章矩形内 / 点真的被画出来（尺寸 > 0、底色非透明）。这正是桩测不到的那一类——"移进徽章"有可能只是标记上写着而已。
- **既有断言一行未改**：`#persistDot` / `#brandVer` / `#brandChip` 三个 id 与它们的文本契约全部保留，故 t24/t37/t50 那 4 条"点变红"与 5 条 chip 文案断言照旧通过。★ 这也是选"文案退成 `sr-only`"而不是"把状态挪到点的 `aria-label`"的原因：后者要连带改 5+ 条断言与冒烟探针，收益完全相同。

### 自验

`node tools/check-all.js --strict-env` → **全部通过 · 实跑 14/14 项**（含 ESLint / tsc / 真实浏览器冒烟）。自动化测试 **2648 PASS / 0 FAIL**（v2.10.9 的 2643 + 本次净增 5 条）；真实浏览器冒烟 **39 项断言**（原 35 + 新增 2 条 × `file://` 与 `http://` 双通道，两条通道都实测到"点在徽章内且被画出"）；看门狗 **58 PASS / 0 FAIL**。

★ **视觉未经人眼验收**：这次动的是**真实布局**（徽章从纯文本容器变成 `inline-flex` 容器、塞进一颗 6px 点），冒烟只能证明"点在徽章矩形内且有尺寸/底色"，**证明不了好看**——徽章内边距在 11px 字号下是否显得点偏大/偏挤、有没有把品牌行撑高，都需要你实机看一眼。这是本轮最需要人眼的改动。

## v2.10.9 · 顶栏不再重复显示版本号：chip 改为只报保存状态（2026-09-23）

> ★ 后续：**v2.10.10 把 chip 整块取消了**（点移进品牌区徽章、文案退成 `sr-only`）。
> 本条记录的是当时落地的中间形态 —— 两版都未发布，最终形态以 v2.10.10 为准。

**来源**：用户实报 —— 「顶部文案：`BeatSight 时值节拍器 v2.10.8 主题 · 经典 练习统计 使用方法 后台保活 v2.10.8 · 稳定版` **重复显示版本号**。我倾向于保留左边的版本号。」

### 机理

顶栏其实有**两处**版本号，都在 v1.3.0 那次"版本号单一真相源"里被保留下来（当时解决了"硬编码三处靠人工同步"的漂移，但**显示两遍**这件事一直没被当成问题）：

| 位置 | 内容 | 由来 |
|---|---|---|
| 品牌区徽章 `.ver`（**左**，紧跟「BeatSight 时值节拍器」） | `v2.10.8` | v1.3.0 起由 `VERSION` 派生 |
| 最右保存状态 chip `#brandChip` | `v2.10.8 · 稳定版` | 同上；同时兼报保存失败 |

而两处之间还夹着四个顶栏控件（主题 · 经典 / 练习统计 / 使用方法 / 后台保活），于是整条顶栏从上到下读出来就是用户贴的那一串——版本号在**首尾各出现一次**。用户选择保留**左侧**那一处。

### 变更

1. **chip 去掉版本号前缀**（`index.html`）：`"v" + VERSION + " · 稳定版"` → `"稳定版"`，失败态 `"… · 保存失败"` → `"保存失败"`（三处赋值：装配段 + 保存失败钩子 + 保存恢复钩子）。chip 的**职责收窄为「只报保存状态」**——这正是它本来在做的事（红点 + 文案），版本号是搭车的。
2. **`VERSION` 的注释同步**（`index.html`「数据」区 + 装配段）：显示处由「`<title>` / `.ver` / `.chip` 三处」改写为「`<title>` 与品牌区徽章**两处**」，并写明 v2.10.9 的这次收敛及其理由。chip 标记处补一条注释说明"文案里不得再写版本号"。
3. **文档同步**：`README.md` 发布前清单、`docs/DEVELOPMENT.md` §5 发版规则两处「三处显示」一并改为两处。
4. **真浏览器冒烟改盯徽章**（`tools/smoke.js`）：原先那条断言盯的是 `#brandChip === "v" + VERSION + " · 稳定版"`——正是本轮要去掉的东西。改为盯 `#brandVer === "v" + VERSION`（版本号该在的地方），**并新增一条"chip 里不得出现版本号"**：只改前者的话，将来谁把版本号加回 chip，两处又会一起显示而没人拦。

### 测试

- `t24` 的四条 chip 断言（失败 / 恢复 / 再次失败三态 + 版本号派生）随文案改写；
- `t25`（版本号单一真相源）把"chip 由 VERSION 派生"换成两组断言：**`chip` 里不含版本号**（钉住"不重复"）+ `chip === "稳定版"`（钉住职责收窄）。★ 这条是必需的——原来那两条"版本号由 VERSION 派生"的断言在"两处都显示"时**同样全绿**，拦不住重复；
- `t50` 的一条 chip 失败态断言同步；`t51` 用的是 `includes("保存失败")`，无需改；
- 真浏览器冒烟的两条断言见上（`--strict-env` 里会真跑）。

### 自验

`node tools/check-all.js --strict-env` → **全部通过 · 实跑 14/14 项**（含 ESLint / tsc / 真实浏览器冒烟）。自动化测试 **2643 PASS / 0 FAIL**（v2.10.8 的 2642 + 本次净增 1 条，即 T25 那条"不重复"）；真实浏览器冒烟 **35 项断言**（原 33 + 新增的「徽章有版本号 / chip 无版本号」两条 × `file://` 与 `http://` 双通道）；看门狗 **58 PASS / 0 FAIL**。

**反向验证（实测）**：把 v2.10.8 那份重建出来（拿上一版的补丁 `git apply` 回原始快照）跑同一份套件 → **2637 PASS / 6 FAIL**；当前版本 → **2643 PASS / 0 FAIL**（2637 + 6 = 2643，账目自洽）。6 条失败**全部**是盯 chip 文案的断言，其中决定性的一条正是 `★★ 顶栏 chip 不重复版本号（期望 -1，实际 0）`——用户报的"重复显示"确实被这条断言抓住了；其余 5 条是 chip 文案的前后缀（保存失败 / 稳定版三态）。这正是为什么必须**新加**那条断言：原来那两条"版本号由 VERSION 派生"在 v2.10.8 上**照样全绿**。

★ **视觉未经人眼验收**：chip 变短后顶栏右侧的排布会再松一点（`.chip` 的 `font-size` / padding 未动，只少 9 个字符），**在 390px 窄屏上是否会因此更宽松或反而错位，需你实机看一眼**。

## v2.10.8 · 修「范围短于窗口时小球与预告格多走一行」：下一行改由节目单算（2026-09-23）

**来源**：用户实报 —— 「滑块只选 **1 小节**、但同屏行数大于 1 时，小球在当前小节快结束时把预备动画画到了**下一小节的位置**（第 2 行）；而按播放范围，下一小节其实是**当前小节自己**（循环回卷），所以它该留在第 1 行」。并指出选 2 小节而行数 > 2、选 3 小节而行数 > 3 同理。

### 机理（触发判据：范围小节数 L < 同屏行数 W）

`W = S.vizRows ∈ {1,2,3,4}`、`L = to − from + 1`。**只有 W > L 才出错**：L=1 时 W∈{2,3,4}；L=2 时 W∈{3,4}；L=3 时 W=4；**L=4 永远出不了错**（W 最大 4）——这正好解释了用户为什么只提到 1/2/3。W ≤ L 时范围末小节恰落在窗口最后一行，`bar === vizBars - 1` 走"有预告行就让给第 1 行"那条既有分支，绕回范围起点，看着是对的。

根因是**「位置」与「内容」两个下标被分开算，其中一个没跟着循环回卷走**：排程侧 `arrNextBar` 在 v2.10.7 已改为在线性小节号上步进、**正确实现了回卷**（越过范围末小节 → 回范围起点），于是待命球的**内容下标** `cand`（取音符用）是对的；但**行下标** `rowNext` 是**位置式**推的（`min(W-1, bar+1)`，页末有预告则 0），`.next` 预告格走 `nextRowOf` → `loopNextBar` → `(bar+1)%W`，同样与回卷解耦。范围短于窗口时"下一小节"是范围起点、可能落在当前行或更前面，位置式 +1 就把它摆到了**范围外那一行**上——球用"正确小节的音符"画在了"错误的行"。

### 变更

1. **`arrangeNextRow` 增加 `lin` 字段**（`index.html`，共享状态区）：返回下一小节的**线性歌曲小节号**。原来的 `bar` 字段是**型内**小节号（用来取那一行的音符），两者自 v2.10.2 起不再相等，混用会算出不存在的小节号。
2. **Viz 新增「小节号 ↔ 行号」映射**：`rowOfSongBar(lin, a)` 求"这个小节**当前**画在窗口第几行"（不在本窗口返回 −1），`arrNextRowOf(bar)` = 由节目单求出下一小节的行号。两处**必须按 `songBars` 取模**：`arrWindowPat` 在**曲尾 / 全曲短于窗口**时会绕回重复铺行（`songBarAt(a, (winStart + i) % total)`），"行号 = 小节号 − 窗口起点"这句在那些页面并不成立（实测最后一页会算出负数行号）。
3. **`nextRowOf` 曲式分支改走节目单**（`.next` 预告格）：算得出就用 `arrNextRowOf`，算不出回落 `(bar+1)%W`。v2.10.7 及更早一律按位置式，于是范围短于窗口时预告格会提前指到范围外那一行。
4. **待命球的 `rowNext` 同口径**（`paintBall`）：复用本帧已经算好的 `nrow.lin`（不额外调节目单），行号经 `rowOfSongBar` 求得；算不出时回落原来那条表达式。取 `-1` 的两档（向前翻页 / 向后跨页）与"范围播完不循环"因此**逐位不变**——`byPos` 就是 v2.7.0 起那套口径，原样保留成回落分支。

### 实现取舍

- **不引入"行号 = 线性号 % 行数"那种写法**（第一版就是这样，已推翻）：它在**曲尾绕回**时是错的（最后一页的窗口内容按 `% total` 铺，行号并非 `lin % W`），实测 `整首 + 4 行档` 会被算成第 1 行而不是第 4 行。必须按 `songBars` 取模。
- **向后退页时不强行落行**：范围跨页回卷（回卷起点与当前小节不在同一页）需要翻**回退**一页，而预告行那套机制只服务向前翻页——把球画到目标页的那一行反而会压在"当前页另一个小节"的内容上，比位置式更难看。故这一档回落原口径，**记为已知边界**：`T89f` 把它钉住，将来若要一并修掉，断红会逼人先回答"向后翻页该怎么呈现"这个设计问题。
- **对照不被牵连**：`W ≤ L`、整首连播、向前翻页（预告行）三条既有行为都逐位不变；`t53g`（待命球跨块取型）的 `arrangeNextRow` 契约只增字段，断言未动。

### 测试

- **新增 `tests/cases/t89-narrow-range-next-row.js`**（5 场景 / 44 条断言）：W > L 全组合的落点表（含"每行都观察到过"的防空断言守卫）/ `.next` 预告格同口径（并与待命球逐位一致）/ W ≤ L 与整首的对照 / **用户实报路径端到端**（拖侧栏滑块选 1 小节）/ `loop=OFF` 不许出现假回卷 / 跨页回卷的已知边界。已登记进 `tests/run.js` 的 `CASE_FILES`。
- **反向验证（实测）**：对修复前的 index.html 跑同一份套件 → **2630 PASS / 12 FAIL**；修复后 → **2642 PASS / 0 FAIL**（2630 + 12 = 2642，账目自洽）。12 条失败全部是 T89 里盯本 bug 的断言，报出的正是用户症状（如 `第 1 行（该行下一小节 = 第 1 小节）→ 球画在第 2 行`），**其余用例一条未红**；`W ≤ L` 与整首的对照组在修复前后都通过，即"没回归"也是验过的。
- **看门狗新增 `narrow_range_short_song`**：全曲只有 1 小节（窗口 4 行全是同一小节，`% total` 模数退化为 1）+ 范围正好是那 1 小节（回卷起点 = 当前小节）——覆盖新增映射的两处易错点（绕回铺行的取模、行号必须钳在窗口内）。实测不挂。

### 自验

`node tools/check-all.js --strict-env` → **全部通过 · 实跑 14/14 项**（语法 / 架构约束 / 装配完整性 / 零依赖 lint / 版本一致性 / 文档一致性 / `_headers` / **ESLint 加强 0 错误** / **tsc 加强 0 错误** / DOM 引用完整性 / **真实浏览器冒烟 ✓（33 项断言）** / FULL_SCAN 全量 / 死循环看门狗 / 行覆盖率）——**14/14 全绿**。

自动化测试 **2642 PASS / 0 FAIL**（v2.10.7 的 2598 + 本轮新增 44 条）；看门狗 **58 PASS / 0 FAIL**（+`narrow_range_short_song` 的 5 条）；行覆盖率 **99.2%**。

★ 关于"两道加强项这次是真跑的"：本机沙箱里 `npm ci` 会长时间卡住（见 v2.10.5 的记载），所以本次是**借用同一份依赖树**（`eslint@10.10.0` / `typescript@7.0.2`，与 `package.json` 声明的版本逐位一致）拿到 `node_modules` 后才跑的 `--strict-env`，**不是**把 ⊘ 当通过。若只按"缺依赖"跑，汇总会是「实跑 12/14 项」——那个数说明 ESLint / tsc **没查**，而本次新增了 JSDoc 标注与 `nrow.lin` 这类类型敏感的写法，正是这两步该管的地方。

★ **视觉与手感未经人眼验收**：本轮改的是"球与预告格画在哪一行"，自动化只在**桩的行几何**上断言行号（行距 86px、`bounce.max` 48px 的区间不重叠，故行号唯一可判），**真实布局下的观感仍需你实机过一眼**——尤其"球回到当前行"这种同格交接，桩看不出会不会显得迟疑。

## v2.10.7 · 播放范围滑块改按小节调节（可从段中间练到段中间）

**来源**：用户提出——「循环设置中的滑块只能以'段'为单位调节循环范围，希望改为以'小节'为单位」。确认过的三个决策：重合 = 单小节循环；旧持久化数据自动换算；轨道标出段边界刻度线。

### 机理（为什么"按段"不够用）

曲式的段长是**可变的**（每段 = 若干块 × 重复遍数，Σ blocks.repeats × 型长），"段"这个单位的实际时长参差；且曲式模式下此前**没有任何小节粒度的循环手段**（单型模式的循环面板在曲式模式下收起）。而 `arrNextBar` 的范围锚定全在段粒度上：起点恒为 from 段第 0 小节、终点在 to 段段尾生效、回卷也回段首。v2.5.2 已备好线性小节号纯函数族（`songBars / songBarBefore / songBarAt`），本次把范围语义搬上去。

### 变更

1. **范围语义：段下标 → 线性小节号（`index.html`）**：`S.arrangeSel.from/to` 改为 0-based 含端点的**线性小节号**，范围可落在段中间（如「从副歌第 3 小节练到桥段第 2 小节」）。存储格式加版本标记 `arrangeSel.v: 2`。
2. **`arrNextBar` 改为在线性号上步进**：下一小节 = `songBarBefore(段)+bar+1`，`songBarAt` 反解回（段, 段内小节）——"段走完换段"成为线性推进的自然结果，不再单独写。三类归位：未开始 → 落到 fromBar（**可落在段中间**，新能力）；当前位置在范围外 → 拉回（v2.0.2 的"即时生效"语义原样保留，与 loop 无关）；推进越过 toBar → 循环回卷 / 不循环停。scheduler 与弹跳球预测共用这一份步进逻辑的架构不变。
3. **旧数据迁移（一次性）**：加载期见不到 `v:2` 的旧值（段下标）暂存进 `Store.takeArrangeMig()`，装配层在 `patLenOf` 注入后经新纯函数 `migrateSecRangeToBars` 换算成该段的起止小节写回并落盘——下次加载走 v:2 快路径。换算不能在加载期做：`secBars` 依赖型长解析，那时还没注入。
4. **滑块改小节刻度**：min=1 / max=`songBars`（整曲总小节数）；读数改为「第 X–Y 小节 · 起点段名 → 终点段名 · 共 N 小节」；aria-valuetext 「第 X 小节 · 段名」由 `songBarAt` 反解；**两个 thumb 重合 = 只循环这一个小节**（v2.10.6 及更早按段调节时重合 = 单段）。想循环一整段就拖出该段起止小节。
5. **段边界刻度线**：轨道上每段起点的小节位置画一条小竖线（`.demo-range-tick`，aria-hidden 纯装饰，z 序在 thumb 之下）——"第 X 小节落在哪一段"一眼可见。刻度线追加在 toEl 之后，测试桩按 children[1]/[2] 定位 thumb 的契约不变。
6. **段粒度入口保持交互不变，写入时换算**：整首连播（0..曲末小节）、跳段「◀ 上一段 / 下一段 ▶」（该段起止小节；停止时基准 = fromBar 反解的所在段）、编排面板「起 / 终 / 全部」、歌词行循环（身份仍是"段"，写入段起止小节 + byLyric 不变）、面板「播放」。编排面板只读范围显示改以小节为主单位（「第 X–Y 小节 · 第 A → B 段」）；「范围循环」开关的"解除锁定"判据从 from===to（旧段语义的单段）改为**范围落在同一段内**。窗口锚定（网格 / 歌词轨的停止态）改直接用 fromBar，不再经 songBarBefore 换算。
7. **帮助文案**同步（滑块按小节、重合 = 单小节循环、刻度线说明）。

### 测试

- T51f 的"按段钳制"用例整体转型为**旧格式迁移用例**（旧值仍有效、换算成小节区间），并补 v:2 快路径断言；
- 示例曲每段恰为 1 小节（v2.6.0 逐小节谱）→ 段下标 == 小节下标，t63/t68/t70/t78/t88 的数值断言不变、文案断言（「第 X 段」→「第 X 小节」）随之更新；
- 新增：段中间起点/终点步进、单小节循环回卷、滑块拖出段内区间、迁移换算（含曲式不存在时的占位回退）。

## v2.10.6 · 修「六线底纹开关点了没反应」：开关本体跟随型显隐

**来源**：用户实报——「四分基础」节拍型下，`六线 · 扫弦谱底纹` 开关点击无反应，并质疑该开关的存在必要性。

### 机理（不是 bug，是合取语义缺了一半的交涉）

底纹显隐 = `S.showTab && vizHasStrum()` 的**合取**（v2.9.0 起底纹只铺给带扫弦记谱的型——无箭头的型铺横线纯属噪音）。节拍型（如四分基础）`vizHasStrum()` 恒为假，开关无论怎么点第二个条件都假，底纹纹丝不动——**体感就是「点了没反应」**。开关在该语境下对画面**永远**没有作用，留着只会制造困惑。

### 变更

1. **开关本体跟随型显隐（`index.html`，`syncTabLayer`）**：同一处收尾顺带 `tabToggle.hidden = !hasStr`——节拍型藏开关，扫弦型（内置「民谣扫弦」、自定义带 dir/zone 的型）现身。与 `no-tab` 同源同调用点（`buildViz` 末尾），不会出现「底纹回来了开关还在藏」的半收敛态。**偏好 `S.showTab` 与热键持久化原样保留**：扫弦型下的开/关语义一字未改。
2. **CSS 补丁 `.toggle-pill[hidden]{display:none}`**：`.toggle-pill` 是 `display:flex`，特异性高于 UA 的 `[hidden]` 规则，不补这条 hidden 形同虚设——与 `.vol-row` / `.arg-now` 同一族坑（第十一个成员），注释已写明出处。

### 测试

- T86a/b/d 增补开关本体显隐断言（扫弦型可见 / 关偏好仍可见 / 节拍型隐藏）；
- 新增 **T86f**：开关跟随型双向显隐（四分基础 → 藏；民谣扫弦 → 现身且底纹恢复；pill 开合视觉仍恒等于偏好）；
- T86d 的型下标由 `idx:1` 订正为 `idx:0`（四分基础，即用户实报的型；两者均为节拍型，断言有效性不变）。

## v2.10.5 · 工程版：可维护性三件（拆 buildBarRow / 消同名 render / Viz 分区）+ 冒烟新增 DOM 规模预算

**来源**：用户提出「9584 行的单文件 `index.html` 是否体积过大、对性能有何影响」。经实测回答（真实 Chrome + CDP 多档 CPU 降速、把脚本停掉的对照实验、5 种拆分形态的 `file://` 可行性验证、DOM 节点分布剖析、启动期 Trace），结论是**性能不是瓶颈**（FCP 160–176 ms、播放态 60 fps 恒定、JS 堆约 2 MB），**超区间的是可维护性**（单文件 9584 行、最长函数 197 行）。据此把可低风险落地的项做成一版：可维护性三件 + 一项防回归预算。**刻意不做**拆文件与压缩（见「为什么不拆」）。

### 变更

1. **`buildBarRow`（197 行）按职责拆成三段子工序（`index.html`，纯搬移）**：`buildRowLayers`（定位层：跑道拍点 / 六线底纹 / 扫弦箭头层 / 拍内座次尺）、`buildRowCells`（音符格 + 刻度线 + 时值标签 + 扫弦竖箭头，返回四个逐格同构数组）、`buildRowAnnotations`（短音符组标签 + 小节和弦胶囊）。本函数退化为编排者。拆分动机是**可读性与可测性，不是性能**——它不在每帧热路径上（只在换型 / 翻页时跑）。三段之间的调用顺序即语义（第 2 段的箭头要挂进第 1 段建好的 `.strums` 层），搬移时一位未换；`vizHasStrum()` 只算一次传下去，保证"层建不建"与"格画不画"两处判据逐字同源。
2. **消除同名 `render`（`index.html`）**：全文件原有 **5 个**同名 `function render()`（Editor / Stats / Ear / Arrange / Help），分别改名 `editorRender` / `statsRender` / `earRender` / `arrangeRender` / `helpRender`，共替换 60 处（按模块行范围逐段替换，`\b` 边界保证 `renderList` 等不受影响）。改名动机：单文件里检索 `render(` 会命中 5 个不同模块的实现，对人对 AI 都是歧义源。**两处注记随之订正**：Help 静态标记里指向隔离区按钮显隐的那条（原写裸 `render()`，实指 `helpRender`）；`Modal.bindOverlay` 头部那条本就是**泛指**各 overlay 渲染函数，改为泛指写法。另有 4 处测试注释与 3 处 `docs/DEVELOPMENT.md` 里**指向具体函数**的提法同步改名；陈述"渲染"这一行为的泛指表述（如「每次 render 重建」）保持原样。
3. **冒烟新增第 5 项性能预算：DOM 规模（`tools/smoke.js`，带反向验证）**：探针取 `document.body.getElementsByTagName("*").length`（口径刻意对齐 Lighthouse「避免 DOM 过大」审计，实测 967），预算 **1200**。定档逻辑与前四项不同、已在代码注释写明：前四项取"实测值 × 安全倍数"，这一项取**已知外部警戒线之间**的位置——Lighthouse 的 body 节点 > 约 800 警告、> 约 1400 错误，实测已越过警告线，所以预算不能取 800（否则常年飘红），取 1200 拦的是"把 DOM 推向错误线"这个真正伤交互性的量级。**为什么必须单列**：其余四项测不出它——DOM 规模是这类应用最容易无声增长的东西，既不体现在帧率（余量巨大）也不体现在首屏（解析很快），可以一路涨过 1400 而整条自验链一声不吭。**反向验证**：向 `</body>` 前注入 300 个空 `div`（967 → 1267）后两条通道断言齐红；还原后 `cmp` 逐字节复核一致。README 发布前清单的冒烟覆盖清单同步补上「DOM 规模」。
4. **`Viz` 模块内加 6 个分区标注（`index.html`，仅注释）**：① 几何缓存与渲染状态 / ② 窗口解析（型 ↔ 行）/ ③ 可听位置与行语义 / ④ 建网格（结构层）/ ⑤ 弹跳球与歌词轨 / ⑥ 逐帧渲染（热路径）。每个分区写清"装什么、不装什么"，并点明 R2 约束只落在 ⑥ 的 `paintFrame` / `paintFrameBody` / `paintBall` 三个函数上。动机：`Viz` 已 1506 行、占全文 15.7%，而 R4 扇出上限只防跨模块膨胀、防不了单模块内膨胀。文件头模块索引行号已由 `node tools/gen-index.js --write` 重算。

### 诊断结论（P1-1，只读，无代码改动）：启动期布局成本的真实形状

实测纠正了"4 次均匀重排"的印象——Trace 显示启动期 6 次 Layout 里，**一次 64.2 ms 的初始布局占全部布局耗时的约 88%**，其余 5 次合计约 1.0 ms（脏对象 2–511 个）。160 次强制同步布局读取全部挤在 126–132 ms 的 6 ms 窗口内，只来自两个调用点：`fitCellAnnotations`（约 128 次，`offsetWidth`/`scrollWidth`）与 `cacheGeo`（32 次，`offsetLeft`/`offsetTop`/`offsetWidth`/`offsetHeight`）——两处都已是"先量测、后写入"的批处理形态，**不存在可合并的读写交错**。因此：启动布局贵在"对首次出现的约 780 个对象做一次整体布局"，这是构建全部 DOM 的固有代价；要降它只能"启动时建得更少"，而 DOM 分布剖析显示屏外节点仅 63 个（桌面），`content-visibility` 这类手段对此无从发挥。**结论：无可落地的布局优化，记为结论不再立项。**（注意：64.2 ms 来自 headless + `--disable-gpu` 的软件渲染，真实 GPU 合成路径下会更低。）

### 为什么不拆文件（本轮实测证据，防止重新立项）

把 `<script>` 改成 `type="text/plain"`（字节数不变、脚本不执行）的对照实验显示：完整应用相对对照组的 DCL 增量只有约 107–121 ms，其中 **JS 执行仅 13–20 ms**，布局（54→77 ms）与样式重算才是大头——**拆文件减不了瓶颈，因为瓶颈不在体积**。同时实测 `file://` 下 `<script type="module" src>` 与动态 `import()` 均被 CORS 拦截（`origin 'null'`），**只有经典 `<script src>` 可用**；而拆分要求重写 9 个解析内联脚本的仓库工具并重建行号映射体系。收益≈0、风险极高，不做。（工具链耦合度实测：14 个工具直接读 `index.html`，其中 9 个提取内联 `<script>` 块。）

### 自验

`node tools/check-all.js` → **实跑 12/14 项**（语法 / 架构约束 / 装配完整性 / 零依赖 lint / 版本一致性 / 文档一致性 / `_headers` / **ESLint 加强 0 错误** / DOM 引用完整性 / **真实浏览器冒烟 ✓** / FULL_SCAN 全量 / 行覆盖率；**tsc 与死循环看门狗在本机沙箱标 ⚠ 工具故障**——子进程 spawn 受限，不是被检项失败，故按仓库惯例用手工通道补跑：**tsc 退出码 0、0 报错**（`tools/tsconfig.typecheck.json` 全部 strict 开关下），**看门狗 19/19 逐个直跑全过**（此路径丢失超时保护这一层，如实说明）。自动化测试 **2583 PASS / 0 FAIL**；冒烟 **33 项断言**通过（31 + 本次新增 2）；行覆盖率 **99.2%**（总阈值 97% / 分区 90%）。索引行号由 `node tools/gen-index.js --write` 重算。

★ **视觉与手感未经人眼验收**：本轮全部是纯搬移 / 改名 / 注释与自验工具改动，网格结构与样式未动（测试逐位不变佐证）；但仍按仓库惯例提示实机过一眼主界面与编辑器。

## v2.10.4 · 侧栏「播放范围」双滑块（取代段号胶囊）+ 删除「切换节奏型」胶囊条（2026-09-22）

**来源**：用户提「节奏型预设库的『自定义』里的按钮太多了」并附侧栏截图，要求精简。经三轮收敛：我先给了三个"减少按钮数"的方案（折叠 / 去重 / 复合控件），**用户纠正了方向**——"改成滑块，用户自由拉动滑块选取想要的范围"。最终拍板三点：①双滑块；②只换侧栏；③「切换节奏型」那 5 颗不要（理由：候选与「扫弦」区重复）。

**为什么这是一次能力升级，而不是"把胶囊换个样子摆"**：
`Arrange.jumpTo` 里写着 `S.arrangeSel.from = k; S.arrangeSel.to = k;` —— 两个字段被**写死成同一个值**，所以 10 颗段号胶囊只能表达 **10 种状态**（"只循环某一段"）；而 `S.arrangeSel = {id, from, to, loop, byLyric}` 本来就支持任意区间（10 段 = **55 种**）。滑块用 **1 个控件**把这 55 种全部解锁，且**不随段数增长**（`arrMaxSections` 上限 24 段时同样只占一行）。"跳到单段"没有丢——两个 thumb 拖到重合即 `from === to`。

### 变更

1. **段序条（N 颗段号胶囊）→「播放范围」双滑块**：`.demo-sec-row` 换成 `.demo-range`（读数行 + 轨道；轨道内 = 填充条 + 起点滑块 + 终点滑块）。读数把"第 3–7 段"翻译成段名与总小节数（`第 3–7 段 · 副歌 → 桥段 · 16 小节`），单段时改说 `第 5 段 · 主歌一 · 4 小节（只循环这一段）`——轨道上放不下段名，这行与 `aria-valuetext` 一起承担翻译。
2. **删除「切换节奏型」胶囊条**（v2.4.1 加、v2.8.26 修的那个）：`buildDemoSegRow` / `applyDemoSeg` / `shortDemoName` 及 `.demo-seg-row` / `.demo-seg-note` 样式一并移除。删除理由有两条，第二条是决定性的——它与新滑块**语义相冲**：`applyDemoSeg` 收尾会把 `S.arrangeSel` 收窄成 `from === to`，拖好的范围会被一次换型清掉。出口仍在：进「编排曲式」→ 该段行 → 换型（那里本来就有内联候选行）。
3. **新增 `Arrange.setRange(from, to)` + 共享区钩子 `onDemoRange`**：滑块经装配层注入调用（`Presets` 声明在 `Arrange` 之前，直接调是反向引用，要登记 R3 白名单）。它**取代了 `onDemoJump`** ——旧 `jumpDemoSection` 随之删除；`Arrange.jumpTo` 保留，现在只服务主界面跳段行的「◀ 上一段 / 下一段 ▶」。

### 实现取舍（都写进了代码注释，此处只留结论）

- **用两个原生 `<input type=range>` 叠层，而不是自绘 pointer events**，三条理由：①原生滑块自带键盘与无障碍语义，自绘要手工补一整套 `role=slider` + `aria-valuenow/valuetext` + 方向键，而**双 thumb 的读屏区分**最麻烦；②**直接复用既有的 `input[type=range]` 样式**（观测台主题、`focus-visible` 环、移动端 22px thumb 全部自动生效），与音量 / BPM 三条滑杆视觉同源；③**测试桩没有 `getBoundingClientRect`**，自绘拖拽在桩里根本驱动不了，而原生滑块在桩里就是 `.value` + `fire("input")`。
- **`input` / `change` 分两级**（这是"拖动途中不做重活"的落点）：`input` 每秒触发几十次，只改 S、读数与填充条、落盘走既有 250ms 热键防抖；`change`（松手/键盘提交）才调 `onDemoRange`，那里会跑 `refreshBar` + 停播时重建网格这些重活。合成一级的话，一次拖动就会跑几十轮 `applyPatternChange` —— 正是 `CONFIG.schedWindow` 注释里"重活拖长主线程会让时间轴被重锚定、静默吞掉音符"那类问题。
- **两个 thumb 的叠层顺序按值动态翻转**：`from === to` 时（最常用的一档）上层 input 会把下层 thumb 整个挡住，故按"哪边还有移动余地"决定谁在上——重合在**最左**时让终点在上（还能往右扩），其余情况让起点在上（还能往左扩）。不做这一步，重合态总有一侧永远拖不动。
- **`from` / `to` 双向钳制**：起点越过终点时把终点一起顶走，反之亦然 —— 于是"两个 thumb 拖到重合"自然表达"只循环这一段"，不必再给一个"单段"开关；也保证任何时刻 `from <= to`（`Store.clampLoop` 注释里写明的"空区间会让调度器永远到不了 to"那条死循环形态）。
- **CSS 选择器必须带 `.demo-range-input`**：全局 `input[type=range]` 与观测台主题的 `body[data-theme="obs"] input[type=range]`（特异性 0,2,2）都会命中这两个 input，不加这一级会把 `height` 压回 4px、`background` 被主题色覆盖。
- **段名上不了轨道**：靠读数行与两个滑块的 `aria-valuetext`（`第 3 段 · 副歌`）——`aria-valuenow` 只能报数字。
- **位置读数没有丢**：段序条的逐段高亮没有了（滑块表达**范围**，不表达位置），"现在播到第几段"改由「整首连播」那一行的状态说明承担（`播放中 · 第 3/10 段`，走 `demoCurSec` 的可听位置）。滑块本身在播放期间**纹丝不动**（T88h 钉住）。

### 踩到的坑（本次唯一的真缺陷）

`setRange` 取曲式原本沿用 `jumpTo` 的 `arrangeCur() || cur()`，但**从侧栏拖滑块时两个都可能是 null**：`arrangeCur()` 在 `playMode !== "arrange"` 时恒为 null（而拖滑块最常在还没进曲式模式时发生），`cur()` 又依赖编排 overlay 开过（它读编辑选中项）。两个都 null → **静默早退** → 模式没切、`loop` 没开、主界面没反馈、网格没换型，而滑块看起来"能拖、值也变了"。代价是 T68h 四条断言全红。修法：补两级兜底 `arrangeCur() || cur() || Store.findArrange(S.arrangeSel.id) || Store.arranges[0]`（`onRangeInput` 已先把渲染中那条曲式的 id 写进 `S.arrangeSel.id`，所以第三级一定指得准）。

### 回归

- **新增 `tests/cases/t88-demo-range-slider.js`**（11 个场景 / **71 条断言**）：控件形状与 1-based 取值域 / 拖出任意区间（★ 断言 `from !== to`——旧实现永远拿不到）/ 双向钳制（含非数字脏值回退）/ **input 与 change 两级契约**（只发 input 不得切模式）/ 重合 = 单段（与旧段号胶囊逐位等价，含停播换型）/ 读数与 `aria-valuetext` 的段名翻译 / 填充条几何（`style.left`·`width` 百分比）+ **重合时 z-index 翻转** / **播放期间滑块纹丝不动** / 越界范围按段数收窄 / 与「整首连播」高亮同源 / 曲式被删后的悬空引用守卫。
- **改写既有断言**（换控件不该改数据语义，故大部分断言口径**逐位保留**）：`t63` 段序条那 8 条改为滑块的形状与取值域断言；`t68` 的 `secRowOf` 换成 `rangeOf` 一族，**T68g / T68h / T68j 保留原判据只换驱动方式**（T68h 从"点第 5 颗胶囊"改成"两个 thumb 拖到第 5 段重合"，锁的还是 `from === to === 4`）。
- **删除 T68k / T68l**（不是改宽蒙过去）：「切换节奏型」胶囊条整体删除后，这两条用例的被测对象不存在了；`t68` 里留了一段注释说明删控件的原因与替代出口。
- **看门狗新增 `arrange_range_dirty`**：`S.arrangeSel.from > to` 是本项目明写过的死循环形态（`Store.clampLoop` 注释），而滑块把这对字段变成了**连续可写**——测的是"加载后写入反向区间，播放仍不卡死、仍在发声"。实测通过（不卡），顺带确认了无既有隐患。刻意**不**seed 已经反向的持久数据：Store 加载期本就有双向归一，过一遍加载就被修好了，测不到运行期这条。

### 自验

`node tools/check-all.js` → **全部通过 · 实跑 14/14 项**（语法 / 架构约束 / 装配完整性 / 零依赖 lint / 版本一致性 / 文档一致性 / `_headers` / **ESLint 加强 0 错误** / **tsc 加强 0 错误** / DOM 引用完整性 / **真实浏览器冒烟 ✓** / FULL_SCAN 全量 / 死循环看门狗 / 行覆盖率），行覆盖率 **99.2%**；自动化测试 **2583 PASS / 0 FAIL**（v2.10.3 的 2518 + 本次净增 65 条）；看门狗 **53 PASS / 0 FAIL**。索引行号由 `node tools/gen-index.js --write` 重算。

★ **视觉与手感未经人眼验收**：冒烟脚本验的是"能启动、无控制台报错、关键元素有样式、SW 注册、播放帧率"，**看不出滑块拖起来顺不顺手**（尤其 thumb 重合时的抓取手感、窄侧栏里 10 档的精度）。按本仓库惯例，这类改动需你实机过一遍。

## v2.10.3 · 修「1 行档下曲式模式格子永不填白」（2026-09-22）

**来源**：用户实拍反馈——「**在播放自定义整首联播时，小球正常滚动，但格子里的白色动画不会自动填满**」。经追问确认：复现时「同屏行数」档位是 **1 行档**。

**根因**（单一缺陷，100% 复现）：`Viz.previewBarFor(k)` 的页末判据 `k !== winAnchor(k) + arrWinBars() - 1` 在 **N = 1** 时**恒成立**——因为 `winAnchor(k) = Math.floor(k/N)*N`，N=1 时退化成 `k`，于是 `k === k + 1 - 1` 永远为真。后果是曲式模式下**唯一那一行每帧都被当成「预告行」**（"下一小节"其实根本不在屏外，N=1 时窗口恒等于当前小节）：

1. `buildViz` 把 `winPat.bars[0]` 换成 `winPrevSteps` → **行内容 = 下一小节**（球走的却是当前小节，画面与声音差一小节）；
2. 该行挂上 `.preview-row`（`opacity:.55`）；
3. `setCell` 的 `isPreview` 豁免让所有格子**恒为 `upcoming`**，永远拿不到 `.active` / `.played`；
4. 而白色填充的底色**只长在** `.cell.played .fill` / `.cell.active .fill` 上（`.cell .fill` 本身是 `background:transparent`）→ 帧内每帧写入的 `scaleX(进度)` **全部不可见**。

**球为什么不坏**：`paintBall` 走 `onsetBuf` / `onsetNext` 自己的端点表，与「格子 class + CSS」这条链完全独立——所以是**只有半边坏**的典型症状（球在正轨、格子不动）。

**修法**（一行守卫 + 一段设计说明）：`previewBarFor` 开头补 `if (arrWinBars() < 2) return { bar: -1 };`，并把「前提 N ≥ 2」的四条理由写进函数头注释（含上面 3 → 4 的因果链，避免后人误删）。1 行档下预告本来就**没有任何信息量**（"下一小节"就在下一小节，翻页即可见），关掉后窗口照常按每小节翻页、格子回到正常态。
★ 只关 **N=1**，预告机制对 N ≥ 2 **逐位不变**（2/3/4 行档改动前后输出完全相同）。

**复现矩阵**（桩实测，判据 = "整段里有没有一帧连一个 `.active` 格都没有"）：

| 场景 | 修复前无 `.active` 帧 | 修复后 |
|---|---|---|
| 曲式 + 1 行档（自定义曲式） | **500/500** | 0 |
| 曲式 + 1 行档（示例曲《在他乡》） | **400/400** | 0 |
| 曲式 + 2 / 3 / 4 行档 | 0 | 0（逐位不变） |
| 预设 + 1 行档 | 0 | 0 |
| 曲式 + 1 行档，带 `.preview-row` 的帧 | **500/500** | **0** |

**回归**：新增 `tests/cases/t79-viz-rows.js` 场景 **T79h · 曲式 + 1 行档**（12 条断言）。观测手法与 T70g 同源——**用「格子数」当型的指纹**（四格型 / 八格型），构造段 A = 四格型 ×2 遍、段 B = 八格型 ×4 遍的 6 小节曲式，逐帧推 scheduler + `paintFrame`，判据落在带 `.current` 的**当前行**（不是写死的第 1 行）。四条核心断言：
- `★★ 1 行档全程没有预告行`（修复前 260/260）；
- `★★ 每帧恰有 1 个 .active 格`（修复前恒为 0）；
- `★★ 每帧行内容 = 当前可听小节`（修复前整屏是下一小节，指纹与节目单差一小节）；
- `ok(kinds.has(4) && kinds.has(8), …)` —— 钉住"两种型都出现过"，防止这是"只撞上一种型"的假绿（`assert` 不在 harness 的导出面里，必须用 `ok` 承载自定义断言名）。

**测试空白（本次才补上）**：`t79` 此前只在**预设模式**测了 1 行档（T79d），曲式侧只测到 2 行档（T79e/f）→ **「1 行档 × 曲式」这条组合从来没人走过**，这正是本 bug 能穿过 2506 条断言直达用户的通道。

**反向验证**（定向变异，逐项复原）：
- 变异 A：`< 2` → `< 1`（等于删掉守卫）→ **2515 PASS / 3 FAIL**，三条 `★★` 断言如期转红（`1 行档全程没有预告行` 期望 0 实际 260 / `每帧恰有 1 个 .active 格` 期望 260 实际 0 / `每帧行内容 = 当前可听小节` 期望 0 实际 56）。
- 变异 B：`< 2` → `< 3`（边界过宽，把 N=2 一起吃掉）→ **2517 PASS / 1 FAIL**，只有对照组 `★★ 2 行档照旧出现预告行` 变红。**顺带发现 T75（预告行五场景）拦不住变异 B**——它全程用默认 4 行档；真正钉住边界的是 T79h 的对照组。

**自验**：`node tools/check-all.js` → **全部通过 · 实跑 14/14 项**（语法 / 架构约束 / 装配完整性 / 零依赖 lint / 版本一致性 / 文档一致性 / `_headers` / **ESLint 加强 0 错误** / **tsc 加强 0 错误** / DOM 引用完整性 / **真实浏览器冒烟 ✓** / FULL_SCAN 全量 / 死循环看门狗 / 行覆盖率），行覆盖率 **99.2%**；自动化测试 **2518 PASS / 0 FAIL**（v2.10.2 的 2506 + 本次新增 12 条）。索引行号由 `node tools/gen-index.js --write` 重算。

## v2.10.2 · 修「同屏行数档位在预设模式无效」+ 预设模式网格窗口化（2026-09-22）

**来源**：用户实拍反馈——「行数按钮在一些情形中无效」。两张截图里档位分别是「2 行」与「4 行」高亮，网格却分别是 4 行 × 4 拍 与 1 行十六分扫弦型；底部还露出一枚孤立的绿色「范围循环」开关（预设模式本应隐藏）。

**根因**（两处独立缺陷）：
- **A 档位说谎**：`S.vizRows` 的唯一消费点是 `arrWinBars()`，而它只被曲式窗口用。预设模式下 `vizPattern()` 直接返回 `activePattern()`、`vizBars = patBars(p)` 恒等于型长 → 点档位除了重建一次 DOM 之外画面零变化。**这是 v2.8.0 的刻意设计**（`t79` 的契约与档位面板的标记注释都写着「只在曲式模式生效」），故本次是**行为变更**而非修 bug——用户拍板改成两种模式都生效。
- **B 开关漏网**：`.arg-now{display:flex}` 是作者样式，优先级高于 UA 的 `[hidden]{display:none}`；而全文件 10 条 `[hidden]{display:none}` 补丁里**没有** `.arg-now` → 承接 `#argJump hidden`（跳段 + 范围循环那一行）的 `hidden` 失效，预设模式下整行照样显示。

**修法**（统一模型：两种模式都是「N 行窗口」，N = `S.vizRows`）：
- **预设模式也拿窗口合成型**（`presetWindowPat`，与曲式 `arrWindowPat` 同构）：第 i 行 = 型的第 `(winStart + i) % 型长` 小节。用户拍板的两条语义由此自然落地——**型短于 N 绕回重复铺满 N 行**（Q1）、**型长于 N 按 N 行翻页**（Q2）。选合成型而不是让渲染层到处支持"窗口"，是因为下行（`buildBarRow` 的 `p.bars[b]`、`setCell` 的 `b < bar` 判据、`repaintCells` 的 `cellsPat.bars[b]`、`paintFrameBody` 的 `vizPattern().bars[bar]`）全部假设"行号 → 小节"是同域的，合成型让它们**一行都不用改**。
- **`vizBars` 拆成两个数**（它此前身兼二职：曲式下是窗口长度、预设下是型长）：`vizBars` = 显示行数 = 窗口长度；新增 `vizPatLen` = 内容模数 = 型的小节数。二者的 6 个读取点逐一改到位：`rowOfOnset`（预设分支改走 `rowOfPatBar`）、`mutedRow`、待命球 `cand`、`.next` 跨行 `nextRowOf`、时钟回落模数、`resetForStop` 取型。
- **行号与型内小节号显式对译**：新增 `patBarOfRow` / `rowOfPatBar` / `nextPatBarOf` / `nextRowOf` 四个小助手。`S.loopRange` 是**型内小节号**口径，此前渲染域把"行号"当"小节号"直接传进 `loopNextBar`（旧代码里两者恰好相等，所以一直是对的）——窗口化后必须先把行号折成型内小节号、再折回**翻页后**的行号。
- **翻页由可听小节驱动**：`paintFrameBody` 补了一条与曲式同构的预设同步分支（新增 `audiblePatBar`，与 `audibleSongBar` 同源同理由——不能用早一个前瞻窗口的 `schedBar`，否则画面先翻声音后到）；页锚 `winAnchor(k) = floor(k/N)*N`。**型长 ≤ N 时页锚恒为 0，本分支一次都不重建**，既有数据逐位不变。
- **`resetWindow()` 改成只丢曲式那个窗口**：预设窗口的内容由 (当前型, 页锚) 现算、页锚每帧按可听小节校正，不需要跨会话留存。若一并清成 -1/null，`start()` 后第一帧必然触发一次整树重建，而那会换掉 `playheadEl`/`ballEl`/`cellEls` 等全部元素引用——所有"取一次 `internals()` 再驱动"的用例（T30/T72/T41…）会拿到一对已被丢弃的节点（症状：球永远不动）。正确性不受影响：页锚对不上时下一帧会自行重建。
- **`resetForStop()` 改取 `vizPattern()`**（原 `curPattern()` + 行号索引小节）：1 小节的型铺 4 行时，第 2-4 行会取到 `undefined` → 丢掉休止虚框。
- **扫弦判据改走 `vizHasStrum()`**：窗口合成型只含当前页的小节，拿它问"这个型带不带扫弦记谱"会让底纹随翻页整层闪烁；预设模式改问 `activePattern()`（= 型本身，口径一字未变），曲式仍问窗口型。
- **Q3**：照 `.vol-row[hidden]` 的惯例补 `.arg-now[hidden]{display:none}`（带一段"为什么这条必须有"的注释）。

**取舍**：
- **预设模式不做"页末预告行"**（曲式 v2.7.0 那一套刻意不搬过来）：预设窗口的内容全部来自同一个型，下一行该显示什么是确定的，`nextRowOf()` 算术即可得出；而待命球的落点本来就取自 `activePattern().bars[下一小节]`，位置在翻页前就是对的。搬过来只会把 `setCell` 的 `isPreview`、`buildViz` 的第 1 行替换、`previewBarFor` 全部染上模式分支——收益不抵风险。
- **静音拍的行标识反而变准了**：窗口锚恒为 N 的整数倍 → 第 b 行的线性小节位置 = `winStart + b`，故 N = 4（= `MUTE_PERIOD`）时「行号 == 乐句相位」，且第 3 行的内容恰好就是会被静音的那一小节——**对任意型长都标得对**（v2.10.1 拿 `vizBars` 当型长判据，1 小节的型标不出来）。档位 ≠ 4 时仍不标（一页只覆盖乐句的一段，落点随翻页轮转）。
- **契约变更涉及 4 个既有真源**：`t79` 的头部契约与 T79d、`t70c`、`t71e`、`t69b`（后两条 prompt 未列出，实测确实与新模型正面冲突——"网格行数 = 型的小节数"这条契约本身被改写了）已按新模型改写；`t72` 回归哨兵（循环关着时 `loopNextBar ≡ (bar+1)%行数`）在 4 小节型 + 默认 4 行档下逐位等价，仍绿。

**自验**：`node tools/check-all.js` → **12 项通过 · ⚠ 2 项工具故障（tsc / 死循环看门狗）· 实跑 12/14**。
其中**浏览器冒烟 ✓（31 项断言）**、自动化测试 FULL_SCAN 全量 ✓、行覆盖率 ✓ **99.2%**、ESLint ✓。
两项 ⚠ 是本机沙箱的已知限制（它拦截一切"非 inherit"的子进程 stdio，EBUSY/管道取不到输出），
**不是被检项失败**——按仓库惯例手工补跑：手工复刻 tsc（shell 直拉 `tsc -p`）**exit 0 零输出**；
看门狗 18 个用例逐个直跑 **18/18 exit 0**（丢掉的是超时保护这一层，已如实标注）。
★ 中途曾出现「冒烟 SW 未注册（SecurityError）」，判为**环境瞬时态**（上一次无头实例/调试端口未释放，
CDP 连到旧实例）——同一条命令换端口重跑 3/3 全绿，v2.8.7 与 v2.10.1 的对照运行亦绿；
**故本次不是改坏 SW，也没有 SW 回归**。
自动化测试 **2506 PASS / 0 FAIL**（新增 `t87-preset-row-window` **10 个子场景 / 41 条断言**）。
**反向验证**逐项做过：把 6 处修法分别退回，目标断言全部如期转红（① `vizPattern` 退回预设直返当前型
→ 26 条红；② 抽掉 CSS 补丁 → T87f 红；③ `nextRowOf` 退回 → T87h 红；④ `resetForStop` 退回 →
T87i 红；⑤ `rowOfOnset` 退回钳制 → T87c/T87d 的"翻页帧行号"红；⑥ `mutedRow` 恒 false → T87j 红），
随后逐项复原。其中 ⑤ 第一轮**零失败**——即该分支原本没有任何断言覆盖，补了"翻页那一帧的当前行
= 新页第 0 行"才钉住。索引行号由 `node tools/gen-index.js --write` 重算。
推送后仓库内 CI（run 35718271632）**两个 job 全绿**：`自验 + 构建` 与 `真实浏览器冒烟`
——那两步在构建镜像里能真跑，本地那两条 ⚠ 的最终结论以 CI 为准。

## v2.10.1 · 修「六线底纹开关显示已开却不显示底纹」（2026-09-22）

**来源**：用户实拍反馈——「每次更新后打开工具，六线 · 扫弦谱底纹的开关默认已开，但是却没有显示底纹」。截图中 ↑↓ 箭头、灰格、座次尺俱在，唯独没有六线谱。

**根因**（三处耦合导致的"开关说谎"）：①标记里 `#tabToggle` 的初值**写死**为 `class="toggle-pill on"` + `aria-checked="true"`，只是"默认值提示"，真实开合态存在 `S.showTab`（点击会改写并持久化）；②显隐由 `Viz.syncTabLayer()` 把 `!(S.showTab && hasStrum(vizPattern()))` 收成 `.viz` 上的 `no-tab` 类（CSS：`.viz.no-tab .tab{display:none}`），**不重建 DOM**；③装配层的启动初始化块原先只同步 mute / bounce / trainer / countIn 四个开关，`tabToggle` 的 `setToggle` **只**出现在点击处理器里。于是「上次关掉 → 重载（含每次更新后的冷启动）」这条路径上，pill 显示"开"、aria 是 `true`，而 `.viz` 已带 `no-tab`——开关与事实相反，用户无从判断该信谁。

**修法**（`index.html` 装配层三条 `setToggle`，零行为变更、不动标记）：
- 在启动初始化块补 `setToggle("tabToggle", S.showTab);`——视觉与语义由 `setToggle` 一次写全，此后 pill 恒等于 `S.showTab`（与既有四个开关同源）。
- 顺带补同族漏改的 `setToggle("keepAwakeToggle", S.keepAwake);`——`#keepAwakeToggle` 标记同样写死 `off`，`setToggle` 原先也只在点击处理器里调；用户打开保活后重载同样会说谎。
- **只修"显示出来的状态"，不改用户记住的偏好值**：`S.showTab` 的默认与持久化语义原样保留。
- `tests/lib/harness.js`：给 `#tabToggle` 补静态初值（`className: "toggle-pill on"` / `aria-checked: "true"`）。此前桩里它的起始 `className` 是 `""`、aria 读到 `null`，与真实标记不同形，导致"启动有没有做收敛"在测试里**完全不可观测**——这正是该缺陷长期无测试可拦的原因。

**取舍**：显隐仍是 `(S.showTab && hasStrum(vizPattern()))` 的**合取**，不被本次启动收敛简化——偏好为开但当前型不带扫弦记谱时依然不铺底纹（v2.9.0 的刻意设计，t86d 专门钉住）。`.tab` 的**创建**只由内容决定、**显隐**只由 `no-tab` 类决定，故藏起来时 DOM 仍在（t86b 断言其存在，补上 t47「只看存在、看不到可见性」的盲点）。开关语义未变（仍 `role="switch"`，`t24` 钉死的恰 9 处不受影响），标记一字未动。

**自验**：`node tools/check-all.js --strict-env` 全绿（tsc ✓、ESLint ✓；仅浏览器冒烟因本机无浏览器 ⊘）；自动化测试 **2461 PASS / 0 FAIL**（新增 `t86-tab-toggle-boot-sync` 5 个子场景 / 27 条断言，已做**反向验证**：临时抽掉两行启动收敛后 6 条目标断言如期转红，随后复原）。行覆盖率 99.2%。索引行号由 `node tools/gen-index.js --write` 重算。

## v2.10.0 · 侧栏三区可折叠 + 「循环本段」排版 + 音量条说明（2026-09-22）

**来源**：用户三条诉求——①节奏型预设库的分类标题增加可折叠，收揽垂直空间；②「循环本段」的说明文字调整排版；③「音量 · 节拍 / 扫弦 / 重拍增强」三条音量条增加说明文字。经确认口径：折叠取「全部收起 · 记忆选择」、说明文字「与开关放同一行」、音量条说明「仅悬浮 / 读屏提示」（不占常显版面）、`#loopHint` 动态状态行**保留在下一行**、折叠后「编排曲式」入口**保持常显**。

**修法**（三处界面改动 + 一套显示偏好状态）：
- **任务 1 · 三区折叠**（`index.html`，行为 + CSS）：`.preset-section` 标题从纯文本升级为按钮——`role="button"`、`tabIndex=0`、`aria-expanded`，箭头用伪元素 `::after`（`[aria-expanded="false"]` 时旋转 −90°），**不新增文本节点**（`className` 仍是严格 `"preset-section"`、`textContent` 逐字不变，以兼容既有断言）。折叠**只置 `hidden`、不删节点**（`children` 数与对象身份不变），故 `.preset-item` / `.preset-arrange-group` / `.sidebar-note` 各补一条 `[hidden]{display:none}`（这些类有 `display:flex`，会盖过通用 `[hidden]`）。开合态存 `S.fold = {beat,strum,custom}`（**展开**布尔，默认全 `false`），随**热键**持久化（同 `showTab` / `vizRows`），脏值严格 `===true` 白名单回落收起；`buildPresetList()` 收尾调 `applyFold()` 重新套用（重建后新节点默认 `hidden=false`）。
- **任务 2 · 「循环本段」排版**：删去 `.group-label` 那行独占的标题，把说明文字 `<span class="loop-desc">只反复磨这几小节</span>` 并入 `.loop-row` 与开关**同一行**；`#loopHint` 动态状态行保留为其下一兄弟行。
- **任务 3 · 音量条说明**：三条 `.vol-row`（节拍 / 扫弦 / 重拍增强）各补 `title` 悬浮提示，并在对应 `input` 上补 `aria-label` 读屏提示；文案取自 Help 文档，不新增常显文本。

**取舍**：折叠做成**显示偏好**而非结构变更——只隐藏既有节点，不重排、不重建，`presetItemEl` 的**对象身份**定位（v2.9.0 引入）与一切按 `children` 序位遍历的既有断言（t30 / t63 / t68 / t73 / t74 / t75 / t84）继续成立；开合态跟热键走、与 `showTab` 同源，重载即恢复。标题用 `role="button"` 而非 `role="switch"`——开关语义已被 `t24` 钉死为**恰好 9 个**，此处不能再增 switch。三区默认**全部收起**是为最大化收揽空间，「编排曲式」入口在 `#presetList` 之外故不受折叠影响、保持常显。

**自验**：`node tools/check-all.js --strict-env` 全绿（tsc ✓、ESLint ✓；仅浏览器冒烟因本机无浏览器 ⊘）；自动化测试 **2434 PASS / 0 FAIL**（新增 `t85-sidebar-fold` 4 个子场景 / 25 条断言，已做**反向验证**：临时令 `applyFold()` 空转后 8 条目标断言如期转红，随后复原）；行覆盖率 99.2%。索引行号由 `node tools/gen-index.js --write` 重算。

## v2.9.0 · 预设库重分类为「节拍 / 扫弦 / 自定义」三类（2026-09-22）

**来源**：用户诉求——现有「普通节拍 / 带扫弦」二分"不严谨，也不实用"。改为三分类：「节拍」（原普通节拍）、「扫弦」（常用扫弦型）、「自定义」（用户编排曲式，内置示例曲《在他乡》）。节拍与扫弦互斥；自定义内部可自由组合二者（示例曲即由 5 个扫弦型组成）。

**修法**（分阶段落地：数据 → 界面 → 架构 → 收尾）：
- **数据层**：示例曲 5 个扫弦型由「在他乡 · 节奏型N」改名成通用名（十六分满扫 / 副歌扫弦 / 主歌扫弦 / 雨夜扫弦 / 桥段扫弦）；型识别由"名字前缀"改为**内容认型**（`demoIndexOf` / `demoPresetEq`），曲式块引用由名字（`refName`）改为 presets **下标**（`refIdx`）；老数据旧名由启动时一次性迁移收敛（`migrateDemoPatternNames`），内容收敛仍归 `ensureDemo`。
- **界面层**：预设库由两态「轨」（普通节拍 / 带扫弦）的切换条，改为**按内容分类、常显的三区侧栏**（节拍 / 扫弦 / 自定义）；分类只认内容 `hasStrum`（`dir` 或 `zone` 任一存在即带扫弦记谱），不再有"当前轨"这一运行时状态。分区渲染不改内置下标——选中项按**对象身份**定位，而非分区序位。
- **架构层**：删掉 `Tracks` 模块（14 → 13 个 IIFE 模块），连带清理 `S.track` / `ensureValidForTrack` / `syncTrackUI` 与双入口相关门控——凡原按 `S.track === "strum"` 判定的地方（六线底纹、方向箭头、鼓组叠加等），一律换成内容判据 `hasStrum(pat)`。
- **收尾**：全仓同步陈旧的"轨式"表述（`index.html` 注释与 Help 文案、`docs/DEVELOPMENT.md` 模块地图与轨判定小节、`AGENTS.md` 项目画像、`README.md` 功能与交互条目、12 个测试文件的注释），模块索引行号交 `node tools/gen-index.js --write` 重算。

**取舍**：型一律按内容而非名字识别——名字已通用化、且与用户自建型可能重名，唯有比内容（meter + 逐步 `[t,rest,dir,zone]` 规范化）才是稳定标识；旧数据辅以旧名序号兜底（`demoLegacyIndex`），仅服务一次性迁移窗口。分类判据 `hasStrum` 只保留**一处实现**（在数据区，只读 `pat.bars`），迁移期与运行时共用同一个函数，不再有"两处实现、一个语义"的分叉风险。

**自验**：`node tools/check-all.js` 实跑 11/14 全绿（ESLint / tsc 因缺 `node_modules` ⊘、浏览器冒烟因本机无浏览器 ⊘）；自动化测试 2397 PASS / 0 FAIL；死循环看门狗 49 PASS / 0 FAIL / 0 超时；行覆盖率 99.2%。

## v2.8.30 · 修 CI 红灯：补 bindImport 形参类型（tsc）+ 冒烟无头启动加固（2026-09-21）

**来源**：推送 v2.8.29 后 CI run 两个 job 皆红（job1「自验+构建」、job2「真实浏览器冒烟」），逐条查清后修。

**修法**：
- **job1 真因（`index.html`）**：`bindImport(inputId, tooBigMsg, onText)` 三个形参无类型 → tsc 报 5 条 `TS7006`（隐式 any，`7037:23/32/43`、`7052:60`、`7074:73`）。该缺陷由 v2.8.28 的 P3-B（抽公共导入装配器）引入，v2.8.28 那次 CI 亦同因变红；本地之所以「假绿」是 `node_modules` 缺失时 `check-all` 把 ESLint/tsc 标 ⊘ 未执行。给三个形参补 JSDoc 类型（`@param {string} inputId @param {string} tooBigMsg @param {(text: string) => void} onText`）后 5 条归零，并把两处调用点回调整好类型。
- **索引连带（`index.html` 头部模块索引）**：上述 JSDoc 新增 1 行使其后所有模块 banner 下移 1 行，触发「文档一致性」7 条行号漂移。用 `node tools/gen-index.js --write` 重算行号（短名与描述未动）。
- **job2 加固（`tools/smoke.js` / `.github/workflows/ci.yml`）**：job2 失败点是两条通道均报「等不到调试目标」，发生在**加载页面之前**（CDP 调试端口未监听），与页面内容无关；对照 v2.8.28 的冒烟曾通过、两版只差几行 CSS，判为 CI 环境问题——Ubuntu 24.04 起内核默认限制非特权 user namespace，Chrome 沙箱起不来导致无头实例当场退出。给无头实例补 `--no-sandbox` / `--disable-dev-shm-usage`；并把浏览器 stderr 捕获进该报错（此前 `stdio:"ignore"` 让失败只剩一句无从下手的话）。CI 里冒烟前新增「报告浏览器版本」一步。

**取舍**：tsc 修法只补类型标注、不动任何行为；索引由生成器重算而非手改数字。`--no-sandbox` 仅作用于冒烟脚本拉起的这一个一次性无头实例、且只加载自备本地内容，不涉及产物与运行时（`index.html` 仍零依赖、`file://` 直开）。断言一条未削弱。

**自验**：`node tools/check-all.js --strict-env` 实跑 13/14 全绿（tsc ✓、ESLint ✓；仅浏览器冒烟因本机无浏览器 ⊘）；自动化测试 2502 PASS / 0 FAIL；死循环看门狗 49 PASS / 0 FAIL / 0 超时；行覆盖率 99.2%。

### 发布渠道（补记）

WorkBuddy 渠道换绑到新链接 **https://beatsight.app.workbuddy.host/**，旧链接 `beatsight-48543` **废弃**（不再更新）。
Cloudflare 那条 `beatsight.chenhuajian1995.workers.dev` 未变，仍是判断"线上是否最新"的基准。

换绑原因与上次同源：应用归属按「工作区（会话）」判定，`beatsight-48543` 所属的工作区已不在当前环境，
无法再更新，只能在新工作区发布并换绑。本次同时把发布副本的来源固定为 `node tools/build-dist.js` 的 `dist/`
（9 个上站条目，含 4 个构建期生成的 PNG），`README.md` / `docs/DEVELOPMENT.md` 的渠道地址同步。

## v2.8.29 · 歌词字号加大两号并加粗（主视图 + 曲式编辑器）（2026-09-21）

**来源**：用户诉求「歌词行的字体加大两号并加粗」。经确认口径：主视图歌词 12px → 16px（+4px）、字重 500 → 700（加粗）；曲式编辑器歌词同步 11px → 15px + 加粗。

**修法**（全在 `index.html` 的 CSS，不动任何 JS 几何）：
- 主视图 `.lyric-char`：`font-size:12px;font-weight:500` → `font-size:16px;font-weight:700`。
- 承载高度配套放大：`.lyric-row` 22px → 28px、`.lyric-chip` 20px → 26px（16px 字压在 20px 高的 `overflow:hidden` 字块里会裁字，必须同步放大，不是样式偏好）。
- 已唱/正在唱两态原为 `font-weight:600`，比新基准 700 更轻（层级倒挂），改为 800，保持「激活态更重」的层级。
- 曲式编辑器 `.arg-lyric-chip`：`font-size:11px` → `font-size:15px;font-weight:700`；`.arg-lyric-lane` 26px 高已足够容纳 15px 字，未改。
- 顺带修正 `.arg-lyric-char` 处的陈旧注释（原文写「灰字 13px」，实际主视图早已是 12px、现为 16px 加粗）。

**取舍**：只改字号/字重与承载高度，行数、定位、走带线等几何一律不动。已核实 `.lyric-lane` 是 `#viz` 的**兄弟节点**（独立 flex 列），字块在 JS 里只写百分比 `left/width`，行/块高度纯 CSS，故改动不触碰任何几何计算；测试用例只锁结构与计数、不锁字号/高度，无需改测试。

**自验**：`node tools/check-all.js` 全绿（实跑 11/14，⊘ ESLint/tsc 缺 node_modules、⊘ 浏览器冒烟环境缺失）；自动化测试 2502 PASS / 0 FAIL；死循环看门狗 49 PASS / 0 FAIL / 0 超时；行覆盖率 99.2%。

## v2.8.28 · 工程卫生 P3：审计 P3 组的 9 条清理（P3-A ~ P3-I）（2026-09-21）

**来源**：按 `docs/AUDIT-2026-09-21.md` 依 P0→P3 逐条落地的第 18 条（P3 组，本组最后一条）。P3 是报告最末一组「工程卫生」，条目分散在源码注释、构建/自验工具与几处工程配置里，逐条独立、互不耦合，故合并为一版。

- **P3-A 死代码与陈旧注释（`index.html`）**：`durName` 里两条恒不可达的分支（时值名映射的兜底已被前面分支完全覆盖）删除；`zoneEls` 上方注释仍描述早已不存在的选择逻辑，改为与实现一致的说明。
- **P3-B 两个近重复的导入处理器合并（`index.html`）**：预设导入与整包导入各自复制了一份「读文件 → 解析 → 报错文案 → 落库」的骨架，抽为 `bindImport(inputId, tooMsg, onText)`，两处调用只传差异项（错误文案与落库回调）。行为不变，重复消除。
- **P3-C 两次导入的数据校验缺口（`index.html`，带测试）**：
  - **耳战绩键名无白名单**：`importAll` 合并外部耳训战绩时照单全收键名，异常包可写入任意键污染后续统计。加 `EAR_KEYS = ["total","right","best"]` 白名单，只接受已知键。
  - **歌词合并不受行数约束**：导入的歌词数组直接并入，绕过了本地新增时的 `CONFIG.lyricMaxLines` 上限（导入即可突破 UI 限制）。合并处补同一上限判据（`if (lyrics.length >= CONFIG.lyricMaxLines) { lyrDrop++; return; }`）。
  - 在 `tests/cases/t83-full-data-pack.js` 加 T82g（越界键名被丢弃）/ T82h（超限歌词被拒收）。
- **P3-D `bindStep` 连发的计时器竞态（`index.html`，带测试 + 反向验证）**：长按 step 键的 `pointerdown` 若在上一次的 `setTimeout` / `setInterval` 尚未触发时又被触发（快速连点 / 指针抖动），旧句柄会被覆盖而不清除，遗留的定时器在松手后仍继续跑——`t1` 泄漏表现为「按键停了但还在步进」。入口补 `if (t1 !== null) clearTimeout(t1); if (t2 !== null) clearInterval(t2);`。在 `tests/cases/t30-wiring-and-lifetime.js` 加 T31b：连续两次 `pointerdown` 后松手，断言无遗留定时器。**旧实现**该断言变红；**新实现**全绿。
- **P3-E 工程配置（`manifest.webmanifest` / `.github/workflows/ci.yml`）**：manifest 补 `"id": "./"`（PWA 安装身份稳定，避免同源多入口被视作不同应用）；CI 工作流补 `permissions: contents: read`（最小权限，遵循 Actions 默认收紧的方向）。
- **P3-F 冒烟工具的路径边界与端口资源（`tools/smoke.js`）**：
  - **路径前缀判断错误**：静态服务用裸 `file.startsWith(ROOT)` 判「在仓库内」，会把同前缀的**兄弟目录**放进服务范围（`ROOT=/a/beatsight` 时 `/a/beatsight2/x` 也命中）。改为按路径分隔符判边界（`file === ROOT || file.startsWith(ROOT + path.sep)`）。
  - **端口被占时不再假红**：CDP 端口若已被**另一个**浏览器/调试实例占用，本脚本 spawn 的实例会因冲突起不来，而 `runPass` 却连上别人的实例、探针拿不到 `__beatBoot`，整串断言被误报成代码失败。改为 `main()` 入口先 try-bind 探测：端口占用则打印 ⊘ 并**以退出码 3 跳过**（端口是环境资源，不是失败项），并支持 `BEATSIGHT_SMOKE_PORT` 覆盖端口基号。
- **P3-G 看门狗的故障分类（`tests/hang-guard.js` / `tests/hang-case.js`）**：
  - **信号 ≠ 死循环**：旧判据 `if (e.killed || e.signal) timedOut = true` 把 OOM（SIGKILL）/ 段错误（SIGSEGV）/ abort（SIGABRT）一律报成「主线程死循环」——把环境/崩溃问题指到一个不存在的地方去查。改为按耗时区分：跑满超时被强杀 = 死循环；未跑满超时即被信号终止 = 疑似 OOM/崩溃，计入**工具故障**（退出码 4，check-all 标 ⚠ 并继续）；子进程自报退出码 4 = 输入/工具故障。新增 `toolFaults` 计数与独立清单。
  - **空值保护**：`hang-case.js` 的 `html.match(/<script>…/)[1]` 在输入文件不是 BeatSight 产物（缺 `<script>` 块）时会 `null[1]` 崩溃，方向完全指错。补判空后显式报「输入/工具故障」并以退出码 4 交回看门狗。
  - **反向验证**：构造一个无 `<script>` 的输入文件跑 `hang-case.js` → 清晰报错 + 退出码 4（而非崩溃）；分别注入 SIGKILL / 死循环 / 退出码 4 / 退出码 2 四类结局，确认分类器各自落到 `killedSignal` / `timedOut` / `toolFail` / 普通失败，无一误判。
- **P3-H 删除过时产物并清理引用（`tests/screenshot.sh`）**：该脚本只在 macOS 上工作（硬编码 `/Applications/Google Chrome.app/…`），`sleep 18` + `pkill`、零断言，早已被跨平台的 `tools/smoke.js` 取代。删除文件，并同步更新 `docs/DEVELOPMENT.md`（目录树 2 行 + 命令区 2 处注释）与 `tools/smoke.js` 头部注释中的引用。（`CHANGELOG` 里的历史提及属快照，按仓库约定不改写。）
- **P3-I 自验汇总表按显示宽度对齐（`tools/check-all.js`）**：步名混有 CJK，`String.padEnd(28)` 只数 code unit（全角字符在等宽终端占 2 列），导致「自动化测试（FULL_SCAN 全量）」这类行右列整体右移、表看着是歪的。加 `dispWidth()`（宽/全角按 2 列）后按显示宽度补空格，14 个步名全部对齐到 28 列。
- **行号漂移说明**：报告给的部分行号已与当前代码不符（如 `tools/check-all.js` 的 `padEnd` 报告写 L192、实际在 L214；`bindStep` 竞态、`durName`、`zoneEls`、`smoke.js` 路径判断等亦有偏移）。本次一律**以代码实际状态为准**定位并修复。
- **自验**：`node tools/check-all.js` 全绿（实跑 11/14 项，另 2 项可选加强项缺 `node_modules` ⊘、1 项浏览器冒烟环境缺失 ⊘；文档一致性 / 版本一致性均通过；自动化测试 2502 PASS / 0 FAIL；死循环看门狗 49 PASS / 0 FAIL / 0 死循环超时；行覆盖率 99.2%，总阈值 97%）。

## v2.8.27 · 文档/工程 P2-9：手抄（且已漂移的）数字与过期注记从文档注释里清除（2026-09-21）

**来源**：按 `docs/AUDIT-2026-09-21.md` 依 P0→P3 逐条落地的第 17 条（P2 组）。本节只落这一条，独立成版与提交。

- **P2-9 文档/注释里的手抄数字已与现实漂移**：
  - **手抄组合数**：T21「全组合不变量扫描」的规模（原写死「243 组」）散落在 4 个受闸门文件与 1 个用例注释里（`README.md` / `tools/check-all.js` / `tests/README.md` ×2 / `docs/DEVELOPMENT.md` ×3 / `tests/cases/t20-pattern-switching.js`）。用例内的预设对 × 点击相位常量一旦调整，这些手抄值全部失真。修法：一律改为**指向运行时事实**（「T21 全量组合扫描」「规模由用例内常量决定」），不再复述具体数字。
  - **过期注记**：`tasks.md:42-43` 的 P0-2（渐进提速训练）/ P0-3（练习数据导出导入增强）实际已落地却仍标 `[ ]`——核对代码与 `CHANGELOG` 后确认 **P0-2 已于 v0.5.0 落地**（字段名 `trainer.start/target/step/everyN`）、**P0-3 已于 v2.8.8 落地**（导入为合并而非替换），故改标 `[x]` 并补注落地版本；**P0-1（音频延迟校准）确未落地，保持 `[ ]`**。`AGENTS.md:14` 相应把「待办池」改为「历史审计清单（已完成项标 `[x]`）」，避免新会话把已落地项当待办重新立项。
  - **过期产物清单**：`.gitignore:31/37` 的注释停留在 PNG 图标（v2.4.4）与 `build-dist`（v2.0.6）之前，仍写「4 个上站产物」；实际由 `tools/build-dist.js` 决定，为 5 个拷贝条目 + 4 个构建期生成的 PNG = **9 条目**。修法：改为指向 `tools/build-dist.js` 这一唯一真相源。
  - **漏网手写耗时**：`tools/install-hooks.sh:5` 的「（约 2 秒）」不在 `check-docs.js` 的 `TIMING_FILES` 覆盖范围内（只钉 4 个文件），属手工漂移，直接删去。（该文件「本项目没有 CI」一句早在 v2.8.18 已订正，无需再动。）
- **自验**：`node tools/check-all.js` 全绿（文档一致性 / 版本一致性均通过；自动化测试 2490 PASS / 0 FAIL；行覆盖率 99.0%）。

## v2.8.26 · 交互 P2-8：退回预设模式后示例段「切换节奏型」胶囊条仍渲染（动作与反馈脱节）（2026-09-21）

**来源**：按 `docs/AUDIT-2026-09-21.md` 依 P0→P3 逐条落地的第 16 条（P2 组）。本节只落这一条，独立成版与提交。

- **P2-8 退回单型练习后，胶囊条仍可改曲式数据**：
  - **根因**：`exitArrangeForPreset`（`index.html:6643`）把 `S.playMode` 退回 `"preset"`、清 `arrangeSel.loop` / `byLyric`，但**不清 `S.arrangeSel.id`**。而 `buildDemoSegRow`（`index.html:6658`）的渲染判据只查 `Store.findArrange(S.arrangeSel.id)` 的 id 是否为 `DEMO_ID`——于是退回预设模式后这条胶囊条依旧渲染。此时点胶囊走 `applyDemoSeg`，改的是**曲式块引用**（要下次进曲式才生效），对当前的单型练习毫无即时效果：动作与反馈脱节。
  - **修法（用户拍板「隐藏胶囊条」）**：`buildDemoSegRow` 的入口判据补一维 `S.playMode === "arrange"`，即 `if (S.playMode !== "arrange" || !ar || ar.id !== DEMO_ID) return null;`。与函数头注释的原始意图（"返回 null = 当前没在编排这首示例曲"）一致。换型能力不受损——从段序行点进曲式即可（`jumpDemoSection` 会把 playMode 置回 `"arrange"`）。
- **测试与反向验证**：
  - 在 `tests/cases/t68-whole-song-and-voices.js` 加 T68l：进曲式（点段序条第 5 段）确认胶囊条已渲染 → 点侧栏节奏型退回单型练习 → 断言 `segRowOf(els) === null`（同时记录根因仍在：`arrangeSel.id` 仍指向示例曲，故判据不能只看它）。**旧实现**该断言变红（2489 PASS / 1 FAIL）；**新实现**全绿。
- **自验**：`node tools/check-all.js` 全绿（含新增 T68l；自动化测试 2490 PASS / 0 FAIL）。

## v2.8.25 · 播放 P2-7：帧路径上 arrangeCur() 的重复线性查找（实测稳态帧 12→1）（2026-09-21）

**来源**：按 `docs/AUDIT-2026-09-21.md` 依 P0→P3 逐条落地的第 15 条（P2 组）。本节只落这一条，独立成版与提交。

- **P2-7 每帧反复线性查找当前曲式**：
  - **根因**：帧热路径里对「当前曲式」的解析走 `arrangeCur()` → `Store.findArrange(id)`，而 `findArrange` 是 `arranges.find(x => x.id === id)` 的**线性扫描**（`index.html:2128`）。单帧内该查找被多处各自触发：`paintBall` 遍历 `onsetBuf`，对**每个端点**调 `rowOfOnset(e)`，每次又各自 `arrangeCur()`；叠加 `audioPosAt` 3 次、`paintLyric` 1 次、`audibleSongBar` 1 次、`arrangeNextRow` 1 次。单帧查找次数 ≈ 端点缓冲长度 + 4，**随数据量线性增长**（长曲 / 多端点时恶化）。
  - **修法（报告的最小修复）**：`paintFrameBody` 顶部**取一次** `arrangeCur()` 快照，向下传给 `audibleSongBar` / `audioPosAt` / `paintLyric` / `paintBall`；`paintBall` 再透传给其内部的 `rowOfOnset` 与终点弧调用的 `arrangeNextRow`。这些被调函数统一加可选形参 `a`，**不传则退回各自现查**（`if (a === undefined) a = arrangeCur()`），保持既有调用方与冷路径（`buildViz` / `buildLyricLane` / `arrangePlayPattern`）行为不变。帧内 `S.playMode` / `S.arrangeSel.id` 不变（中间只读状态、不切选择），故快照与现查等价。
  - **顺带修一处报告未列出的热点**：`paintBall` 终点弧调用的 `arrangeNextRow` 原先也各自现查，本次一并纳入快照下传（否则「取一次」不彻底）。
- **测试与反向验证**：
  - 在 `tests/cases/t24-audit-hardening.js` 加 T27b：包一层计数器改写 `beat.Store.findArrange`，驱动 240 BPM / 单块 8 次重复的曲式，热身 50 帧后统计 300 帧的每帧查找次数。断言**稳态帧中位 = 1**、多数帧恰好 1 次、每帧至少 1 次。**旧实现**该组断言变红（中位 12）；**新实现**中位 1。
- **实测量化**（240 BPM / 单块 8 次重复 / `onsetBuf.length=8` / 热身 50 + 观测 300 帧）：
  - 旧：min 12 · median 12 · max 26 · avg 12.66
  - 新：min 1 · median 1 · max 14 · avg 1.29（直方图 {"1":244,"2":48,"3":5,"4":1,"13":1,"14":1}）
  - ⇒ 稳态帧 **12 → 1**（约 12×）；残余的 13/14 为小节边界帧，那里另经 `Arrange.refreshNow` → `arrangeCur()`。
- **自验**：`node tools/check-all.js` 全绿（含新增 T27b）。

## v2.8.24 · 播放 P2-6：曲式换块时 scheduleRef 与 applyPatternChange 重复全量重建侧栏列表（实测 −11%）（2026-09-21）

**来源**：按 `docs/AUDIT-2026-09-21.md` 依 P0→P3 逐条落地的第 14 条（P2 组）。本节只落这一条，独立成版与提交。

- **P2-6 每个小节边界把整张预设列表重建两遍**：
  - **根因**：曲式（整首连播）换块时，scheduler 在小节边界先调 `Presets.scheduleRef(nxt.ref)`（`index.html:7079`），它除了登记挂起外还顺手 `buildPresetList()` + `updateFallbackNote()` 全量重建侧栏；紧接着**同一个边界**又经 `consumePending` → `applyPatternChange`（`index.html:6954-6980`）**再做一遍同样的全量重建**（L6972 / L6978）。`scheduleRef` 只由曲式路径调用（`scheduler` L5385 与 `arrangeStart` L5850），两处调用点后面都紧跟 `consumePending` / `flushPending` → `applyPatternChange`，故 scheduleRef 里的重建是纯冗余，且正撞在「小节边界那一轮必须最省、阻塞超余量就吞拍」的纪律上（CONFIG 注释 L1738-1746）。
  - **修法（报告的最小修复）**：`scheduleRef` 退化为**只登记挂起**（置 `pendingRef` / `pendingPattern` / `sigChgPending`），删去 `buildPresetList()` 与 `updateFallbackNote()`；列表重建统一交给紧随其后的 `applyPatternChange`。段序条高亮不受影响——它由 `Arrange.refreshNow` / `refreshBar` → `syncDemoSecRow()` 每小节同步（L8468 / L8460），另有 `stop()` / `playWholeSong` / `jumpDemoSection` 三处兜底，`syncDemoSecRow` 本身幂等安全。
- **测试与反向验证**：
  - 在 `tests/cases/t24-audit-hardening.js` 加断言：`resetProbe()` 后调 `Presets.scheduleRef(...)`，要求 `PROBE.classWrites === 0`（排一个型不得产生任何 className 写入）。**旧实现**该断言变红——**期望 0，实际 42** 次 className 写入（探针钉死「顺手重建」）；**新实现**为 0。
  - 顺带订正两处因本次改动而过期的注释（`index.html` 中 `buildPresetList` 尾注、`tests/cases/t68-whole-song-and-voices.js` 头注与 L252），把「scheduleRef → buildPresetList」改为实际路径「applyPatternChange → buildPresetList」。
- **实测量化**（临时脚本 `tmp-measure-p26.js`，240 BPM、strum 种子、600 帧、3 次换块，用后即删）：每次换块边界帧的 className 写入 **770→687 / 730→647 / 730→647**（**每块 −83 次**，约 **−11%**）；600 帧合计 **8257→8008**（Δ=249=3×83）。
- **备注**：`index.html` 内以注释形式出现本版号（`scheduleRef` 处 `★ v2.8.24`），`check-version.js` 第 3 项据此要求 VERSION 同步到 2.8.24——版本号与代码改动同版落地。

**自验**：`node tools/check-all.js`（全量）**2480 PASS / 0 FAIL**，实跑 11/14 项、⊘ 3 项（ESLint / tsc 缺 node_modules、浏览器冒烟缺环境，均为正常态）；行覆盖率 **99.0%**（6695/6762，阈值 97%/90%，Presets 分区 95.2%）；死循环看门狗通过。版本号五处一致由 `check-version.js` 与 `check-docs.js` 强制。

## v2.8.23 · 工具 P2-5：check-dom-ids 的引用正则只认双引号（单引号/模板串整段逃逸审计）（2026-09-21）

**来源**：按 `docs/AUDIT-2026-09-21.md` 依 P0→P3 逐条落地的第 13 条（P2 组）。本节只落这一条，独立成版与提交。

- **P2-5 `$()` / `getElementById()` 引用收集只匹配双引号**：
  - **根因**：`tools/check-dom-ids.js` 的引用正则为 `/(?:\$|getElementById)\(\s*"([^"]+)"\s*\)/g`，只认双引号字面量。写 `$('x')` 或 `$(\`x\`)` 时该引用**完全不进审计面**，悬空 id 不会被发现——当前 `index.html` 恰好全用双引号，属惯例而非闸门保证。
  - **修法（报告的最小修复）**：正则改为引号无关 `/…\(\s*(["'`])([^"'`]+)\1\s*\)/g`（用反向引用钉住首尾同引号），id 捕获组随之从 `mm[1]` 改为 `mm[2]`；同步把文件头「检查两件事」的口径注释补上「引号无关，`\"` / `'` / `` ` `` 三种都认」。
- **测试与反向验证**：构造 `repro-domids-tmp.html`（声明 `realId`，代码里 `$('ghostId')` 与 `getElementById('realId')` 两条**单引号**引用）——**旧判据**「代码引用 0 个」、exit 0（两条单引号引用整段逃逸，悬空的 `ghostId` 被漏报，**假绿**）；**新判据**「代码引用 2 个」、报 1 个悬空 `$("ghostId")`、exit 1（正确捕获）。对真实 `index.html`：`index.html` 全文**无任何单引号/模板串引用**（`grep` 验证），故新旧结果一致——165 声明 / 159 引用、零悬空、exit 0，本次修复对现有代码**零误报、零行为变化**。复现文件用后即删。
- **备注**：只动工具，未改 `index.html` 逻辑；本版号只出现在五处版本落点，均不在 `index.html` 内（`check-version.js` 第 3 项不涉及）。

**自验**：`node tools/check-all.js`（全量）**2479 PASS / 0 FAIL**，实跑 11/14 项、⊘ 3 项（ESLint / tsc 缺 node_modules、浏览器冒烟缺环境，均为正常态）；行覆盖率 **99.0%**（6692/6759，阈值 97%/90%）；死循环看门狗通过。版本号五处一致由 `check-version.js` 与 `check-docs.js` 强制。

## v2.8.22 · 工具 P2-4：check-wiring 槽位收列的两处静默失效（多为漏收 + 字符串假绿）（2026-09-21）

**来源**：按 `docs/AUDIT-2026-09-21.md` 依 P0→P3 逐条落地的第 12 条（P2 组）。本节只落这一条，独立成版与提交。

- **P2-4 注入槽自动收列在「一行多声明符」与「字符串字面量」上失灵**：
  - **根因**：`tools/check-wiring.js` 的 `DECL_RE`（L81）锚定整行且只认单个声明符——`let onA = null, onB = null;` 这种写法**两个槽都不收**，此后完全不受本闸门管辖（静默逃逸）。同时收列与赋值判定都在未遮蔽字符串的行上跑，字面量里的 `onX = fn` 会被计为"已装配"（**假绿**）。
  - **修法（报告的最小修复）**：① 收列改为「`let` 语句 → 按逗号切分声明符 → 逐个匹配 `name = null`」，支持一行多声明符；② 新增 `maskStrings` 一层等长遮蔽（行号/偏移不变），收列、赋值判定、`setPatLenOf` 调用判定一律在**遮蔽后**的行上做。赋值右值（RHS）用**等长偏移从原文回读**——这样 `onX = "str"` 这类字符串右值不会被误判成空/复位。
- **测试与反向验证**：两份 repro 均实现 **0→1 翻转**——
  - `repro-wiring-multi.html`（`let onAlpha = null, onBeta = null;` 且无赋值）：**旧判据收列 0 槽、exit 0**（两个槽静默逃逸），**新判据收列 2 槽、exit 1**（两条 `onAlpha/onBeta 从未被赋过非 null 值`）。
  - `repro-wiring-str.html`（`let onGate = null;` + 字符串 `"onGate = someFn"`）：**旧判据收列 1 槽但把字符串当装配、exit 0**（假绿），**新判据收列 1 槽、exit 1**（正确报未装配）。
  - 真实 `index.html`：新旧收列**均为 12 个槽且名单逐字一致**（`onArrangeEnd … onKeepAliveFail`），全部接上、exit 0——本次修复对现有代码**零误报、零行为变化**。repro 文件用后即删。
- **备注**：只动工具逻辑，未改 `index.html`；本版号只出现在五处版本落点，均不在 `index.html` 内。文件头 L3「15 个 IIFE 模块」的陈旧数字不在本条范围，归入 P2-9 处理。

**自验**：`node tools/check-all.js`（全量）**2479 PASS / 0 FAIL**，实跑 11/14 项、⊘ 3 项（ESLint / tsc 缺 node_modules、浏览器冒烟缺环境，均为正常态）；行覆盖率 **99.0%**（6692/6759，阈值 97%/90%）；死循环看门狗通过。版本号五处一致由 `check-version.js` 与 `check-docs.js` 强制。

## v2.8.21 · 工具 P2-3：check-lint 的 no-var / eqeqeq 在未遮蔽字符串上判定（实测复现）（2026-09-21）

**来源**：按 `docs/AUDIT-2026-09-21.md` 依 P0→P3 逐条落地的第 11 条（P2 组）。本节只落这一条，独立成版与提交。

- **P2-3 「零依赖 lint」的两条规则会被字符串字面量误报**：
  - **根因**：`tools/check-lint.js` 的 no-var（L105）与 eqeqeq（L107-114）在 `raw = clean[i-1]` 上判定，而 `stripComments` **刻意保留字符串**（`scan-util.js:20` 注释即写明），只有 no-undef 走了 `maskStrings`。于是字面量 `const a = "x == y";` 会被判 eqeqeq、`const b = "var z = 1";` 会被判 no-var——文件头那句「字符串与注释已剥离，不会误报」是错的。当前全绿纯属 `index.html` 的字符串恰好不含 `==` / `var x`。
  - **修法（报告的最小修复）**：在 `flat` 旁一次性算出 `maskedFlat = maskStrings(flat)` 与 `maskedLines`（等长遮蔽，行号/偏移不变），no-var / eqeqeq 改在 `maskedLines[i-1]` 上跑；no-undef 复用同一份 `maskedFlat`，不再重复计算。同步更正文件头 L15 的口径注释。
- **测试与反向验证**：构造 `repro-lint-tmp.html`（含 `"x == y"` / `"var z = 1"` 两个字面量）——**旧判据**（在未遮蔽文本上裸判）报 `L2 eqeqeq`、`L3 no-var` 两条**假阳性**；**新判据** `node tools/check-lint.js repro-lint-tmp.html` **exit 0、五条规则全过**（仅剩 no-unused-vars 警告，不拦 CI）。对真实 `index.html` 仍全绿（其字符串不触发这两条，故行为不变）。复现文件用后即删。
- **备注**：只动了工具，未改 `index.html` 逻辑；本版号只出现在五处版本落点，均不在 `index.html` 内（`check-version.js` 第 3 项不涉及）。

**自验**：`node tools/check-all.js`（全量）**2479 PASS / 0 FAIL**，实跑 11/14 项、⊘ 3 项（ESLint / tsc 缺 node_modules、浏览器冒烟缺环境，均为正常态）；行覆盖率 **99.0%**（6692/6759，阈值 97%/90%）；死循环看门狗通过。版本号五处一致由 `check-version.js` 与 `check-docs.js` 强制。

## v2.8.20 · 文档 P1-9：wrangler.jsonc 注释写「无 `.nvmrc`」，而 `.nvmrc` 早在 v2.8.8 已入库（2026-09-21）

**来源**：按 `docs/AUDIT-2026-09-21.md` 依 P0→P3 逐条落地的第 10 条（P1 组收尾完成）。本节只落这一条，独立成版与提交。

- **P1-9 wrangler.jsonc 的「最后一处不受仓库控制」把已闭合的洞重新说成开放项**：
  - **根因**：`wrangler.jsonc:44-45` 注释仍写「Workers Builds 的 Node 版本仓库侧仍未声明（无 `.nvmrc`），目前依赖平台默认值」，但 `.nvmrc`（内容 `22`）已在 **v2.8.8（f43d5aa）** 入库，且 `.github/workflows/ci.yml` 用它钉 CI 的 Node（两处 job 的 `node-version-file: .nvmrc`）。wrangler.jsonc 最后改动停在 v2.8.5，此后的 v2.8.8 变更没回填到这段注释。
  - **后果**：把「最后一处不受仓库控制的事实」这个**已闭合**的洞重新描述成开放项，下一个审计者会按它再查一遍，属于显式误导。
  - **修法（报告的最小修复）**：改写为「同一张清单上的**最后一处已闭合**：Node 版本。v2.8.8 起 `.nvmrc`（内容 `22`）已入库，`ci.yml` 显式消费（`node-version-file`），Workers Builds 构建镜像亦读 `.nvmrc`（**需实测**确认 Cloudflare 构建镜像确实兑现）」——保留报告要求的「需实测」限定，不把平台侧行为说成已核实。
- **备注**：本次未改 `index.html` 逻辑；本版号只出现在上述五处版本落点，均不在 `index.html` 内（`check-version.js` 第 3 项不涉及）。

**自验**：`node tools/check-all.js`（全量）**2479 PASS / 0 FAIL**，实跑 11/14 项、⊘ 3 项（ESLint / tsc 缺 node_modules、浏览器冒烟缺环境，均为正常态）；行覆盖率 **99.0%**（6692/6759，阈值 97%/90%）；死循环看门狗通过。版本号五处一致由 `check-version.js` 与 `check-docs.js` 强制。

## v2.8.19 · 文档 P1-8：AGENTS.md 写「16 个 IIFE 模块」，实际为 14（2026-09-21）

**来源**：按 `docs/AUDIT-2026-09-21.md` 依 P0→P3 逐条落地的第 9 条（P1 组收尾）。本节只落这一条，独立成版与提交。

- **P1-8 AGENTS.md 的 IIFE 模块数错了 2 个，且来源正是它自己禁止的「凭旧文档记忆」**：
  - **根因**：`AGENTS.md:7`（一句话项目画像）写着「16 个 IIFE 模块」，而实际是 **14 个**——`tools/check-module-order.js` 的 `EXPECTED_ORDER` 与 `index.html` 头部模块索引一致为 Store → Modal → Viz → AudioEngine → Trainer → Controls → Tracks → Presets → Editor → Stats → Ear → Arrange → Help → KeepAlive（14 模块 + 数据/共享状态/初始化 3 个区块）。"16" 是从 `spec.md` / `tasks.md` C5 / `checklist.md`（均为 2026-09-17 审计、基线 v2.0.2 的历史快照）抄来的旧数。
  - **后果**：AGENTS.md 是写给所有 AI 会话的常驻入口，且它自己第 1 节明令「**不要凭训练记忆描述项目状态**」——这条数字恰恰是凭旧文档记忆写入的，属于自我打脸式误导。
  - **修法（报告的最小修复）**：改为「14 个 IIFE 模块：Store → … → KeepAlive，权威清单见 `tools/check-module-order.js` 的 `EXPECTED_ORDER`」，并顺带把 14 个名字按声明顺序列出，读者一眼可核。**不改** `spec.md` / `tasks.md` / `checklist.md`——它们是带日期的历史快照（审计报告亦如此定性），改它们等于篡改快照。
  - **备注（同批发现的、未在本条范围内的漂移）**：`tools/check-wiring.js:3` 写「本项目 15 个 IIFE 模块」、`.github/workflows/ci.yml:6` 写「全部 12 步自验」（实际 14 步）——两处均不在 P1-8 的报告条目内，遵「只处理当前这一条」暂不动，已在汇报里单列供决策。

**自验**：`node tools/check-all.js`（全量）**2479 PASS / 0 FAIL**，实跑 11/14 项、⊘ 3 项（ESLint / tsc 缺 node_modules、浏览器冒烟缺环境，均为正常态）；行覆盖率 **99.0%**（6692/6759，阈值 97%/90%）；死循环看门狗通过。

## v2.8.18 · 文档/评论 P1-7：四份文件仍写「本项目没有 CI」，与 v2.8.8 落地的仓库内 CI 相反（2026-09-21）

**来源**：按 `docs/AUDIT-2026-09-21.md` 依 P0→P3 逐条落地的第 8 条（P1 组）。本节只落这一条，独立成版与提交。

- **P1-7 「不用 GitHub Actions」口径已过时，四份文件与事实相反**：
  - **根因**：v2.8.8 已落地 `.github/workflows/ci.yml`（`README.md` 亦明说「v2.8.8 起有 CI 了」），但另有四处仍沿用旧口径宣称「本项目没有 CI / 不再依赖 GitHub Actions」：`docs/DEVELOPMENT.md:764`（标题就叫「为什么不用 GitHub Actions」）、`tools/check-all.js:3`、`tools/install-hooks.sh:3`、`tests/README.md:16`。DEVELOPMENT.md 是 `AGENTS.md` 指定的第 2 必读文档，误导权重最高；而 `check-docs.js` 现有七条规则没有一条能抓这种「机制级措辞漂移」。
  - **修法（报告的最小修复）**：四处统一改写为「本地 check-all 为主入口、仓库内 CI（v2.8.8 起）为补位」，并点明 CI 覆盖「所有分支与 PR」、是补位而非替代。改写均在原行内完成，**未增删任何行**（避免 DEVELOPMENT.md 行号漂移触发「模块索引行号 ↔ 实际 banner」检查）。
  - **取舍**：报告另附一条「（可选）给 check-docs 加『workflows 存在性 ↔ 文档措辞』规则」，本轮**未采纳**——它属新增闸门（非缺陷修复），会影响 check-docs 的规则计数与汇总口径，宜单独评估；本次先闭合四处的措辞漂移。
- **备注**：本次未改 `index.html` 的逻辑；`v2.8.18` 前向标号只出现在上述四份文件（均不在 `index.html` 内，`check-version.js` 第 3 项不涉及）。

**自验**：`node tools/check-all.js`（全量）**2479 PASS / 0 FAIL**，实跑 11/14 项、⊘ 3 项（ESLint / tsc 缺 node_modules、浏览器冒烟缺环境，均为正常态）；行覆盖率 **99.0%**（6692/6759，阈值 97%/90%）；死循环看门狗通过。

## v2.8.17 · 架构闸门 P1-5：R1 判据改为「不在任何函数体内」，堵住「顶层块内可被白名单放行」的口子（2026-09-21）

**来源**：按 `docs/AUDIT-2026-09-21.md` 依 P0→P3 逐条落地的第 7 条（P1 组收尾）。本节只落这一条，独立成版与提交。

- **P1-5 R1 漏判：IIFE 顶层 `if`/`for` 块内的反向引用被误判为「运行时回调」**：
  - **根因**：`tools/check-module-order.js` 的 R1 原判据是「引用所在行的**括号深度** `===` 模块体基准深度」。IIFE 顶层一个 `if (...) { LaterModule.x(); }` 的花括号使该行深度变成 `bodyDepth + 1`，于是这类引用落到 R3（运行时回调）分支——而 R3 是**白名单可放行**的。但该语句就在 IIFE 求值期执行，与顶层直接调用 `LaterModule.x()` 的 TDZ 风险完全等同：加一条白名单理由即可让闸门放行一个真实的初始化期反向引用。
  - **修法（与报告字面方案一致）**：R1 判据改为「该引用**不在任何函数体内**」。新增 `funcDepth`——先逐 `{` 判定「是不是函数体」（`isFuncBrace`：`=>{` 的箭头体，或 `(...)` 前是 `if/for/while/switch/catch/with` 等控制关键字的块**不算**函数体，其余算），再逐字符累计「位置 i 之前已打开的函数体括号个数」。引用所在行首代码字符处 `funcDepth === 模块体基线 funcBase` ⇒ 不在任何函数内（含顶层 `if/for` 块内）⇒ R1；更深 ⇒ 在函数/回调内 ⇒ R3。块（`if/for/while/switch/catch`、对象字面量、class 体）一律按 R1 零例外处理。
  - **取舍**：报告另给了一个「保守替代」（模块体内、深度 ≤ `bodyDepth+1` 且不在函数内），实测**未采用**——函数体深度判据本身已能精确区分「块」与「函数」，替代方案在「顶层块里再套一个顶层块」时会漏，且两套深度判据并存更难维护。原 `braceDepths` 产物保留，仅用于括号配对自校验（`if (depths[last] !== 0) exit 4`）。
- **测试与反向验证**：真实 `index.html` 上闸门**仍全绿**（强化后的判据对现有 14 个模块零误报，R3 白名单条目数与 R4 扇出输出逐项不变）。反向验证：构造 `repro.html`，在 `Store` IIFE 顶层插一个 `if (typeof window !== "undefined") { Modal.__p15probe(); }`，并给旧闸门加一条「Store→Modal」白名单——**旧逻辑 exit 0**（白名单成功放行顶层块内引用，复现漏洞），**新逻辑 exit 1**（`✗ R1 初始化期反向引用：Store(L464) → Modal`）。0→1 的翻转证明修复确实堵住了该口子。
- **备注**：本次 `tools/check-module-order.js` 注释带 `v2.8.17` 前向标号，连同五处版本号一并落版（该文件不在 `index.html` 内，`check-version.js` 第 3 项不涉及）。

**自验**：`node tools/check-all.js`（全量）**2479 PASS / 0 FAIL**，实跑 11/14 项、⊘ 3 项（ESLint / tsc 缺 node_modules、浏览器冒烟缺环境，均为正常态）；行覆盖率 **99.0%**（6692/6759，阈值 97%/90%）；死循环看门狗通过。版本号五处一致由 `check-version.js` 与 `check-docs.js` 强制。

## v2.8.16 · 工程化 P2-1/P2-2：全量套件去重（覆盖率不再重跑一遍）+ 用例清单双向对账（2026-09-21）

**来源**：按 `docs/AUDIT-2026-09-21.md` 依 P0→P3 逐条落地的第 6 条（报告把两条并作一组「测试工程化」）。本节只落这一组，独立成版与提交。

- **P2-1 全量模式下最贵的那一遍被跑两次**：
  - **根因**：`tools/check-all.js` 第 12 步 `tests/run.js`（FULL_SCAN=1）与第 14 步 `check-coverage.js --full` 各自独立执行完整组合扫描——后者为采集 V8 覆盖率**又 spawn 了一遍同一套件**。全链最贵的部分（实测 13.9s）因此白跑两次。
  - **修法**：给 `check-coverage.js` 加 `--reuse=<dir>` 入口——直接分析上游已落盘的 V8 覆盖率区间、**跳过 spawn**；`check-all.js` 第 12 步带 `NODE_V8_COVERAGE=<共享临时目录>` 跑（同一遍既出测试结论又落盘区间），第 14 步用 `--reuse` 复用该目录，跑完清理。单独运行 `check-coverage.js`（不带 `--reuse`）行为完全不变。
  - **取舍**：覆盖率插桩在 FULL_SCAN 下区间累积约 2GB（会顶穿默认 old-space 上限），故第 12 步带上 `--max-old-space-size=4096`（该旗标原先在 check-coverage 的 spawn 里，现随「采集职责」一并前移）。实测第 14 步由「重跑整个套件」降为 **0.25s** 纯分析，覆盖率数字前后一致（99.0%）。
- **P2-2 两份手工清单会漂移、且漂移方向是静默的**：
  - **根因**：`tests/run.js` 的 `CASE_FILES`（执行顺序）与 `tests/cases/` 目录手工对账；`tests/hang-guard.js` 的 `CASES` 与 `tests/hang-case.js` 的分支实现手工对账。新增用例只改一处 → 要么**静默漏测**（PASS 数照旧好看），要么运行时才报「未知用例」。
  - **修法**：`tests/run.js` 启动时 `readdirSync("cases")` 与 `CASE_FILES` **双向对账**，任一方向漂移即 `exit 1`；`tests/hang-case.js` 作为清单实现真相源、新增 `--list` 子命令输出 `id|说明`，`hang-guard.js` 改为向它索要（拿不到清单按工具故障 `exit 4`），不再自带副本。
- **测试与反向验证**：P2-2 的对账闸门做了反向验证——临时新建一个未登记的 `tests/cases/*.js`，`run.js` 立即以「未登记进 CASE_FILES」`exit 1`（删除后恢复 45 个用例、全量复跑仍全绿）。`hang-case.js --list` 与消费方 `hang-guard.js` 结果一致（18 例，49 PASS / 0 FAIL）。P2-1 为等价重构：带插桩的第 12 步与 `--reuse` 的第 14 步跑出的覆盖率与旧路径逐位相同。
- **备注**：本次 `tools/check-coverage.js` / `tools/check-all.js` 注释带 `v2.8.16` 前向标号，连同五处版本号一并落版（`check-version.js` 只约束 `index.html` 的标号不高于 VERSION）。

**自验**：`node tools/check-all.js`（全量）**2479 PASS / 0 FAIL**，实跑 11/14 项、⊘ 3 项（ESLint / tsc 缺 node_modules、浏览器冒烟缺环境，均为正常态）；行覆盖率 **99.0%**（6692/6759，阈值 97%/90%）；死循环看门狗通过。版本号五处一致由 `check-version.js` 与 `check-docs.js` 强制。

## v2.8.15 · 修复 P1-2/P1-3/P1-4：三处「状态纪律」缺口（死状态表 / 绕过 setMode / 会话开关不复位）（2026-09-21）

**来源**：按 `docs/AUDIT-2026-09-21.md` 依 P0→P3 逐条落地的第 5 条（报告把三条并作一组「状态纪律」）。本节只落这一组，独立成版与提交。

- **P1-2 `subCellEls` 声明了、读了、却从未写入（v2.4.4 重构断头路）**：
  - **根因**：注释承诺「建网格时登记一次、帧内只读表」，但 `buildBarRow` 的刻度线循环从未写入它、`buildViz` 的重建清空列表也不含它，表永远是 `[]`。`paintBeatFlash` 里 `cellNode` 恒为 `undefined` → `onWhite` 恒 `false` → 十六分闪烁在**已弹白块**上永远走 `fc.soft`（淡光）而非设计的 `fc.strong`（强光）。
  - **修法**：`buildBarRow` 刻度线循环内补 `subCellEls[b][startT / T16 + u] = c;`；`buildViz` 重建处补 `subCellEls = []`。
- **P1-3 `loopLyricSection` 裸写 `S.playMode`，绕过 setMode 契约**：
  - **根因**：`setMode` 是 `playMode` 的唯一写入口（t66 契约），全文件其余 6 处都走它，唯独这里裸赋值——不进 `modeTrail`（迁移轨迹缺一条）、不触发 `onPlayModeChange`，`Tracks.syncTrackUI()` 不跑、轨说明文字不更新（与 v2.6.3/v2.6.4 修的「argJump 残留」同一类半状态）。
  - **修法**：改为 `setMode("playMode", "arrange", "歌词行循环")`。
  - **取舍**：报告建议「删掉后面冗余的 `refreshBar()`」，实测**保留**——`setMode` 对**同值**早退、不触发 `onPlayModeChange`（幂等），删掉会在「已是曲式模式再点循环本行」时丢刷新；故保留这处显式刷新。
- **P1-4 `Trainer.loopPerSec` 一旦置 true 永不复位**：
  - **根因**：`loopPerSec` 是模块级**会话**状态，`Trainer.reset()` 不复位它；此后任何曲式模式下的普通变速训练都悄悄按「整段一级」而非面板的「每 N 小节一级」爬坡，无界面提示。
  - **修法（与报告字面方案不同，见取舍）**：`Trainer.reset()` 复位 `loopPerSec = false`；把「按歌词行循环」的意图**持久化**到 `S.arrangeSel.byLyric`（`loopLyricSection` 置 true；跳段/跳演示段/退出预设/改范围/换型等非歌词入口置 false）；`Controls.start()` 在 `reset()` 之后按 `byLyric` 重推 `setLoopPerSec`。两条路径（播放中调用 / 开播前调用）由同一意图源 `byLyric` 兜底。
  - **取舍**：报告字面方案（只在 `reset()` 里加 `loopPerSec = false`）**实测会让 T61b/c/d 变红**——「循环本行」通常在按下播放**之前**点，其 `setLoopPerSec(true)` 必然被 `Controls.start()` 里的 `Trainer.reset()` 抹掉，功能当场失效。隔离实验证得：仅上 P1-2/P1-3 时 2476 PASS / 0 FAIL，加上字面 P1-4 后 T61 三例转红；故改为「意图持久化 + 开播重推」。代价是 `arrangeSel` 增加一个字段（热键体积仍 < 1 KB），换来两条路径语义一致。
- **测试与反向验证**：新增 T61e 钉住 `byLyric` 的写入 / 非歌词入口清除 / 开播重推 / 不跨会话泄漏（非歌词入口仍按 `everyN` 升一级）；既有 T61b/c/d 与 T51f、T68g/h/k 同步新字段。反向验证：把 P1-4 回退成报告字面方案（仅 `reset()` 复位），T61b/c/d 立即变红——证明修复确为目标缺陷。
- **备注**：本次代码注释带 `v2.8.15` 前向标号，按 `check-version.js` 第 3 项口径，连同五处版本号一并落版再验收。

**自验**：`node tools/check-all.js`（全量）**2479 PASS / 0 FAIL**，实跑 11/14 项、⊘ 3 项（ESLint / tsc 缺 node_modules、浏览器冒烟缺环境，均为正常态）；行覆盖率 **99.0%**（6692/6759，阈值 97%/90%）；死循环看门狗通过。版本号五处一致由 `check-version.js` 与 `check-docs.js` 强制。

## v2.8.14 · 修复 P1-6：工具故障退出码不统一，检查器自身故障被误记为「未通过」并中止后续步骤（2026-09-21）

**来源**：按 `docs/AUDIT-2026-09-21.md` 依 P0→P3 逐条落地的第 4 条。本节只落这一条，独立成版与提交。

- **根因**：`tools/check-all.js` 只认 `TOOL_FAIL_CODE = 4` 为「工具故障」（记成 ⊘ 未被验证、**不中止**后续步骤），
  但各检查器自身的故障路径却各用各的退出码——`check-tsc` / `check-eslint` / `check-coverage` /
  `check-dom-ids` / `check-module-order` / `check-lint` 的「找不到 script 块 / 括号不配平 / 输出无法解析」
  等自故障统统自报 **2**，`check-module-order` 另有两处甚至自报 **1**。于是「工具故障」被 `check-all`
  误判成「✗ 未通过（退出码 2/1）」，**并中止它之后的所有步骤**——一次与代码无关的解析失败能把整套自验拦腰截断。
  更隐蔽的是 `check-dom-ids` / `check-lint` / `check-wiring`：`extractScript` 在前置位置**直接抛错**，
  连自报退出码的机会都没有，Node 默认以退出码 1 收场，同样伪装成「失败」。
- **修法**：按报告最小修复的第一方案，把全部检查器的「自身故障」路径统一到 **4**（`TOOL_FAIL_CODE`）——
  `scan-util.js` 注释、六个检查器的文件头退出码口径与全部自故障分支一并改齐；
  对 `extractScript` 会抛错的三个检查器（`check-dom-ids` / `check-lint` / `check-wiring`）补 try/catch，
  捕获后打印「这是工具故障，不是违规」并 `exit(4)`；`.catch` 型异步故障（`check-eslint`）同样归到 4。
- **取舍**：选「统一到 4」而不是「让 check-all 把 2 也当故障」——只保留一个故障码，口径唯一、无歧义；
  代价是要动 6 个文件（而非改 check-all 一行），换来的是「任何检查器故障都走同一条 ⊘ 通道」。
- **测试与反向验证**：以无 `<script>` 块的副本 `noscript.html` 喂入，对比 git HEAD 与工作区——
  `check-module-order` / `check-dom-ids` / `check-lint` / `check-wiring` 全部由 **exit 1 → exit 4**；
  真实 `index.html` 仍全部 exit 0。仓库既有测试无一处断言检查器退出码，故不新增用例、以反向脚本为准（数据见上）。
- **备注**：本次代码注释带 `v2.8.14` 前向标号（`check-version.js` 第 3 项只扫 index.html，工具注释不受影响），
  仍按该守卫的既定修法**连同五处版本号一并落版再验收**。

**自验**：`node tools/check-all.js`（全量）**2476 PASS / 0 FAIL**，实跑 11/14 项、⊘ 3 项
（ESLint / tsc 缺 node_modules、浏览器冒烟缺环境，均为正常态）；行覆盖率 **99.0%**（6674/6741，阈值 97%/90%）；
死循环看门狗 49 PASS / 0 FAIL。版本号五处一致由 `check-version.js` 与 `check-docs.js` 强制。

## v2.8.13 · 修复 P1-1：wakeLock「授予晚于释放」竞态，保活开关会「关了但没关」（2026-09-21）

**来源**：按 `docs/AUDIT-2026-09-21.md` 依 P0→P3 逐条落地的第 3 条。本节只落这一条，独立成版与提交。

- **根因**：`KeepAlive.acquire()` 里 `navigator.wakeLock.request("screen")` 是**异步**的，而 `.then(l => { lock = l; … })`
  无条件把落定结果写进 `lock`。若用户在 Promise 落定前停止播放 / 关掉开关，`release()` 执行时 `lock` 还是 `null`
  （空操作收场）；随后 Promise resolve、`lock = l`——**屏幕唤醒锁永久持有**到页面隐藏被系统回收。
- **后果**：保活开关「关了但没关」，耗电与系统策略代价在用户以为已关闭时继续发生。与 v2.8.8 修的 CSP 静默废保活
  是同一功能区的**对称缺陷**（那边是"起不来不可见"，这边是"关不掉不可见"）。
- **修法**：落定后先按 `sync()` 的同一条判据再问一次"当前是否还需要"——`if (!(S.keepAwake && S.playing)){ l.release(); return; }`，
  不需要就地释放；对称地，被拒后的降级落定（`.catch`）也补同一条判据，避免过期的拒绝偷偷起静音音频兜底。
- **取舍**：判据复用 `sync()` 的 `S.keepAwake && S.playing`，不引入新的"是否该持有"状态源——两个写入口共用一条真值条件，
  日后改一处不会又裂一处。代价：极端场景（落定前 stop→start 连点）下两个并发 request 可能各自落定、
  先落定的锁会被后落定覆盖而短暂泄漏；该窗口需连点在毫秒级完成，且下次 `sync()`/页面隐藏即回收，本次不引入代际令牌（留观测）。
- **测试与反向验证**：`tests/cases/t37-stats-and-training.js` 新增场景 T42b（6 条断言，3 条带 ★ 为反向锚点）：
  用手动可控 thenable 让 request **落定晚于** release——旧实现 `state().locked` 恒为 true，锚点立即变红；
  并对称覆盖"被拒的降级落定已过期，不得再起音频兜底"。
- **备注**：本次代码注释带 `v2.8.13` 前向标号，`check-version.js` 的版本漂移守卫据此要求先 bump——
  故本条按该守卫的既定修法**连同五处版本号一并落版再验收**（守卫会拒收"代码含该版改动却没 bump"）。

**自验**：`node tools/check-all.js`（全量）**2476 PASS / 0 FAIL**，实跑 11/14 项、⊘ 3 项
（ESLint / tsc 缺 node_modules、浏览器冒烟缺环境，均为正常态）；行覆盖率 **99.0%**（6670/6737，阈值 97%/90%）；
死循环看门狗 49 PASS / 0 FAIL。版本号五处一致由 `check-version.js` 与 `check-docs.js` 强制。

## v2.8.12 · 修复 P0-2：`check-module-order.js` 对新模块失明，新增模块可带 TDZ 风险全绿通过（2026-09-21）

**来源**：按 `docs/AUDIT-2026-09-21.md` 依 P0→P3 逐条落地的第 2 条（该项已在报告中实测复现）。本节只落这一条，独立成版与提交。

- **根因**：检查器收集模块时用 `if (!EXPECTED_ORDER.includes(name)) continue;` 把正则命中的**不认识的名字整个剔除**，
  而第 0 步又只拿"剔除后剩下的模块"去比对声明顺序——两处都只看 `EXPECTED_ORDER` 里那几个名字，
  于是**任何新模块对检查器完全不可见**：它即便在 IIFE 顶层反向引用了后方模块（真 TDZ 风险），R1 也一律全绿通过。
  实测复现：往副本注入 `const ZzNewMod = (() => { const leak = Controls.boom; })();`，旧实现照常「通过」。
- **修法**：收集循环改为把正则命中的**全部**名字记进 `matchedNames`（`const X = (() => {` 名集合），
  第 0 步做**双向 diff**：命中但既不在 `EXPECTED_ORDER`、也不在新增登记表 `NON_MODULE_IIFE` 的一律报错
  （与 `gen-index.js`「只报错不猜」同约定）；反向——`NON_MODULE_IIFE` 里登记了、代码里却没命中——同样报错，
  防止登记表腐烂成"什么都放行"。
- **取舍**：正则还会命中非架构模块的 IIFE（目前仅初始化段的 `diagOn`——调试开关求值，不参与模块间通信），
  故不能简单"多出即报错"，而设一张显式登记表 `NON_MODULE_IIFE`：非模块 IIFE 必须登记并写明理由，
  二者之外的**多出者才报错**。新增模块因此无法再"悄悄溜进"——要么进 `EXPECTED_ORDER` 接受 R1–R4 约束，要么显式声明自己不是模块。
- **测试与反向验证**：工具链无独立测试框架（架构闸门不在 `tests/cases/` 覆盖内），故做**人工反向验证**：
  · 真文件 → 新增一行 `✓ 正则命中的模块名集合与 EXPECTED_ORDER 双向一致（无非模块逃逸）`，退出码 0；
  · 注入 `ZzNewMod` 的副本 → `✗ 新模块逃逸：ZzNewMod(L462) …`，退出码 1（旧实现此处为"通过/0"）。
  盲区已堵：新模块现在只有两种下场——被约束，或被显式声明为非模块。

**自验**：`node tools/check-all.js`（全量）**2470 PASS / 0 FAIL**，实跑 11/14 项、⊘ 3 项
（ESLint / tsc 缺 node_modules、浏览器冒烟缺环境，均为正常态）；行覆盖率 **99.0%**（6670/6737，阈值 97%/90%）；
死循环看门狗 49 PASS / 0 FAIL。版本号五处一致由 `check-version.js` 与 `check-docs.js` 强制。

## v2.8.11 · 修复 P0-1：`importAll` 合并练习记录校验错字段名，会静默丢弃全部记录（2026-09-21）

**来源**：按 `docs/AUDIT-2026-09-21.md` 依 P0→P3 逐条落地的第 1 条（该项已在报告中人工复核确认）。本节只落这一条，独立成版与提交。

- **根因**：`Store.importAll` 合并练习记录时把校验字段写成 `r.start`，而记录自 v1.4 起字段就叫 `t`
  （加载路径 `KEY_LOG` 归一化、`appendSession`、`serializeLog` 用的都是 `t`）。字段名对不上 → 每条记录都在
  首个守卫处被判为「缺时间戳」而丢弃 → 「导出全部 → 导入全部」后 `added.log` 恒为 0，
  跨设备迁移时练习记录**整份静默消失**（而记录恰是重做代价最高的一类数据）。
- **修法**：
  ① `isNum(r.start)` → `isNum(r.t)`，并补 `r.sec > 0`（与加载路径归一化同口径）；
  ② `sessions.push(r)` 换成与加载路径相同的白名单归一化 `{ t, sec, bpm:numOr(r.bpm,0), name:…slice(0,40) }`
     ——外部包不再能把任意键塞进冷键；
  ③ 合并后 `sessions.sort((a,b)=>a.t-b.t)` 再按 `logMax` 截断，与 `appendSession`「尾部 push、超限 shift 头部」
     的方位一致，保留的确实是**最近**的 400 场。
- **取舍**：沿用加载路径口径（`sec>0`、白名单）而非只改字段名——同一份数据在「冷键读取」与「全量导入」两条路上
  判据必须一致，否则日后改一处漏一处还会再裂；代价是 `bpm`/`name` 缺省补值（本就是缺省语义，无信息损失）。
- **测试与反向验证**：`tests/cases/t83-full-data-pack.js` 的夹具此前**同步抄了 bug**（写 `log:[{start:…}]`），
  故 T82b 断言 `added.log===1` 长期假绿。本次把夹具改成 `t:`（T82b 与 T82f 的 450 条批量各一处），
  并新增 5 条锚点：记录真落进 `logSessions`、`sec` 保真、`bpm`/`name` 归一化缺省、以及「只带 `start` 键的记录必须被丢弃」。
  **反向验证**：把产品侧改回 `r.start`，`added.log===1` 与「按 t 找得到」两条立即变红——盲区已堵。
  索引行号随本次改动漂移 8 行，已 `node tools/gen-index.js --write` 回写。

**自验**：`node tools/check-all.js`（全量）**2470 PASS / 0 FAIL**，实跑 11/14 项、⊘ 3 项
（ESLint / tsc 缺 node_modules、浏览器冒烟缺环境，均为正常态）；行覆盖率 **99.0%**（6670/6737，阈值 97%/90%）；
死循环看门狗 49 PASS / 0 FAIL。版本号五处一致由 `check-version.js` 与 `check-docs.js` 强制。

## v2.8.10 · 落盘 2026-09-21 全仓代码改进诊断报告（docs/AUDIT-2026-09-21.md）（2026-09-21）

**来源**：应要求对整仓做了一次只读代码改进诊断（三方并行：主应用逐段通读 / tools+tests 工程链路含临时副本实测复现 / 文档与配置一致性；P0 条目经人工复核确认，未改任何代码）。本版把诊断报告落盘进仓库，供后续按批次立项修复。

- 新增 `docs/AUDIT-2026-09-21.md`：P0×2 / P1×9 / P2×9 / P3 若干 + 修复优先级表，每条带文件+行号+根因+最小修复方案。
  文件头部按 tasks.md 同款惯例加「历史快照」横幅——行号与覆盖率是基线 v2.8.9 的当时值，修复状态以本文件为准。
- 报告要点（详细论证见报告本体，这里只登记结论）：
  - **P0-1（已人工复核确认）**：`Store.importAll` 合并练习记录时校验字段写成 `r.start`，而记录字段从来叫 `t`
    ——「导出全部 → 导入全部」会**静默丢弃全部练习记录**（v2.8.8 新功能的自毁按钮，落在测试与闸门的共同盲区）。
  - **P0-2（实测复现）**：`check-module-order.js` 把不在 EXPECTED_ORDER 里的新模块整个剔除，
    新增模块可携带 TDZ 风险全绿通过。
  - 其余为闸门判定漏洞（R1 深度判据、退出码 2/4 不统一）、状态纪律（wakeLock 竞态 / subCellEls 死状态 /
    setMode 契约破口 / loopPerSec 不复位）与文档漂移（AGENTS.md「16 个模块」实为 14 等）。
- 纯文档版：产品代码、自验链、上站产物一字未动。仍然 bump 版号的理由同 v2.8.4/v2.8.9——
  推送触发 Cloudflare 构建，线上版本号变为 v2.8.10 即「本次构建真的跑了」的直接证据。

**自验**：本版未触及任何产品代码与检查逻辑。`node tools/check-all.js`（全量）12/14 步通过、
⊘ 2 项（ESLint / tsc 缺 node_modules，可选加强项，正常态）；行覆盖率 99.0%（阈值 97%/90%）。
版本号五处一致由 `check-version.js` 与 `check-docs.js` 强制。

## v2.8.9 · 新增 AGENTS.md：把 AI 协作约定固化进仓库（2026-09-21）

**来源**：与 AI 编码助手协作开发时的提示词约定，固化成仓库级常驻指令文件——后续每个新会话自动生效，不必再逐次重复约束，也避免不同会话各自发挥。

- 新增 `AGENTS.md`（仓库根），五个部分：
  ① 项目画像与红线（单文件、运行时零依赖、`file://` 直开）；
  ② 动手前必读清单（README → DEVELOPMENT.md → CHANGELOG → tasks.md）与「不凭记忆报版本号/测试数」；
  ③ 改动纪律（check-all 全绿、R1–R4 闸门、测试+反向验证、发版四处版本号一致、数字不抄进文档）；
  ④ 提交纪律（默认不 push、commit message 的根因→修法→取舍→自验风格）与禁区（`_headers`、闸门判定逻辑、视觉类改动需人眼验收）；
  ⑤ 任务分流四模式（诊断/修复/新功能/规划）与环境约定（可选步骤 ⊘ 是正常态、沙箱 EBUSY 走工具故障路径）。
- 纯工程版：产品代码、自验链、上站产物一字未动。
- 为什么仍然 bump 版号：既定规则每次发版都 bump，工程版不例外；且推送触发 Cloudflare 构建，
  带版号才能让线上版本号从 v2.8.8 变为 v2.8.9，作为「本次构建真的跑了」的直接证据（同 v2.8.4 的理由）。

**自验**：本版未触及任何产品代码与检查逻辑。版本号五处一致——`index.html` 的 `const VERSION` /
package.json / package-lock.json 两处 / README「当前 vX.Y.Z」/ 本条 CHANGELOG，由 `check-version.js` 与 `check-docs.js` 强制。

## v2.8.8 · 全仓审计落地：CSP 静默废掉后台保活 / 架构闸门不再依赖缩进 / 仓库内 CI / 全量数据包（2026-09-21）

**来源**：对整仓做的一次全量代码审计（5 维度：代码质量 / 性能 / 架构 / 安全 / 工程化）。本版落地其中优先级最高的 5 项；审计报告里其余条目按「近期 / 规划 / 有余力」分批，未在本版动。

### 一、★ 修一个真实的静默失效：`_headers` 缺 `media-src`，后台保活在 Cloudflare 线上被 CSP 拦掉

- 现象链：`KeepAlive` 有两级能力——① `navigator.wakeLock`（Chrome/Edge 系可用）；② 无 Wake Lock 时降级为
  **循环播放一段程序生成的静音 WAV**，而它是一枚 `data:audio/wav;base64,…` URL。
  `_headers` 的 CSP 此前**没有 `media-src`**，于是它回落到 `default-src 'self'`——而 `data:` **不在 `'self'` 内**。
  结果：桌面 Firefox / Safari / 老 Android 这些没有 Wake Lock 的浏览器，在线上这段兜底音频被 CSP 拦掉，
  **后台保活静默失效**；且失败被 `.catch(() => {})` 吞掉，诊断面板上一个数字都没有。
- 修两处（缺一不可，只修一处仍然静默）：
  ① `_headers` 补 `media-src 'self' data:`（顺带补此前同样缺失、同样回落到 `default-src` 的 `object-src 'none'`）；
  ② `KeepAlive.useAudioFallback()` 的三个失败出口（无 play / 抛错 / play 被拒）不再空吞，
     统一经新钩子 `onKeepAliveFail` 报一次 → 装配层接进 `diag.keepAliveFail` + 一条 `console.warn`。
- 与 `onReanchor` 同一类做法：**不做处置，只让"只在暗处发生的事"变成可读数字**。
  保活失败意味着"切到后台可能被节流"，但那是环境能力问题，弹窗只会打扰用户；让它出现在诊断面板与
  「复制诊断信息」里，用户报障时就能一句话指认原因。
- 面板口径：该计数**只在非 0 时**占一行读数（与戳记两档同一条纪律）——有 Wake Lock 时它恒为 0，
  恒 0 是正常态，不该为两个 0 买单；一旦非 0 就是"后台没保住"的确证。

### 二、★ 架构闸门不再依赖缩进（`index.html` 自此可以用 prettier 了）

- 旧实现：`tools/check-module-order.js` 的 R1 用「IIFE 顶层 = **恰好 2 空格缩进**」判定语句在哪一层。
  代价是 `docs/DEVELOPMENT.md` §5 明文规定「index.html 禁止过格式化器」——prettier 一次全文件重排
  就会让这条架构闸门**静默失效**。把架构约束押在"永不格式化"这条约定上，是典型的形式依赖结构。
- 新实现：改成**括号深度**——先 `blankNonCode()`（新增，scan-util）抹掉注释与字符串内容，再逐字符数 `{`/`}`，
  判定"这一行在模块体的第几层"。与缩进、换行、是否格式化**完全无关**。
- 三条验证（都做了，不是声称）：
  ① 改后输出与改前**逐字一致**（R1/R2/R3/R4 结论不变）；
  ② 把整份 `index.html` 的缩进**翻倍**再跑，输出仍逐字一致；
  ③ 故意注入一条「缩进 6 空格、但结构上在 IIFE 顶层」的反向引用，新判定仍正确报 R1
     （旧判定会把它误判成 R3，若恰好在白名单里就**静默放过**）。
- 新增一条自检：整段脚本跑完深度必须回到 0，否则 **exit 2** 报错退出——宁可报错，不可带着错位的深度表给结论
  （与 `check-lint.js` 的"括号必须配平"自检同源）。
- 顺带修掉 R2 的一个**假阴性**：旧写法用「剥过注释的行内下标 + 原始行起点」算字符偏移，两者不同源，
  算出来的偏移系统性偏小（剥掉的注释越长偏得越多），R2 可能把真命中判成"不在热路径里"。现改按行区间判定。
- 纪律不变：**往帧路径抽 helper 时，名字仍必须进 `HOT` 名单**（v2.4.4 已定）。

### 三、★ 补仓库内 CI（此前唯一的硬闸门在仓库外）

- 问题：仓库内**没有任何 CI**。唯一的硬闸门是 Cloudflare Dashboard 里手工填的一行 `npm run ci`——
  改不动、review 不了、diff 不出来，而且只拦"推到 main 触发的那次构建"：任何协作者都能直接 push 绕开
  全部 14 步自验；WorkBuddy 那条手动发布通道更是零拦截。
- 新增 `.github/workflows/ci.yml`：两个 job——`verify`（`npm run ci`）与 `smoke`（真实浏览器冒烟）。
  命令**一律复用 `package.json` 已有的 `npm run ci`**，不在 workflow 里重写那串命令（抄一份就等于
  把三个文件路径钉在第二个地方，改脚本名或挪路径时两边都会静默漂移）。
- **浏览器冒烟单开一个 job**，这是本版的重点之一：check-all.js 里那步是"环境可选"，而构建镜像里必然没有
  浏览器——于是**唯一能抓真实布局/样式/帧率/SW 的那一步，在部署链路上恒为 0 次**。放进 CI 后它终于会真的跑。
- 新增 `.nvmrc`（`22`）：Workers Builds 的 Node 版本此前由平台默认值决定，`wrangler.jsonc` 文件头
  已把这一处记在"下一处待办"清单上，本版一并声明。
- 钩子自动安装：`package.json` 加 `prepare` → `node tools/install-hooks.js`（Node 实现，跨平台）。
  `core.hooksPath` 是**本机配置、不随仓库走**，此前每个 clone 都要手动跑一次 `sh tools/install-hooks.sh`
  （且那是 POSIX shell，Windows 下跑不了）。挂在 prepare 上之后，clone 完任何一次 `npm install` 都会顺带装好。
  脚本刻意**不会**让 npm install 失败：不在 git 仓库里 / 配置写不进时只打印提示并正常退出。

### 四、★ 全量数据包导出导入（曲式 / 歌词 / 记录 / 战绩不再只能存本机）

- 问题：`beatsight.arranges` / `beatsight.lyrics` / `beatsight.log` / `beatsight.ear` 四个冷键此前**只能存本机**，
  只有预设能跨设备带走。而 README 明确写了 `file://` 与 `https://` 是两个 origin、localStorage 不共享——
  换设备或换通道，这几类数据**整份消失**。它们恰好是重做代价最高的一类：曲式逐段编、歌词逐字对到
  十六分格、练习记录攒几个月——比预设更值得有备份通道。
- 新增 `Store.serializeAll / exportAll / importAll`，侧栏预设卡加「导出全部数据 / 导入全部数据」。
  放在预设按钮同一行是刻意的：它导入的**包含**预设，并排也让两者差别一目了然。
- 三条校验口径**各自沿用既有路径，不另创一套**：
  · 预设 → `validatePreset` 严格校验 + 同一组护栏，**任一坏条目整批拒绝**（同 `importPresets`）；
  · 曲式 / 歌词 → 复用加载期的 `normArrange` / `normLyricLine`，坏条目**丢弃并计数**（同它们落盘时的口径）；
  · 记录 / 战绩 → 取并集后按 `logMax` 截断 / 逐字段取较大值（它们是累计量）。
- 导入是**合并而非替换**（撞 id / 撞 (曲式,段) 时以本机已有为准）——替换会静默抹掉用户正在用的那一份，
  而这类数据丢了没法撤销。老预设包（`kind:"presets"`）也能被吃进来：用户手上只有那份 JSON 时，
  不该逼他"先去找另一种导出"。

### 五、收敛两处长期债

- **合并两份扫弦判据**：`Store.strumOf`（加载期用）与共享区 `hasStrum`（运行期用）是同一判据的两份实现，
  此前靠 `t64` 的两条断言同时钉住。而它**只依赖 `pat.bars`、不查任何状态**，上移到数据区（Store 之前）即可
  两边同调一份——"共享区在 Store 之后够不着"从来不是真理由。对照：`patLenOf` 要查已落盘的型，所以它
  **必须**留在 Store 里（上移与否看的是"要不要状态"，不是"加载期能不能调"）。
- **`window.__beat` 改为条件挂载**：此前无条件把 13 个模块（含 `Store`、`clock()`、`onsetBuf()`）挂到 window。
  改为只在 `?debug=1` 或宿主预置 `window.BEATSIGHT_TEST`（测试沙箱）时挂载；生产环境只留
  `window.__beatBoot`（只含 `version` / `booted` 两个只读标量）。
  ★ 由此牵出**两处必须同步改的地方**（都是"同一个默认行为有两份沙箱实现"的典型：
    判断依据是「谁构造了沙箱」，不是「谁在用沙箱」）：
    · `tools/smoke.js` 原先**等到 `window.__beat` 就判定"启动成功"**——句柄一收起，它就会等满 4 秒
      然后把"句柄已收起"误报成"应用白屏"。启动探针与内部句柄的用途就此拆开：前者恒存在、
      用来判白屏；后者只在调试/测试态、用来驱动性能读数（smoke 两条通道改带 `?debug=1`）。
    · `tests/hang-case.js` **自带一份私有沙箱、不走 `tests/lib/harness.js`**——harness 里加的
      `BEATSIGHT_TEST` 标记覆盖不到它，漏了就会让看门狗**每个用例都以
      "Cannot read properties of undefined" 崩掉**（本地实测 0 PASS / 18 FAIL）。
      已在该私有沙箱里同样声明标记；四类用例（正常路径 / 试听清空小节 / 脏数据 / 饥饿兜底）
      单独跑均为 PASS。

- **修 `tools/gen-index.js` 的自动回写死循环**（本版顺手撞见）：它的回写条件是
  `problems.every(p => /偏 \d+ 行/.test(p))`，而 `\d+` **匹配不了负号** —— 于是
  "索引行号比实际 banner **大**"（偏移为负，删掉一行注释就会让其后所有模块整体 −1）
  这一整类漂移**永远进不了回写分支**：脚本打印一份完全正确的诊断，然后给出"修法：
  `node tools/gen-index.js --write`" —— 而那条命令跑出来还是同一份诊断（**修法指向的正是刚刚失败的那件事**）。
  已改为 `偏 -?\d+ 行`。本版的实际触发：删掉一行悬空 JSDoc 后 14 条索引整体偏 −1。

**未在本版动（审计里明确记录，按批次另做）**：冷键 `persistCold()` 防抖（代码注释里"立即写，不防抖——丢不起"
的理由仍然成立，且 t74/t13 依赖其同步性；真正的缓解是 500 条上限与将来的"存储占用面板"，不是拿数据安全换
毫秒级收益）；分支覆盖 / 变异测试自动化；`Controls` 扇出 7 的注册表化；源码拆 `src/*.js` + 构建期内联。

**自验（在 v2.8.7 之上实测）**：`node tests/run.js` **2465 PASS / 0 FAIL**（新增 t82 保活兜底 / t83 全量数据包）；
`node tools/check-module-order.js` 输出与 v2.8.7 原判定**逐字一致**；真实浏览器冒烟 **31 项断言全通过**（
file:// 与 http://127.0.0.1 两条通道）；架构 / 代码卫生 / 版本 / 文档 / DOM 引用五项闸门全绿。

## v2.8.7 · 让"巧合"变成"声明"：`_headers` 结构闸门 + 性能预算 + SW 可观测（2026-09-21）

**来源**：v2.8.2 全维度审计报告 §四 行动清单，第二批——按「清单里『功能』类型之外的事项全部完成」
的指令，本版对应 §S1（安全）、§F11（基建）、§Q4（工程化）三行。

### 一、`_headers`：一个"生效纯属巧合"的配置，现在把话说清楚了（§S1）

- **审计点出的两个问题，实测后只有一个成立**：
  - ① **成立**：`_headers` 里那个 `/*` **不是 C 风格注释的开头**（`_headers` 规范只认 `#` 为注释），
    它的真实身份是**匹配所有路径的路径 glob**；6 条安全头能作用于全站，靠的正是它。
    于是"删掉 `/*` → 6 条头全部失去作用域"这条路径**既无告警也无报错**，只有打开 DevTools
    逐条看响应头才会发现。这是典型的"最坏情况无告警"陷阱，且与本项目反复批评的"隐性依赖"同类。
  - ② **不成立**：审计称"收尾的 `*/` 会被解析成一条无意义的路径规则（`*/`）"。实测
    `git log --all -- _headers`（本文件历史上只被 `d7fdd1a` 引入过一次）+ 全文扫描：
    **`_headers` 自加入起从未出现过 `*/` 行**，故"删除 `*/`"这一步无需执行——
    审计描述的是它以为的形状，不是实际形状。★ 记在这里是为了让后来者不必再查一遍。
- **本版改法**：把"巧合"升级为"声明"——在 `/*` 上方补一段**作用域声明**，写明它是路径 glob
  而非注释、为什么不能删、为什么不能在下面再插一行，并**明确点出它生效的机制**。
  配置本身的功能一行未动（6 条头的取值、顺序、缩进全部保持原样）。
- **渠道覆盖差异（审计说"未知，需实测"→ 现在有确定答案）**：安全头**只对 Cloudflare 生效**。
  渠道 ② 的发布集在 `docs/DEVELOPMENT.md` 里写着——**8 个文件、不含 `_headers`**，
  所以那 6 个头在 WorkBuddy 渠道**按构造就不生效**。WorkBuddy 的静态托管是否支持某等价机制
  **未实测**，本仓库也没为它准备配置。结论：**"两渠道等价"这句话从未成立**，
  今后安全头相关结论一律只对 Cloudflare 负责——已写进 `docs/DEVELOPMENT.md` 部署一节。

### 二、新增 `tools/check-headers.js`（第 7 步）

`_headers` 是本仓库**唯一一个"写错了不报错、只会静默失效"**的配置文件，所以它的结构必须机器判。
断的是四件事：`/*` 恰好一行、6 个安全头齐全且缩进、无 BOM、无孤立收尾标记。

**反向验证（六种变异，逐一实测，均为"报出具体原因 + 退出码 1"）**：

| 变异 | 应当报出 |
| --- | --- |
| 删掉 `/*` 行 | 找不到全路径 glob——6 条头会失去作用域 |
| 再插一行 `/*` | 出现了 2 次（L28、L29）——多写一行会截断作用域 |
| 删掉任一条安全头 | 缺少安全头 X（删掉它不会有任何报错） |
| 把某条头改成顶格 | L32 顶格写头「X-Frame-Options」——在 glob 作用域之外，不会生效 |
| 末尾加一行收尾标记 | L35 是孤立的「\*/」——它会被当成路径规则，应删除 |
| 注入 UTF-8 BOM | 文件带 BOM——首行会被解析失败 |

★ 第五条的判定用**字符串相等**（`l.trim() === "*" + "/"`）而不是正则：本文件的注释里必须先
讲清这个标记是什么，而写正则字面量容易把它自己讲断（第一版就写崩过——注释块里出现该字面量
会把注释提前截断，直接语法错误）。

### 三、性能预算：把 smoke 的"能跑"升级为"跑得好"（§F11）

- **缺口是实测出来的**：`tools/smoke.js` 早已量出 `paintFrameMs`（同步循环单帧耗时），
  但**只打印、从不断言**——于是"改一处就拖慢渲染"这类隐性退化，在整条自验链上
  **没有任何一处会变红**。这与本项目对隐性退化的一贯态度（`check-coverage` 的 97%/90% 双阈值）
  正好相反，本版把它补齐。
- **改法**：新增 `PERF_BUDGET` 常量块（首屏 / 帧率 / 整树重建 / 单帧四项），断言一律引用它而
  不再用散落的字面量，并把**预算值连同实测值一起打印**——只看实测值的话，从 5ms 退化到 45ms
  是无声的，直到某天越过阈值才突然变红（那时已很难定位）。
- **新增一项此前根本没有的指标——首屏**：取 **Navigation Timing** 的
  `domContentLoadedEventEnd`，而**不是**探针自己的 `performance.now()`：探针要等 CDP 连上才注入，
  那时页面早启动完了，用探针的时间戳量出来的是"CDP 握手耗时"而非首屏。实测首屏 **247–257ms**。
- **阈值刻意留 10–100 倍余量**，依据逐条写在 `PERF_BUDGET` 上方：`bootMs` 5000、`fps` 50、
  `buildVizMs` 50、`paintFrameMs` 1。理由与 §F12 那次一模一样——假红的代价本项目自己算过
  （`eslint.config.js`："一个常年飘红的检查很快就会被所有人无视或直接关掉，等于没写"）。
  `paintFrameMs` 的读数**是下界**（同步循环期间音频时钟几乎不动，增量重绘走"状态没变"的短路分支，
  该探针第一版就因此量出 0.001ms 的假象），所以 1ms 这个预算只拦"每帧都在重建整棵树"那个量级
  ——那也正是唯一值得拦的。
- 冒烟断言数 **27 → 31**（两条通道各 +2：首屏、单帧）。

### 四、SW 可观测：把"本地与线上不等价"从静默变成可见（§Q4）

- **问题**：PNG 图标只由 `tools/build-dist.js` 在构建期生成到 `dist/`，**仓库根的开发环境里
  必然 404**。`sw.js` 已用 `ASSETS_OPTIONAL` + 逐个 `catch` 正确兜住（离线能力不受影响），
  但 `catch(() => {})` 是一句**彻底的静默**——"本地跑得对"与"线上跑得对"在这里并不等价，
  而这层不一致被完全掩盖了。
- **改法**：`catch` 里补一条 `console.info`，指名道姓记下**跳过的是哪一个文件**，并说明缘由。
  **用 `info` 而非 `warn`/`error` 是刻意的**：这是**预期内**的正常情况（开发环境本来就没有这两个
  文件），用 `warn` 会让每次本地加载都多出黄色条目，很快就会被人无视——那与本版要达成的目标相反。
- **为什么没选审计给的另一个方案（让 smoke 改以 `dist/` 为根）**：`smoke.js` 的两条通道是刻意
  **测同一份 `index.html`** 的（只差协议），一条验"双击即用的 `file://`"、一条验"在线版"。
  把在线通道改成测 `dist/`，两条通道就在测**不同的文件**了——为了修一处 PWA 资源可见性，
  换掉整条冒烟通道的对象，不划算。方案 ② 改动更小，且顺带提升了 SW 可观测性。

### 五、顺带修掉三处既有的文档漂移（都不是本版引入，属"改到这里才看见"）

1. **`tools/check-all.js` 文件头把"文档一致性"写成"第 5 项"**——它其实是第 6 项。v2.8.6 把
   装配完整性插为第 3 步时，这句交叉引用没跟着改。**同一份文件头的步骤清单是对的、正文引用是错的**，
   所以当时没被任何闸门抓到（闸门只管文档里的**步数总数**，不管正文里这种"第 N 项"的交叉引用）。
2. **`docs/DEVELOPMENT.md` 的 `tools/` 目录树漏了 `check-docs.js`**——它已经存在很多个版本了，
   树里一直没有。本版新增 `check-headers.js` 时顺手把两者一起补上（否则每个新工具都在往一个
   已经不全的清单里加行，清单只会更不像样）。
3. **`tests/README.md` 的"六项静态检查"**——这个数字**没有任何闸门在管**，是纯手写、注定要烂的那类。
   本版的处理是**把数字删掉**（改成"全部静态检查"），而不是更新成七：v2.8.6 已经对覆盖率数字用过
   同一招（"一律不抄在这里"），理由是同一个——能被 `check-all.js` 现算出来的数字，不该在文档里存第二份。
   ★ 不把它做成闸门是刻意的：什么算"静态检查"本身就有歧义（内联的语法校验算不算？
   `check-version` 算不算？），拿这种模糊口径去写正则，只会制造假红。

**取舍说明（三条）**：

- **为什么又新开一个工具（步数 13 → 14）**：与 v2.8.6 的 `check-wiring.js` 同一条理由——
  本仓库的既定形态是"一个检查器 = 一个文件 = 一份文件头"，且独立文件才能支持
  `node tools/check-headers.js` 单独跑与反向验证（已实测六种变异）。代价是三处文档要同步步数，
  正好由 v2.8.6 新增的步数闸门管住——它当场报红并指出该改成 14，**又一次"闸门抓自己的文档"**。
- **`_headers` 的闸门只判结构、不判取值**：取值该收紧时应当改 `_headers` 本身并走 review，
  而不是让一个正则去猜意图。**它拦不住什么**要说清：它不管你写的 `Content-Security-Policy`
  是不是太宽，只管"这条头还在不在、会不会生效"。
- **性能数字刻意不抄进任何文档**：它们由 `smoke.js` 每次现场打印（连同预算）。抄进 `docs/`
  就变成又一个"抄一遍就等着烂"的数值，与 `check-docs.js` 的立身之道直接冲突。

**自验**（14 步）：

- **12 步 ✓，2 步 ⚠（工具故障、未被验证），实跑 12/14，用时 94.8s**——新增的 `_headers 结构` 步通过。
  两个 ⚠ 仍是本机沙箱拦截管道式 `spawnSync`（`EBUSY`）导致，与 v2.8.6 同形，**不是代码问题**；
  两步已按 `beatsight-sandbox-verify` 的手工通道补跑（见下）。
- `check-headers.js`：绿态通过（34 行 · 6 头齐全 · glob 恰好 1 行 · 无 BOM、无孤立收尾）；
  **六种变异全部实测报红且退出码 1**（见上表）。
- `smoke.js`：断言 **27 → 31**，两条通道全绿；实测首屏 **247 / 257ms**、buildViz 3.8 / 6.2ms、
  帧率 61 / 60fps、单帧 0.012 / 0.009ms（预算分别是 5000 / 50 / 50 / 1，余量充足）。
- **tsc 手工通道**：由 shell 直接起 tsc（不经 `spawnSync`）→ **退出码 0、零输出**。
- **覆盖率手工通道**：`NODE_V8_COVERAGE` 插桩跑全量 + 复用 `check-coverage.js` 的聚合逻辑
  （只去掉它唯一的 spawn，口径不变）→ **99.4%（6501/6537）**，通过总阈值 97% / 分区阈值 90%；
  函数 683/703 至少执行过一次。
- 主套件 **2418 PASS / 0 FAIL**（含 v2.8.6 的 T81）；看门狗 **49 PASS / 0 FAIL / 0 超时**。
- **★ 订正 v2.8.6 的一处自验数字**：v2.8.6 记「未覆盖行 36 → 39（+3），多出来的正是
  `patLenNotInjected` 这个抛错守卫，**按设计不可达**」。本版复测是 **36 行**，且
  `patLenNotInjected` **已不在"从未执行的函数"名单里**（该名单现在是 `(匿名) · TRACKS · onDemoRestore`）。
  原因不难找：v2.8.6 自己新增的 **T81 正是用 `setPatLenOf(null)` 去触发那个抛错**，
  所以那三行**不是"不可达"，而是被 T81 覆盖了**——把它写成"按设计不可达"会让人误以为
  测试永远碰不到它，从而在将来重构时放心删掉。v2.8.6 那个 39 应是 T81 接进 `tests/run.js`
  之前的读数（本版无法回到当时状态复现，故此处只陈述观测：现在这 3 行是被覆盖的）。
  ★ 本条不改写 v2.8.6 的正文，只在这里记录复测结论——与 `tools/check-docs.js` 文件头
  那处「★ 订正」同一做法：**旧条目的判断保留，新证据另起一条**。
- **刻意记一笔环境坑（本轮真踩了）**：反向验证第一次**失败**——备份写到 `/tmp`，而本机
  **Git Bash 的 `/tmp` 是 `C:/Users/chenh/AppData/Local/Temp`，Node 却把 `/tmp` 解析成 `C:\tmp\`**，
  两个"`/tmp`"不是同一个目录，于是"备份成功、还原无效"，变异层层累积把 `_headers` 改坏了。
  改用**仓库内**备份路径 + 每次还原后 `cmp` 复核才可靠。教训：**跨工具（shell / Node）传递路径时，
  不要把 `/tmp` 当公共约定**。这也顺带解释了为什么"看起来只是备份一下"的脚本值得写 `cmp` 断言。

## v2.8.6 · 让闸门说真话：自验链区分「工具故障 / 检查失败」+ 三条新闸门（2026-09-21）

**来源**：v2.8.2 全维度审计报告 §四 行动清单。按「清单里『功能』类型之外的事项全部完成」的指令，
本版是第一批——**自验链的可信度 + 新增闸门**，对应清单 §E2/§E5、§A1、§Q1、§F12 四行。

### 一、自验链不再把「工具故障」报成「检查失败」（§E2 / §E5）

- **问题**：`spawnSync` 失败时 `status` 为 `null`、`error` 有值，而 `check-all.js` 只看
  `r.status === 0`，于是打印「✗「行覆盖率」未通过（退出码 null）」；`check-coverage.js`
  同样按 `status !== 0` 处理，打印「测试套件未通过，覆盖率无意义。先修测试：」
  **外加一张空的失败清单**（stdout 是空的，那条过滤正则一条都匹配不到）。
- **实测代价（本机沙箱，非推测）**：管道式 `spawnSync` 返回 `EBUSY`，而当时仓库 12 项全部健康
  （手工补跑得覆盖率 99.4%、测试 2412 PASS / 0 FAIL、tsc 退出 0），汇总却是"1 项失败"，
  且把原因说成「类型检查未通过」——**让人去查一个不存在的类型错误**，整一轮被带偏。
  这正是审计 §E2 的实测证据来源。
- **本版改法**：补上第三类状态——
  - `check-all.js` 检查 `r.error`（或子步骤按约定返回**退出码 4**）→ 标 ⚠、**单独计数**，
    打印「本步骤**未被验证**」，并明确提示"这是检查没跑成，不是检查没通过"；
  - `check-coverage.js` 与 `check-tsc.js` 统一到**退出码 4** 自报"未能执行"。
    注意 `check-tsc.js` 原先写的是 `throw r.error`——方向是对的，但抛异常会以退出码 1 收场，
    `check-all` 只能把它记进"失败项"，于是整条链的汇总**照样**说成「类型检查未通过」。
    本版**不是**把这条做对的地方改错，而是把"只对自己正确"改成"全链可读"；
  - 汇总行改为**分解式**：`✓ 9 项通过 · ⊘ 2 项未执行（可选加强项，缺 node_modules）·
    ⚠ 1 项工具故障（未被验证，不是被检项失败）· 实跑 10/13 项 · 用时 Ns`，并附**一行**处置建议。
    有工具故障时**不再打印「全部通过」**——本次确实有 N 项没被验证，说"全部通过"就是撒谎。
- **一个刻意的取舍**：本地（非 `--strict-env`）工具故障**不**让退出码变 1，因为它的成因在环境
  （权限 / 沙箱 / 资源），不是被检代码；把它算失败等于"因为尺子坏了就宣布测量结果不合格"。
  但 `--strict-env`（CI）下按失败处理——CI 里工具起不来是真的问题。
  工具故障也**不再终止后续步骤**（只有真失败才级联），这样一次跑完能看清全貌：
  本机沙箱恰是这个形状——用 `stdio:"inherit"` 的步骤全部正常，只有管道式 spawn 的两步故障。
- **一个立刻可观察到的后果**（实测，非推断）：`hooks/pre-commit` 跑的是
  `node tools/check-all.js --quick`，而它在受限环境里必然撞上同一个 `EBUSY`。
  改之前这次提交**被钩子拦住**，只能靠 `--no-verify` 绕过；改之后钩子**自己放行**了
  （退出码 0，同时把那两步响亮地标成 ⚠）。也就是说：**钩子不再因为"环境跑不起来"
  去阻拦提交**——那本来也不是它该管的事。本版的这次提交就是首次不经 `--no-verify` 的提交。

### 二、新增三条闸门（§A1 / §Q1 / §F12）

1. **`tools/check-wiring.js`（新第 3 步）——注入槽装配**（§A1）：每个 `let onXxx = null;` 约定的
   钩子必须**被赋过非 null 值**，`setPatLenOf(...)` 必须被实际调用。它守的是"漏赋值 = 功能静默失效"
   ——这类故障**逃过此前全部闸门**（`check-module-order` 只查引用顺序、`tsc` 看 `null` 类型完全合法、
   测试要改了才红，而它防的恰恰是"只动装配段"这类不碰测试的改动）。
   **名单是自动收列的**（以 `on` + 大写字母开头即纳入），新增钩子无需改工具——这比维护一张
   会腐烂的清单更强。首次运行即核对出 11 个钩子 + 1 个注入点全部接上。
   ★ 它与下面第 3 条的 `patLenOf` 改动是**一对**：静态管"装没装"，运行时管"没装就别想安静地拿到 4"。
2. **`check-docs.js` 由 4 条规则扩到 7 条**（§Q1 / §F12）：
   - **README 的「当前 `vX.Y.Z`」必须等于 VERSION**。这条声明此前**恰好落在两个闸门之间**
     （`check-version` 管四份文件、`check-docs` 只管索引/耗时/归档/快照），于是自 v2.8.0 起
     漏更新两版（一直写着 v2.7.0）而闸门一路绿灯——**不是笔误，是制度性盲区**；
     另附一条与 `check-version` 第 3 项同口径的前瞻规则：README 也不得引用高于 VERSION 的版本号。
   - **自验步数一致**：文档里的步数声明必须等于 `STEPS` 数组条目数（步数从 `check-all.js` 源码数出来）。
   - **不许手写"当前覆盖率"**：与"不许手写耗时"同源同处置。实测证据：`tests/README.md` 写着
     "当前 99.7%…只剩三组共 11 行"，而 v2.8.3 自验实测已是 **99.4% / 36 行**。
3. **`patLenOf` 的默认实现改为显式抛错**（§A1）：原为 `() => DEF_BARS`，是一句
   **恒 4 的静默谎言**——与真实实现（按型实际小节数）语义完全不同，忘了注入不会崩，
   只会让段长悄悄退回「4 的倍数」（v2.5.1 前《在他乡》被从 30 小节垫成 44 小节就是这个口径）。
   V8 覆盖率显示它**从未被执行过** = 生产中恒被覆盖，却没有任何检查器保证这一点。
   同时**订正**一处 v2.5.1 的表述不精确：旧注释把"老数据引用了不存在的型"当成默认值的职责，
   事实不是——那个场景永远走**注入的真实实现**（`resolveRef → null → patBars(null) → DEF_BARS`）。

### 三、新增测试 T81（6 条断言）

`tests/cases/t81-wiring-slots.js` 钉住 §A1 的**运行时那一半**：正常装配下段长正确、
摘掉注入后必须抛错、传非函数同样抛错、重新注入后恢复可用。
**反向验证**：把默认实现退回"恒 4 的静默谎言"，`BEATSIGHT_HTML=<回退版> node tests/run.js`
→ **只有 T81 那 2 条目标断言变红（2416 PASS / 2 FAIL）**，其余全绿。

**取舍说明（三条）**：

- **为什么新开一个工具，而不是按审计建议内联 20 行进 `check-all.js`**：本仓库的既定形态是
  "一个检查器 = 一个文件 = 一份文件头"，且独立文件才能像其他工具那样支持
  `node tools/check-wiring.js <index.html>` 的反向验证（已实测：删掉一条装配 → 退出 1）。
  代价是**步数 12 → 13**，要同步 README / DEVELOPMENT 三处——正好由本版新增的步数闸门管住，
  它当场就报了红（这次是"自己造的闸门抓自己的文档"，算一次顺带的自证）。
- **窄口径是刻意的，并且要说清它拦不住什么**：步数规则只认两种形状（`# 共 N 步…` 与
  "N 步里有 M 步可能标 ⊘"），覆盖率规则只认「当前…N%」。理由是两份文档**大量引用历史数字**
  （技术债清单里写着"第 4 步（现共 8 步）"、覆盖率叙述整段在引用旧值讲教训），宽正则会一并误伤。
  ★ 这个判断是**实测换来的**：步数规则的第一版用了宽正则，当场把 `docs/DEVELOPMENT.md` L798
  的两处历史引文误判成漂移。假红的代价本项目自己算过（`eslint.config.js`："一个常年飘红的检查
  很快就会被所有人无视或直接关掉，等于没写"）——这一条与本版修 §E2 是同一个价值观的两面。
- **没有让步数闸门去管"实跑了几项"**：实跑数随环境而变，本就该由命令自己打印；
  仓库的**总步数**才是仓库自己的结构事实。

**自验**（13 步）：
- 可正常执行的 **11 步全部 ✓**：语法 / 架构约束 / 装配完整性 / 零依赖 lint（7650 行 · 1251 声明）/
  版本一致性（五份对齐）/ 文档一致性（7 条规则全过）/ ESLint（0 错误）/ DOM 引用 /
  浏览器冒烟 / 自动化测试（**2418 PASS / 0 FAIL**，新增 T81 六条）/ 死循环看门狗。
- **2 步在本机沙箱走的是「工具故障」路径——本版刚修的这条路，正好当场自证**：
  `check-tsc.js` 与 `check-coverage.js` 都因沙箱拦截管道式 `spawnSync` 报 **EBUSY**，
  现在被如实报成 ⚠ **未被验证**，而**不再**被说成"类型检查未通过"。两步已**手工复刻补跑**：
  - `tsc -p`（由 shell 直接起 tsc，不经 `spawnSync`）：**退出码 0、零输出**；
  - 覆盖率：`NODE_V8_COVERAGE` 插桩跑全量 + 复用 `check-coverage.js` 的聚合逻辑（只去掉它唯一的 spawn，
    口径不变）：**99.4%（6498/6537）**，通过总阈值 97% / 分区 90%。
- **未覆盖行 36 → 39（+3）**，多出来的正是 `patLenNotInjected` 这个新的抛错守卫
  （`index.html` L3075–L3077）——它**按设计不可达**，属本项目一贯"刻意保留的防御性代码"那一类。
  V8 的「从未执行的函数」名单里现在是 `patLenNotInjected`，而旧版这个位置正是 `patLenOf` 本身：
  **名单换了个名字，性质没变——"恒被覆盖的默认值"仍然存在，只是它现在会响亮地失败。**
- 本版在 `index.html` 头部注释区插入了 14 行，触发模块索引漂移，由 `node tools/gen-index.js --write`
  回写 14 条（这正是 `check-docs.js` 第 1 条规则的日常）。

## v2.8.5 · 把 Wrangler 版本钉进仓库：部署链路上最后一处「不由仓库决定」的事实（2026-09-21）

**来源**：v2.8.4 验证通过（Cloudflare 构建绿、线上版本号已成 v2.8.4）后，去核对 Cloudflare 官方文档时发现。

- 官方文档（Workers Builds → Configuration）在 deploy command 与 preview deploy command 两处都写着：
  「Workers Builds will use the Wrangler version set in your `package.json`.」
  而本仓库的 `package.json` **从未声明过 `wrangler`** —— devDependencies 一直只有 `eslint` + `typescript`。
- 后果：线上那次 deploy 实际用哪个 Wrangler 版本，**由 npx 的解析结果决定，不由仓库决定**。
  于是 `wrangler.jsonc` 文件头那句「使本地 `npx wrangler deploy` 能完整复现线上构建」
  **在 deploy 这一步并不成立**——两侧各自的 Wrangler 版本都没被钉住，只是碰巧一致而已。
  这与 v2.8.3 修的 `package-lock.json` 漂移是**同一类病**：一个影响构建/部署结果的事实，没有被声明。
- 本版把 `wrangler: ^4.135.0` 加进 devDependencies 并锁进 `package-lock.json`。
  用 caret + 锁文件（与 `eslint` / `typescript` 同一风格），实际生效版本由 `npm ci` 按锁文件确定。
  选 `4.135.0` 是因为它正是当前 `latest`：**与"现在能正常部署的那个版本"一致**——
  本版不借机升级 Wrangler，只是把既有行为钉下来；升级是另一件事，应单独做、单独验证。
- 顺带记录一条同类问题的**下一处潜在落点**：`wrangler@4.135.0` 的 `engines` 是 `node >=22.0.0`，
  与本仓库 `engines: ">=22"` 一致；但 **Workers Builds 实际用哪个 Node 版本，仓库侧仍未声明**
  （没有 `.nvmrc` / `.node-version`），目前依赖平台默认值。本版不动它，记在此处备查。

**取舍（含实测代价，不靠估）**：`wrangler` 是 devDependency、不进产物（上站仍只有 9 个条目），
「`index.html` 可 `file://` 直开」这条运行时零依赖约束不受影响。但**依赖树确实变大了**，实测：
锁文件包数 **98 → 189（+91）**、锁文件体积 46.7 KB → 95.9 KB。多出来的是 Wrangler 自带的运行时与打包器
（`workerd` / `miniflare` / `unenv` / `esbuild` / `blake3-wasm` / `@cloudflare/kv-asset-handler` 等），
其中相当一部分是**各平台的可选依赖**（`workerd-{darwin,linux,windows}-*`、`esbuild-*`），
`npm ci` 只会装当前平台那一份。判断：可接受——构建本来就要跑一遍全量测试与覆盖率，这点安装量相对很小；
而且线上 deploy 本来就要用 Wrangler，只是把它从「npx 现场取」换成「按锁文件装」。

**自验**：本版只动依赖声明与文档，未改任何产品代码，故沿用既有结论（12 步全绿）。针对本版另确认两项：
① 跑 `npm install --package-lock-only` 后，锁文件把 `node_modules/wrangler` 解析到 **4.135.0**，
   且 `packages[""].devDependencies` 已含 `wrangler: ^4.135.0`；
② 四份版本号一致（VERSION / CHANGELOG 首条 / package.json / package-lock.json 均 2.8.5）。
⚠ **本机跑不了的那一步要说清**：`wrangler` 本体的实际安装（含 `workerd` 的 install 脚本）
在开发沙箱内无法完成——与本项目已知的"管道式子进程"限制同源，不是仓库问题。
所以 **「`npm ci` 能装上 wrangler」这一步没有在本地被验证过**，只验证了锁文件的解析结果。
最终以「推送后 Cloudflare 构建是否绿、线上版本号是否变 2.8.5」为准。
若构建转红：回退本版即可（`git revert`），部署链路其余部分不受影响；
且 Cloudflare 构建失败时线上继续提供上一个成功版本，站点不会中断。

## v2.8.4 · 文档修正：部署机制那段补上「Dashboard 填 `npm run ci`」（2026-09-21）

**来源**：v2.8.3 落地后去核对 Cloudflare 侧配置时发现的一处文档滞后。

- `docs/DEVELOPMENT.md` 的「发布渠道 ①」描述部署机制时，写的仍是「构建命令里串了
  `node tools/check-all.js` 全量检查」，没有反映 v2.8.3 起 Dashboard 只填一行 `npm run ci`。
  它不算错（`npm run ci` 内部就是同一套全量检查），但描述没跟上现状——而这一行正是读者了解
  「线上到底是怎么构建的、那道闸门到底在哪」的入口。
- 本版**只改文档，不改任何代码与检查逻辑**。之所以仍然 bump 版本号，有两个具体理由：
  1. 项目既定规则是每次发版都 bump（v1.6.4 起，「功能版与工程版一视同仁」；此前连 v1.8.2
     那种"仓库卫生收口 + 文档对齐"也是带版号发的），本版不破例。
  2. 更实际的原因：**这次推送会触发一次 Cloudflare 构建，带版号才能验证新构建命令真的生效。**
     线上版本号从 `v2.8.3` 变成 `v2.8.4`，就是「Dashboard 那行 `npm run ci` 在构建镜像里
     跑通了」的直接证据。若只改文档不 bump，线上版本号不变，构建到底跑没跑新命令将无从判断
     ——而那正是本版想确认的事。
- 本版不新增功能与检查项，代码一字未动，故自验沿用 v2.8.3 的完整结论（12 步全绿）；
  本次只跑 `check-version.js` / `check-docs.js` 等元信息闸门确认版本号与文档无漂移。

## v2.8.3 · 构建侧收口：Cloudflare 构建命令收敛为「一行指针」+ 版本闸门补上第四份手抄（2026-09-21）

**来源**：v2.8.2 基线的全维度代码审计（只读扫描，未改仓库）。审计指出工程化的第一缺口是
「检查没有必经之路」，顺着它查出三件事：构建命令内容只能写在 Dashboard（**平台约束，非缺陷**）、
`package.json` 缺 `build` / `ci` 入口（**真缺陷**）、版本闸门宣称覆盖 `package-lock.json`
却从未读过它（**真缺陷**）。本版收口前两件，并修掉第三件。

### 构建侧：仓库外只剩 11 个字符

- **新增两个 npm 入口**：`npm run build`（`node tools/build-dist.js`）与
  `npm run ci`（`npm ci && node tools/check-all.js --strict-env && npm run build`）。
- **Cloudflare Dashboard 的 Build command 从此只填一行**：`npm run ci` —— 闸门的**内容**
  留在 `tools/check-all.js`、**编排**留在 `package.json`，两者都在版本库里；仓库外只剩这 11 个字符，
  几乎永不改动。此前 Dashboard 里必须**硬写三个文件路径**，改脚本名或挪路径会让线上构建
  **静默断掉**，而仓库里没有任何东西会提醒你。
- **⚠ `--strict-env` 与 `npm ci` 缺一不可（这是"假绿"防线，不是风格问题）**：
  Cloudflare Workers Builds 默认注入 `CI=true`，但 `check-all.js` L63 是
  `process.argv.includes("--strict-env")` —— **它不读 `CI` 环境变量**。若图省事写成
  `npm run verify`（无 flag），构建镜像又没有 `node_modules` 时，ESLint / tsc 两步会静默标 ⊘、
  汇总照样打印「全部通过 · 实跑 10/12 项」——**恰好复现 `--strict-env` 当初要防的那个假绿**。
  这两个前提现在被封在 `npm run ci` 内部，Dashboard 上填不出错。
- **记录一条平台约束（不是缺陷，无需补救）**：Workers Builds **不读**仓库里的 Custom Builds。
  Cloudflare 官方文档原文：「Currently, Workers Builds does not honor the configurations set in
  Custom Builds within your Wrangler configuration file.」→「那一行调用」本身**结构性地无法**
  进版本库。本版的做法是**把仓库外的残留压到最小且稳定**，而不是试图把它搬进来。
  （原文用 "Currently"，即"目前不支持"，值得后续回看。）
- **同时更正 v2.0.5 条目的一处历史描述**：那条写的是「`CI=true` 且可选步骤缺依赖时报错」，
  但实现落地为**显式 CLI flag**（`--strict-env`），并不读环境变量。语义等价，但"靠 `CI=true`
  自动触发"在代码里不成立——而 Cloudflare 恰好会注入 `CI=true`，这个不一致很容易让人误以为
  严格模式会自动生效。按「历史条目保持原样」的既定约定，v2.0.5 那条**不改**，更正记在这里。

### 版本闸门：补上第 5 项（宣称与实现差了整整一年）

- `tools/check-version.js` 的注释从 v2.0.6 起就写着「package.json / **package-lock.json** 里
  各有一个 version……只能靠这条闸门保证**三者**同步」，但代码**只读了 `package.json`** ——
  `grep "package-lock"` 在该文件里**所有命中都在注释中，没有一行代码**。
- **实测漂移**：`VERSION` 已到 `2.8.2`，而 `package-lock.json` 仍是 `2.7.4` —— 横跨
  v2.8.0 / v2.8.1 / v2.8.2 **三次发版**，闸门一路绿灯。连带地，`README.md` 发布前清单里
  「`package.json` / `package-lock.json` 的 version 由 `check-version.js` 一并把关」**这句是失实的**。
- **本版补上第 5 项**：顶层 `version` 与 `packages[""].version` **两处**都必须等于 `VERSION`。
  为什么单列一项而不并进第 4 项——**漂移通道不同**：`package.json` 是**手改**的（失败姿势是
  "发版时漏了"）；`package-lock.json` 是 `npm install` **顺手改**的（只在跑过 install 时才动），
  于是"改了前者忘了后者"才是最自然的失败姿势。两者只能分别守。
- 本次 `package-lock.json` 已同步至 `2.8.3`；README 那句失实描述**现已成真**（文案无需改）。

### 文档

- `wrangler.jsonc` 头部注释补上「Dashboard 该填 `npm run ci`」及 `--strict-env` 的必要性。
- **`README.md` 的「当前 `vX.Y.Z`」从 v2.7.0 改正为 v2.8.3**：该行自 v2.8.0 起就漏更新
  （v2.8.0 的「同屏行数档位」也一直没进功能清单）。它能一直歪的原因很具体——落在两个闸门之间：
  `check-version.js` 只管 `VERSION ↔ CHANGELOG ↔ package.json/lock`，而 `check-docs.js` 按设计
  **不校验正文数值**（见该文件「刻意不做的事」一节）。属审计报告 §Q1，本版顺手改正。
- `README.md` / `docs/DEVELOPMENT.md` 的构建命令说明同步为 `npm run ci`。

**取舍说明**：本版**不引入 GitHub Actions**。「要有 CI」与「要用 GitHub Actions」是两件事——
前者要的是"闸门不可被跳过"，而 Cloudflare 这条路本身就能承担强制力（构建失败即不部署）。
外部 CI 能补的只有三件（PR 阶段拦截、WorkBuddy 手动渠道零强制、那一行调用本身入库），
**三件都不紧急，且都要新增一套要维护的流水线**。本版先用"一行指针"堵上最容易出事的那个洞
（路径硬编码在仓库外），是否上外部 CI 留给后续独立决策。

（历史对照：CI 曾于 v1.0.0 上线，v1.3.2 随"发布渠道迁移"连同整个 `.github/` 目录被移除。
本版动作与那次**打包移除**不同——只补仓库内的入口，**不动发布渠道、不删任何东西**。）

## v2.8.2 · 修整首连播 + 预备拍：小球动画不再跳过第一小节最后两拍（2026-09-21）

**来源**：用户报「整首连播在打开预备拍时，小球动画会跳过第一小节的最后两拍」。

- **根因**：曲式（整首连播）每遇段/块边界时，`schedOneStep` 的 `consumePending` 分支会把
  `loopStart` 重映射到**未来**（≈前瞻窗口处：`loopStart = nextNoteTime - schedBar*S.sig*spb()`）。
  而预备拍视觉门控 `paintFrameBody` 旧写法用的是 `if (ciBeats > 0 && c.currentTime < loopStart)`
  ——`ciBeats` 只在 `stop()` 清零、播放途中一直保持 >0，于是每当 `loopStart` 被改写到未来，
  这个条件就**重新成立**：状态栏切回「预备拍 · 4 / 4」，`paintFrameBody` 在调用 `paintBall`
  之前提前 return，小球冻结、该小节末尾几拍被藏掉。
- **修法**：预备拍门控改用新增的 `ciEnd`（= `ciStart + ciBeats 拍`），它**只在 `start()` 设置、
  只在 `stop()` 清零**，播放途中不随 `loopStart` 的重映射而移动；门控判据变为
  `c.currentTime < ciEnd`，跨段不再误判为"又进了预备拍"。无预备拍时 `ciBeats=0 && ciEnd=0`，
  门控恒不进入，正常路径逐位不变。
- 测试：新增 `t80-countin-arrange-no-reenter`——240BPM、两段各 1 小节，断言跨段**不重新**出现
  「预备拍」、第 1 小节第 4 拍状态栏读数出现过；对照场景（预备拍关）不出现预备拍文案。
  用缺陷版（门控仍用 `loopStart`）单跑该用例得 8 PASS / 2 FAIL，失败恰为上述两项，证明用例
  能忠实抓回归。全量 2412 PASS / 0 FAIL。

## v2.8.1 · 修 v2.8.0 两处画面回归：六线底纹消失 / 和弦胶囊贴格（2026-09-21）

**来源**：用户发实拍图报 v2.8.0 引入两处观感回归（本版一次性收口）。

- **六线谱底纹恢复**（问题 1：「六线谱底纹不见了」）：v2.8.0 把根变量 `--gtop` 从 `5px` 改成
  无单位的 `0`，而六条弦线的 `top` 走 `calc(var(--gtop) + N*var(--gt))`——calc 里「无单位 0」
  是 `<number>`，不能与 `<length>` 相加，整条 calc 失效，六条线全部退回 auto 叠在一处，
  观感就是底纹"不见了"。补回单位 `--gtop:0px`，六线恢复落 0/8.8/17.6/26.4/35.2/44px。
- **和弦胶囊与格子留距**（问题 2：「和弦胶囊和预告信息都贴在格子上，应该保持距离的」）：
  `.bar-chord` 未设行高，继承了 body 的 `line-height:1.5`——11px 字号 → 16.5px 内容高 + 2px
  边框 = 18.5px 盒高，`top:-18px` 时盒底落在 +0.5px，正好压在格子顶边上（用户实拍"贴在
  格子上"）。给 `.bar-chord` 加 `line-height:1`，盒高收到 13px、盒底落到 -5px，与格子顶边留
  ~5px、与上一行内容底留 ~6px，两侧均衡，既不压本行格子也不挤上一行座次尺。
- 测试：两处均为纯 CSS/根变量修正，不新增用例；t47（弦线 top 内联式）与 t75/t78（胶囊文本/
  类名）断言的是内联样式与类名而非计算布局，故改动不破坏既有断言（全量 2402 PASS / 0 FAIL）。

## v2.8.0 · 主视图四改：预告信息并入和弦胶囊 / 和弦胶囊上移左对齐 / 同屏行数可调 / 六线铺满格子（2026-09-21）

**来源**：用户连拍四张主视图实景，提出四条观感问题（本版一次性收口）。

- **预告徽标上移移出格子**（需求 1：「预告徽标会遮挡格子影响用户看扫弦标记，应该上移移出
  格子范围。」）：曲式翻页时第 1 行会临时换成"下一小节"的内容，原来在格子上方贴一颗
  「预告」徽标，压在格子上沿挡住扫弦箭头。本版取消这颗独立徽标，其信息不再落在格子范围里。
- **和弦胶囊左对齐 + 并入预告信息**（需求 2：「和弦胶囊左对齐，但也不要直接占据原来是预告
  徽标的位置。可以这样将二者一起显示：和弦名（预告信息）。」）：小节和弦名胶囊（v2.7.4 引入）
  由行顶中段居位改为 `left:6px` 左对齐；预告行（第 1 行）不再另挂徽标，而是把预告信息**并进
  同一颗胶囊**，文本 =「和弦名（下一小节 · 型名）」（该小节无和弦时只剩括号里的预告信息）。
  行距由 14px 加到 20px、胶囊 `top:-18px`，给行顶条带腾出竖向空间，胶囊整体落在格子之上、不遮格子。
- **同屏行数可调**（需求 3：「扫弦和歌词显示的行数改为可选择的，比如可调整为歌词两行，
  扫弦两行。」）：新增 `S.vizRows` 档位（值域 1/2/3/4，默认 4 = 旧口径「同屏 4 小节」，
  老用户升级后逐位不变），在预设区生成一行 `.viz-rows` 档位 pill（`syncRowsUI` 经 `buildPillRow`
  生成，带 `data-rows=N`）。**两轨共用一个档位**——扫弦网格的曲式窗口长度与歌词轨行数同源
  （都是 `vizRows` 驱动的 `arrWinBars()`），保证两轨逐行对齐（各设一档只要行数不一致就立刻错行）。
  改档位即时重建网格与歌词轨；值走白名单 `VIZ_ROW_COUNTS`，脏值/越界回落 4；只对**曲式模式**
  生效（预设模式网格行数仍随型走「型多长画多行」）。档位是用户视图偏好，写进热键载荷 `beatsight.state`。
- **六线铺满格子**（需求 4：「六线谱调整为将格子铺满，其实说六弦贴在格子的底边上，一弦贴在
  顶边上。」）：吉他六线图由「7px 弦距 + 顶端留白（线落 5..40px，六弦压出格子）」改为**铺满**——
  `--gtop` 归零、弦距 `--gt = 44/5 = 8.8px`，六条弦线落 0/8.8/17.6/26.4/35.2/44px，一弦贴格子
  顶边、六弦贴格子底边，整体落在 44px 格高之内（历史 P0 缺陷"六弦画在格子外面"彻底消除）。
- 测试：新增 T79 系列（档位↔窗口长度同源 / 档位选择器生成与选中态 / 脏值白名单 / 预设模式不受
  影响 / 改档位同步重建网格与歌词轨 / 播放中按新档位翻页 / 档位进热键载荷）；t70 相应更新措辞并
  留 T79 指针（行数档位的可调性另由 T79 系列守）。

## v2.7.4 · 和弦名显示在对应小节上方（2026-09-21）

**来源**：用户实拍——段名尾部的和弦（如「副歌 · 上 C·Am·Dm」）只出现在远端侧栏的
「曲名 · 段名」里，离主视图的扫弦与歌词区太远，弹唱时眼睛要在侧栏与网格之间来回跳。

- **和弦的落点**：本工具**无和弦轨**，和弦一直寄生在段名里（`<结构名> · <歌词首句>
  <和弦序列>`，一小节一个和弦，和弦数 == 段的小节数）。新增纯函数 `secChords(段名)`
  取「最后一个空格之后」那一段记号、按「·」拆分，并用「以 A–G 开头」过滤——
  用户自建段、歌词里带空格的段名不会被误读成和弦。
- **每小节行上方一颗胶囊**：`.bar-chord` 挂在 `.bar-row` 的**直接子节点**（与预告徽标
  同一挂法），落在行顶 `top:-9px` 条带的**中段居位**——左端是预告徽标、右端是
  「静音小节」标签，中段空着，三者互不撞位。绿色系与网格高亮同色，视觉上从属"这一小节"。
- **窗口口径与网格/歌词轨同源**：预告行（第 1 行）取 `winPrevBar`（它此刻显示的正是
  下一小节内容），其余行取 `winStart+b` 对全曲长度取模——预告/翻页时和弦随内容一起换，
  不会与格子/徽标错位。无和弦的小节/段不挂胶囊（在 `barChordEls` 里存 `null` 占位，
  与 `rowEls` 逐位同构）；预设模式（非曲式）不生效。
- 测试：新增 T78 系列（`secChords` 解析与容错 / 示例曲"和弦数 == 段长"自洽 / 逐行渲染
  取对和弦 / 预告行取下一小节和弦 / 翻页转正 / 无和弦段不挂 / 预设模式全 null）。

## v2.7.3 · 跳段加「范围循环」解除开关（跳段不再把人困在某一段）（2026-09-21）

**来源**：用户实拍——点「◀ 上一段 / 下一段 ▶」跳段后，播放就永远在那一小段里
来回循环，再也走不出去。

- **根因（是一条链路，不是单点）**：`jumpTo` 把范围锁成单段并打开循环
  （`from=to=k, loop=true`，这是"反复磨这一段"的默认）；而 `arrNextBar` 走到段尾
  遇 `loop` 为真就 `s = from` 回卷、永不返回 `null`，`onArrangeEnd` 于是永不触发，
  播放永不收尾——整条链路此前**没有任何出口**。
- **出口 = 跳段行新增常驻开关「范围循环」**（`.toggle-pill`，复用开关三件套的
  视觉与 `aria-checked`）。它与编排面板的「范围循环」是**同一个字段**
  （`S.arrangeSel.loop`）——两处入口共用一份数据、`refreshBar` 一处收口同源刷新，
  不存在第二份真相（与预设区「循环本段」共用 `S.loopRange` 是同一套做法）。
- **开关语义**：开 = 只把 `loop` 置真、范围不动（跳段即"锁本段"，与面板同义）；
  关 = 解除锁定——`loop` 置假；若范围已被跳段收成**单段**（`from===to`），把 `to`
  放开到曲末，变成"**从当前段一路播到曲末后自然停**"（用户选定：解除后继续往下走，
  而不是原地再放一遍就停）。范围本就是多段（面板设的起/终、「全部」、整首连播）时
  只关循环、不动用户设的 `from/to`。
- 测试：新增 T77 系列（默认锁定循环 / 开关与字段同源 / 单段解除放开到曲末 /
  多段只关循环不动起终 / 面板↔跳段行双向同步 / 解除后播放真的走出该段直至曲末收尾）。
  连带更新 T24 的 `.toggle-pill` 计数（8 → 9）。

## v2.7.2 · 歌词轨按小节分行（与网格窗口逐行对齐）（2026-09-20）

**来源**：用户实拍——歌词轨整条单行铺开一段词，与上方 4 行小节网格对不上拍，
跨小节时视线要在两条不同结构的轨之间来回换算。

- **每行 = 网格窗口的一小节**：`.lyric-lane` 从单条横轨改为 4 条 `.lyric-row`，
  与上方 4 行网格窗口逐行对齐（同一窗口 `W..W+4`）。字块按「小节内 tick /
  小节长」在行内铺开；跨小节延音在小节边界截断、下一行续显（`dur` clamp 到
  小节边缘）。
- **首行可回看上一小节**：播放中窗口首行显示 `winPrevBar`（上一小节）的词，
  加 `.preview` 半透明，与网格首行的回看行语义一致。
- **行走带线只出现在可听域当前行**：`paintLyric` 以可听小节 `kk` 换算**行号**
  （`curIdx = kk - W`，不用小节号匹配——窗口环绕时同一小节号可出现两次）；
  过去的行整行 played，当前行按 tick 阈值逐字点亮，未来的行不点。停止/切歌
  时各行带线全部隐藏、字面复位。
- 无歌词行时整块 hidden、不占位的契约不变。
- 测试：T60d 重写为分行结构断言（4 行、行内字块数、行内几何、4 条带线停止态）；
  T76c/d 适配行结构；新增 T76e 播放态逐行断言（过去行 played+fill=1、当前行
  on+fill 比例、带线只在当前行、停止后全隐藏）。反向验证：v2.7.1 代码上 5 处
  新结构断言失败，确认断言非空转。

## v2.7.1 · 双声部打通（鼓组/纯方向谱）+ 歌词轨与计数器改走可听域（2026-09-20）

**来源**：两条用户实拍——①扫弦轨上把「节拍」拉到 0 就全静默、「扫弦」滑条怎么调
都不影响声音；②《在他乡》第 1 段末尾（「我多想」的「想」还没唱完），歌词轨已整轨
切到第 2 段并点亮「到」，与弹跳球位置对不上。

### ① 双声部在两种情形下失效 → 打通

- 「节拍 / 扫弦」两条独立声部此前有两个未声明的前提：**音色不是鼓组**
  （`playClick` 里 zone 分支对 drum 整体除外）、**谱面的扫弦格带 zone 字段**
  （内置「民谣扫弦」只有 dir 没有 zone）。任一不成立，全部声音都走 `S.vol`，
  「扫弦」滑条恒无效却仍在界面上——正是设计注释自己警告过的"死滑条"。
- 修法：**凡带扫弦记谱（dir 或 zone）的发声音符都归扫弦声部**——dir-only 的格
  按中弦区发声（与"省略 zone = 中弦区"的数据契约同义）；鼓组下扫弦格仍按层级
  选鼓件（底鼓/军鼓/踩镲自带频段分工，不叠加弦区频段），但音量改走 `S.strumVol`。
  三种音色下「节拍拉 0 扫弦照响、扫弦拉 0 节拍照响」都成立。
- 连带变化（语义正确的一面）：扫弦轨 + 纯方向谱时，拍点节拍音现在**叠在**扫弦之上
  （与带 zone 的谱一致），不再被扫弦格顶掉——民谣扫弦从每小节 6 声变 10 声
  （6 扫弦 + 4 拍点）。`stepIsStrum` 判据与 `playClick` 声部入口保持逐字一致
  （drum 不再除外）。

### ② 歌词轨 / 段小节计数器 / 段序条高亮的"先跳"（可听域对齐）

- 根因：`paintLyric` 的段/小节号读**调度游标** `arrSec/arrBar`（比发声早一个
  前瞻窗口：前台 0.3s / 后台 1.2s），而小节内 tick 读**可听位置**——两个时间域
  拼在一起，每逢小节/段边界前那一小段，段内位置被拼错将近一小节。跳段行
  「第 N 段 · 第 M 小节」计数器与段序条高亮同属此类（调度域 `onArrangeBar` 刷新）。
- 网格窗口在 v2.5.2 修过一模一样的问题（`audibleSongBar`），这三处是漏网的同款。
- 修法：新增 `Viz.audibleArrangePos()`（onset 表 → 歌曲小节号 → 段/段内小节），
  歌词轨、计数器、段序条三处共用；触发侧新增可听域钩子 `onAudibleBar`
  （paintFrameBody 每帧检测可听小节变化才调，与调度域的 `onArrangeBar` 并存）。
  停机预览（未起播显示第 0 段）行为不变。

### 测试

- 新增 **t76**（双声部打通：dir-only 谱的 strumVol 路由 / 鼓组下扫弦格走 strumVol /
  拍点叠加不顶掉；歌词轨与计数器的可听域边界：调度越界、可听未越界时不许先跳）。
- 行为变化带动既有断言更新：t48（民谣扫弦 8 小节 80 声）、t53k 等发声计数口径。

## v2.7.0 · 跳段按钮基准分语境 + 翻页档网格与页末预告行（2026-09-20）

**来源**：两条用户实拍——①停止 + 段序条定位第 3 段后点「下一段」无反应；
②整首连播时球永远在第 1 行跳（滚动窗口让"当前小节"恒为第 1 行），
想要普通模式那样"从第 1 小节弹到第 4 小节"的手感。

### ① 跳段按钮基准分语境

- 「◀ 上一段 / 下一段 ▶」恒用调度游标 `arrSec`（上次播到哪）做基准，而
  `jumpTo`（段序条定位）只改 `arrangeSel.from` 不动它——两者分裂后，停止态下
  点按钮相对一个过期游标步进，常常原地不动（实拍"无反应"）。
- 修法：**播放中基准 = 正在播的段（arrSec），停止时基准 = 当前定位
  （arrangeSel.from）**——与跳段行显示的「第 N 段」同源，按钮永远相对用户
  眼睛看到的那个位置走。

### ② 翻页档 + 页内第 4 小节预告行（用户拍板的方案）

- 网格从"跟播放头逐小节滚动"（`winAnchor(k)=k`）改成**按 4 小节翻页**
  （`floor(k/4)*4`）：第 1…4 小节弹第 1 页，进第 5 小节整页翻——普通模式
  "从第 1 小节弹到第 4 小节"的手感在整首连播里成立。
- 翻页的新问题：扫弦练习到页内第 4 小节时需要**提前为下一小节做准备**
  （节奏型可能变，扫弦要换手法）。逐小节滚动天然满足，翻页后如何解决——
  按用户方案：**页内第 4 小节期间，把第 1 行（最陈旧的历史）原地替换成
  下一小节的内容**，并挂徽标「下一小节 · 型名」。
- 两个细节（与用户对齐后实施）：
  - **预告行豁免"已弹"态**：它是未来不是历史，格子恒按未弹画，半透明降权
    （`.preview-row`），绝不出现"已弹刷白"；
  - **范围末尾不循环则不替换**（没有"下一小节"可预告）；**范围循环时
    预告 = 范围起点**（内容与节目单同源：走 `arrNextBar`，不走歌曲 k+1）。
- **接力不断**：页末终端弧期间待命球落在**第 1 行预告行**首音上
  （`rowNext` 在"页末 + 有预告"时取 0 而不是 bar+1 钳制），翻页瞬间
  内容原地转正，主球在同一点接管——跨页交接与普通跨行同一套接力。
- 重建时机：`paintFrameBody` 除盯窗口起点外再盯预告行
  （进页末预告生效 / 翻页预告随窗口换），两处变化各重建一次；非页末时
  `previewBarFor` 只是算术，不解析节目单。

### 测试

- 新增 **t75**（跳段基准分语境 / 预告行内容徽标已弹豁免 / 页末待命球落预告行 /
  范围末尾不替换 / 范围循环预告 = 范围起点，5 场景）；t70 改写为翻页口径；
  t53k 待命球断言改"页末 → 第 1 行预告行"（仍抓得住退回 bar+1 纯钳制的回退）。
- 反向验证：新代码全绿 2266/2266；喂回旧代码 t75 红 11 条、t70 红 4 条；
  只回退 `rowNext` 预告分支则 t53k 红、t75 红 1 条——三条断言都不是恒绿摆设。

## v2.6.4 · 切轨假选中去除 + 示例曲版本迁移（2026-09-20）

**来源**：两条用户实拍——①连播（曲式模式）中切到普通轨，侧栏「四分基础」被高亮、
可视化却还在在他乡，像"切不过去"；②整首连播与参考谱对不上（用户给出参考页
逐格核对）：全曲应该是 30 小节，实测 120 小节。

### 核对结论（②的第一 half）

- 谱面数据本身**逐格一致**：5 个扫弦型（方向 × 弦区 × 空扫）、10 段结构
  （段长 1/3/4/2/4/2/4/2/6/2）、全部歌词锚点（含桥段跨小节延音「夜」）
  与参考页 `STRUMS`/`BARS` 逐项相同。
- 差异来自**本地数据落后**：示例型名字从 v2.4.4 起没变过，内容却在 v2.6.0 从
  「4 小节型」换成「1 小节型」；`ensureDemo` 只按名查重，于是"旧型 + 新曲式"的
  混杂态被判成"数据齐全"——30 小节 × 4 = **120 小节**（与用户实拍读数逐字吻合）。

### 修复

- **切轨不再制造假选中**（①）：曲式模式下 `ensureValidForTrack` 不再回退 S.sel
  （曲式播放不读它，它只是"退回预设后选谁"）；侧栏"选中高亮"在曲式模式下整体
  不显示（高亮语义 = "正在练这个型"，曲式播放由节目单决定，任何高亮都在说谎）；
  `trackNote` 在曲式模式下给"以节目单为准 · 点任一节奏型退回单练"的提示，
  并随 playMode 迁移（`onPlayModeChange`）同步刷新——进出模式各刷一次，不再漏。
- **示例曲版本迁移**（②）：同名再**比内容**（`demoPresetEq`：meter + bars 逐步
  规范化比对）；`ensureDemo` 对内容不符的示例型**就地收敛回参考谱**（保住 id——
  曲式块按 id 引用，换 id 会把整首曲式打成坏引用）；新增 `demoStale` 检测
  （缺型 / 内容不符 / 曲式逐段小节数不符）；init 增加迁移分支：闩已落但
  `demoStale` 为真 → 启动自动收敛一次。**只收敛，不复活**：曲式被用户删掉的
  恒为 false，绝不补回来（与带出闩"用户删掉就不再自动写"的口径一致）。

### 测试

- 新增 `tests/cases/t74-demo-version-migration.js`（5 场景 / 31 断言）：切轨无假高亮 +
  模式提示 + 出口契约；120 小节混杂态复现 → `demoStale` 抓住 → 收敛回 30 且
  id 不变、再跑 0 更新（幂等）；init 迁移端到端（第一份会话的 localStorage 原样
  倒进第二份）；删除不复活。
- **反向验证**：回退四处改动喂给 T74 → 9 条断言红（混杂态停在 120 小节、
  假高亮在场）——不是恒绿摆设。

## v2.6.3 · 曲式模式的主界面收口（2026-09-20）

**来源**：同一场连播使用里的两条实拍——①预设模式 + 已停止下，主界面还挂着
「◀ 上一段 / 第 2/10 段 · 第 5/120 小节 / 下一段 ▶」；②连播中切到普通轨，
侧栏显示选中「四分基础」，可视化区却仍是「在他乡 · 节奏型1」。

### 修复

- **退出曲式后跳段行不再残留**（①）：argJump 的显隐规则只住在 `Arrange.refreshBar` 里，
  而"退出曲式"的动作（点侧栏预设、曲式校验失败回退）不经过 Arrange 的任何一个入口——
  行就带着「第 N/M 段」残留在预设模式的主界面上。修法：`setMode` 是 playMode 的
  **唯一写入口**（t66 契约），在它上面挂变更钩子 `onPlayModeChange`（装配层注入
  `Arrange.refreshBar`，与 onArrangeBar/onArrangeEnd 同一套钩子纪律），模式一迁移就
  同步显隐，不可能漏。顺带修正了一个不对称：**整首连播进入曲式时跳段行也立即显示**
  （此前要等一次跳段或编排面板动作才出现）。
- **曲式播放中切轨不再画面分裂**（②）：切轨导致 S.sel 被 `ensureValidForTrack` 回退成
  普通型后，`refreshAfterPatternChange` 拿 `curPattern()`（= S.sel 指向的型）去
  `resyncToNow` + 重建画面——时间轴被拽进另一个型的网格，标题/画面与节目单分裂。
  修法：曲式播放中的取型唯一来源是**节目单**——新增 `arrangePlayPattern()`
  （节目单游标 arrSec/arrBar 处的块解析出的型），`refreshAfterPatternChange` 的接续
  目标与 `applyPatternChange` 的回退链都改走它。切轨 = 同型幂等（相位零跳动）；
  胶囊条换当前段的型仍"点下即生效"；点预设退出（先退模式、再刷新）走原路径不变。

### 测试

- 新增 `tests/cases/t73-arrange-mode-exit.js`（3 场景 / 32 断言）：退出后跳段行 + 进度 +
  曲式名一并消失；播放中切轨后标题**逐步**（12 步）始终 = 节目单游标处的型；
  播放中点预设退回单练（v2.5.0「两个对等入口」契约回归）。
- **反向验证**：回退三处改动喂给 T73 → 15 条断言红，红的内容逐字复现两张实拍
  （「第 2/10 段 · 第 2/30 小节」残留、切轨后标题变「四分基础」）——不是恒绿摆设。

## v2.6.2 · 循环回卷的渲染层修复 + 开关归位（2026-09-20）

**来源**：三条用户实拍反馈——①「循环本段」那一行的「小节」二字被挤到下一行；
②打开「循环本段」并循环「第 1 到第 1 小节」时，小球跳到最后一个音符会**倒着返回**，
视觉上像个 bug；③预备拍 / 弹跳球 / 六线底纹 / 后台保活这四个开关不属于「训练模式」。

### 修复

- **练习循环的渲染层回卷**（②，本轮最核心的一条）：循环 [k,k] 的每一遍都落在**同一行**，
  渲染层按「同 row 的下一端点 = 本小节的后续音」插值，于是球与播放头从行尾向行首画一条
  反向弧（时间向前、位置回退）。修法与"不循环时"完全同一套逻辑：
  - `paintBall` / `audioPosAt`：同 row 但 `cumT` 不增的下一端点 = 循环下一遍的起点 →
    按**终端弧**处理——球向前跳完本行全程（终点 = 行右缘），播放头同向收束，不倒退；
  - 待命球的"下一行"与 `.next` 预告格改走 `loopNextBar`：循环开着时 = **区间起点**
    （[k,k] 就是本行开头、[1,2] 是第 1 行），快跳完时在循环起点做起跑预备动画；
    循环关着时 `loopNextBar ≡ (bar+1)%vizBars`——旧行为一字不差（T30/T41 继续钉住）。
  - scheduler / predictNext / loopRange 数据口径一行未改，本次纯渲染层修复。
- **「小节」不再换行**（①）：「第 X 到 Y 小节」包成一个 nowrap 单元（`.loop-range`），
  窄屏下整组折行，末尾二字不再落单。

### 改动

- **开关归位**（③）：预备拍（含拍数输入）/ 弹跳球 · 预判提示 / 六线 · 扫弦谱底纹
  从「训练模式」区迁到**时值可视化卡片顶部**（`.viz-toggles`，网格之上）——它们管的是
  "这张图上看到什么 / 开演前怎么数拍"；后台保活迁到**顶栏版本 chip 旁**——它是
  "这台设备别睡"的应用级环境开关。四个开关的 id、处理器归属（Controls / KeepAlive）、
  持久化全部不变，纯 DOM 位置调整。

### 测试

- 新增 `tests/cases/t72-loop-rewind-ball.js`（2 场景 / 16 断言）：循环 [0,0] 时球逐帧单调
  向前、跳到行右缘收束、待命球在本行开头预备且不串行；循环 [1,2] 时第 2 小节末尾的
  待命球与 `.next` 预告格都落在第 1 行（区间起点），不落在区间外的第 3 行。
- **反向验证**：把三处新逻辑回退成旧写法喂给 T72 → 9 条断言红（球倒退回行首 x=-0.8、
  待命球 0 帧可见、待命球落在第 3 行 y=293）——证明用例真的抓得住旧 bug，不是恒绿摆设。

## v2.6.1 · 第 Ⅲ 期：编辑器支持增删小节（2026-09-20）

**来源**：第 Ⅰ 期把"型恒为 4 小节"放开了（域 1–64），但**界面上一直没有改小节数的入口**——
于是 1 小节的型（示例曲的节奏型 1~5 就是）在编辑器里永远只有一行、改不动。
**能力放开 ≠ 能用**；这一期把入口补上，第 Ⅰ 期那条契约才算真正落地。

### 新增

- **「＋ 在其后加一小节」**：在**当前小节之后**插入一小节，内容是**当前小节的副本**。
  取副本而不是空小节的理由：用户按这个键时几乎总是想要"再来一遍同样的东西"，
  而且副本天然满足"每小节恰好占满 N 拍"这条硬校验（给空小节会让保存键立刻变灰、状态栏报红，
  用户会以为坏了）。想改成别的，照旧点下方音符块库。
- **「− 删当前小节」**：删掉当前小节，走应用内确认框（与「清空当前小节」「复制到全部」同一口径：
  破坏性动作先确认，虽然 Ctrl+Z 也能救）。**下限 1 小节**——0 小节的型没有合法解释
  （渲染 / 发声 / 段长都要除以它）。**上限 `MAX_PAT_BARS`(64)**，到顶即禁用。
- 禁用而不是"点了才报错"：与扫弦方向 / 弦区行的口径一致。

### 顺带修掉两处"写死 4 小节"的界面谎话

- 编辑器卡片标题原写死「编辑区 · 4 小节」→ 改为跟着实际小节数走。
- 编辑器标题「基于「X」创建 · 4/4 拍」→ 追加「· N 小节」。
- 「复制小节到全部」的确认文案原写死"全部 4 小节"→ 改用实际小节数。

### 一处必须加的守卫（否则删小节后会崩）

**小节数会变了，`editBar` 这个"当前小节"游标就必须每次渲染前收进范围**。
此前型恒 4 小节、游标不可能越界，所以没有这道守卫；现在删小节 / 撤销（撤销只回滚 `bars`、
不回滚游标）都可能把它留在数组外，而音符块库的追加路径 `d.bars[editBar].push(...)` 会直接抛
`TypeError`。收在 `render()` 开头而不是各调用点：所有会改 `bars` 的路径最后都要过 `render()`。

### 测试

新增 `tests/cases/t71-editor-bars.js`（6 个场景 / 31 条断言）：加一小节是当前小节的副本且校验通过、
标题跟着小节数走、删小节走确认框、**删后追加音符不越界**（上面那处守卫的回归）、
下限 1 时禁用且硬点无效、撤销能连小节数一起回退、存成预设后端到端生效（主视图随即渲染 3 行）、
加到上限 64 后禁用。

### 顺带修掉一条会随机变红的门禁（`tools/smoke.js`）

**「Service Worker 已注册」这条断言是竞态**：`register()` 只是**发起**注册作业，不是同步完成；
而探针一等到 `window.__beat` 就立刻查 `getRegistrations()`——`__beat` 的赋值点离 `register()`
只有几十行，作业常常还没落地，于是读到 0。同一份代码连跑两次，一次 0、一次 1。

一条会随机红的门禁比没有门禁更糟：它教会所有人"重跑一次就好"，真红的那次也会被当成抖动忽略掉。
改法是给一个有上界（4s）的轮询等待——**等的是"异步动作完成"，不是"把失败等成成功"**：
真没注册的话，4 秒后照样读到 0、照样判失败。`file://` 通道整段跳过注册，所以那里不等待。

发现它的前提值得一提：这台机器上 Chrome 自带的沙箱起不来，`smoke.js` 一直是**挂住**而不是失败，
于是这条断言从没真正跑过。这次用 `--no-sandbox` 的包装脚本喂给 `BEATSIGHT_CHROME`（该环境变量
本就是为这种场景留的），第 9 项才算第一次真跑起来——**一直跳过的能力，等于没有**。

## v2.6.0 · 第 Ⅳ 期：示例曲《在他乡》换成**30 小节逐小节谱**（2026-09-20）

**来源**：用户最初报的两件事的闭环——「4 小节都是同一节奏型」「只能重复练习单一节奏型」。
第 Ⅰ 期拿掉了"型恒为 4 小节"、第 Ⅱ 期把网格变成歌曲小节上的滚动窗口，
**这一期把示例曲的数据换成逐小节谱，滚动窗口才第一次真的看得出东西**。

### 数据：从"10 段 × 4 小节垫法"改成"30 小节逐小节指名"

- **全曲 30 小节**（此前把每段垫到 4 的倍数 → **44 小节**）。段长 `1 / 3 / 4 / 2 / 4 / 2 / 4 / 2 / 6 / 2`，
  逐格来自用户给的参考页 `var BARS`（那一页每一小节都带自己的 `strum` 字段）。
- **5 个型各是 1 小节**（此前 7 个型、每个 4 小节）：`节奏型1~5` 与参考页的 `STRUMS.P1~P5` 一一对应。
- **取消「收束」「收尾」两个 4 小节型**——它们存在的唯一理由是"两个相邻小节各用不同的型"，
  那正是把 4 小节循环硬套到逐小节谱上的产物。现在还原为本来的样子：
  段 4 = （P2 × 1 遍）+（P1 × 1 遍）；段 10 = （P4 × 1 遍）+（P1 × 1 遍），**两块而已**。
- `demoBuildSpec` 新增"逐小节型 id → 块"的合成：**相邻同型合并成一块**（块 = 型 × N 遍），
  顺序与相邻关系不变，所以块的展开结果与逐小节序列逐一对应。
- **歌词零改动**：现有的 10 段词行数（1/3/4/2/4/2/4/2/6/2）与真实逐项吻合，每行内容也对得上
  ——被垫错的从来只有扫弦。所以这次**不需要用户补任何数据**。

### 两处必须同步改的映射（否则会静默吃掉内容）

- `Arrange.ensureDemo()` 原先**只映射 `blocks[0]`**。逐小节谱之后「收束」「收尾」各有 2 块，
  只映射第一块会把第二块整块丢掉（段长从 2 小节缩成 1，而歌词还是 2 行 → 最后一句对不上、没声音）。
  改为逐块映射。
- 侧栏「切换节奏型」胶囊条（v2.4.1）原先**把整段重建成单块**。改为只换**第 1 块**的型、其余块原样保留：
  胶囊条的语义是"这一段用哪个型"，不是"把这一段重编"，换一次型不该把段长改短。

### 可见效果（真实浏览器已验）

网格滚过段边界时，**同一屏里会出现不同的型**。判据用"每行的扫弦箭头数"（型的指纹）：
- 第 0 小节起 → `P1 / P2 / P2 / P2`（16 / 14 / 14 / 14 支箭头）
- **第 8 小节起 → `P2 / P1 / P3 / P3`（14 / 16 / 14 / 14）—— 一屏三种型**
- 第 28 小节起 → `P4 / P1 / P1 / P2`（跨曲尾绕回开头）

### 测试

- 新增 `T63e`：全曲 30 小节 / 段长数组 / **12 个块的骨架串**（逐段逐块与参考页对齐）/
  "没有字越界"（段长与词行逐小节对齐的直接判据）/ 每段最后一行落在该段最后一小节。
- 新增 `T70g`：用示例曲实拍"同一屏出现不同的型"（上面那三组指纹）。
- 口径更新（都是**数据变了**而不是功能坏了）：`customs.length 7→5`、示例组条目 7→5、
  胶囊 7→5、`lyricSpanTicks(段 9) 1536→1152`（6 小节不再垫到 8）；
  T63b 里"收束是组合型"的断言换成"收束段 = 2 块各 1 遍"；
  T63d 的歌词锚点只数**第 1 小节内**的（段 1 现在是 1 小节，下一小节已属段 2，前瞻窗口会把它的锚点一并排进来）；
  T68j 的推进时长 4.4s→1.4s（第 1 段 = 1 小节 = 1s）。

## v2.5.2 · 第 Ⅱ 期：网格 = 歌曲小节上的 4 行滚动窗口（2026-09-20）

**来源**：用户要的「4 小节对应曲子**实时播放**的那 4 小节，跟着播放头滚动」。
v2.5.1 之前网格画的是"当前型的 4 小节"——对逐小节指的谱（节奏型各 1 小节）就是同一小节被盖 4 遍。

### 核心做法：把窗口的 4 个小节**合成为一个临时型**

这样渲染层的每一行仍然是"某个型的第 bar 小节"，**行可以来自不同的段、不同的型**，
而下游（格子 / 进度填充 / 十六分闪烁 / 弹跳球 / 待命球）**一行都不用改**——
比"让渲染层到处支持混合型"少动一个数量级的代码，也把风险压在窗口合成这一个纯函数里。

- 新增共享区三个纯函数 `songBars / songBarBefore / songBarAt`（把 (段, 段内小节) 摊平成
  **歌曲线性小节号**），与 `secBars / blockAt` 同族、同样只通过注入的 `patLenOf` 问型长。
- Viz 新增窗口态：`ARR_WIN_BARS = 4`、`winStart`、`winPat`（合成型，buildViz 时算好并缓存，
  热路径 O(1) 取用）、`vizPattern()`（曲式模式 = 窗口合成型，预设模式 = 当前型，原行为一字未改）、
  `arrWindowPat()`、`audibleSongBar()`、`rowOfOnset()`、`mutedRow()`。
- **窗口锚点是一行的事**：`winAnchor(k)` = `k` 就是"跟播放头滚动"（当前小节恒在第 1 行），
  改成 `Math.floor(k / ARR_WIN_BARS) * ARR_WIN_BARS` 就变成"每 4 小节翻页"。
  手感档位集中在这一个函数里，改它不用动别处（注释里写明了）。
- **重建由渲染侧驱动、按「可听小节号」**：调度器提前一个前瞻窗口（默认 300ms）推进
  `arrSec/arrBar`，按它重建会"画面先跳、声音后到"。所以 `paintFrameBody` 开头先同步窗口
  ——**且必须在取 `playheadEl/trailGlowEl` 别名之前**（重建会换掉这两个元素）。
  每小节一次，不是每帧。

### 两个"小节号"必须分清（本期最容易错的地方）

`e.bar` 是**型内**小节号（调度器与端点表的口径），窗口行是**歌曲小节号 - 窗口起点**。
所以端点在画之前一律走 `rowOfOnset()` 换算（`audioPosAt`、`paintBall`、待命球三处）。
待命球那块还多一层：**"内容取自型的第几小节"与"画在第几行"在曲式模式下不再相等**
（前者要解析下一小节的型，后者恒为当前行 + 1），代码里拆成 `cand` / `rowNext` 两个名字。

为使换算可行，端点补齐了三个字段：`aSec/aBar`（节目单位置，v2.0.2 已有）与新加的
`pb`（发声时的**乐句相位**，状态栏静音提示要用）；`predictNextArrange` 的两个返回也补上了
`aSec/aBar`（否则预测端点会被画到错误的行上）。

### 静音拍：从"型内最后一小节"改成"乐句位置"

旧判据 `schedBar === 型长-1` 对 4 小节的型恰好等于"每 4 小节静第 4 小节"，但对 **1 小节的型**
会把每个小节都判成最后一小节 → 一开静音拍就整段静音（真实用例的节奏型就是 1 小节）。
改为线性小节计数 `schedPlayBar % 4 === 3`（新增 `schedPlayBar`，四处游标重置点一并归零）。
对 4 小节的型**逐位等价**（T69d 守着）；1 小节的型变成"每 4 遍静一次"（T70f 守着）。

### 口径跟着改的两处显示

- **状态栏**：曲式模式下报**歌曲第几小节**（窗口起点 + 行号），不再报"第几行"
  ——滚动窗口下第 1 行永远是当前小节，报行号等于把位置信息删掉。
  「静音拍」那一句走**发声侧真值**（可听端点的 `pb`，拿不到再退回 `schedPlayBar`），
  因为"这一遍到底静没静"是确定的，而"哪一行会被静"在 1 小节的型上轮转、标不出来。
- **卡片标题**：曲式模式报「歌曲第 N-M 小节」（窗口走）而不是笼统的"同屏 4 小节"；
  预设模式报「同屏 N 小节」（N = 型的小节数，不再写死 4）。

### 已知边界（本期刻意不做，写清以免误当 bug）

- **歌词轨仍是"当前段"**：它按段内 tick 定位、按 `arrSec` 重建，不跟着窗口跨段。
  窗口跨段时，网格上是 A 段尾 + B 段头，歌词轨仍只显示其中一段。
- **预设模式下的行标识**：型长 ≠ 4 时不静态标"哪一行会被静音"（相位随循环次数轮转，标不出来），
  到点那一刻由状态栏说明。

测试：新增 `tests/cases/t70-arrange-window.js`（滚动取样 / 跨段混合行 / 状态栏口径 / 卡片标题 /
静音拍乐句相位 / 坏引用回落）。**T53k 的判据改写**（不是放松）：滚动窗口下"行"与"内容"已分开，
旧的"待命球落在第几行"不再能区分可听位置与调度游标——那一类错行从结构上不可能再发生；
新判据仍能抓住"把行号改回 `nrow.bar`"的回退。

## v2.5.1 · 第 Ⅰ 期：拿掉「型恒为 4 小节」这个核心不变量（2026-09-20）

**来源**：《在他乡》的扫弦是**逐小节指名**的（节奏型 1~5 各是 1 小节的型，段长 1/3/4/2/4/2/4/2/6/2，
全曲 30 小节）。而 BeatSight 的 `secBars = Σrepeats × 4` 把段长锁死成 4 的倍数 →
**30 小节被垫成 44 小节**，每段里同一个小节被盖 4 遍 —— 用户报的「4 小节都是同一节奏型」
就是这么来的。要按真实谱面走，第一步必须是**型的小节数自由**。

**本版是纯能力放开，行为零变化**：现有数据（含 12 个内置型）全是 4 小节的型，逐位不变。

- **`LOOP_BARS` → `DEF_BARS`（改名即改义）**：这个常量此前被当成"型恒为 4 小节"用在 40+ 处，
  而那**从来不是**它的本义——真正决定型有几小节的是 `pat.bars.length`。
  现在它只剩两个用途：① 新建型的默认小节数；② 长度不可解析时的兜底。
- **新增 `patBars(pat)`：全应用唯一该被用来问"型有多长"的函数**（`bars.length`，空则退回默认）。
  校验 / 循环游标 / 网格行数 / 段长 / 练习循环下拉全部改走它。
- **校验域**：`validatePreset` 从 `bars.length !== 4` 放宽到 `[1, MAX_PAT_BARS]`（新增上限常量 = 64）。
  上限是**导入护栏**：没有它，一份改坏的 JSON 能造出几十万小节的型，校验循环本身就变成卡死入口。
  每小节仍然必须刚好填满 `meter×TPB`——放开的是"有几小节"，不是"每小节多长"。
- **段长**：`secBars/blockAt` 从 `×4` 改成 `× 该块所引用型的实际小节数`。
  ★ 怎么做到既不破坏"纯函数可直测"（T52）又能知道型长：新增**装配层注入**的查询钩子
  `setPatLenOf(ref => patBars(resolveRef(ref)))`，默认值恒为 `DEF_BARS`（老数据/假 ref 行为不变）。
  漏注入不会崩但会退回 4 的倍数——由 T69e 的"3 小节 × 1 遍 = 3"钉住。
- **循环区间分两层钳制**：`clampLoop`（加载期，只做绝对域 `[0, MAX_PAT_BARS-1]` + 保证 from≤to）
  与新增的 `loopRangeFor(n)`（使用期，按当前型的小节数二次收窄）。原因：`S.loopRange` 是
  持久化的练习偏好，与"当前型"是两份独立状态，型变短后存下来的 to 会指到不存在的小节上。
- **渲染层新增 `vizBars`**（与 `vizSig` 同时刻锁定）：行数、静音拍标识、播放头折行、
  弹跳球行界必须用**同一个**模数，否则换型瞬间会"网格已按新型重建、播放头还按旧模数折行"。
- **练习循环下拉**的项数改为当前型的小节数；「循环本段」= `[0, n-1]`；提示文案跟着型走。
- **已知边界（留给第 Ⅱ 期）**：静音拍目前仍是"型内最后一小节"。对 4 小节的型等于一直以来的
  "每 4 小节静音第 4 小节"（一字未变），但对 1 小节的型会退化成"把自己整个静掉"——
  第 Ⅱ 期网格变成"4 行滚动窗口"之后，这个判据要改成按**乐句位置**（跨型、跨段线性计数）。

测试：新增 `tests/cases/t69-pattern-length.js`（校验域 / 网格行数 / 回绕 / 静音拍不变 /
段长与 `blockAt` / 循环下拉）；T65a 的钳制域断言按"两层"重写；
死循环看门狗新增 `pat_bars_max`（64 小节的型必须"能跑"而不只是"能存"）。

## v2.5.0 · 功能：扫弦/节拍拆成两条独立声部（节拍器每拍都响 + 独立音量条）+ 主界面「整首连播」（2026-09-19）

**来源**：两条使用中的反馈。两者表面上一个是"听感"、一个是"流程"，根子是同一件事——
**扫弦轨此前把"哪只手在动"和"拍子在哪"混成了一条声部**，于是既听不出拍子，
也退不出去单练某一个型。

### ① 扫弦谱不再吞掉节拍器：节拍/扫弦拆成两条独立声部

- **现状问题**：示例曲那几种型是逐十六分格的扫弦谱，几乎每一格都带扫弦记谱。
  而 `playClick` 里 `zone` 分支是**优先并 return** 的——带弦区标注的音符走弦区音色、
  不再发节拍音。结果是整首放下来只有"右手"，**没有一拍节拍器**；
  空扫（`rest+dir`）与纯休止所在的拍更是连一点声音都没有。
  节拍器"每一拍都发声"是它之所以能当节拍器的本质，这一刀把它整个砍掉了。
- **改法**：`schedOneStep` 里新增一条**节拍器声部**——以半开区间 `[cumT, cumT+step.t)`
  扫出本步跨度内的拍点，逐拍补一声 click；扫弦声叠在扫弦记谱的位置上（同时刻，不顺延）。
  四条边界一起守住：
  - **只对"带扫弦记谱的谱"生效**（`S.track === "strum" && hasStrum(pat)`）：
    不带 `dir`/`zone` 的谱**本身就是节拍器**，给它再补一层网格会把长音中间多敲出几下
    ——那是行为变化而非修复（实测会打翻 T48/T65 那批"发声个数恰好 N"的断言）。
  - **自身就落在拍上的普通音符不被补成双声**：判据与 `playClick` 里那条 `zone` 分支
    **逐字一致**（drum 音色下 zone 不生效、仍走节拍音），写错一处就会"某一拍整拍没声音"。
  - **静音拍下两条声部一起静音**（`muted` 一视同仁）。
  - **拍点不参与 Swing**：补的是"第几拍"这个绝对位置，而 `swingShift` 只描述拍内后半的八分
    ——能被 swing 的位置必然不是整拍，两者天然不相交。
- **两条独立音量条**（对齐参考页那对「节拍 / 扫弦」滑条）：
  - 原「**总音量**」在界面上改名为「**节拍**」——它管的一直是节拍器那一下（每拍拍点、
    预备拍、歌词锚点提示音），旧名字比职责大。
  - 新增「**扫弦**」条 + 新状态 `S.strumVol`（热键持久化，`clamp01` 加载校验，默认 1）。
  - **并列而非串联**：`playClick` 里两条声部各取各的音量，`strumVol` **不**乘在 `vol` 上——
    所以把「节拍」拉到 0 时扫弦照响，反之亦然（"独立调节"的定义）。
    「重拍增强」与层级（重拍/正拍/细分）对两条声部同样生效：那是"这一下有多重"，
    与"这条线整体多响"是两个正交维度。
  - 「扫弦」整行**只在带扫弦的节拍轨出现**（普通轨没有任何扫弦声可调）。
- **刻意的边界**：节拍网格**不写入 `onsetBuf`**——弹跳球跟的是"你要弹的那条声部"
  （扫弦声部）的落点，节拍层是参照网格。这条写进注释与 DEVELOPMENT §3.2。

### ② 主界面一键「整首连播」+ 段序条

- **现状问题**："按原曲顺序走完 10 段"这个能力**早就存在**（曲式编排的「从头播」，
  `play(false)` 会把范围设成 0..末段），但它有两个门槛：① 要先钻进全屏 overlay；
  ② 默认**不循环**（走完就停）。而日常最顺手的那个键（主播放键）走的是预设模式 = 单型死循环。
  更关键的是——**曲式模式此前没有任何主界面出口**：全文件只有"曲式校验失败"与"删除曲式"
  两条路会退回预设模式。于是"点侧栏某个节奏型 + 按播放"播的仍是节目单，而画面标题已经换成
  他点的那一项；用户报的"只能重复练习单一节奏型"，一半就是被这个半状态困住的。
- **改法**（补的是入口与顺手度，机制一行没改）：
  - 预设库「示例曲」分组顶部新增 **「整首连播」按钮**：一键切曲式模式 + 范围=整首 +
    开范围循环 + 起播，不必进 overlay；按钮在整首模式下高亮，说明文字实时显示
    「整首连播中 · 第 N/10 段」。
  - 其下新增**段序条**：按原曲顺序列出 10 段（`序号 + 段名前半`，如「8 主歌二」），
    当前段高亮；点某一段 = **只循环那一段**（复用 v2.4.3 的 `jumpTo`：`range=[N,N]` + loop），
    停止态下还会把网格换成那一段引用的型。
  - **点侧栏任一节奏型即退回单型练习**（`exitArrangeForPreset`）——两个入口从此对等：
    点节奏型 = 单练它，点「整首连播」= 按原曲顺序练全曲。这是本次真正的"流程修复"。
  - 段序条呈现收口在 `Presets.syncDemoSecRow()` 一处，由 `Arrange.refreshBar/refreshNow`
    与 `Controls.stop` 调用（`Arrange → Presets` 是合法方向，不需要钩子；
    跳段反向则经新钩子 `onDemoJump` 注入）。

### 工程

- 模块索引行号重算（`tools/gen-index.js --write`）；README / DEVELOPMENT / 使用方法页同步。
- 新测试组 `tests/cases/t68-whole-song-and-voices.js`（扫弦每拍出声 / 静音拍 / 不双声 /
  无记谱不补网格 / 双声部音量独立 / `strumVol` 脏值与持久化 / 整首连播 / 跳段 / 退回单练 /
  播放中高亮跟随）；死循环看门狗新增 `strum_vol_dirty`（第二个进热路径的声部音量）。
- **T64g 的口径修正**：原断言「两条轨的发声**点数**相同」在本次功能下必然失败
  （扫弦轨多出一条节拍网格），且它测的其实不是"zone 有没有碰时间轴"。
  改为只比对**节拍音声部**的时刻逐位相同 + 扫弦声叠加在同一时刻。
- `tests/lib/harness.js` 的 `HTML_ATTRS` 补 `volStrumRow: { hidden: true }`。

## v2.4.4 · 外部审计驱动的结构收口：安全头补齐 + 模块改名 + 巨函数拆分 + 产物戳记自检（2026-09-18）

**来源**：一次全仓外部审计（五维：代码质量/性能/架构/安全/工程化）。结论与 v2.0.2 内审一致——
代码本体健康，缺口在链路。本版只做结构与工程收口，**零功能语义变化**。

- **安全头补齐（`_headers`）**：新增 `Content-Security-Policy`（default-src 'self'，脚本/样式保留
  'unsafe-inline'——单文件内联架构的既定取舍）、`frame-ancestors 'none'` + `X-Frame-Options: DENY`
  （关点击劫持面）、`Permissions-Policy`（mic/camera/geolocation 全关）、`HSTS`。
- **`Audio` 模块改名 `AudioEngine`**：原模块名遮蔽全局 `window.Audio` 构造器——虽无实际调用
  `new Audio()`，但名字本身就是地雷。改名波及 check-module-order 的 EXPECTED_ORDER/WHITELIST、
  全部测试用例与 DEVELOPMENT.md（共 80+ 处，机器可验证）。
- **四个百行级函数拆分（纯搬移，行为等价）**：`buildViz` 拆出 `buildBarRow`（建一小节行）；
  `schedulerBody` 拆出 `schedOneStep`（主循环的一步，return/continue 语义换算为返回值）；
  `paintFrameBody` 拆出 `paintBeatFlash`（十六分闪烁+座次尺+状态栏）与 `repaintCells`
  （全量/增量重绘）；`Arrange.render` 拆出 `buildSecRow`（建段行）。
  ★ 拆出 paintFrameBody 的两个 helper 后**同步登记进 check-module-order.js 的 HOT 名单**——
  否则拆分会静默缩小 R2 热路径检查的覆盖面（这条教训已写进该文件 HOT 注释里）。
- **复制粘贴收口**：四个 persist 冷键函数抽成 `makePersister(key, payload)` 工厂；
  四个开关处理器抽成 `bindToggle(id, flip, after)` 工厂。
- **渲染性能两点**：Editor/Arrange 的全量重建改用 DocumentFragment 一次插入（测试桩同步补了
  DocumentFragment 语义：append 片段 = 子节点摊平搬家，harness.js 与 hang-case.js **两份桩都改了**）；
  十六分闪烁的 `sub.closest(".cell")` 改为建网格时登记的 `subCellEls` 正查表（帧内零 DOM 上溯）。
- **部署闸门（产物戳记自检）**：`build-dist.js` 装配时把「check-all 通过」戳记写入 dist
  （版本 + 时间戳 + 文件校验和），index.html 启动时校验——缺戳记/版本不符时在诊断面板可见，
  把「线上那次构建到底跑没跑自验」从不可知变成可读。
- **工程杂项**：`lineOffset` 三处重复实现收进 `scan-util.js` 统一导出；check-module-order.js
  的 stripComments/matchBrace/lineOf 同样改为复用 scan-util（删掉三份逐字相同的私有拷贝）；
  check-module-order 文件头补「index.html 禁止过格式化器」的硬约束说明（R1 依赖缩进约定）；
  sw.js 文件头注释版本漂移修正；PWA 补 192/512 PNG 图标回退（含 maskable）——
  **二进制不入库**，由 `tools/gen-icons.js`（零依赖光栅化 + PNG 编码）构建期生成落 dist/
  （任何"只能推文本"的发布路径都带不动二进制）；sw.js 预缓存拆成关键资源（严格 addAll）
  + 可选图标（逐个 catch，仓库根开发环境没有 PNG 也装得上）；sw.js 纳入
  测试（tests/cases/t67-service-worker.js，同步 Promise 桩 + 写回队列时序修正）；
  新增模式字段契约 `setMode()`（tests/cases/t66-mode-contract.js：值域白名单 / 幂等 / 迁移轨迹）；
  checklist.md / tasks.md 补「快照声明」横幅（它们是 2026-09-17 审计的当时实测值，会漂移，
  口径以 check-all 实时输出为准）。
- **关键入口补 JSDoc 签名**：buildViz / schedulerBody / start / stop。

## v2.4.3 · 练习循环（只反复磨第 X 到第 Y 小节）+ 扫弦箭头改挂独立图层（2026-09-18）

**功能**：新增**练习循环**面板（预设库卡片内、轨切换条之下、列表之上）。
把播放限制在当前型的第 `[from, to]` 小节之间无限回绕，专治"一两小节老是弹不顺"。

- **★ 与参考页的语义差异（必须先说清）**：参考页 [kimi.link 扫弦 demo](https://4ste2er6uae56.ok.kimi.link/demo.html)
  的 **per-row「循环本段」**在 BeatSight **没有直接对应物**——那个页面的"段落"是变长的，
  而 BeatSight 的一个节奏型**恒为 4 小节**（`LOOP_BARS`），选中一个型本来就会无限循环。
  所以"循环某个节奏型"这件事**本来就成立**；真正缺的是**更小的粒度**。
  于是本版做成**一个区间 = 一个开关**，两种入口操作同一份数据：
  - **「循环本段」按钮** → 一键把区间置为 `[0, 3]`（= 全部 4 小节，语义上就是"循环这个型"）；
    已是整段时再点即关（同一颗按钮兼管开关与归位，比另加一颗"重置"少一个控件）。
  - **两个下拉框**「第 X 到第 Y 小节」→ 精确指定区间；改完**顺手开启**循环
    （用户意图已经很明确，再要求他点一下开关是多余的一步）。
  - **互斥是设计**：两者说的是同一件事（循环范围），共用 `S.loopRange.on` **一个**开关。
    若各存一份，就会出现"开关亮着但循环的是别的区间"这种没有合理解释的状态——共用后物理上不可能分裂。
- **只作用于预设模式**：曲式模式的小节推进由节目单（`arrNextBar`）唯一决定，
  区间循环套上去会把节目单卡死在某一小节。此时整个面板**收起**（不是灰掉——
  没有"稍后可用的操作"值得保留位置）。
- **持久化**：`S.loopRange {on, from, to}` 跟**热键**走（250ms 防抖）。这是练习偏好
  而不是会话状态——下次打开还要接着磨那两小节，理由与 `S.showTab` 相同。
  读取一律经 `clampLoop()`：**localStorage 当不可信输入**，越界/倒置一律归位。
- **实现要点**：
  - **`loopNextBar(b)` / `loopStartBar()` 抽成模块级函数**。原先"小节边界"有**两个**出口
    （空小节跳过 + 常规走完），v1.9.0 的注释把「本处是小节边界的唯一出口」写在常规出口上、
    靠人工保持一致。循环若只改一处，空小节就会**漏出区间**。抽成单一函数后，两个出口都调它，
    不可能不一致；`loopStartBar()` 管"从哪开始"（少了它，循环区间第一遍会从 0 开始 →
    用户选了第 3-4 小节却先白弹两小节，功能看着生效、起点是错的）。
  - **`clampLoop()` 做跨字段归一**：`from`/`to` 各自钳到 `[0, LOOP_BARS-1]` 后再交换保证
    `from <= to`。两次独立钳制做不到这件事——`from=3 / to=1` 各自都合法，合起来是空区间，
    调度器会算出"永远到不了 to"从而卡死。
  - **`Audio.rescheduleLoop()`：改完立刻可听**。循环是"小节边界往哪走"的规则，而 scheduler
    是**前瞻式**的（已有一批音符排进了音频时钟）——规则改了不会自己回头，不重排的话
    用户点下开关要等当前 4 小节放完才生效，像开关没反应。做法：游标挪到区间起点、
    时间轴对齐到"现在稍后"、清 onset 缓冲、复位 `cueBarT`。
    **与 `resyncToNow` 的分工**：那个是"换了节奏型要在新型里找回相位"（保相位），
    这个是"循环规则变了、相位本来就该重置"（弃相位）——语义不同，故不合并。
    **固有差异（不是遗漏）**：参考页能清 `sched` 队列，BeatSight 的音符已经交给
    Web Audio 排程（`osc.start(t0)`）没有可取回的队列，已发出的 ≤1.2s 前瞻窗无法撤回；
    听感上最多是"点下去后还有半拍旧位置的声音"，不会错拍。
  - **`resyncToNow` 里加了区间外钳制**：它按"绝对时间轴位置"反解小节，解出的 `b` 可能落在
    区间**之外**（开始循环前时间轴已走过更远的位置）。若不拽回，循环回绕只在"越过 to"时回头，
    从区间外会跳过区间起点、甚至永远回不去。

**修复（同版并入，源自用户截图）**：

- **★ 扫弦箭头被裁掉尖端**（用户："第二个箭头标记为什么缺失了头顶的尖端？还有好几个箭头都有这个问题"）。
  根因：`.cell` 是 `height:44px` + `overflow:hidden`，而箭头是它的**子节点**，
  `.kF` 需要 `top:8px; height:45px`（8→53 贯穿六线）→ 底部被裁 9px；尖端在 `top:-5px`
  又超出格子上边界 → **头顶被裁**。实测 **24/24 支箭头全部被裁**（`_cdp-clip.js` 读数）。
  **修法照抄参考页**：箭头改挂**独立的全高图层** `.strums`，它是格子的**兄弟节点**而不是子节点，
  用同一套 `%` 定位到格中心（与格子的 `left`/`width` 共口径，都不依赖像素宽度）。
- **★ 弦区箭头的尖端越过弦区边界**（用户："高音区上扫的箭头尖端碰到了 4 弦，但高音区只在 1-3 弦"）。
  参考页把尖端画在跨距**之外**（`dn` 在 `--y1-6`，`up` 在 `--y2` 向下长 7px）——在 `.kF`（8→53）
  上不可见，但在**弦区**上是错的：`.kT` 上扫的尖端（26→33）逼近 4 弦（35）、
  `.kB` 下扫的尖端（35→29）越过 3 弦（26）。**修法：尖端改画在跨距之内**——
  `.dn::after{top:var(--y1)}`（顶点落在 y1）、`.up::after{top:calc(var(--y2) - 7px)}`（顶点落在 y2），
  墨迹永不越过所声明弦区的边界。`_cdp-span.js` 复测 **`OVERSHOOT_COUNT=0`**。
- **`.strumv.hit` 的发光原先画在宿主盒上而不是墨迹上**：箭头可见像素来自 `::before`/`::after`，
  宿主盒只有 2px 宽，`box-shadow` 等于没发光。改用 `filter: drop-shadow(...)`（照搬参考页），
  它作用于**实际像素**。
- **★ 箭头方向整体颠倒（D 渲染成 ↓、U 渲染成 ↑，且空扫无头）**（用户："箭头错误"+截图）。
  上一条"尖端收进跨距"的编辑顺手把 `.dn::after`/`.up::after` 的 **border 方向写反了**
  （dn 误用 border-top=▼、up 误用 border-bottom=▲），而 ghost/hit 的染色规则仍按原始方向写
  （base 染 border-bottom-color、.up 变体染 border-top-color）——于是 D/U 全颠倒之外，
  ghost dn 的三角保持白色、ghost/hit up 的三角被 `border-bottom-color:transparent` 整个隐掉
  （截图中蓝色虚线无头即此）。**修法**：两行 border 方向换回（dn=border-bottom ▲ / up=border-top ▼），
  位置不变（尖端仍在跨距之内），染色规则零改动即自愈；T47f 新增源码级断言把
  「CSS 注释的方向映射 ↔ dn/up 的 border 方向 ↔ ghost/hit 染色配对」三者绑死。
- **★ 六线底纹整体掉在格子外面**（用户："你是把六弦放在格子外面了？六根弦都改成在格子内部"）。
  根因有二：① **几何**——照搬参考页的 `--gt:9px;--gtop:8px` 使六线落在 8/17/26/35/44/53，
  而 `.cell` 高 44px → 第 5 线贴底缘、**第 6 线悬在格子下方 9px**（窄屏 34px 格子同样超界 1px）；
  ② **层叠**——`.tab` z-index:0 被 DOM 在后的格子（半透明背景）整层压住，格内的线淡到几乎不可见，
  唯一"清晰"的恰是掉在外面的第 6 线。**修法**：变量收为 `--gt:7px;--gtop:5px`
  （线落 5/12/19/26/33/40，顶留 5px 底留 4px；窄屏同步收到 5/10/15/20/25/30），
  六线全部落进格子高度内；`.tab` 提到 **z-index:1** 压在格子上面，六根线在格内清晰可见。
  箭头跨距（kF/kB/kT）引用同一组变量**自动跟随**（实测 5→40 / 26→40 / 5→19，6/6 支几何入格）；
  建线公式（`calc(var(--gtop) + N * var(--gt))`）未动，t47b 断言零影响；
  `_cdp-tab.js` 实测 `LINES_OUT=0 / ROD_BAD=0 / inCell=6/6`。
- **「预设库条目重复」经查不是代码缺陷**：用户截图来自一个**残留的本机预览服务进程**
  （旧会话留下、其 `_serve.js` 已删），它按旧代码在内存里渲染。代码侧实测 `dupNames: []` /
  `customsDup: []`（`buildDemoGroup` 只被调用一次、`splitCustoms` 的 `demo`/`rest` 不重叠）。
  **未改任何渲染代码**，只重启了预览服务。

**★ 定位过程中修掉的两处「测试在说谎」（值得单独记，因为差点导致误判产品缺陷）**：

- **★ `.strums` 层里的孩子是「紧凑」的，不能用 `children[i]` 定位第 i 格**。
  层只 append 真正产生箭头的格（无方向的格不建节点），所以"层里第 i 个"≠"第 i 格"——
  只有**全格都有方向**时才偶然相等。v2.4.3 的窄格隐藏场景（T47b：一格有向、其余无）
  当场暴露了这一点：`disp(0, 4)` 取到 `undefined`，报 `(无箭头)`。
  **修法**：生产代码在箭头节点上写 `dataset.i`（格子下标），三处测试助手（t47/t62/t64）
  一律按 `data-i` 查表而不是按子节点序数。同时**没有**改成"每格塞占位节点"——
  那会让一行凭空多十几个空 `div`，而 `.strums` 每帧都可能重建，白增 DOM 与合成开销。
- **★ T65d/T65e 的「小节号」是伪读数，差点把能用的功能判成坏的**。
  第一版用"首音当 t0、再 `floor((t-t0)/barSec)`"反推小节号。两个错误叠在一起：
  ① 循环开启后首音落在 `from`，于是 `from` 被标成 0、整体平移；
  ② 默认型**各小节音符数完全相同**，纯靠时刻**根本区分不出**第 1 与第 2 小节。
  结果"没回绕"与"回绕了但被平移"给出**字节相同**的读数——我据此误判"循环没生效"，
  连写了 4 个探针都得出错误结论，直到把内部 `schedBar` 直接读出来才看到
  `[1,2,1,2,1,2,1,2,1,2]` / `[0,1,0,1,...]` / `[2,3,2,3,...]` **全部正确**。
  **修法**：改用**指纹型**（第 i 小节排 i+1 颗音，时值取合法档且和小节长），
  于是小节身份由**音数**直接读出，与时刻估算、与平移全部无关。
  实测：ON `[1,2]` → `[2,3,2,3,2]`（恒在区间内且交替）；OFF 对照 → `[1,2,3,4,1]`（完整 4 小节）；
  T65e `from=2` → 首小节 3 颗音（而非第 0 小节的 1 颗）。
  **教训**：断言的**观测量**必须能区分所有待区分的真相；否则测试通过与否都与被测行为无关。

## v2.4.2 · 视觉：扫弦谱照搬参考页的六线谱（贯穿式竖箭头 · 六线底纹 · 六线开关）（2026-09-18）

**只改视觉，不改结构**（用户明确要求）。格子仍是那个格子、箭头仍挂在各自格内、
仍由 `fitCellAnnotations()` 按格宽整体隐藏——换掉的是"箭头长什么样"和"底下有没有纸"。

- **参考页**：[kimi.link 的扫弦 demo](https://4ste2er6uae56.ok.kimi.link/demo.html)。
  它的核心读法是**迷你六线谱**：六条弦线常驻淡色打底，扫弦写成**竖箭头**，
  **箭头跨哪几条弦 = 弦区**。这比旧版"14px 方形徽标 + 徽标下方 2px 色带"更贴谱面直觉。
- **改：方向徽标 → 贯穿弦区的竖箭头**。类名由 `.cell-strum` / `.cell-air` 统一为
  `.strumv`，语义从"字形"改标到"类名"上：
  - 方向 `.dn`（下扫，箭头画在顶端）/ `.up`（上扫，箭头画在底端）——与 v2.1.0 的
    手部动作翻转同源，只是从 `textContent` 的 `↑↓` 换成 CSS `::before` 三角形；
  - 弦区 `.kF` 贯穿六线（`zone` 1 或缺省）/ `.kB` 只跨下三线（0）/ `.kT` 只跨上三线（2）——
    **旧 `.cell-zone` 色带随之删除**（弦区已内化为箭头自身跨距，不必再单画一条带子）；
  - 空扫 `.ghost` = 蓝色**虚线**箭头（旧版是蓝括号）。
  - **空扫的弦区跟随前一记实扫**（参考页明文），跨小节不延续——新小节从"全部弦"重起。
    小节是扫弦谱的语义单位，跨小节沿用上一小节末格的弦区会让新小节首格凭空落进低音区。
- **新增：六线底纹**（`.tab`，每行 6 条 `<i>`）。几何常量搬进 CSS 变量
  （`--gt:9px` 弦间距 / `--gtop:8px` 顶端留白 → 六线落在 8/17/26/35/44/53），
  **箭头跨距与底纹共用同一组变量**——否则"箭头端点压在弦线上"这件事一改就错位。
  底纹只在扫弦轨铺：普通轨没有箭头，留一层无解释的横线纯属噪音（同 v2.4.0 轨门控口径）。
- **新增：「六线 · 扫弦谱底纹」开关**（开关组内，`S.showTab` 默认**开**）。
  它只控**底纹**显隐（`.viz.no-tab`），**不影响箭头**——箭头是内容，底纹是参照物。
  关掉后谱面即回到"只有箭头"的 v2.4.1 观感，为快速练习留一条减法路径。
- **新增：播放中箭头高亮**（`.strumv.hit` = 绿色 + 外发光），与格子的 `.active` 同一判据。
  写法上**必须落在 `setCell()` 里**：增量重绘只重画 3 格、旧 `active` 那格也在其中，
  所以离场时自动摘掉 `.hit`，不需要第二条复位路径；`resetForStop()` 只补一朵
  "停机时摘掉残留高亮"的兜底（此时 `setCell` 不会再被调用）。
- **阈值随形态改**：`STRUM_MIN_W` 20 → 14（箭头仅 2px 宽，旧阈值是按 14px 方形徽标的
  宽度定的）。判据随之从"够不够放下一个方块"变成"与邻格至少隔开 10px"。
- **窄屏**：媒体查询里把六线几何按 0.66 收到 6px 弦距（六线落在 5..35px）。

测试：T47b 改为覆盖新形态（六线底纹条数 / `zone`→`.kF|.kB|.kT` 跨距 / 空扫沿用前一记 /
空扫不跨小节重起 / 窄格阈值新临界点），T62c 由"色带"口径改写为"箭头跨距"口径，
T64h 的"零色带"断言换成"零六线底纹"。断言从"读字形 `textContent`"改为"读类名语义"——
旧写法把"画了什么字形"当契约，一改字形就得改测试；新写法测的是数据有没有正确翻译成
谱面信息，与像素无关。

## v2.4.1 · 修复：示例曲《在他乡》自动带出并置顶 · 修两处切换缺陷（2026-09-18）

用户三条反馈的落地 + 一处根因修复。

- **示例曲《在他乡》放进预设库第一位，不再需要手动载入**：
  - 预设库顶部新增独立的「示例曲」分组（`Tracks.splitCustoms()` 把示例型与用户自定义
    分开，示例组恒置于内置组之前）。分组头带「示例曲」徽标 + 绿色竖线做身份区分。
  - **启动时 `Arrange.ensureDemo()` 静默自动带出**（幂等：数据已在就什么都不做），
    「载入示例」按钮随之删除（用户明确要求）。手动重载入口移到侧栏
    「恢复示例曲」按钮，且**只在示例缺失时出现**（正常用户永远看不到它）。
  - 示例型识别按**名字前缀** `"在他乡 · "` 而非 id——示例经 `importPresets` 落成
    随机 id（`c<时间戳>-<序号>`），每次载入都不同，id 不可作稳定标识。
- **★ 修复：载入示例后 7 个节奏型在预设库里根本不出现（用户报的"切换有问题"根因）**。
  `loadDemo()` 只调了曲式 overlay 的 `render()/refreshBar()`，两者**都不碰侧栏**；
  而 `Store.importPresets()` 只落数据、不重建列表（Store 不该知道列表怎么画）。
  结果数据写进了 `beatsight.customs`（实测 16 KB），侧栏却纹丝不动——用户在预设库里
  找不到示例型，"换一个节奏型"变成做不到的事。修法：抽出 `ensureDemo()`，
  在导入后统一调 `Presets.refreshAfterPatternChange()`（与 T64d/T64j 踩过的同一个坑收口）。
- **★ 修复：编排面板点「换」看不到候选列表**。候选行原先追加在 `#argSections` 末尾——
  点第 6 段的「换」，候选出现在第 10 段 + 歌词轨**之后**，用户视觉上「点了没反应」
  （必须滚动到底才看得见）。改为 `insertAdjacentElement("afterend")` 插到**被点段行的正下方**。
- **新增常驻「切换节奏型」胶囊条**（预设库示例分组内）：点一下就把当前段的块换成该型，
  下一个小节边界生效，不必进 overlay 逐个「换」。复用 `Store.upsertArrange()`
  走完整校验（绕过它直接改 Store 会跳过曲式结构校验）。
- **顺带收敛**：自定义型条目渲染抽成 `buildPresetItem()`，示例组与"我的预设"组共用
  （此前两处内联 = 两条会漂移的渲染路径）。`onDemoRestore` 钩子按 `onArrangeBar`
  同套模式注入（Presets 声明在 Arrange 之前，直接调用是反向引用）。

## v2.4.0 · 功能：普通节拍 / 带扫弦的节拍拆为两个独立入口（轨切换条 · 参数全共享）（2026-09-18）

把「普通节拍」与「带扫弦的节拍」做成两个相互独立的入口。**不是两个节拍器，是一个
节拍器的两个门面**——节拍计算与定时逻辑（`spb()` / scheduler）一行未改。

- **入口位置**：侧栏预设库顶部新增切换条 `#trackRow`（两个 pill：普通节拍 / 带扫弦），
  由新模块 `Tracks` 生成；紧接一行随轨变化的说明文字 `#trackNote`。
- **参数全部共享，仅入口区分**：`bpm/sig/vol/swing/timbre` 仍是同一个 `S` 单例，
  切轨**一概不动**——用户切到扫弦轨接着按原速度练，不必重调一遍。
  两个入口的差别只有三处，且都落在"呈现"与"轨道过滤"上：
  ① 预设库列哪些型；② 记谱层画不画方向箭头 / 弦区色带；③ zone 音色走不走。
- **扫弦预设从普通入口过滤掉**：新增纯函数 `hasStrum(pat)`（判据：`dir` 或 `zone`
  任一 `!== undefined`）作为**唯一分界线**。普通轨列表不含带扫弦记谱的型（内置 12 个
  里滤掉「民谣扫弦」1 个 + 同名自定义），并在说明文字里告知「已隐藏 N 个」——
  少一项而不解释会像 bug。
- **切轨后自动回退（不静默）**：`Presets.ensureValidForTrack()` 是运行期不变量的
  唯一执法点，挂在 `refreshAfterPatternChange()` 最前。普通轨容不下扫弦型时回退到
  **同轨第一个可用项**（真预设，不是基础节奏）并播报；试听中不动用户选择。
- **零迁移**：`beatsight.state` 里没有 `track` 键时**由 `saved.sel` 反推**
  （该型带扫弦 → `strum`，否则 `plain`），而不是硬编码常量——v2.4.0 之前应用默认
  开着「民谣扫弦」，所以老用户升级后听到的仍是他原来那一个。显式 `track` 键优先。
  `sel` 解析不出来（越界的废引用）时落到默认轨 `strum`，与全新用户结论一致。
- **索引完整性**：过滤后的自定义项删除改为按**对象身份**（`indexOf(c)`）定位，
  此前用过滤序位 `splice(i,1)` 会在普通轨下删错项。
- **既有调用方不受影响**：`__beat` 导出面只增不减；`BUILTINS` 索引未重排
  （曲式 / Ear 题库仍能取到被普通轨隐藏的型）；编排页的曲式预设选择器**刻意不按轨
  过滤**——切轨不该对既有编曲造成破坏性影响。

测试：新增 t64（T64a–T64j：轨状态与脏值回退 / `hasStrum` 判据 / 切换条 UI /
列表过滤与索引 / 零迁移与运行期不变量 / 参数全共享 / Audio 门控与时刻逐位不变 /
记谱门控与 `strums` 逐位同构 / 既有调用方回归 / 过滤下删除的下标安全），
t62 改为统一在扫弦轨上运行；合计 1828 PASS / 0 FAIL。

## v2.3.1 · 修复：加强项闸门转红（ESLint no-shadow / tsc 26 条类型错误）（2026-09-18）

v2.1.0 起 ESLint / tsc 两道**可选加强项**在装了依赖的构建环境里已经转红，只是本机
没装 node_modules 时会被标 ⊘ 跳过、一直没暴露——Cloudflare 构建（`--strict-env`
要求缺依赖即失败）因此中止。

- **ESLint · no-shadow**：Arrange 歌词编辑轨的 4 处拖动上下文局部量 `ctx` 遮蔽了
  脚本顶层的 AudioContext `ctx`。**选择改名（`ctx` → `lctx`）而不是把 ctx 加进白名单**：
  `ctx` 恰是 AudioContext 最常用的名字，放行它等于削弱整份文件的遮蔽保护。
- **tsc**：给音符类型补上 v2.2.0 引入的 `zone?: number`（6 处内联类型定义），
  并为歌词轨 / 示例曲 / 爬坡粒度开关补 JSDoc 标注与内联类型，合计消化 26 条
  隐式 any 与属性缺失报错。
- **行为零改动**：全部是标注与局部重命名，不碰任何运行时逻辑。

测试：1702 PASS / 0 FAIL（与 v2.3.0 一致）；ESLint / tsc 在装了依赖的环境下
首次实跑到全绿。

## v2.3.0 · 功能：内置示例曲《在他乡》（编排页一键载入 · 谱面逐格映射 · 幂等）（2026-09-18）

编排页新增「载入示例」钮，一键载入完整示例曲《在他乡》：7 个节奏型预设（5 个
基本型 + 2 个跨段组合型）、10 段曲式、全曲 10 行歌词对齐轨。

- **谱面逐格映射**：示例源页的逐格扫弦表 [格, 方向字形, 弦区字母, 空扫?] 映射为
  本工具音符字段——方向字形转手部动作（↓→"D"/↑→"U"）、弦区字母转 zone 三态
  （F→1 全部弦 / B→0 低音 / T→2 高音）、空扫格 = `rest+dir` 蓝括号不发声、
  无标记格 = 纯休止。
- **结构决策**：节奏型固定 4 小节而词行长短不一——词行不足 4 小节的段用
  同型扫弦补齐（手不停嘴休息）；本工具无和弦轨，和弦序列写进段名。
- **幂等载入**：曲式按固定 id / 曲名查重、预设按名查重——重复点不重复建，
  预设已有同名残留时直接复用；歌词随曲式一起写入对齐轨。
- 数据构建为纯函数 `demoBuildSpec()`（挂在 `__beat` 上），谱面转换与载入接线
  分离，测试直接断言转换结果。

测试：新增 t63（载入三件套与锚点抽查 / 逐格映射 / 幂等 / 播放冒烟与循环本行
落点），合计 34 条新断言。

## v2.2.0 · 功能：F2 按歌词行选段循环 + 扫弦轨升级（弦区三态音色 / 空扫 / 录入 UI）（2026-09-18）

两项来自第 0 轮契约的增强：

- **F2 按歌词行选段循环**：编排页每条歌词行多一个「循环本行」钮——落点 =
  「只播这一段 + 循环 + 变速爬坡」。爬坡粒度可选**每段一级**（loopPerSec，
  句跟句练）替代 everyN 小节；循环边界按段内绝对 tick，跨小节的延音字完整落在
  段内，回卷只在段尾发生。
- **扫弦轨升级**：方向标注扩展为 [方向, 弦区, 空扫]。新增音符字段 `zone ∈ {0,1,2}`
  （低/中/高弦区）——**参与发声**（带通三频段 700/1400/2800Hz）但不碰时间轴；
  空扫（rest+dir）保持静默，蓝色括号不变；方向显示按六线谱惯例（下扫=↑）。
  编辑器加「弦区」四档（默认/低/中/高），与方向档同一套 undo/高亮纪律。

测试：新增 t61（落点 / 爬坡粒度精确性 / 跨小节延音完整）与 t62（zone 校验 /
三弦区音色可区分 / 空扫静默 / 录入 UI），合计 45 条新断言。

## v2.1.0 · 功能：F1 歌词对齐轨（段内锚点 · 延音高亮 · 锚点提示音 · 编排页编辑轨）（2026-09-18）

新增**歌词对齐轨**：歌词按字绑定在曲式段落上。播放时主视图下方出现一条卫星轨——
走带线随段落推进，正在唱的字高亮、延音字在整个时值内铺满；编排页每段多一条歌词
编辑轨：粘贴整句按字均分（默认每字一个八分），拖字块改起点、拖右缘改时值
（吸附十六分格），邻居边界互相顶住、不吞字。

- **数据契约**：一行 = `{ arrangeId, sec, chars:[{t,dur,ch}] }`，`char.t` 是**段内绝对
  tick**（段首 = 0）——「循环该段 = 循环该句」，锚点不产生绝对时间残留。Store 只做
  结构校验（吸附 12tick / 排序 / 重叠跳过靠后的字 / 上限），越界剔除在共享状态区
  `lyricCharsAt` 完成且**不写回**（段落日后被加长，录入不丢）；剔除结果按行对象身份
  缓存，`upsertLyric` 换掉行对象即自然失效。
- **锚点提示音**：每个字进场补一声短木鱼（复用 wood 带通噪声路径，音高固定、不参与
  重拍分级），延音占位不触发；在小节起点一次排完，`cueBarT` 幂等护栏顶住 25ms 轮询
  重排。开关默认**关**（叠加发声属「显式要了才开」），音量取 levelBeat 档而非
  accentGain——它是提示不是重拍。
- **测试**：新增 t60（数据层 CRUD/归一化 · 加载去重/上限 · 段内解析 · 渲染层字态与
  收起不变量 · 锚点音时刻/音色/幂等 · 编辑轨拖拽边界），并登记进 tests/run.js。

## v2.0.6 · 工程：审计落地 P1（调度可见化 · 生命周期收口 · 数据边界 · 真实浏览器冒烟）（2026-09-17）

承接同日 v2.0.5 的 P0 批次，本版落地审计的 **P1 批次**，并新增一件**此前完全缺失的能力**：
真实浏览器冒烟。用户可见变化有三处：**重锚（漏拍）第一次变得可观测**、
**「使用方法 → 数据与隐私」多了一个隔离数据清理入口（仅在有脏数据时出现）**、
以及**变速/练习量到点停播的容错窗口翻倍**（见 P1-1）。其余为内部收口。

### P1-1 · 调度：把"静默吞拍"变成可读数字，并把窗口余量翻倍

- **窗口 0.15 → 0.3**（`CONFIG.schedWindow`）。调度回调跑在主线程 25ms 轮询上，单轮余量 =
  窗口 − 轮询间隔；150ms 时只有 125ms，而小节边界那一轮还要顺手做变速爬级、到点停播、
  换型重建网格——任何一次阻塞超过余量，时间轴就被重锚定、那段时间的音符被静默丢掉。
  翻倍到 300ms 等于把容错翻倍，代价是"变更生效"最多多等 150ms（后台窗口 1.2s 不变）。
- **重锚计数（新增）**：两条防线（饥饿兜底、单轮超限）现在共用 `reanchor(why)` 并上报计数，
  分成 `饿 starved` / `超 overflow` 两档——前者是环境（被节流），后者是数据可疑（属 bug），
  处置完全不同。诊断面板因此多一列「重锚 N(饿X/超Y)」：**0 = 从未漏拍**，>0 即有据可查。
  此前这条路径毫无痕迹，用户只能说"好像卡了一下"。
- ★ **刻意没有照审计建议把重活"投到 microtask / setTimeout(…,0)"**：微任务与定时器跑在
  同一根主线程上，总耗时一分不少，只是把阻塞从回调内挪到回调之后；而定时器不会重叠执行，
  所以对"下一轮会不会迟到"**没有任何改善**。真正有效的只有：窗口留足余量 + 让吞拍可见
  ——即上面两条。理由写在 `CONFIG.schedWindow` 上方，免得下次又有人"顺手优化"回去。

### P1-3 · 起停句柄：重入保护 + 句柄必清空

- `start()` 补**重入保护**：它会新开一个 `setInterval` 并覆盖 `timer` 句柄，旧句柄一旦被覆盖
  就再也清不掉——两个 25ms 循环同时排程 = 每个音符排两遍（听感是每拍都多了个装饰音），
  外加一个永远跑着的孤儿定时器。此前靠"每个调用点自己记得先判 `S.playing`"来规避，
  等于把不变式存在调用方的记忆里。
- `stop()` 把 `timer` / `rafId` **置 null**（原先只 clear 不置空，于是 `if (timer !== null)`
  恒为真、判据形同虚设），并一并归零 `ciNext` / `ciStart` 两个预备拍句柄型状态。

### P1-4 · `AudioContext` 生命周期收口

- `ensureCtx()` 前置判定 **`state === "closed"` 即废弃重建**：`watchCtx` 那条路只在**播放中**才重建
  （`S.playing` 为假时它什么也不做），于是停机期间被系统关闭的上下文会一直留着——
  之后 `start()` 拿到的 `currentTime` 是冻结的，表现为「界面在播放、永远不出声」且毫无报错。
- 新增 `resumeCtx()` 并替换三处裸 `ctx.resume()`：Safari 无手势时会 reject，此前返回值从未被接住
  （未处理的 Promise rejection）。它显式判 `.catch` 是否存在——真浏览器返回 Promise，
  而测试桩的 `resume()` 返回 undefined，无脑 `.catch` 会当场 TypeError（与装配层 L5350 同一套写法）。

### P1-5 / P1-6 · 数据边界收口

- **导入总量闸门**：`importMaxCount` 原先只管**单份文件**，`customs` 没有总量上限——导入 10 份备份
  就是 5000 条，而 `persistCold` 是不防抖的全量同步写（一次约 7 MB），正是冷热分离当初要消灭的
  715 KB 场景的十倍。现在按"库里现有 + 本次"判上限。
- **隔离区读/清出口**：`beatsight.quarantine` 原先只写不读、无回收，每次启动还要白付一次同步写。
  现在写前判重（内容一致就跳过），并暴露 `Store.quarantine()` / `clearQuarantine()`，
  在「使用方法 → 数据与隐私」里给了一个**只在有脏数据时出现**的清理按钮（带条数、二次确认）。
- **原型污染**：`S` 的合并目标由 `{}` 改为 `Object.create(null)`——`Object.assign` 走 `[[Set]]`，
  JSON 里的自有 `__proto__` 键会改写目标原型，让缺失字段从被污染的原型读到注入值
  （同文件早年为 `trainer` 逐字段白名单规避过同一个坑）。回归用例 T59e 用"注入 `vol:0.1`
  后 `S.vol` 必须仍是默认 0.8"钉住它。

### P1-7 · 单轮迭代超限不再是"半截状态"

`MAX_SCHED_STEPS` 溢出后原先只是 `break`，留下"已排大量音符 + 游标停在远未来（或脏数据导致的
永不前进）"的混合状态：接着要么长时间静音，要么下一轮继续溢出、每 25ms 重来一次。
现在改走与饥饿兜底同一个 `reanchor()`——把轴与游标对齐到当下，下一轮从干净状态开始。

### P1-8 · 跨平台构建 + 版本真相源补全

- 新增 **`tools/build-dist.js`**：`wrangler.jsonc` 的 `build.command` 原先用 `rm -rf` / `mkdir -p` / `cp`，
  **Windows 的 cmd 下跑不了**，"本地可完整复现线上构建"这句承诺在本机不成立。装配那段收进脚本
  （`fs.rmSync` / `mkdirSync` / `copyFileSync`），并逐个断言产物存在且非空、打印字节数。
- `check-version.js` 增加**第 4 项**：`package.json` 的 `version` 必须等于 `VERSION`。
  它是同一事实的第二份手抄（还有 `package-lock.json`），此前全无检查器在读它。

### P1-9 · 诊断面板正名 + 补齐读数 + 一键带走

- **正名**：`frameErr` 统计的是"渲染帧抛异常的次数"，此前标签写作"掉帧"——而真正的掉帧
  （rAF 没按时来）从来没被统计过，读数名不副实。现改称「渲染异常」。
- **补读数**：新增「重锚（饿/超）」、「脚本错」（`window.onerror`）、「未捕获」（`unhandledrejection`）
  ——最后两项此前逃出各模块 try/catch 的错误在面板上完全没有计数。
- **复制诊断信息按钮**：面板此前只能看不能带走。现在一键生成"版本 / URL / UA / 全部计数 /
  各存储键占用字符数"的可粘贴文本；剪贴板不可用（`file://` 非安全上下文）就退回弹窗，
  至少让人能手动复制。

### P1-10 · 真实浏览器冒烟（新增能力）

- 新增 **`tools/smoke.js`**（零依赖：Node 22 自带 `fetch` + `WebSocket`，直接用 CDP）。
  它补的是桩测不出的那一整类：真实布局与样式、真实 rAF 帧率、真实 Service Worker、
  真实控制台报错。两条通道都验：`file://` 直开与 `http://127.0.0.1`（PWA 需要安全上下文）。
- 它替代了 `tests/screenshot.sh` 的角色（那个脚本硬编码 macOS Chrome 路径、只抓一张图、不做断言）。
- **实测数据（本机 Windows / Chrome / 无头）**：播放态真实帧率 **60 fps**（rAF 自跑 1 秒）、
  `buildViz` 全量重建 **2.9–3.7 ms**、控制台零报错、Service Worker 注册成功、27 项断言全过。
- 接入自验链为**第 9 步**，并引入第三种记账：`环境可选`（退出码 3 = 本机没有浏览器）。
  它与"可选加强项"的区别写在 `check-all.js` 头部——**缺依赖**可以 `npm ci` 装上，所以
  `--strict-env` 该报错；而**缺环境能力**（构建镜像里必然没有浏览器）不该堵住部署。

### ★ 有意未做的三项（附实测数据与理由，不是遗漏）

- **P1-2「播放中切预设改节点复用」**：实测 `buildViz` 全量重建只用 2.9–3.7 ms（桌面无头；
  按低端机 5–10× 估 15–35ms），而窗口已提到 300ms——占比 < 12%，**不是饥饿风险**。
  重写整树重建（555 行有测试覆盖的热路径）去省几毫秒，风险收益比很差。留数据在此，
  将来真出现卡顿再动它，而不是凭"代码里有两套纪律"就重构。
- **P1-11「每帧预计算」**：播放态真实帧率稳定 60fps，同步循环测得的单帧成本是 0.01ms 量级
  （且那是"状态未变"短路路径的下界）。**没有可测量的收益**，纯风险。
- **P1-12「Store 按数据域拆分」**：审计报告自己给的方案（log→Stats、ear→Ear、arranges→Arrange）
  **违架构约束**——`Controls`（声明在前）要调 `appendSession`，把它挪进 Stats（声明在后）
  就构成"引用后方模块"，`tools/check-module-order.js` 会当场拦下。正确做法是新建一个
  声明在 Controls **之前**的 `Log` 模块，属结构级改动，值得单独立项而不是塞进这批。

### 发布阻塞修复：本地自验"假绿"，类型错误直到构建环境才第一次被照到

v2.0.6 推 `main` 后 Cloudflare 构建**失败**在 `✗ 类型检查 · tsc（加强）`。根因**不是那道闸门**，
而是**本地那次自验是"假绿"**：

- 本地没装 `node_modules`，于是 ESLint 与 tsc 一直是 **⊘ 跳过**（= 没查）；而构建环境装了依赖，
  这两步**真的跑**——一跑就红。**⊘ 只说明"这一项没查"，它不等于通过。**
- `--strict-env`（P0-5 加的）在这件事上**并不改变结果**：它只在**缺依赖**时才把 ⊘ 变成报错，
  而构建环境依赖是齐的，有没有这个参数 tsc 都会跑、都会红（初版 CHANGELOG 把它说成"开关起作用"，
  属归因不准，此处已更正）。它真正的价值是另一件事——本地只要带它跑一次，就会先因为
  "缺依赖不允许静默跳过"而报错、逼你 `npm ci`，从而让「本地口径 ≠ 构建口径」当场可见。
  这次偏偏没带它跑，⊘ 就被当成了通过。
- 三个错误**全部来自本批新增的代码**，本地一次都没照到：
  1. `TR_RANGE[k]`：`k` 是 `string`，而 `TR_RANGE` 没有索引签名（TS7053 ×2）
     → 抽出 `TR_KEYS` 并标成字面量联合类型 `("start"|"target"|"step"|"everyN")[]`，
     键表成了具名常量，索引也随之类型安全；
  2. `function diagCopy(btn)`：参数缺标注，`noImplicitAny` 下报 TS7006 → 补 `@param {HTMLElement}`。
- 本地补做并留档：`npm install`（79 个包）→ `node tools/check-all.js --strict-env` →
  **全部通过 · 实跑 12/12 项**（ESLint 0.95s / tsc 0.89s / 浏览器冒烟 4.89s 全部真跑）。
  修复推送后核对线上：**v2.0.6 已生效**（`Cf-Cache-Status: MISS`，367751 字节）。
- **教训写进流程**：改到带类型标注的接口（JSDoc / `S` 的字段 / 模块返回面）时，推之前必须
  `npm install` 后跑一次全量 `--strict-env`——**⊘ 不等于通过**，它只说明"这一项没查"。
  这正是本文件头一直在强调的那件事，这次是它自己被忽略了。

### 回归测试与验证

- 新增 `tests/cases/t59-p1-audit.js`（T59–T59h）：起停重入/句柄泄漏（用新增的桩能力
  `intervalCount()` 断言活跃定时器数）、closed 上下文重建、导入总量闸门、隔离区读/清
  （含"点击 → 取消 → 再确认"的完整交互闭环）、`__proto__` 注入、重锚计数、
  诊断信息的复制（剪贴板可用走剪贴板、不可用退回弹窗）。断言总数 1446 → **1489**。
- 桩新增两个能力：`intervalCount()/timeoutCount()`（"句柄不泄漏"此前完全没有抓手）。
- 三处既有用例随改动同步更新（不是"改测试掩盖问题"，每处都写明了原因）：
  `t56` 的面板文案与计数器初始集合（正名 + 新增计数）、`t48b` 的墙钟容差
  ——**容差改为由 `CONFIG.schedWindow` 与谱面最长音符算出**，不再写死（窗口一变它就假红）。
- 全量自验：**全部通过 · 实跑 10/12 项**（第 6/7 步缺开发依赖 ⊘，其余全跑）·
  **1489 PASS / 0 FAIL** · 覆盖率 **99.7%**（P0 批次为 99.8%）。
  新增的 11 行未覆盖全部集中在"出事才走"的防御分支（Audio 8：重锚超限、ctx 关闭重建、resume 异常；
  Ear 3：不可达的时值档位），已在 DEVELOPMENT §5 逐条说明性质——**不为了数字去造人工状态点亮它**，
  也不把这段说明抄成会过期的行号。

## v2.0.5 · 工程：审计落地 P0（首屏白屏 · 脏值域 · 几何失控 · 索引生成物 · 文档闸门）（2026-09-17）

承接 2026-09-17 的**全维度代码审计**（代码质量 / 性能 / 架构 / 安全 / 工程化五维 + 功能池）。
审计共给出 8 项 P0（低成本高收益），本版**只做这 8 项**，P1/P2 一律未动（清单见文末"已知未做"）。
用户可见功能语义零变化，唯一能看见的差别是：**切主题 / 出现回退提示后，弹跳球不再偏**。

### P0-1 · Store 里唯一未设防的 `localStorage` 访问

- `Store` 模块开头读取旧键 `beatsight.m2` 的那行直接裸调 `localStorage.getItem`，是全模块**唯一**
  没包 `try` 的访问——而紧邻的注释正承诺"数据损坏时回退默认值，**不再整页白屏**"。
  沙盒 iframe（本项目明确要在其中运行）或"站点数据被禁用"时 `getItem` 抛 `SecurityError`，
  `Store` IIFE 求值即失败 → `const S = Store.S` 失败 → 后面 16 个模块全部拿不到 `S`。
- 改为与 `readJSON` 同口径兜住（`let rawSaved = null; try{...}catch(e){}`）。这是"一处不设防、
  全站陪葬"的典型：**容错要么全模块一个口径，要么等于没有**。

### P0-2 · 加载期脏值只判"是不是数字"、不判"是不是合法值"

- 新增两份**唯一来源**的域常数（此前 HTML 的 `min/max`、Trainer 的实参、Store 的校验三者互不相干）：
  `BPM_RANGE = [30, 240]`、`TR_RANGE = { start:[30,236], target:[31,240], step:[1,20], everyN:[1,8] }`；
  配合新助手 `clampRange(v, lo, hi)`（取整 + 钳制，与 `isNum`/`numOr` 分工不重叠）。
- `S.trainer` 四项改走域钳制，并补一条**组合约束**：目标必须高于起始（与 UI 侧 `bindTrParam`
  的"抬回起始 +1"同规则）。此前只判有限数，`step:0` 能一路进 `S.trainer`——那会让 `total()`
  变 `Infinity`、`bpmFor(k)` 恒等于 `start`，于是**爬坡既不升速也永不自动停**。
- `S.bpm` 补同口径钳制。**如实说明：这一条当前不可达**——装配段 `Controls.setBpm(S.bpm)`
  初始即走统一入口补了一次钳制。补它是因为它曾是 `S` 里唯一没有域的数值，让"能读到的 `S`
  永远是合法值"这条性质**不依赖调用顺序**，属纵深防御而非在修的 bug（注释里已写明）。

### P0-3 · 布局变了却没人通知渲染层：主题切换后弹跳球/尾迹一直偏

- 现象：**播放中切「主题」**，球与尾迹会按旧几何继续画（看起来"飘在行外"），直到下一次
  `buildViz` 或窗口 resize 才自愈。原因：观测台主题把 `.topbar` 从 64px 压到 56px、
  `.card`/`.pattern-head` 的内边距也跟着变（行 y 位移约 22px），而几何缓存 `cacheGeo()`
  全文件只有 `buildViz` 与 resize 两个调用点——**换主题这条路径谁都没通知 Viz**。
- 把 resize 里那段取舍抽成 `Viz.relayout()`（播放中：只重采几何 + 重跑标注自适应，不重建 DOM
  以不打断动画；停机时：整树重建），resize 与 `applyTheme` 共用它。
  顺带修掉同源的**首屏静止态**：初始主题就是观测台的用户，首屏几何是按经典算出来的。
  约定写进注释：**谁在布局上动手脚，谁就有责任调它**。

### P0-4 · 先采几何、再改布局：提示带一出现就把球推偏 44px

- `#fallbackNote`（拍号回退提示，约 44px 高）夹在节奏型头部与可视化卡片之间，而
  `Viz.buildViz()` 内部会 `cacheGeo()` 采集行几何。三个调用点都写成"先 `buildViz()` 再
  `updateFallbackNote()`"——等于采完几何再改布局，球与播放头整体偏 44px。
- 三处（`Presets.applyPatternChange` / `Editor.stopAudition` / `Ear.close`）统一改为
  **先定提示带显隐，再建网格**。

### P0-5 · 构建命令补 `--strict-env`，并纠正 README 里"硬闸门"的说法

- `wrangler.jsonc` 的 `build.command` 改为 `node tools/check-all.js --strict-env && …`：
  自验链里 ESLint / tsc 是可选加强项，缺 `node_modules` 时标 ⊘ 跳过而不是失败——本地这样设计
  是对的（零依赖产物不该被开发依赖卡住），但在**构建环境**里就成了漏洞：忘跑 `npm ci` 时
  两步永远是 ⊘、汇总照样写"全部通过"，而"实跑 N/M"那个分母没人会核对。
- 更正一处**事实性错误**：README / DEVELOPMENT 此前写"Cloudflare 构建命令里串了全量检查，
  不通过就不部署（硬闸门）"，而 `wrangler.jsonc` 自己的注释就承认 **Workers Builds（Git 集成构建）
  不读 wrangler 配置里的自定义构建命令**——线上跑什么取决于 Dashboard，那不在版本库里。
  现已按事实改写：**能称作"硬闸门"的只有本地/命令行 `wrangler deploy` 这条路**，
  并明确写"别拿 Cloudflare 会拦当跳过本地自验的理由"。同理登记一条 P1：本命令用了
  `rm/mkdir -p/cp`，Windows 的 cmd 下跑不了（待抽 `tools/build-dist.js`），已在配置注释里写明而不是装作没这回事。

### P0-6 · 把"抄进文档的数值"变成机器可判：新增第 5 步「文档一致性」

新增两个零依赖工具 + 自验链第 5 步（共 10 步 → **11 步**）：

- `tools/gen-index.js`：**模块索引从手写改成生成物**。索引原本自己写着"在其之前增删代码后需
  整体重算"，而 v2.0.4 审计实测 **16 条行号全部偏移**（最大 59 行）。现在行号从实际 banner
  反算：默认只校验，`--write` 回写；短名与描述由人维护（按 key 对齐，不会被生成器覆盖）；
  条目数不符时**只报错不猜**——生成器替人编一条描述，等于把噪音固化进文件。
- `tools/check-docs.js`：三项机器可判的规则——
  ① 模块索引行号 ↔ 实际 banner（复用上面那份解析，口径唯一）；
  ② **禁止手写耗时**（README / DEVELOPMENT / tests-README / check-all 头部）：这一处尤其说明问题，
     D1/D9 已经"修"过一次（约 6 秒 → 约 17 秒），本轮实测 20.8 秒，**又歪了**。手写数字注定烂，
     所以规则是"不许写"而不是"再写一次对的"——耗时由命令末尾自己打印；
  ③ 归档状态一致：README 文档表标了"已归档 / 已落地"的 Markdown，正文必须有同名横幅。
- 配套修掉③抓出来的 4 处陈年漂移：`docs/PLAN-v1.9.md`、`docs/PLAN-v2-arrangement.md` 补归档横幅；
  `docs/PLAN-v2-impl.md` 补"已落地"横幅（此前 README 说"已落地"、文档结尾却写着"确认后按 §8 开工"，
  两边直接矛盾）；`docs/DEVELOPMENT.md` §5 里那句抄了 5 个行号的"未覆盖行清单"改为不抄行号
  （实测那 5 个行号**全部失效**，现以 `check-coverage.js` 的输出为准）。

### P0-7 · 变速训练器：步长不整除时会永久卡死

- `total()` 用 `Math.round` 算总级数，**凡是（目标−起始）不能被步长整除、且小数部分 < 0.5**
  的组合都会被低估：70→100 步长 7 时算出顶级是 98，而 `onBarBoundary` 判"练到目标"用的是
  `bpmFor(idx) >= target`，`98 >= 100` 永远不成立 → `trStepIdx` 被 `Math.min` 钉死在 `total()-1`，
  每 `everyN` 小节重复设同一个 BPM：**既不升速、也永不自动停**（现象是"训练卡住了"，控制台一片安静）。
  比审计初判更普遍——不只"步长大于差值"这一种。
- 改 `Math.ceil`（不整除时补出最后一级，再由 `bpmFor` 的 `min(target, …)` 把末级收在目标值上），
  并加 `Math.max(1, step)` 作第二道防线。**等价性已逐条核对**：现有全部测试组合在 round 与 ceil
  下结果相同（70→80/步4 的 2.5、70→120/步4 的 12.5、70→140/步4 的 17.5 都取 3/13/18），
  所以这是"只修坏点、不动好点"的替换。

### P0-8 · 两处无障碍：够不着的 BPM 大数字、淹读屏的可视化网格

- `#bpmNum` 原为 `<div>` 且只绑 `click`：全局 Space 分支只认 `INPUT/BUTTON/SELECT/TEXTAREA`
  与 `role=button`，它三者都不是 → **键盘完全够不着、读屏也报不出可点**。改真 `<button type="button">`，
  并给点击处理器补 `blur()`（与文件里其它按钮同规矩，否则焦点留在按钮上时按空格会再次弹输入框）。
  **静止态外观一致**（已用无头 Chrome 实拍新旧两版对比 + `getComputedStyle` 实测确认：
  背景透明、无边框、无 outline、字号 46px、盒子 92×54，与旧 `<div>` 相同——全局 `button` 重置已覆盖背景/边框/颜色）。
  **唯一可见差异是「聚焦时才出现绿色焦点环」**（`:focus-visible` 的既有规则）：旧 `<div>` 永远不会有，
  因为它压根不可聚焦。这不是副作用，正是这次改动要的效果——键盘用户需要看见焦点在哪。
- `#viz` 加 `aria-hidden="true"`：里面每格都带一个"休止 八分"之类的文本节点，播放时约 360 个，
  读屏撞进去只会被淹没；语义信息由 `#srAnnounce` 与 `#statusText` 承担。
  **安全性已核实**：格子全是 `div`、不可点、无 `tabindex`、无键盘契约（Viz 模块除 resize 外不注册任何监听器）。

### 本轮未做（P1/P2，留待下批）

- P1：调度回调去同步 I/O（把 `persist`/`appendSession`/`syncBpmUI` 移出 25ms 调度链）+ `schedWindow` 0.15→0.3s；
  播放中切预设改节点复用而非整树重建；`stop()` 句柄置 null + `start()` 重入保护；`ctx.state === "closed"` 重建
  与 `resume().catch()`；导入总量闸门 + 隔离区回收；`Object.assign` 原型污染改用 `Object.create(null)`；
  `MAX_SCHED_STEPS` 溢出改重锚定；`tools/build-dist.js` 跨平台构建；`check-version` 纳入 `package.json`；
  诊断面板正名 + `window.onerror`；跨平台无头浏览器冒烟；每帧预计算；`Store` 按数据域拆分。
- P2：CSS 合成属性与 `will-change`、重复媒体查询合并、装饰性 SVG 的 `aria-hidden`、
  死代码清理（`.cell.upcoming` / 恒不成立的条件 / 未纳管定时器）、音频节点 `onended` 释放与母线余量、
  `FileReader.onerror`、`check-dom-ids` 下限断言、语法闸门改 `vm.Script`、`sw.js` 纳入测试。
- 仍**不是**本版职责的：真实浏览器截图自验与"后台持续发声"的真人验收（README 发布前清单③④）——
  本次改动触及可视化几何与 HTML 结构，合并前建议按清单走一遍。

## v2.0.4 · 工程：审计落地 B（诊断面板 · 监听器协议 · 扇出护栏 · 可版本化部署）（2026-09-17）

承接 v2.0.3 的「阶段 A + C」，本版落地审计报告的**阶段 B**——四件「高收益、低成本」的事：
把静默劣化变成可读数字、把弹窗监听器的注册/清理收成一个协议、给模块扇出加一道机器护栏、
让 Cloudflare 部署命令进仓库。**除新增一个 `?debug=1` 诊断面板外，不改任何用户可见功能语义。**

### B1 · 隐藏诊断面板（`?debug=1`）

新增一段**隐藏诊断面板**，聚合四类静默劣化计数器：掉帧（`frameErr`）、调度异常（`schedErr`）、
持久化失败/恢复（`persistFail` / `persistRecover`）、限流脉冲（`limitPulse`）。

- 计数器本体 `diag` **始终存在**（`window.__beat.diag`），只有面板 DOM 受 `?debug=1` 控制——
  测试因此可以无 UI 断言计数，`file://` 直开也只是没有面板而已，单文件性质不受影响。
- 复用既有注入钩子（`onFrameError` / `onSchedError` / `onLimitPulse` / `Store` 的两个持久化钩子），
  **不引入任何新依赖边**，模块地图与 `EXPECTED_ORDER` / `WHITELIST` 保持 13 条不变，
  故这段代码落在装配层而非新开 IIFE 模块。
- `onLimitPulse` 只包一层用于计数，且**只计返回 `true`（真的到点停播）**——该钩子每个 25ms 调度周期
  都会被问一次，若按调用次数计，计数器会以 40/s 空转、把唯一有意义的读数淹没；返回值原样透传，停播语义零改动。
- 回归：`tests/cases/t56-diagnostics.js`（T56–T56f）。

### B2 · 弹层监听器统一注册 / 清理协议

把 5 个 overlay（编辑 / 统计 / 听辨 / 曲式 / 说明）的开合从"各写一遍"收敛成 `Modal` 上的
一对原语，并给"打开期间要绑的监听器"提供统一的登记出口：

- `Modal.openOverlay(id)` / `closeOverlay(id)` 统一负责挂/摘 `.open`、`refreshInert()`、焦点进出；
  此前散落在各模块、各持一份的 `untrap` 局部变量已全部删除。`openOverlay` **幂等**——
  已开再开不再重复 `trapFocus`（此前重复 open 会让前一个归还闭包被覆盖后永久丢失）。
- `Modal.bindOverlay(id, target, type, fn, opts)` / `unbindOverlay(id)`：**登记与解绑共用同一份账本**，
  `unbindOverlay` 不需要调用方复述绑了哪些，结构上杜绝"关闭时忘了解绑"。编辑器 / 听辨 / 曲式里
  随内容重建的节点监听器全部改走这对原语，且**每次 `render()` 重建前先 `unbindOverlay`**。
  迁移后 `index.html` 0 处直接 `removeEventListener`（只在 `unbindOverlay` 内）。
- 回归：`tests/cases/t57-overlay-listeners.js`（T57–T57e）——反复开合 N 次计数不累积、
  同一打开内多次 `render()` 不增长、`open` 幂等不翻倍。因 harness 的 `removeEventListener`
  是空操作，用例断言的是应用级账本 `Modal.overlayListenerCount(id)`（与真实 DOM 摘除解耦）。
- 详情见 `docs/DEVELOPMENT.md` §3.10。

### B3 · 扇出护栏 + 「新功能 UI 内聚各模块」约定

`Controls` 是 UI 层中枢，直接引用 7 个下游模块，是依赖图里最大的扇出点——合理，但也是"每加一个
新 overlay 就顺手在它的 keydown 里再补一段、慢慢长成上帝对象"的风险面。给这条线加一道机器护栏，
并把约定写进文档：

- `tools/check-module-order.js` 新增 **R4（扇出上限告警）**：统计每个模块**直接引用**的下游模块
  个数（distinct `to`），超过 `MAX_FANOUT`（=7）即告警并失败。`Controls` 当前正好顶在上限上，
  于是"再给控件中枢挂一条下游引用"会**当场报红**，必须显式抬高常量并写明理由——把一次悄悄发生的
  架构漂移，变成一次看得见、需要辩护的改动。**反向验证**：把 `MAX_FANOUT` 临时调成 6，
  `Controls`（7 条）确实报红、`check-module-order.js` 退出码 1。
- 约定「**新功能的 UI 内聚在各自模块内部，`Controls` 只做事件转发**」：新 overlay/面板落在它自己的
  模块里，开合与监听器复用 `Modal` 的原语（B2）。文档见 `docs/DEVELOPMENT.md` §3.0 与 §3.11。
- 本项**只加检查、不动 `index.html`**：13 条模块声明顺序与 10 条 R3 白名单均无改动。

### B4 · 构建命令进版本库（`wrangler.jsonc`）

此前 `wrangler.jsonc` 只声明 `assets.directory`，"构建什么、怎么构建"只活在 Cloudflare Dashboard
的表单里——不可 review、不可 diff、不可回滚，本地也无法完整复现线上构建（审计指出的"配置漂移"）。
现在把构建步骤落回仓库可版本化字段：

- `wrangler.jsonc` 增 `build.command`：**全量自验（`node tools/check-all.js`，不通过即 `&&` 短路）
  + 装配 `dist/`（4 个上站产物加 `_headers`，共 5 个条目）**。本地 `npx wrangler deploy` /
  `wrangler versions upload` 会先跑它，部署前的检查与产物与线上口径一致。
- **诚实边界**：Cloudflare 的 Git 集成构建（Workers Builds）**不读** wrangler 配置里的 Custom Builds
  （官方明确说明），所以线上那次构建仍以 **Dashboard 里配的构建/部署命令为准**。本字段解决的是
  "本地可复现 + 配置进版本库"，**不是**"线上改由仓库驱动"——这句话写进了 `wrangler.jsonc` 头部注释
  与 `docs/DEVELOPMENT.md` §2，免得后人误以为线上也读它。
- `_headers` 随之成为构建命令里的显式一项（`docs/DEVELOPMENT.md` §2 仓库树、`.gitignore` 相关注释同步）。

### 版本

`VERSION` 2.0.3 → **2.0.4**。

## v2.0.3 · 工程：审计落地 A + C（信任修复 · 工程卫生）（2026-09-17）

承接 v2.0.2 的审计报告（`spec.md`），把其中**收益最高、成本最低**的两档一次落地：
先用「阶段 A」修掉文档 / 工具与代码现实之间的漂移，再用「阶段 C」顺手清掉几处工程卫生债。
**不改任何功能语义**，上站对外资源仍只有 `index.html` / `sw.js` / `manifest.webmanifest` / `icon.svg` 四个文件。

### A · 文档与自验工具去漂移

审计证据显示多处「说明与实际不符」，逐条对齐：

- **耗时口径（A1）**：`README.md` / `docs/DEVELOPMENT.md` / `tools/check-all.js` 头部原写
  「约 6 秒 / 约 2–3 秒」，与实测（完整约 17s、`--quick` 约 11s）差一个量级，已改为实测值。
- **严格开关口径（A1）**：`README.md` 原写「它不开严格模式」，与 `tools/tsconfig.typecheck.json` 里
  strict 家族全开的现实相反，已改为如实描述。
- **覆盖率注释版本号（A2）**：`tools/check-coverage.js` 头部硬编码「当前（v1.3.1）…」，早已过期，
  改为不写具体版本号，避免再次漂移。
- **行号偏移（A3）**：`tools/check-dom-ids.js` 报的行号取自内联脚本切片、未加 `index.html` 起始行偏移，
  与 `check-eslint.js` / `check-tsc.js` 不一致，报错定位对不上文件；已补 `baseLine`。
- **仓库卫生（A4）**：`.gitignore` 增加 `.uploads/`，并把误入库的 2 个 PNG `git rm --cached` 掉。
- **`--strict-env`（A5）**：`tools/check-all.js` 新增该开关——`CI=true` 且可选步骤（ESLint / tsc）
  缺依赖时**报错**而非静默 `⊘`，让 CI 里的「没查」不再被当成「查了通过」。

### C1 · 抽取 `clear(el)`

全文件 11 处「先清空容器、再逐个 `appendChild`」的渲染函数，起手都是 `el.innerHTML = ""`，
现统一走共享区的 `clear(el)`。实现**刻意保持 `innerHTML = ""` 语义**（而非 `replaceChildren`）：
真实 DOM 与测试桩都以它连带清空子节点，换实现会让桩与真实行为分叉。

### C2 · 清理 5 个未引用 id

`argBar` / `editorCardTitle` / `editorDot` / `earHead` / `earDot` 在代码、CSS、测试中均无引用
（纯历史遗留），删掉 `id` 属性、保留 `class` 钩子。`check-dom-ids.js` 由「声明 147 / 引用 142」
收敛为 142 / 142，零未用、零悬空。

### C3 · `_headers`

新增入库的 `_headers`（构建时拷进 `dist/`，文件自身不对外提供），下发
`X-Content-Type-Options: nosniff` 与 `Referrer-Policy: no-referrer`——都对功能无影响，属默认关上更安全。

### C4 · `package.json` 补字段

补 `version`（与 `VERSION` 对齐）、`engines.node >= 22`（README 已如此要求）、`test`（`node tests/run.js`）。

### C5 · `<script>` 开头模块索引

脚本头的模块清单此前停在 v1.3、且缺 `Ear` / `Arrange` / `Help`。补成完整的 **16 段索引**
（模块名 + `index.html` 绝对行号），作为 5400+ 行单文件内的导航；并注明行号需随增删重算。

### 版本

`VERSION` 2.0.2 → **2.0.3**。

## v2.0.2 · 工程：审计驱动的健壮性加固（2026-09-16）

一轮技术负责人视角的全维度代码审计后，挑出的「高收益低成本」项一次落地：**不改任何功能语义**，
只把此前会**静默失效**的路径补成显式可见，并给版本纪律加一道机器闸门。

### D2 · 曲式播放前复检（不再静默播错型）

`arrangeStart()` 此前只在**保存时**校验曲式引用的自定义预设是否存在、拍号是否一致。
曲式一旦落盘就成了冷数据——预设被删除 / 改名 / 重新导入后引用即失效，此时点「播放」
会**静默退回预设模式**，用户看到的是「我选的曲式，播的却是别的型」。

现在 `arrangeStart()` 播前**再判一次** `arrangeProblems()`，并把问题清单**返回**给调用方
（而非就地弹窗——本函数只管"落到起点"，弹窗是 `start()` 的职责；返回值也让判定能被测试直接断言）。
`start()` 拿到非 null 即弹窗说明原因。**刻意不中断播放**：用户按的是「播放」，
退回预设继续响比静音更符合预期，但必须讲清楚——这正是要消灭的静默降级。

### D3 · 调度器异常边界

`scheduler` 挂在 `setInterval(25ms)` 上，此前**没有任何异常边界**。一次意外
（脏数据走到未设防分支、`AudioContext` 被系统关闭、DOM 引用失效）会让同一个错误每 25ms
重抛一次：控制台刷屏、音频时钟继续走而画面/状态停更，表现为「在播放但没声音」——
比渲染帧异常更隐蔽（`paintFrame` 挂在 rAF 上，异常会中断整帧，症状明显得多）。

现在收成一个边界：记日志 → 经注入钩子 `onSchedError` 停播并弹窗（与 `onFrameError` 完全同构）。
原主体抽成 `schedulerBody()` 是**纯搬移**，所有 `return` 语义一字未改。

### D5 · 保存失败恢复出口

`persistFailNotified` 此前**一旦置 true 就永久锁死**：用户清掉存储、或退出隐私模式之后，
即便保存早已恢复正常，顶栏仍然红着「保存失败」，且**再也不提示**（`if (persistFailNotified) return`
把后续提示全吞了）——用户会误以为「一直存不进去」，进而放弃保存动作。

现在给失败状态一个**可撤销的出口**：任何一次写成功都证明存储已可用，就复位标志，并经注入钩子
`setPersistRecoverHandler` 复原 chip / 状态点。「下一次成功写入」本就是最可靠的恢复证据，
不必为探测额外付出同步写盘代价。

### D4 + 版本闸门 · 版本唯一真相源

`VERSION` 早已是唯一真相源（v1.3.0 起三处显示由它派生），但**它本身会漂移**：审计时代码里
已散落 48 处 `v2.0.2` 的注释引用，而 `VERSION` 仍停在 `2.0.1`——「注释说已修复、版本号说没有」，
正是发版纪律松动的信号。

新增 `tools/check-version.js` 作为第 10 步硬闸门（零依赖），查三件事：
① `VERSION` 是合法 semver；② `CHANGELOG.md` 首条 `## vX.Y.Z` 必须等于 `VERSION`；
③ 全文件出现的版本字面量**不得高于** `VERSION`（注释里写着"未来版本"，等于代码已含该版改动却没 bump）。

### D9 · Service Worker 资源策略

静态资源（manifest / icon）此前走 cache-first：**只有 CACHE 名字变了才会清旧缓存**，
于是图标 / 清单更新后用户会长期看到旧的那一份。改为 **stale-while-revalidate**——
先用缓存立刻响应，同时后台拉一次新版本写回，下一次加载即是新的。CACHE bump 到 `v2`。

### 去重下载

「导出预设」与「导出练习记录」此前是**两份逐字复制**的下载实现（Blob → objectURL →
`<a download>` → click → 延时 1s revoke），连 `revoke` 延时都一样。抽成 Store 级
`downloadJSON(kind, text)`，两处复用。

### 修复 · 曲式播放的实时进度（`refreshNow`）

同一族「编辑选中项 vs 正在播的曲式」身份混淆：`refreshNow()` 是**播放回调**（由 Audio 在每个
小节边界经注入钩子 `onArrangeBar` 调用），却取了 `cur()`——那是**编辑选中项**（模块内 `curId`）。
`curId` 在刷新页面后为 `null`（只有打开过编排 overlay 才会被赋值），而 `playMode: "arrange"`
是从持久化恢复的，于是「刷新后直接接着播」时本函数第一行就 `return`：
主界面那行「第 N/M 段 · 第 X/Y 小节」与段名**永不刷新**，看起来像卡死。
改为 `arrangeCur() || cur()`（播放优先、编辑兜底），与同文件 `jumpTo()` 完全一致。

### 自验

- `node tools/check-all.js` 全绿（新增版本闸门后共 10 步）；`npm ci` 后 ESLint / tsc 两步**实际执行**而非跳过。
- `node tests/run.js` 1342 PASS / 0 FAIL，语句覆盖率 99.8%（Arrange / Editor 均 100%）。
- 本轮新增 4 组回归断言，并逐条**反向验证**过（在无修复的旧构建上必须失败，否则就是橡皮图章）：
  - **T53l**（D2）：旧构建坏引用时 `playMode` 仍停在 `arrange`、不弹窗 → 断言失败 ✓
  - **T23h**（D3）：旧构建调度器异常直接冒泡，无日志、无弹窗 → 断言失败 ✓
  - **T24**（D5）：旧构建 chip 永久卡死「保存失败」、且无恢复播报 → 断言失败 ✓
  - **T46b**（去重下载）：旧构建里的两份下载实现**逐字相同**，抽公共函数本就不改变行为，
    故在旧构建上必然通过——改用**变异测试**证明断言有效：`kind` 由 `"log"` 改成 `"logs"`
    仅挂掉文件名模板那条；删掉 `revokeObjectURL` 排程仅挂掉清理那条 ✓
- 覆盖收口轮又补 3 组断言，同样逐条**反向验证**（临时变异 `index.html` 后必须变红，验完即还原）：
  - **T53m**（实时进度）：把 `refreshNow` 的 `arrangeCur() || cur()` 变异回 `cur()` →
    `1338 PASS / 4 FAIL`，进度条与段名断言全红 ✓
  - **T30**（编辑器轨道键盘）：删掉 `keydown` 里的 `e.target === track` 守卫 →
    `1340 PASS / 2 FAIL`（"事件来自音符格不切换"/"普通字母键不切换"两条变红）✓
  - **T54h**（删整条曲式）：去掉确认弹窗并去掉 `playMode` 回落 →
    `1339 PASS / 3 FAIL`（"要确认"/"回落 preset"/"清空 arrangeSel"三条变红）✓
  - 余下未覆盖行经核实为**刻意不覆盖的防御代码**（Audio 的 `arrNextBar` null 兜底与
    `MAX_SCHED_STEPS` 上限守卫）与**不可达分支**（`durName` 的 192/144/96/6 时值——内置预设与
    听辨组表都不产生这些时值，函数亦未导出），故不硬凑断言。

### 追加 · 类型闸门严格开关扩面（2026-09-16）

落实 DEVELOPMENT.md §6 那条债的**第一步**（原文的开启路径是"逐块补标注、逐块开开关，别一次性开"）。
在 TS 7.0.2 下逐项实测，**8 项严格检查打开后 0 报错**，故一次直接开——改的是
`tools/tsconfig.typecheck.json` 的 `compilerOptions`，**不动 `index.html`、不动产物**，故不 bump `VERSION`：

- 新开 8 项：strict 家族 6 项（`strictFunctionTypes` / `strictBindCallApply` / `noImplicitThis` /
  `alwaysStrict` / `useUnknownInCatchVariables` / `strictBuiltinIteratorReturn`）+ 非家族但同样只抓真错的
  2 项（`noImplicitReturns` / `noFallthroughCasesInSwitch`）
- 仍关着两项，各对应一笔**已量化**的债（同一份实测）：`noImplicitAny` 打开 → **727 条**
  （TS7005 337 / TS7006 291 / TS7034 74 / TS7053 24 / TS18047 1）、`strictNullChecks` 打开 → **251 条**
  （TS2339 95 / TS18047 91 / TS2322 43 / TS2349 14 / TS2345 8）。`strictPropertyInitialization`
  依赖 `strictNullChecks`，故随之暂关（本文件无 class，本就无影响）
  （**2026-09-17 更新**：`noImplicitAny` 一笔已清零并打开，`strictNullChecks` 重测为 **96** 条——见下条追加）
- **口径纠偏**：旧文档写的"759 条"是 v1.9.1 当时的数字，现树重测为 **727**——四处引用
  （本文件 v1.9.1 条目 / DEVELOPMENT §6 / PLAN-v1.9 §7 / tsconfig 头）已一并改为 727 并注明来龙去脉
- 扩面后 `node tools/check-all.js` 仍全绿 **10/10**（tsc 步 0 报错）

### 追加 · noImplicitAny 闸门打开（2026-09-17）

接上条追加的开启路径，落**第二步**。开工前那笔债由 **727 → 282 → 0** 分两批补完，全文件 0 报错，
故在 `tools/tsconfig.typecheck.json` 把 `noImplicitAny` 由 `false` 改为 `true`（改的仍是
`compilerOptions`，**不动 `index.html`、不动产物**，故不 bump `VERSION`）：

- **补债方式**（全部是纯注释，零运行时改动）：给函数签名补前置 `@param` / `@returns`（清 TS7006），
  给同名容器补 `@type {Record<string, any>}` / `Record<string, number>` 或收窄成 `"from"|"to"` 键联合
  （清 TS7053），回调参数就地内联 `(/** @type {any} */ s) => …`；省略实参的位置一律写成可选形
  `@param {any} [name]`（防止 TS2554 回归）
- strict 家族至此已开 **7 项**（新收 `noImplicitAny`），只剩 `strictNullChecks` 一项
- **该债重测**：`strictNullChecks` 打开 → **96 条**（TS18047 74 / TS2345 10 / TS2322 4 / TS18048 4 /
  TS2769 3 / TS2531 1），比上条的 **251** 降下来——大量隐式 any 消失后，原先被 any 传染出来的
  空值报错一并消失，剩 96 条才是真正要逐处判空的
- 扩面后 `node tools/check-all.js` 仍全绿 **10/10**（tsc 步 0 报错）

### 追加 · strictNullChecks 闸门打开（2026-09-17）

接上两条追加的开启路径，落**第三步（收官）**。那 **96** 条已量化空值债按模块分七块逐块消化，
全文件 0 报错，故在 `tools/tsconfig.typecheck.json` 把 `strictNullChecks` 由 `false` 改为 `true`
（改的仍是 `compilerOptions`，**不动 `index.html`、不动产物**，故不 bump `VERSION`）：

- **按模块分组与启动顺序**（开工前先按模块列清单，再挑第一块动手）：Trainer 6（`S.plan`）→
  小尾 9（Arrange 4 + Ear 3 + Store 1 + Modal 1）→ Controls 11 → Audio 20（`ctx`）→
  Viz 25（缓存 DOM 引用）→ Editor 25（`draft`）
- **补债方式**（统一是「取本地别名 + 判空守卫」，零运行时语义改动）：模块级可空 `let` 状态
  （`ctx` / `playheadEl` / `trailGlowEl` / `shadowEl` / `resizeTimer` / `draft`）在函数顶部
  一次性取本地别名（`const c = ctx, ph = playheadEl, tg = trailGlowEl;`）并早返回守卫，
  收窄跨不过闭包/调用边界的问题就此消化；定时器句柄先判 `!== null` 再 `clearTimeout`；
  `.closest()` 返回值补 `!!` 守卫
- strict 家族至此已开 **8 项**（新收 `strictNullChecks`），`strictPropertyInitialization` 随之生效
  （本文件无 class，本就无影响）——**strict 家族与两项额外严格检查全部打开且全绿**
- 收官后 `node tools/check-all.js` 全绿 **10/10**（tsc 步 0 报错）、`node tests/run.js` **1342 PASS / 0 FAIL**

## v2.0.1 · 功能：使用方法页 + 顶栏常驻入口（2026-09-16）

顶栏新增「**使用方法**」按钮（在状态 chip 之前），打开一页分层的说明：

- **简明版（默认显示）**：三步上手 → 画面怎么看 → 先试这几个开关（静音拍 / 变速训练 /
  练习量 / 预备拍）。重点是讲清这个应用最核心的事：**格子的宽度 = 音符的时值**
  （不只看拍子，还看每个音有多长）
- **完整说明（默认收起，页尾一个开关）**：全部控制项逐条（BPM / 拍号 / Swing / 音色 / 音量 /
  弹跳球 / 后台保活）、训练模式（变速训练 / 练习量 / 听辨训练 / 练习统计）、曲式编排、
  自己画节奏型、数据与隐私、键盘快捷键

### 几个刻意的取舍

- **不自动弹窗**（用户明确选了这项）：顶栏常驻，想看再看。首访弹窗对老用户是打扰，
  而且"这台设备是否首访"判不准——数据清过一次就会重新弹
- **打开时不停止播放**：说明里提到弹跳球、静音拍这些"正在响才看得懂"的东西，
  让它继续响着更有用。曲式编排/编辑器是"要动手改"，才需要停
- **完整说明默认收起**：全展开会把"先试这几个开关"埋掉，而那才是新用户最先要看的
- **正文不写版本号**（例如"v1.9.0 新增…"）：用户不需要知道哪个版本加了什么，
  而它一旦写进去就会随版本漂移变成错的——P2-9 那条"版本唯一真相源"的纪律在这里同样适用
- **内容是静态标记**：不渲染、不取数据。好处是没有 JS 也能看到全文，
  也不会被 JS 故障连带弄丢一份纯说明文字

### 自验

- 新增 T55（4 个子场景，34 条断言）
- **反向验证 5 个变异全部命中具名断言**：按钮不打开 / 不置 inert / 默认展开 /
  键盘路由漏掉 / refreshInert 漏掉新 overlay
- 过程中修一处测试自身的静默错误：切标记段时**先去了注释**，而结束锚点本身就是一行注释
  → indexOf 返回 -1 → slice 一路切到文件末尾（实测 179225 字符），
  于是"含某某文字"的断言全是假通过。改为先定位再去注释，并给切片长度加了**上界**

### 发布渠道（补记）

WorkBuddy 渠道换绑到新链接 **https://beatsight-48543.app.workbuddy.host/**，
旧链接 `beatsight-34873` **废弃**（停在 v1.6.0，不再更新）。

原因值得记一笔：**WorkBuddy 的应用归属是按「工作区（会话）」判定的，不是按目录**。
每个工作区根目录有一个 `.<appId>.genie` 标记文件，记录它发布到哪个应用；
实测——显式指定旧应用的 appId 会被拒（"not one of the apps recorded in this workspace"），
从旧工作区自己的目录发布也仍会被路由回当前工作区的那个应用。
也就是说**一个应用只能在当初创建它的那个工作区里更新**，
旧应用在新工作区里就是更新不了，只能换绑。

发布目录也一并固化下来：`beatsight-publish/`（4 个文件，约 300 KB），
因为**发布的是一整份目录**——直接发 `beatsight/` 会把 44 MB 的 `node_modules`
和 `tests/` `tools/` `docs/` 一起变成公开可访问的。

## v2.0.0 · 功能：曲式编排——把 4 小节死循环变成完整歌曲结构（2026-09-16）

立项目标原本只有"一个节奏型反复练"。本版把**播放的单位**从"一个节奏型的 4 小节循环"
提升为"**一首歌的段落结构**"：`前奏 ×4 → 主歌 ×8 → 副歌 ×8 → 间奏 → 主歌 → 副歌`，
并且能从头练到尾、也能只练副歌、还能跳段。

### 数据模型（与 D8=混合 的答案直接相关）

```
{ id, name, sections: [ { name, blocks: [ { ref, repeats } ] } ] }
```

- **段 = 有序的若干块**，块 = 一个节奏型 × 整数遍。
  (a) 型段（同一个 4 小节伴奏型放 2 遍）就是 1 个块；
  (b) 型段（两个不同的乐句）就是 2 个块——**所有段统一用 blocks 数组**，
  不做双形状（差别只有一层数组，双形状意味着每条读写路径都要判两次）
- `ref` 复用 `S.sel` 的形状，解析走 S1 抽出的 `resolveRef()`——不产生"两套解析边界"
- 段长由数据派生（`Σ(repeats) × 4`），不存字段
- 冷键 `beatsight.arranges`（曲式库，可存多首）；热键新增 `S.playMode`（preset|arrange）
  与 `S.arrangeSel{id, from, to, loop}`（from/to/loop 进热键：用户会长期"只练副歌"）
- **校验分成两层**：Store 只做结构校验（在共享状态区之前，拿不到 resolveRef）；
  共享状态区的 `arrangeProblems()` 做引用存在性与拍号一致性。不在 Store 里抄一份解析
- 播放前也调一次 `arrangeProblems`：曲式存下之后它引用的预设可能被删

### 核心机制

- **`arrNextBar()` 是唯一的一份"沿节目单步进"**，被 `scheduler`（实际排程）、
  `predictNextArrange`（落点预测）、`arrangeNextRow`（待命球）三处共用。
  两边各写一份的话，一旦在段/块边界不一致就会分叉——同时打坏弹跳球、播放头、待命球交接
- **块/段切换接进既有的挂起路径**（`consumePending` 的 `{applied, resetBar}`），
  只把"待应用的东西"泛化成 `pendingRef`（S1）。不另写一条切换路径
- 跳段 = 改 `S.arrangeSel.from/to` → 下一个小节边界由"落在范围之前就拉回 from"生效。
  **不需要任何相位换算**（阶段一探针据此把跳段从档位③ 下移到 ①′）
- 走到范围末尾且不循环 → 经注入钩子 `onArrangeEnd` 停止，状态写「曲式播放完毕」
  （区别于"我自己按停的"）；练习量到点仍是「已练满」——两者不会混
- 次序刻意固定：练习量计数 → 变速训练器 → 曲式推进 → 统一挂起消费
- 可视化仍由 `schedBar` 驱动（`buildViz` / `paintFrameBody` 一行未改）；
  **没做**段落自适应行数——(a) 型段落的第 5–8 小节与第 1–4 逐音符相同，多画 4 行无新信息

### UI

预设卡片里「编排曲式」入口 → 全屏 overlay：曲式库列表（段数/小节数）、
段落结构（段名 / 块的预设与遍数 / 换预设的内联候选行 / 加删移）、
播放范围（在段行上点「起 / 终」）、范围循环、从头播 / 播选中范围。
主界面在曲式模式下显示「曲式名 · 当前段名」与「第 k/N 段 · 第 m/M 小节」，
并提供 ◀ 上一段 / 下一段 ▶ 跳段。
窄屏（≤640px）把段行拆成三行（名字 / 块 / 操作钮）——390px 下一行放不下，
实测预设名会被压成一字一行、操作钮跑出屏外。

### 过程与验证

- **反向验证 27 个变异全部命中具名断言**（S1 5 个 / S2 13 个 / S3 11 个 / S5 3 个；
  S4 8 个中 7 个具名 + 1 个改的是待命球调用点、效果在渲染层，按 T22 的既有先例
  列入人工截图验收清单）。过程中被变异逼出来的断言问题：
  - "断言测错了对象"：不清 `pendingRef` 时"第二次 consumePending 返回 applied:false"照样通过
    （拦在 `!pendingPattern` 那关），真正的后果在下一条**预设路径**上（切了预设却播上一个曲式块）
  - "用例分不出差别"：块遍数相同时"累加边界"与"每 4 小节一换"等价 → 必须用遍数不同的块；
    单段曲式下 `to<from` 的钳制测不出 → 必须用多段
  - "在 null 上崩溃"：断言要兜空对象，但**期望 null 的断言必须走原函数**（兜了就永远成立）
  - "测试挂死"：把"末尾停止"变异成"回绕"会让 while 循环永不终止 → 给循环加上界，
    把"无限回绕"变成一条具名断言
- 删掉 2 个构造性变异（`BUILTINS[999]` 本就返回 undefined；"强制循环"与原行为结果等价）——
  变异清单里不该有"为凑数而编"的条目
- 三个闸门各抓到真问题：ESLint 抓到 `draft` 遮蔽编辑器的同名模块变量（改名 `snapshot`）；
  零依赖 lint 抓到两个内联回调里重复声明 `t`（抽成 `mkMove` 助手顺带去重）；
  tsc 抓到 `Date - Date` 不是算术运算（显式 `getTime()`）

### 已知边界（写下来，免得当成 bug）

- **整首必须同拍号**（D3）：混拍号在保存/播放前会被 `arrangeProblems` 拦下并说明是哪段。
  变拍号是档位③′ 唯一剩下的昂贵部分（`S.sig` 要变成"当前段的拍号"）
- **不做段落自适应行数**（档位②）：见上
- **播放中改遍数/结构不会立刻生效**：改的是下一轮的曲式；播放中的游标不受影响。
  这是刻意保守——播放中改结构要处理"当前段已经不存在了"的一整类边界
- **窄屏上限**：编排面板在 390px 视口下仍会横向溢出约 95px——这不是本版引入的：
  实测 **v1.11.0 基线在 390px 视口下 `documentElement.clientWidth` 就是 485**
  （主页面本身放不进 390px）。那是"手机练习场景打磨"那个方向的活，本版不顺手修
- 预设改名后，曲式里引用它的块会显示新名（按 id 引用，不按名字）——引用被删才显示"（已删除）"
